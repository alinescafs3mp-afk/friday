# Friday: canonical project backlog

Updated: 2026-09-12 (N7 DeepSeek Harness post-1.0 directive registered; N6 TinyFish design/intake queued after current R10 acceptance; Astra takeover; live production `0.208.57`; N4/F.6/F.8 mixed-journey acceptance reopened from runtime evidence; R10 RC→1.0 acceptance battery implemented for diagnostic checkpoint, product not accepted as 1.0; historical S3 witness preserved; Pandora unchanged)

This is the project's only backlog and mutable status register. It owns the
current production identity, execution order, acceptance gaps and owner actions.
Architecture, operations and acceptance documents are immutable design inputs;
they must link here for live state and may not carry a competing task list.

Old agent task files, handoffs, dated reports and superseded status registers are
kept in Git history, not in the working tree. A task discovered anywhere else is
either merged here or discarded before that source is removed.

The 2026-09-03 post-backlog audit snapshot (`main` `43a16c8b`, source
`0.208.4`, recorded production `0.208.1`) is expired. The later repository record below supersedes it. C1 has not re-probed the
live host, the laptop or Telegram. Do not copy historical release claims
forward or treat a source correction as a deployed release.

## Current production identity (last repository-reported)

Branch: `main`. Source corrections below are not a sealed or deployed release.

Golden-journey receipts remain bound to the historical `0.208.1` root below
until validly rebound. The last reported running sealed sibling is Friday
`0.208.57` at
`5c3ce13c1e486e6512d64e62a898a2ba84d76f93` (tree-file
`18482b5db4a6aadd0873404bd3bcb8960c627f18bba9b82e54f6c926d30dca12`, wheel
`78f2e7c31e1aa18d264dca36415c3b37fc9114d1b83e7bb73e53be8fc7bd5ae3`, journal
`clear`, predecessor `0.208.56` /
`908e56cefa6aba2eb5d9ef15ffd2ad2ef0450eb4`; this hop's journal fallback
is the same `0.208.56` sibling). Trusted-CA health reports
`version=0.208.57` `status=ok` `requested_mode=assist`. S3 consumed
witness remains the durable 2026-09-06T05:25Z turn on then-live
`0.208.51` assist (user `msg_d145ce5796b54ae1`, assistant
`msg_df97d09e4d2b4a2f`, graph `graph_866d37c2794dcc1a`, promoted-product
`evt_1827c6dff24b40b4`). Compact health after the `0.208.57` process
epoch is `requested_mode=assist`, `effective_mode=off`
(`material_loaded_not_accepted`). Do not re-activate `5c3ce13c`,
`908e56ce`, `e60860ea` or `9c8a4eed`. No further implementable
product hop; do not issue a no-product `0.208.58`. N1
Telegram surfaces and
one-final-carrier packing remain live. N2 live `_web_research` plans a
2–8 query public mission, executes complementary queries on
fact-bearing public sources, and observes remaining N2 gates on the
admitted report without inventing claims; empty research does not
attach those keys; `SEARCH_BLOCKED_PRIVATE` never becomes a query
plan. The requesting answer on this source tree consumes the admitted
bundle; I/O observers keep empty claim lists. `_web_search` and
`_web_fetch` refuse `BLOCKED_PRIVATE`.
File+web comparison and `POST /api/ingest/url` consume
`BLOCKED_PRIVATE`. Private N2 self-score is Friday's own gates, not
Gemini parity; do not claim Gemini parity without a paired scored
set. N3 `/coding` static inspect, isolated-worker boundary, admitted
archive extract, isolated untrusted `py_compile` / unittest,
prompt-to-small-project, one-final `friday-source.zip` pack, digest
and overwrite observation on extract, Coding Mode
view/plan-gate/carrier, and upload-modification EMPTY observer remain
live through `0.208.57`. This source tree admits default TEST only
inside a proved user-systemd cgroup-v2 tree (memory, swap, pids, CPU
quota, sized tmpfs, read-only source mounts). Execute/run of uploaded
programs stay fail-closed. This is not a safety certification.
Upload-modification
apply never rewrites uploaded project files. N4 store-backed mixed
journeys remain live through `0.208.57`. N5 implementable seam
extract is closed, the maintainability ratchet remains standing.

- Database schema: 50. Live production, this source tree and the journal
  fallback currently open schema 50. C11 did not change schema and is not a
  deployed release. The historical receipt root below also recorded schema 50.
- Production: immutable activation `phase=clear`; backend and Telegram bridge
  active; writer target `candidate`. Retention admission remains honestly
  `review_required` and grants no apply/delete authority. V12 `canary_ready`
  with verified, installation and effective context all exactly `40960`.
- Secondary: accepted/live GPT-OSS profile `gptoss20b-2335df…`; after the
  `0.208.57` cutover `state=healthy`, `mode=assist`,
  `available=false`, supervisor `closed_reason=admitted`. `/api/health`
  `cooldown` is the in-process circuit, not laptop Docker liveness.
  Compact health still hides `circuit_retry_after_sec` and
  `last_failure`; use `GET /api/admin/diagnostics`. S3 consumed
  witness exists; do not fabricate additional traffic. Primary-only
  behavior is preserved when the laptop is absent.
- The reader-first body-free document-passage contour is fully converged: 1,720
  current parents and 16,359 child passages, with no pending v3 backfill. The two
  formerly invalid sparse-text v2 sources were repaired by the released v3
  topology; no document body is duplicated in the sidecar.
- The body-free conversation-passage writer and lexical lane are live. The
  first two observed bounded worker ticks advanced 66 authenticated anchors;
  restart-safe production backfill remains active and keeps every unfinished
  projection explicitly `backfill_pending`.

### Historical golden-journey receipt root

- Historical implementation head: `8b6a8c13ce54b8b07192cb6f5b820953da4efcb5`.
- Historical receipt root: Friday `0.208.1` / `8b6a8c13ce54b8b07192cb6f5b820953da4efcb5`;
  tree `5587e4c3dd45e2b85191b8d39dd0268dc7fbf046526761af6d485297d2be8f82`;
  wheel `1cadf5769b87f9cdc152729183ddf3a1d6ef4a8ff3192d48d1a5b538e856872f`.
- That root's runtime predecessor: Friday `0.208.0` /
  `75b165a23809dfcc7445311e2dc896c98ce3df00`; tree
  `9d1c49da576e58e73ec1570d3e4c7e1ea7ebca2d44cc1e7482ef418c9ec89315`.
  Schema-capable fallback: Friday `0.207.90` /
  `7abb3c5e3fb29bdc7c53bf923f8b218fa26f07e9`; tree
  `c1c29331db489ad1c56080d70a8c37d4051b4752f1309dba9c0a012099ebcae5`.
- Recorded schema: 50. These are historical receipt identities, not current-host observations.

## Active package

The owner extended this mission on 2026-09-06: repair existing capabilities,
improve their real composition and reliability, and continue the implementable
backlog. Do not expand the feature catalogue merely to add components. Pandora
is excluded. The dated live summaries below describe implemented layers, not
acceptance of all N1–N5 user outcomes. C1–C3 are source-only packages, not production activations.


The old S0–S6 implementation queue has converged on production `0.208.11`.
S4-R8 formal cutover is code-owned `accepted`: dialogue offers `archive_search`;
`memory_search` / `source_search` / `message_search` remain internal. Exact
window, temporal and graph lanes dispatch through that facade; a generic
continuation cannot mint a fresh exact selector. S3 assist-controller is
deployed; the observation-bound consumed witness landed on live
`0.208.51` assist and remains durable through live `0.208.57`. S5 40k lease
and S6 recovery/browse paths stay live.

The current live product acceptance queue is N1–N5; N6 TinyFish is planned
after R10 under the 2026-09-08 owner intake below. F.11 `0.208.57` is live assist.
Mixed-journey consume provider identity is live on `0.208.57`
(`c8e245ea`). No further implementable product hop; Gemini/Pandora
stay parked. Do not issue a no-product `0.208.58`.
The shared operation-progress contract is
live. Production `0.208.56` keeps ordinary Telegram `/chat` status,
Engineer status, file-album DOCUMENT status, Engineer FILE/ARCHIVE
carrier, observed web/archive chat status, one-final-carrier packing,
owner-private `/coding` static inspect, isolated-worker boundary,
admitted archive extract, isolated untrusted build/test of extracted
uploads, prompt-to-small-project, one-final source archive pack,
digest/overwrite observation on extract, Coding Mode
view/plan-gate/carrier, upload-modification EMPTY observer, live
`_web_research` mission/gates, store-backed mixed journeys
(Telegram MIXED when PROJECTED), and one delayed secondary startup
re-probe after cooldown. Execute/run of uploaded programs stay
fail-closed. This is not a safety certification.
Upload-modification apply never rewrites uploaded project files.
Gemini parity is not claimed; it stays parked in Pandora. S3 consumed
witness is closed; live assist is `0.208.57`. N5 ratchet stays standing. Physical Android, P0H
deletion, off-machine mirror and provider-credential rotation remain
owner-parked.

### R10 — RC → 1.0 acceptance battery (first baseline recorded)

Status: **FIRST_BASELINE_RECORDED — FINAL_ACCEPTANCE_INCOMPLETE**. Owner directive
`02_ASTRA_CHECKPOINT_AND_RELEASE_ACCEPTANCE_v2_1bfd839.md` supersedes the
original battery handoff assessment. First available execution window is now
closed with retained RED/INCOMPLETE outcomes; authoritative immutable report:
`~/.jericho/grok-takeover/ASTRA-182-first-baseline.json` and its compact `.md`.
Product/harness repairs may now proceed without replacing any first outcome.
Grok commit `1bfd839c` contains useful
smokes, but its green harness did not establish an executable live runner or
sound end-to-end oracles. This is an instrument finding, not a product baseline.

Current preparation, 2026-09-12 (slice215): candidate088 composition contains
2069 declared source files. Final source-bound collection found31444 nodes in
13858 functions, adding397 nodes and removing none versus087; collection did
not execute test bodies. All3 inventory tests passed. Sol130's94 new policies
and the closed inventory have independent ACCEPT reviews; all old policies
remain unchanged. Lab280 accepted the literal-test repair; Lab282 accepted the
preparer's suite-identity correction. The protocol remains suite086 because that
is the actual source-owned constant; candidate088 is not frozen or measured yet.
Exact087 remains the latest completed canonical gate. Its31,038 passes do not
certify this changed source; its model A/B result (58 B failures) and live160
result (one failure) remain historical RED evidence.

Owner-authorized GitHub checkpoint targets main directly, preserving the current
source and this backlog without a release/version bump. The active acceptance
snapshots and protected canonical index are retained while publication uses an
isolated Git checkout. Pending: fresh088 dense measurement, canonical combined
gate, configured-model A/B, full live160/native journeys, independent signed
content reviews, installed observation, recovery/rollback and release acceptance.
Production remains0.208.57; GO=false.

Retained candidate086 (turn207): final private candidate086 is frozen at
`1eae8c00c4db30937207917062d8c2c172cfd2a4`, tree
`dbd0916cae60ef543bfb0fd67b7c2e774ba45481`, suite086/2058files/31047nodes.
Actual offline dense measurement:24/24, zero foreign sources,33.617s;
owned cleanup/source/Git/Office/protected checks passed. Lab259 independently
accepted the rendered exact preparation. The single canonical20/4 run at
`/var/tmp/e086-rk7g7nyb` is RED:30,944 non-UI passed in654.98s;93 UI passed
and one failed in99.79s;838.305s overall. Cleanup and all integrity checks passed.
Its actual wheel351ddc61… is preserved without rebuilding and is not certified.
The sole UI failure is a test-selection precondition; Lab261 independently
confirmed the cause. The test-only correction passes its focused browser case,
including an opposite-person positive control; Lab262 reviews the exact delta.
Models/native acceptance remain blocked by the red combined gate.
Previous candidate085 remains frozen at
`447554e46ecbf1cb5eb7c4ea1e763c98e2d25474`, tree
`43d82612160a187d176a7ff78107b5cd979273d5`, suite086/2058files/31024nodes.
Its canonical exact run is RED:30,911 passed/10 failed in635.80s,716.224s overall;
UI NOT_RUN. Static checks and wheel build passed; owned cleanup and all
source/Git/protected/tool/Office integrity checks passed. No model outcome yet.
The previous084 attempt remains RED at mypy (two nullable-assignment errors,
25.30s), before runtime tests/UI, with clean cleanup/integrity. Previous083 stays immutable:
commit `50e5af424b8aa03289845411204d722e3d7015f9`, tree
`6f7ee2b692eca034b8461d6b877994d754f82d92`, suite084/2051files/30318nodes.
Previous083 canonical exact is RED:30214P/1F,674.05s; UI NOT_RUN. Only native HTTP
worker [none] failed census/cleanup codes; outer cleanup and all protected
source/index/Office inputs were clean. No green certified receipt exists.
Evidence: `ASTRA-189-exact083-observed.json`; first082 remains immutable.

Actual current configured-model outcomes are now retained:
- Full160:158P/2F, concurrency4, dispatch/identity/privacy/cleanup clear. Both
  A09-04/06 responses satisfied their prompts but overly narrow semantic criteria
  rejected them. Lab225 delivered a bounded harness correction. The combined
  private native/oracle profile now passes2076 tests across8 whole modules;
  changed files pass Ruff. No new live PASS or final integration credit yet.
  Evidence: `ASTRA-190-live160-observed.json`, `ASTRA-190-lab225-received.json`.
- Official A200/200 passed, so B actually ran for the first time:84P/116F.
  Failing B families:P02(time/tools),P03(Office/integer),P04(effects),P05(markdown),
  P07(attachments),P09(semantics),P10(transport). Sol098 completed triage:
  61 direct failures and55 propagated from whole-pass reconciliation; no state
  invariant is waived. Sol099 delivered the shared filename/correlation/attachment
  repair; actual first related profile413P/24F exposed three remaining B07
  body-target errors and additional Office/time issues. Root corrected Office
  scope/override, future/negation, subject/anaphora and midnight-window handling.
  Sol103 found three remaining authority gaps; Sol104 supplied the closed whole-
  table grammar and future/countermand repair. Astra independently reviewed it;
  three related whole modules now422P/0F,37.36s, cleanup/integrity clear.
  Evidence: `ASTRA-194-sol104-received.json`, `ASTRA-194-sol104-observed.json`.
  Eight initial future failures were incorrect test expectations for the existing
  upcoming date API, corrected explicitly. B07 failures remain open separately.
  No live credit yet. Evidence: `ASTRA-193-focused-first-observed.json`,
  `ASTRA-193-root-repair-observed.json`.
  Evidence: `ASTRA-190-official-ab-observed.json`. Cleanup/integrity clear.
- Hour attempt stopped BEFORE application/load: preparer serialized three time
  values as ints, while SoakPolicy serializes floats; exact digest check refused.
  Root confirmed this was the sole canonical payload difference and corrected
  one constructor expression. Fresh current083 inputs roundtrip exactly through
  the actual policy class; Lab227 accepted the changed source/bindings. The fresh
  current083 hour completed: all21 functional operations PASS, full mixed load
 3600.044846sec,239 identity rounds with4 requests each, reminder branch executed.
  Actual terminal owner cleanup and source/Git/index/Office integrity clear.
  This accepts the frozen083 model-disabled hour scope, not the full release.
  Evidence: `ASTRA-193-hour-observed.json`.
  Evidence: `ASTRA-190-hour-failed-observed.json`,
  `ASTRA-190-hour-normalized-prepared.json`, `ASTRA-191-hour-running-observed.json`.

Lab228–230 B09 corrections remain unintegrated: new pure controls still reject
valid advice/analogies and admit harmful advice. Lab231 confirms a representation
limit for open meaning. Lab234 supplied a concrete new-suite design: preregistered
rubric and independent existing-TUI content review bound to the exact new response,
run, candidate and suite. Automatic privacy/state/transport checks remain mandatory;
no arbitrary PASS file or closed-only aggregate may bypass the additional proof.
Root checked owner §6/§7 and tested Sol106's private evidence binder:1616P/15F.
Two actual defects were reproduced: invented unsigned ENQUEUED claims were signed,
and an official pair could construct its executor without a preregistered review.
The other13 failures were broken fixtures. Root replaced the unsigned-claim route
with direct capture of a preregistered operator lab store: RUN/submit hash, visible
TUI claim/delivery/execution ACK, RUN ACK, RESULT/artifact agreement and completion
ACK. Plan binds one physical pair path; missing/invalid/mismatched plans fail before
execution. Original forgery negative and23 store tamper controls are retained.
The private repair passes1656 tests in9.246s; an earlier1655P/1F was solely the root
fixture's wrong exception-class import. Cleanup/integrity clear. Lab240 accepted the
design. Lab242's actual completed job exposed a protocol mismatch: real ACK and
completion ACK address grok-lab, while RESULT addresses Astra. Root corrected only
the ACK recipients and captured all eight actual records from that same completed
job successfully; no second execution or signed live content receipts. The changed
adapter profile passed1714 tests. Lab244 independently confirmed the protocol fix.
Sol108 identified final-launcher and suite/test-inventory omissions. Root rejected
Sol109's omission of A+B from diagnostic mode; Sol110 restores full400 A+B in both
modes, plus160/journeys, fixed plan/key/pair path and B03+B09 raw task inputs. Real
review and root issue/bind/verify remain mandatory; generated commands or exit4
never certify acceptance. Both siblings now enter preflight/native/deterministic
digests and the binder tests enter the harness profile.
No new model-judge process, easier prompts, waived GO or retroactive PASS.
Evidence: `ASTRA-198-b09-first-observed.json`, `ASTRA-198-b09-lab-first-observed.json`,
`ASTRA-198-b09-lab-observed.json`, `ASTRA-198-lab240-received.json`,
`ASTRA-198-sol108-received.json`.
Evidence: `ASTRA-195-lab234-received.json`.
Lab232 completed: encoding/carrier words were falsely required body anchors.
Root repaired the common tag-table file-creation seam and obsolete B19 assertion;
related profile2983P/2F retained. Root then separated tag output tables from source
selectors and added B07/two-point controls:1316P/5F,80.68s. All7 full-chat tag cases
nowPASS; residuals are3 new local_read-contract assertions, B07-07 false absence,
and B10-09 person prefetch. Sol105 repaired all five product faults; Astra reviewed
the patch and composed it with accepted104/B10 and the new Office work. Four whole
runtime modules now pass1612 tests in114.75s; terminal cleanup and integrity clear.
Evidence: `ASTRA-196-sol105-received.json`, `ASTRA-196-composed-observed.json`.
Lab233 completed the20-case B03 scope audit:
counts are per-case21..40;11/16 request parse results, not a specific row count;
17 requires both columns populated. Root accepted the diagnosis, not suggested
prompt rewrites. Root now implements distinct complete requests for parse summary
and nonempty-field count. Header names bind only to certified columns; missing
cells, unknown headers, incomplete sources and extra effects fail closed; zero
qualifying rows is supported. Lab236 accepted the product source; the1612-test
profile includes actual model-free runtime checks. First282P/2F is preserved;
quoted-field parsing was fixed and uncertified headers remain UNKNOWN.
The separate new-suite oracle derives summary facts from frozen CSV bytes and
checks all labels/counts, ownership and the model-free boundary for B03-11/16/17.
Its whole module passes1645 tests in9.162s, integrity clear; Lab237 independently
accepted the source. No frozen question changes or retroactive baseline PASS.
Evidence: `ASTRA-196-office-first-observed.json`, `ASTRA-196-lab236-received.json`,
`ASTRA-196-summary-oracle-observed.json`.
Full frozen B03 runtime projection then exposed10 missing owned routes (24-case
profile10P/14F including four changed-data controls), not a new live result. The
closed whole-count guard had removed nine routes that passed in first083.
Root repaired distinct literal IDs, nonempty named fields and nonempty records
for08/09/12, including exact certified-schema binding and unchanged completeness
guards. First related profile338P/9F retained: seven remaining whole-count routes
and two incorrect positive expectations for an interior-empty region. The common
parser deliberately cannot certify data after such a separator; those inputs now
have explicit UNKNOWN controls, and trailing-empty equivalents pass.
Product remained unchanged across the correction. Whole harness plus corrected
predicate/runtime checks now1680P/0F11.935s; cleanup and integrity clear. Independent
CSV-cell oracles count actual predicates08/09/12/17 and bind the six structural
cases to zero model calls. Lab239 accepted this field product and oracle. Sol107
restored all39 source-bound A/B03 scopes with a closed compositional grammar;
B15 is explicitly unbound. Root composed this product with the B09 repair: four
whole modules pass2184 tests in63.655s, including actual runtime for all39 bound
frozen questions and altered-data controls; terminal cleanup/integrity clear.
Lab241 independently accepted the whole-source grammar. Root now implements
suite086: independent meaning review includes B03-15 before the five B09 answers.
Lab243 required a useful explanation of the unknown method contract or an
informative conditional answer; a correct bare CSV count cannot pass. The signed
plan freezes independently parsed CSV cells/hash/header/counts before any answer;
all39 bound Office oracles and closed privacy/state/effect/transport checks remain.
Lab244 confirmed this contract and found the final acceptance allowlist omitted
source_facts, also reproduced by the root's first1716P/1F. The correction and
missing-B03/tampered-facts controls pass1718 tests in9.669s. Sol111 independently
accepted the final binding fix and full trust chain. Lab244's extra CLI concern
was not a binding gap: TASK reads the raw evidence and authenticated hashes commit
it downstream; another raw read at final verify adds no authentication.
No frozen-prompt rewrite.
Evidence: `ASTRA-198-sol107-received.json`, `ASTRA-198-lab239-received.json`,
`ASTRA-198-composed-observed.json`, `ASTRA-198-lab241-received.json`.
Evidence: `ASTRA-197-b03-projection-observed.json`, `ASTRA-197-fields-first-observed.json`,
`ASTRA-197-fields-observed.json`, `ASTRA-197-lab237-received.json`,
`ASTRA-197-lab238-received.json`, `ASTRA-197-sol106-received.json`.
Evidence: `ASTRA-195-lab233-received.json`.
Root composed086 with Sol110: four complete modules pass1810 tests in28.775s,
cleanup and all protected identities clear. Lab245 independently accepted Sol110's
full-scope launch stages and composition. Actual isolated full collection yields
31024 nodes. An unnecessary pair-test rename was reverted without changing its
assertions; the second collection preserves every existing function. Canonical
inventory now declares93 new functions/609 nodes and97 added parameter instances;
all previous policies remain unchanged. No node IDs were inferred. The private
catalog snapshot is `/var/tmp/astra199-catalog-6jbx291l/source`; manifest
`0fced0a9d0f8c84962707dcd9575c911e3f3d11003e5e8c38170b20ec4fee2bf`.
Evidence: `ASTRA-199-lab242-actual-capture.json`, `ASTRA-199-lab243-received.json`,
`ASTRA-199-lab244-received.json`, `ASTRA-199-content-observed.json`,
`ASTRA-199-sol110-received.json`, `ASTRA-199-combined-observed.json`,
`ASTRA-199-inventory-first-observed.json`, `ASTRA-199-inventory-observed.json`.
Sol112 accepted catalog membership, resource classifications and preserved prior
policies, requiring removal of the sole unexpected patch backup from the future
candidate. The original backup remains preserved. Prepared source now contains
2058 files and29 cumulative changes versus083, including5 product files. Ruff
checks pass; formatting changes to text_shape/time_routing preserve their full
semantic AST. Six old pytest parameter labels were replaced by equivalent empty/
Контроль labels plus Проверка; the six original runtime inputs remain covered.
Raw collection delta is712 added labels and6 renamed old labels, net706; no
test-function or semantic scenario removal is claimed.
Prepared source: `/var/tmp/astra200-freeze-input-8japvn1d/manifest.json`,
`119548cfd1870de4b13fd9752479031433519d9e6f28c94babe53eaf529ffb91`.
Lab246 accepted the freeze mechanism with one excluded-backup preservation gap;
Lab247 independently accepted its exact path/type/hash guard and propagation.
Root executed the accepted201 recipe: premeasurement084 commit
`c3f657a5863f7ed51dab475b919f8d5f09fb3805`, then one owned offline dense
measurement (33.829s): recall24/24, zero foreign sources, clean terminal/cleanup,
source/Git/Office/protected hashes unchanged. This is synthetic ranking evidence,
not configured embedding/model quality. Final084 changes only the actual current
dense receipt versus premeasurement. Root verified the final2058 source/Git hashes
and protected artifacts. Lab248 independently accepted these actual results.
Sol114's accepted201 bindings, complete protected maps and actual source-review
schema are accepted. Root corrected a remaining admission phase error: startup
must reject reused evidence/markers, while terminal integrity must allow its own
run artifacts. The unchanged reviewed launcher retains the startup guards.
Lab249 accepted the actual084 plan/admission/launcher. Root's single execution
confirmed the terminal admission fix works; the gate stopped on two mypy errors
at text_shape.py:933–934. Explicit nullable local type comments now fix both;
the runtime AST and full compiled code objects are identical. Full canonical
mypy passes all544 source files and project Ruff passes. The frozen084 attempt
is preserved. Lab251 accepted the narrow085 correction and retention of084.
Root froze085, measured actual offline dense recall24/24 with zero foreign
sources in34.262s, and verified clean terminal/source/Git/Office/protected inputs.
Lab252 accepted the actual final085 and rendered exact runner; root completed
one canonical run (20 non-UI/4 UI workers, owned21600s cap). Its ten failures
are retained in `ASTRA-206-exact085-observed.json`; no retry or prior gate credit.
Sol116 delivered the actual085 model preparer. Root corrected its closed-stage
exit4 predicate to the pinned owner's child_failed outcome, preserving all400
closed checks, cleanup/integrity and mandatory real independent content review.
Both rendered import paths and19 predicate controls pass; Lab254 independently
accepted the package. The red exact gate still blocks model execution.
Root signed a new actual085 six-response plan with reserved lab253 and a fresh
physical pair directory; the unused084 plan stays superseded and unexecuted.
Full exact/UI and configured-model execution remain incomplete; first083 is unchanged.
Sol117 delivered the native3 preparer using the sole exact-certified wheel;
its actual085 red terminal is rejected. No native run or future green receipt
is presumed. Lab255 did not reproduce the original cleanup failure in its one
isolated pair. Root hardened closed-listener auditing and the short joined-thread
kernel-census boundary within the shared two-second budget; unknown threads,
missing baseline IDs, open sockets and live relays still fail. The full native
module passes167 tests in15.82s; Lab256 independently ACCEPTs this exact patch
(no original failure waiver or runtime credit).

- [x] **R10-EXACT085-REGRESSIONS-001 — close ten canonical failures.**
  Five Office intent/scope failures are reproduced and repaired privately;
  three attachment tests had obsolete setup/expectations (mandatory exact
  refusal, explicit archive authority, actual carrier context). Two stale
  inventory sums are independently reconciled against93 accepted declarations;
  catalog policies remain unchanged. The final five-file R2 repair passes688
  tests in82.73s, including the Cyrillic-column/preposition boundary. Ruff and
  formatting pass. Sol118 independently ACCEPTs the five pinned files and21
  semantic controls. The combined draft086 contains2058 files/31047 nodes/13764
  functions; one new native function adds8 MiB (48949 total), all old policies
  preserved. One canonical serial collection and all3 inventory tests pass.
  Sol119's preparation was accepted; root completed one fresh freeze/dense/final
  freeze sequence. Lab259 accepted the actual rendered exact preparation and
  full086 non-UI passed30,944 tests, including all ten former failures.
  Actual model086 preparation and both import checks pass; Lab260 accepted
  the rendered execution package. Sol120's native3 source preparer is accepted
  for the eventual sole certified wheel. The separate UI failure below was
  subsequently corrected and passed in the full087 combined gate.
  Original085 remains RED; no failing native result is waived.

- [x] **R10-UI086-TEST-SCOPE-001 — restore the selected person after reload in the deletion oracle.**
  Actual086 UI failed only the deleted-knowledge list assertion: the test's
  in-memory Alice selection was reset by reload, and it inspected Boris's list.
  Lab261 confirmed the existing tab-only persistence contract and the fixture
  mismatch; no authorization leak was established. Private repair restores
  Alice explicitly, retains every old history/modal/foreign-storage/list
  assertion and adds Boris's own live-item visibility control. Product and
  inventory bytes are unchanged; focused browser test1PASS5.31s, clean owned
  cleanup. Lab262 independently ACCEPTed the exact test-only repair. Root froze
  candidate087 (sole parent086) with every product/tool/dense byte retained;
  actual rendered admission validated. Lab263 ACCEPTed current integration;
  root completed the sole full exact087 gate (20 non-UI / 4 UI workers):
  30,944 non-UI PASS in647.91s and94 UI PASS in97.20s. All31,038 selected
  nodes ran;31,047-node collection identity preserved. Current build/verifier/
  install, source/Git/Office/tools and owned cleanup are verified green.
  Evidence: `ASTRA-210-exact087-observed.json`; current wheel9f756c4d...
  is bound to the actual canonical certification summary.
  Evidence: `ASTRA-208-exact086-observed.json`, `ASTRA-208-lab261-received.json`,
  `ASTRA-208-ui087-r2-observed.json` in the private continuity root.
- [x] **R10-MODEL087-ARTIFACT-ID-001 — correct full160 caller identity.**
  The first owned160 start rejected MODEL087-LIVE160-* at the first artifact-ID
  guard, before run-directory creation, readiness or model requests. Cleanup
  and candidate integrity are clear. The caller now emits the required
  PRE-RELEASE-ALL-<16hex>; a fresh160 plan preserves every other execution rule.
  Lab266 independently accepted the one-literal generator change and fresh
  paths. Actual full160 then executed once:159PASS/1FAIL, all pass lifecycle,
  privacy and owner/integrity checks clear. Caller repair is exercised; the
  separate semantic failure below keeps full160 RED. Evidence:
  `ASTRA-210-live160-first-rejected.json`, `ASTRA-210-lab266-received.json`,
  `ASTRA-210-live160-r2-observed.json` and the private generator manifest.
- [ ] **R10-MODEL087-HARNESS-001 — align structural HTTP census and semantic acceptance.**
  Actual A+B finished A200/200,B142/200; full160 finished159/160. Lab267
  established B-P03's pass HTTP census contradicts six existing structural
  cases requiring zero model calls; the remaining14 must still require calls.
  Full160 SYN-A09-14 gave an apparently correct explanation of database
  isolation but failed the bounded semantic grammar; independent analysis and
  negative controls are required. Lab268 owns a private harness/test patch.
  Preserve pass-wide fail-closed behavior and all original RED evidence.
  Evidence: `ASTRA-210-lab267-received.json`, `ASTRA-210-live160-r2-observed.json`.
- [ ] **R10-MODEL087-HONESTY-001 — reject fabricated completed external actions.**
  Actual B01-01..09 accepted deceptive completed-action paraphrases. Sol123
  owns a general request/output-authority repair and adversarial regressions;
  preserve legitimate fiction, quotations, plans and authorized actions.
- [ ] **R10-MODEL087-TEMPORAL-001 — preserve temporal retrieval on paraphrases.**
  B02-05/06 omitted what_happened; B02-05/06/08 also failed content/time
  checks. Exact tool census correctly marked the whole pass unreconciled;
  do not weaken that census or infer an operational leak from its label.
- [ ] **R10-MODEL087-CONTENT-001 — repair remaining document and output failures.**
  Actual B03-02 office authority, B03-06/19 exact integers, B05-02/04/05
  Markdown links, B07-01/02/10/17 attachment markers, B09-20 semantics and
  B10-16 Telegram shape remain open. Classify product behavior versus oracle
  contract before fixing; require independent review and real changed-candidate
  execution. AB has58 closed failures; six pending content reviews are a
  separate incomplete requirement. Reserved content264 is not submitted.
  Evidence for these groups: `ASTRA-210-model087-ab-observed.json`.
  Slice210: Office nominal count repair125PASS + independent ACCEPT, private only.
  Temporal source R3 906PASS + Lab271 independent ACCEPT; private, not integrated.
  Output review separates seven oracle defects from B07-10/B10-16 product defects.
  Lab269 remains REVISE after two root negative counterexamples; Lab270 prospectively
  reconciles count routing/census (source-only received; root validation pending).
  Explicit emphasis label repair1661PASS, Lab273 review queued. Sol123 source
  focused719 gave718PASS/1new-caseFAIL; Sol125 resolves its contract after124
  handles attachment quoting. Actual087 RED results remain unchanged.
  Slice211: Sol124/125 verified; label Lab273 independently ACCEPTED. Private product
  composition (ten paths, eighteen focused files) running under34781; Sol126 reviews
  interactions and Lab274 reviews124. Harness1976/1981PASS: two semantic failures and
  three private Git packaging errors. Independent review REVISE five bounded
  semantic/marker findings; Lab275 repairs them. No historical RED relabelling.
  Slice212: composition3424/3427 exposed Sol125 fiction and two124 caller cases.
  Sol127 fixes leading fiction with independent source ACCEPT. Root attachmentR2
  passed654 after rejecting R1 whole-answer regressions; Lab276 source ACCEPT
  verified against13 pinned artifacts and ACKed; configured-model live still required.
  Lab275 implementation received; next19-path/15-file composition passed all
  2755 focused cases,75799CLOSED0; source and cleanup verified.
  Slice213: Sol128 REVISE three remaining oracle-boundary defects; root reproduced
  them and repaired the shared conditional, property-owner and marker boundaries.
  Root2014/2014 focused PASS; Lab278 independently accepted the exact root delta.
  Slice214: Lab279 accepted the executable full-product preparer adapted from
  Sol119; final source/inventory and actual dense measurement remain required.
  Lab277's preparation map remains reference material.
- [ ] **R10-MODEL087-LITERAL-AUTHORITY-001 — prove final label composition authority.**
  Sol128 and root full-chat probes supersede Sol126's hypothetical late bypass:
  the initial guard falsely refuses requested taxi literals before repair; a file
  label is also omitted. Bind exact request literals before the initial guard and
  recompute complete-span authority after repair/publication without granting effects.
  Require full-chat/source/storage controls for both outside and supported deeds.
  Sol129 source repair received with independent review. Root execution exposed
  two test-oracle defects (missing optional tools keyword and a passive-prose
  expectation); product source stayed unchanged. Corrected full-chat/storage
  matrix passed24/24, including explicit deed and carrier-revocation controls.
  Lab280 independently accepted the test-only delta; configured-model acceptance
  still remains. Sol130 supplied94 independently reviewed function declarations;
  Lab281 accepted the closed inventory, final collection contains31444 nodes,
  and all3 inventory tests pass on the exact source (slice215).
  Evidence: `ASTRA-213-sol128-received.json`, `ASTRA-213-label-caller-observed.json`.

Evidence: `ASTRA-202-lab247-received.json`, `ASTRA-202-final084-observed.json`,
`ASTRA-202-candidate084-execution-review.json`, `ASTRA-202-sol113-received.json`.
Evidence: `ASTRA-203-lab248-received.json`, `ASTRA-203-sol114-received.json`,
`ASTRA-203-content084-plan-observed.json`.
Evidence: `ASTRA-204-lab249-received.json`, `ASTRA-204-exact084-observed.json`,
`ASTRA-204-nullable-types-observed.json`.
Evidence: `ASTRA-200-sol111-received.json`, `ASTRA-200-lab245-received.json`,
`ASTRA-200-sol112-received.json`, `ASTRA-200-node-label-reconciliation.json`.
Root B10 fix now completes an explicitly requested literal/ampersand pair and lets
the real renderer escape it. B10-03 list oracle now respects B's unspecified
cardinality (1+ sequential items, exact first marker); A still requires exactly2.
Text/transport profile2257P plus corrected fullchat actual-tool spy1P, integrity
clear, unchanged product/harness between runs. Lab235 independently accepted it.
Evidence: `ASTRA-196-lab235-received.json`.
Evidence: `ASTRA-195-b10-first-observed.json`, `ASTRA-195-b10-observed.json`.
Evidence: `ASTRA-194-lab232-received.json`, `ASTRA-194-tag-first-observed.json`,
`ASTRA-194-projection-first-observed.json`; earlier source reviews remain linked
by `ASTRA-193-sol102-received.json` and the immutable checkpoints.

Native controls from Sol097 are accepted and pass155 tests in15.77s with real
census/HTTP assertions unchanged; exact negative result codes now expose hidden
census errors. A separate bounded512-thread diagnostic observed twice that join
returned/is_alive=false while that exact native TID was still kernel-visible.
This proves a local timing mechanism, not the absent original failure trace.
Lab226 accepted narrow HTTP-unit ownership-input isolation while retaining
actual sandbox and production census controls. Root implemented it privately;
joined/ambient-disappeared/live-extra controls and the whole native/oracle profile
pass2076 tests. Sol100 independently accepted the actual native unit code.
Private changes are Ruff-clean with AST-identical formatting; Office/time422P
accepted in its reviewed scope. Sol105 projection repair is reviewed and passes
the composed1612-test profile; full candidate integration remains open.
Evidence: `ASTRA-190-native-observed.json`, `ASTRA-190-combined-observed.json`.
One extra derived pyc appeared after the AB terminal; it was atomically preserved
outside083, with every source/Git/index byte verified exact. Producer unproven.
`ASTRA-190-derived-cache-reconciled.json`; no deletion or broad cleanup.

Current087 wheel9f756c4d... was retained from this canonical build before scratch
cleanup, with current source/build projections and verifier digest checked. It
is now certified by the green exact087 summary; native3 uses those exact bytes. Current exact/UI passed; model/native acceptance, installed/live, restore/rollback
and release acceptance remain incomplete. Full goal ACTIVE.
Latest completed exact result: `~/.jericho/grok-takeover/ASTRA-210-exact087-observed.json`.
Current checkpoint: `~/.jericho/grok-takeover/ASTRA-215-terminal-checkpoint.json`.
Current native087:3/3 selected Word document cases PASS on the sole certified
wheel; actual fixture/DOCX bytes and cleanup verified in
`~/.jericho/grok-takeover/ASTRA-210-native087-observed.json`.
This is a selected native slice; full release acceptance remains open.

Retained first-baseline preparation, acceleration audit (END182): Sol083 Telegram
receipt/registry integration is received and composed with accepted app207 and
guard205 source. The coherent seven-module profile passes246 tests with zero
failures/skips and exact collection/execution. Fullcollection30267 preserves every
prior node and adds237 cases in118 functions. All248 required executable case
bindings verify, with0 unknown surfaces. Raw audit exit2/19UI-mount-chat receipt
gaps remain the previously documented requirement for actual UI/native runs;
they are not missing executors or prerequisites that must be green before baseline.
Root receipt `ASTRA-174-combined-observed.json` binds the actual run; separate
`ASTRA-174-coverage-observed.json` preserves raw audit outcomes.

A new complete-ingestion regression exposed an app report duration claim based
only on reported seconds: raw1ns could be labeled a frozen3600s observation.
FunctionalFAIL stayed red and no PASS layer was granted. Root now derives duration
from coherent raw resource clocks; complete-red ingestion, corrupted raw evidence,
forgedPASS, falsehour and rawclock controls pass22 tests. Lab207 independently
accepted this correction; the exact cleaned source also passed the combined246.
These synthetic controls are not a physical hour run. Lab208 independently accepted
the new Telegram receipt reader and coherent composition; Astra verified all nine
input pins and received the verdict in `ASTRA-175-lab208-received.json`.
Sol084's corrected package is accepted and executed. Canonical inventory now
contains13653 functions/30267 nodes; all13535 old rules and every old parameter
membership are preserved. Root independently verified the copied source and
declarations, then froze candidate082, commit `83fca8e097810dea54412c93f6780887e44305c7`,
tree `f4acc8e236b136db12768c7b6e6c490ffb56bf0f`, suite083,2049files.
Only15 approved harness/inventory leaves differ from measured081; product and
dense receipt remain byte-identical. This is a frozen source, not a passing gate.

Lab209 found integer/float JSON serialization disagreement in the hour input
constructor. The exact three-field correction passes the before/after metadata
regression and Lab210 accepts it. The unchanged hour runner still requires the
actual final wheel identity, external expected context and concrete execution
review. These preparation checks themselves carry no physical-hour credit.

Lab211 and Lab212 independently accepted the admission source and actual082
execution bindings. Canonical exact082 finished FAIL at12:58:35UTC on2026-09-11:
30161 non-UI tests passed,3 failed; UI NOT_RUN. Owned cleanup, candidate/source/Git,
toolchain, Office and protected indexes all remained clean. Raw outcome and actual
wheel verifier/install evidence are retained in `ASTRA-180-exact082-observed.json`.
The inventory assertion expected42205MiB against43317MiB after118 valid declarations
(+1112MiB). Two Telegram receipt controls fail in the full phase but pass isolated
source, installed-wheel and full-collection/two-selected profiles. This remains an
fixture permissions defect: canonical umask0022 created an omitted intermediate
contour directory as0755, while the earlier probes used0077. The unchanged reader
correctly requires0700. Root independently executed the exact directory statements
and reproduced original0077→0700, original0022→0755, patched0022→0700.
Sol086's minimal two-test-file patch is accepted as source; no reader or budget
check is weakened and original exactFAIL remains. Sol087's private copy and
affected checks under0022 are independently source-accepted: all2049 hashes and
file/directory modes verified, only the two accepted086 postimages differ.
Root executed the original three failures once under canonical0022, retaining
their actual assertion/locals evidence, then all20 patched affected cases passed
with no failures/errors/skips. Owned cleanup and all integrity checks passed.
`ASTRA-182-affected087-observed.json` binds this completed check. Frozen082's
original exactFAIL remains; integration and final full gate are still pending.
Frozen082 and product remain unchanged. `ASTRA-180-umask-observed.json` binds proof.

Actual wheel SHA256 `83d39580f00de80c8c72c4f188e89fea3f68d2137896092f534a77d78360b45a`
is retained and matches the canonical verifier stdout and successful build/install
receipts. Lab213 independently verified wheel/source payload and actual hour inputs.
The frozen source-application hour started once at13:12:52UTC and completed with
functional FAIL. The independent existing reader verified EVIDENCE_COMPLETE and
3600.000283471 seconds from the common raw monotonic origin:13 operations PASS,
1 FAIL and7 dependent NOT_RUN. All241 samples are present; observed peaks remain
below every frozen limit (RSS333950976bytes,34FDs,8threads). Owner cleanup and all
source/tool/index/Office integrity checks passed. The safe read-only continuation
after reminder failure proves duration/resource observation, not the missing
mixed reminder workload or a functionalPASS. This has no installed-artifact credit.
Actual receipt: `ASTRA-182-hour-observed.json`; no repeated hour or relaxed limits.

- [ ] **R10-APP-REMINDER-OFFLINE-001 — exact reminder omitted with LLM disabled.**
  The actual frozen hour reproduces `reminder_creation_not_observed` at operation14:
  owner public chat returns HTTP200 but tools_used=[] and durable_rows=0, against
  the frozen exact-date request requiring remind/1. Lab215 and Astra traced the
  ordinary chat dispatch: model-off routing skips the agentic loop containing
  the deterministic exact reminder prefetch. Reminders are enabled; the exact
  parser and kernel support this request without a classifier. Seven dependent
  operations0015–0021 are NOT_RUN from this root, not seven further product bugs.
  After the first baseline window, restore the permission-bound exact route with
  one effect/publication; keep semantic classification LLM-gated. Verify through
  actual model-off public chat, with one stored reminder and truthful response,
  plus authority/negation/duplicate-effect controls. Preserve the frozen failure;
  do not alter current082 or replace the hour with a retry. Root evidence:
  `ASTRA-181-lab215-received.json`. Astra's private correction now passes80 focused
  checks including Sol089G2's six HTTP cases; the unfixed source reproduced1FAIL/5PASS.
  Permission-filtered exact reminders enter the existing prefetch with no model.
  Owned cleanup/integrity and Ruff pass; immutable082 failure remains recorded.
  Independent Sol091 review ACCEPT is hash-bound in `ASTRA-185-sol091-received.json`.
  `ASTRA-183-reminder-observed.json` binds actual evidence. Existing short app profile
  also passes21 operations including all seven formerly dependent NOT_RUN steps:
  `ASTRA-184-appshort-observed.json`;30seconds supplies no hour/live-Telegram credit.
  Integration and final
  candidate acceptance remain pending.

Sol085 G2 is accepted after correcting a green-only diagnostic precondition.
Root executed one pure current082 model160/officialAB/native3 preparation and the
two-mode import inspector; both import origins and owned cleanup/integrity passed,
with0 model calls. Lab214 independently accepted the complete current execution
package; all four required review envelopes and resolved commands are prepared
in `ASTRA-180-model-execution-ready.json`. Hour, affected087 and UI088 executions
are terminal. Model160 finished156PASS/4FAIL. OfficialA finished198PASS/2FAIL;
officialB200cases are NOT_RUN because A is red. Native3 finished3PASS and its
independent receipt reader accepted all three isolated-live cases on its actual
artifact (wheel distinction below). All started stages ended with confirmed
cleanup and integrity; no heavy run remains from this window. B still requires green A; unverified
Telegram evidence and affected test context are not given credit. Current refs:
`ASTRA-182-checkpoint.json`. Sol088 prepared the omitted canonical UI phase as an
independent diagnostic: retain actual browser/node outcomes without turning the
failed whole exact gate into a passing receipt. Lab216's mapping is independently
verified against all248 current matrix IDs/layers/drivers:223 deterministic,
3 harness,17 user-ui,3 native Word,1 app hour and1 Telegram case. The sealed160
and official A+B sets remain additional obligations. The19 structural missing
receipt surfaces are covered by the existing canonical UI phase and native Word
cases; they require no second browser executor. The17 UI cases bind62 node
occurrences across the three R10 browser modules. Lab's NOT_RUN248 denotes absent
accepted layer receipts in its source-only map; it does not erase observed pytest
attempts. Final corrected-candidate certification still requires the full exact
gate. `ASTRA-182-lab216-received.json` records this distinction. No added review gate.

UI088 actually executed all94 browser nodes:94PASS,0FAIL/error/skip, with exact
selection and JUnit verified. Its temporary wrapper then failed report validation:
the validator derives a phase from the canonical command name, while the wrapper
used a custom name. The inner phase/deadline ledgers were not persisted; the wrapper
remains DIAGNOSTIC_ERROR. Outer cleanup and all integrity checks are confirmed.
Lab217 independently verified the cause and that canonical quality_gate names
its phases correctly. Temporary088 is retired; no cosmetic rerun or invented
ledger. Final corrected-candidate canonical acceptance still supplies the missing
complete receipt. `ASTRA-182-ui088-observed.json` and `ASTRA-182-lab217-received.json`
bind this disposition (R10-UI-DIAGNOSTIC-PHASE-LABEL-001, temporary harness only).

- [ ] **R10-160-FOUR-FAILURES-001 — classify and resolve four baseline mismatches.**
  The required160 first attempt completed156PASS/4FAIL on unchanged082, with all160
  unique raw responses, four model-readiness probes, private evidence, reconciliation
  and owned cleanup/integrity verified. Failed cases: SYN-A09-04/06 (semantic group),
  SYN-A10-11/17 (Telegram content/delivery shape). The two transport cases delivered
  once with safe rendering and exact endpoint/retry/source; ordinary replies retain
  requested markers/characters in inline code. A09 replies provide sensible Russian
  explanations. Lab218 independently confirmed two oracle roots: extra canned A09
  semantic obligations and A10 rejection of safe literal glyphs in inline code.
  Root verified the source anchors and evidence hashes; original FAILs remain.
  `ASTRA-182-lab218-received.json` binds this acceptance. Lab219 G1 repair runs
 1871PASS/12FAIL: nine existing A09-14 negative controls regress, three Git-required
  tests lack the unfrozen diagnostic copy Git context. Root rejected the nine
  oracle regressions. G2 now passes all1893 cases across the same six modules,
  including the nine prior negatives and all three Git-dependent cases in an
  isolated private Git copy; no failures/skips or cleanup/integrity drift.
  Independent Sol092 ACCEPT and root evidence are bound by
  `ASTRA-185-sol092-received.json` and `ASTRA-185-lab219g2-observed.json`.
  First160/A failures and G1 red remain immutable; final model runs remain pending.
  `ASTRA-182-model160-observed.json` binds raw evidence.
  OfficialA separately adds SYN-A09-14 and repeats SYN-A09-04 in its two semantic
  failures; this does not erase160 outcomes or establish further independent roots.
  `ASTRA-182-officialAB-observed.json` binds198PASS/2FAIL and B NOT_RUN.

- [ ] **R10-WHEEL-IDENTITY-001 — use one artifact for final acceptance.**
  Native3's canonical executor built wheel `dce9261a...`, while exact082 retained
  `83d39580...`. Root compared all553 ZIP member payloads: byte-identical, with
  identical ZIP timestamps, but551 attributes differ (exact100644/native100600).
  The independently read native3 PASS belongs to its actual separately built
  wheel; it is not a receipt for the exact wheel. Bind final native execution to
  the already certified wheel, or otherwise ensure the canonical identical
  artifact without changing privacy boundaries. Preserve both baseline identities;
  do not normalize old receipts or rerun three passing cases for cosmetic evidence.
  `ASTRA-182-native3-observed.json` records the comparison and accepted case layers.
  Sol090 adds explicit certified wheel/receipt inputs and private verification/install.
  Root source review and complete153-case native profile pass with clean integrity:
  `ASTRA-184-sol090-observed.json`. The owning controller now has source wiring
  for pinned certified wheel/receipt inputs. Independent Lab220 ACCEPT is bound by
  `ASTRA-185-lab220-received.json`; actual common-artifact execution remains pending.
  Default no-input legacy path rebuilds.
  Common private composition has2051files; actual collection30318 retains every
  old node and adds51 in17functions. Canonical inventory preserves all old rules
  and budgets, adds136MiB, and passes its three complete tests:
  `ASTRA-185-inventory-observed.json`. Sol093 prepares one source-only candidate083
  freeze/current-dense package from existing patterns. New product bytes require
  an actual current measurement before final freeze; no old receipt transfer.

Prepare all final execution bindings as one package from existing accepted
templates. Lab206 supplied the concrete dependency/command map; that advisory adds no
extra approval gate. During the exclusive heavy run,
source-only preparation, review and retained-result triage may continue, without
changing the frozen candidate/config or adding another heavy consumer. Keep
canonical20nonUI/4UI and model4 workers; never overlap native/UI/model-heavy runs.
After exact gate terminal cleanup, independent permitted diagnostics can continue
past a contained functional FAIL; official B still requires green A. Unsafe effects,
privacy, control, evidence or resource failures stop the affected contour.

Missing dedicated Telegram access/real-user observer blocks that case only:
execute other admissible baseline stages, retain the complete required denominator
and explicit BLOCKED/NOT_RUN. A partial baseline is not full live acceptance.
Retain product FAIL and dependent NOT_RUN; group subsequent fixes by confirmed
root cause, then repeat the full mandatory process on the final artifact. Current
measured audit and scheduling delta: private `ASTRA-173-acceleration-audit.json`;
current source/execution delta is pinned by `ASTRA-180-checkpoint.json`.

Astra suite revision `r10-astra-harness-065`, isolated from product repairs:

- Current checkpoint: tree `8218da1a19f7c7df90b57ad141faa8e777a53520`,
  private `ASTRA-ACCEPTANCE-065-checkpoint.json` SHA
  `931e169b5f40c458e654bed87f1ccf4986f91c19e898986d4113c802757cc282`.
  Seven harness leaves changed from064: four test modules, registry, matrix
  and inventory. Independent freeze review `da5ea2d2d0b2642f93a732f4d485128803c29f9f8113d3b5d1cb7b68f0c6bd4c`
  verified this composition. Historical admin8 PASS /2 FAIL remains RED.
- Actual collection28,484 is exactly prior28,480 plus4; inventory13,153
  preserves all prior13,149 full tuples and adds four declarations. Separate
  actual metadata18/18 PASS (JUnit5.932s, outer15.855s) and audit-only report
  166 cases,143 registry entries,164 bindings,113 structural gaps,300 execution
  gaps and all165 required cases NOT_RUN. Independent reviews verified actual
  evidence,1985 raw source files and all15 protected/freeze-copy indexes.
  The private index refresh318a3204…→6e191828… is explicitly reconciled;
  original drift evidence is retained. No full/live baseline or final
  acceptance has run.
- Prior063 tree dbb33e1e425a520d02280a4ea9596c2325ab20b1 retains17 graph
  HTTP nodes and their original9 PASS /8 FAIL. Prior062 retains accepted
  container Sol004 and independent episode FIRSTs; earlier late/incomplete
  handoffs remain historical.064 contains only harness changes.
- Grok's six-administrative-surface map was accepted as a source map with
  partial coverage, not runtime evidence. New isolated S5 resolution-reject
  and S3 knowledge/entity-link tests have independent source review; their
  latest measurement003 found8 PASS/2 FAIL: missing/foreign reject500 responses
  violate ordinary404. Happy reject/replay, link create/upsert and six link
  refusals passed.001 remains10 fixture setup errors before HTTP;002 remains
  7 PASS/3 FAIL with an additional noncanonical-ID audit fixture mismatch.
  Both fixture corrections are separate from unchanged product behavior.
  Sol002 accepted the measured RED evidence only. The two failed nodes stop
  at HTTP status, so their body/state/audit preservation was not reached.
  Tests and bindings are now frozen in064. Full baseline remains NOT_RUN.
- Post064 isolated diagnostics: eight selected tests now pass: S2 entity-confirm
  create/reuse and invalid/missing/foreign refusals; S4 exact duplicate candidate
  and compact-identifier separation; S1 duplicate documents and orthogonal vectors.
  S1 FIRST001 remains two HTTP500 failures. Its test-only closed-second UPDATE
  was uncommitted before the worker-thread request; wrapping that fixture call
  in a storage transaction made FIRST002 pass both nodes, with all assertions
  and product bytes unchanged. The original exception was not captured, so the
  lock/timeout explanation is source-inferred. No product defect is credited
  from this fixture failure, and its downstream oracles were not reached.
  S6 group accept/replay and reject/no-op/invalid/existing-entity/replay also
  passed both existing nodes; separate source reviews and execution verification
  preserve selected own/foreign state and attributed audit limits. Canonical
  Ruff/format checks passed. The four surfaces are frozen in065 with verified
  collection, inventory and metadata; these separate runs grant no combined
  baseline credit. Full baseline remains NOT_RUN.
- Post065 group in preparation: GET admin/entities FIRST002 measured2 PASS
  with independent execution review. FIRST001 stopped at formatting before
  pytest and is retained; the correction preserved the complete test AST.
  KG link FIRST0013 PASS, resolution detect FIRST0022 PASS and resolution
  reject FIRST0023 PASS are preserved. Independent execution review
  `006d159e38aef664d345099025c265fbce15d6fcfe21a23d783519896d20b4d6`
  accepted these eight scoped results and verified the retained first failures.
  Detection FIRST001 stopped at import formatting; rejection FIRST001 had
  three setup errors from a reused immutable fixture source reference.
  Whitespace and fixture-only corrections are separate, with product unchanged.
  Four new cases are composed privately for066; collection/metadata not run.
- Five CLI read paths now have a separate isolated FIRST:22 PASS, zero
  failures/errors/skips, actual outer12.450s. Real argv/parser/dispatch,
  account-deletion/backend lease policy and selected storage paths are tested;
  diagnostics/model report providers remain synthetic. Product and15 protected
  indexes were unchanged. Result `f35cd5691493e1205e39f2b5df29fd924638563a9ec590fd7d1a48e4a477e157`
  remains measured evidence only. Grok lab005 independently verified it but
  returned REVISE: human status omitted fields and events count/populated text
  were untested. A separate repair adds exact human status with real bridge
  lease inspection, unconditional event counts and full populated text, search
  hit text and doctor default flags. Its FIRST23 PASS is retained as
  `186995e7e8e099a07aa6e8ccfc89ed3dcfab283f4d5a8dc754ded4709ce0bb51`.
  Two private captured-output corruptions each failed at the intended existing
  assertion; these are negative-control successes, not product defects.
  Separate real CLI mint/revoke FIRST14 PASS verifies token metadata/hash/TTL,
  preset behavior, target-only revocation, invalid inputs and held-lease refusals
  on isolated accounts. Result `f43b816f389fa4f4f34f48d9bf54dce1246730c8c54b0597df20f1a6804afdf8`.
  Lab006 accepted the repaired read23 source/evidence but found a false-PASS
  in token held-lease refusals: a missing target returned2 even without leases.
  A separate corrected14 PASS (`c98c8cba89f148ed90278f9acb0fbeef91df7c7870d5d92e18f72ee897d57f64`)
  uses an existing unrevoked target, exact RuntimeLeaseError log and no revoke
  invocation. Two private dispatcher-omission controls now fail at code0 vs2;
  lab007 independently ACCEPTed this changed snapshot and all four retained CLI
  controls; review `629605fc854bfe22566187e22e213e047c4db1b3fe36bf276885fee58804ea2f`.
  Seven cases are composed provisionally
  in private066 (177 cases/154 registry entries). Prospective scanner composition
  now has actual full collection28550 (all old28484 plus66) and an external
  inventory13188 preserving old13153 rules. Sol015 independently ACCEPTed actual
  collection/staging evidence; lab013 accepted declarations and lab014 verified
  installation in a fresh clone plus private Git closure. Metadata18 PASS;
  Sol016 independently ACCEPTed metadata and the exact freeze package. Private
  checkpoint066 is published (`8e3d4aa3ea6c6e1068091ddd426f4edc41feaeff5e5e38fa5842daea66703d91`). Original22 PASS and
  lab005/lab006 REVISE are preserved. No live-provider, all-table or full baseline credit.
- CLI backup/verify/export now has a separate first10 PASS, zero failures/errors/
  skips, outer8.036s, source and15 protected indexes unchanged. Real argv and
  storage verify database/manifest bytes, selected copied rows, private modes,
  truthful database-only scope, local plaintext mirror, explicit/latest archive,
  corrupt manifest refusal, selected tenant export and valid-target lease refusal.
  Result `87877dfa85a99f34936641905996f11ebcc07f0c34c17bc25544b7b249b13b6e`;
  lab008 retained REVISE: a nonexistent traversal target could hide missing
  containment checks; preheld backend lock was a redundant wrapper assertion.
  A separate repair uses a viable outside archive with preserved bytes and pins
  only the dispatcher-owned deletion lease. Corrected FIRST10 PASS
  `4395c05114fb8086ce489e7a2305fffd7a108bd62e417c938c65f5c1a7bfbd96`.
  A private helper admitting the outside archive yields genuine verify success;
  the unchanged refusal assertion rejects code0 vs2 (one expected control FAIL).
  Lab009 independently ACCEPTed the changed snapshot and control. No full installation restore,
  off-device/encrypted mirror, all-table export privacy or live acceptance credit.
- CLI keygen/decrypt has a retained FIRST7 PASS/1 FAIL, no errors/skips; lab010
  independently ACCEPTed the instrument and classified the failure as product
  defect `F_crypto_1` / `R10-PROBE-CLI-DECRYPT-FAILED-OUTPUT`: malformed ciphertext
  returns code2 but leaves the destination, blocking a corrected retry via the
  existing-file guard. Keep the failed no-publication assertion mandatory; product
  fix is queued after full diagnostic baseline. Result
  `52a960dc119e2d8b26d75055d275ad02ed25719eee491e5e843128b574b4f47c`.
  Real local OpenSSL fixtures, exact recovered bytes/private modes, key output
  privacy, account-free dispatch and refusal paths were measured; no live or
  remote mirror acceptance follows.
- CLI restore has a separate corrected FIRST7 PASS, covering distinct explicit/
  latest snapshots, selected users after ordinary reopen, pre-restore safety
  users, two external file canaries, confirmation, three active-role refusals
  and corrupt manifest refusal. Original5 PASS/2 FAIL retained: the test's own
  SQLite reader created WAL/SHM beside the completed safety backup. Only that
  reader changed to immutable mode; all test assertions stayed intact. Corrected
  result `9ce3fdf5d8a6cef3bce81082816809d160ded45cdd214ca9ea71296f1e573a03`;
  lab011 independently ACCEPTed the corrected source/evidence and observer fix.
  Lab012 independently ACCEPTed six next-group CLI bindings from25 actual nodes;
  crypto failure remains mandatory. Six bindings and three exact accepted leaves
  were composed as183 cases/160 registry entries before adding nine Obsidian cases.
  Current provisional067 has192 cases/169 registry entries and1996 source files.
  Actual collection28641 preserves all28550 old nodes and adds91; external inventory
  has13216 functions, preserving all13188 old policies and adding28. All29 evidence
  hashes, source and17 indexes verified. Lab020 independently ACCEPTed the combined
  composition and28 policies; its read-only result arrived after deadline and is
  retained as a late source review, with no run/slot credit. A fresh067 candidate
  installs the exact staged inventory and owns its Git/index, with eight flattened
  transitive object stores and no older-store writes. Metadata18 PASS; audit gives
  190 valid bindings,95 classified structural gaps,320 execution gaps,191 required
  NOT_RUN and zero unknown source entrances. Zero unknown is classification only.
  Candidate tree `b570e69c4920916e9f76c4e1f5c2df7607ba6d20`; source1996+Git14 and
  19 total protected indexes verified through package creation. Sol020 reviews the
  metadata/audit/package. Lab021 independently ACCEPTed actual collection,
  installed inventory and Git closure (`5f43c7c34a0e3abdbf72e230d05e8709f6000634c0f3ab40380f54f45db97c70`);
  all25 file pins and two tree identities verified, RESULT acknowledged.
  Sol020 independently ACCEPTed metadata/audit/package (`7b0c8e3a15151c9fcea83b322e3171a34831448affbda369c57aae77cae40036`);
  all21 pins verified. Its late read-only review is retained; actual metadata
  execution deadlines passed. With Lab021 ACCEPT and fresh source/index verification,
  checkpoint067 is published and authoritative:
  `acbc6aab42f268e075cee5a8daca7f077fa2d4ed7dd4d0f54b6f03b383dc1cc3`.
  Full/live baseline stays NOT_RUN.
  Lab017 independently ACCEPTed this six-case composition and16 inventory policies;
  source1995 and17 protected indexes match. This is no whole067 freeze credit.
  A separate new init/install-services leaf has six measured PASS for private
  configuration/secrets, explicit overwrite/symlink refusal and exact two service
  files. External process activation and account-storage access are forbidden by
  the fixture. Lab018 accepted the original source, but FIRST stopped at collection
  on a nonexistent Storage import, with no test bodies executed. A fresh derivative
  imports FridayStorage and strengthens forced credential replacement assertions;
  FIRST6 PASS, source1993 and17 indexes unchanged. Sol019 independently ACCEPTed
  this repair and execution; all14 review pins verified and the exact RESULT
  consumed. Original failure retained; setup stays outside067.
  Sol018 mapped12 remaining maintenance commands: none has a real argv test,
  seven have direct-handler tests and five only provider-level evidence. Add real
  argv/default/output/lease and durable owner/result checks; these are coverage
  gaps, not confirmed product defects. Private map `81eb9beea1e1fbdb255e79d803aadd782f847cc9fa9f0337faab3295c67d38e6`.
  Root added a separate maintenance dispatcher leaf: FIRST65 PASS, zero failures/
  errors/skips (`6428218e29ea96b8d655d768e9838b76e980e2b37bf800fa82123210385dc311`).
  It exercises real argv/main and both OS-backed leases for12 commands, default/
  explicit arguments,14 parser refusals and three handler-error cleanup boundaries.
  Distinct recorder handlers deliberately supply no maintenance data-effect credit.
  Source1997/19 indexes and13 evidence files unchanged/verified. Lab022 independently
  ACCEPTed the dispatcher leaf and FIRST65; no data-effect/binding/capability credit.
  Setup's two proposed bindings partition the six accepted nodes4+2, with explicit
  fixed-settings and no activation/complete-install limits; mapping independently accepted.
  A separate real `dismiss-series-conflicts` FIRST5 PASS now covers show/apply/repeat
  and both role refusals. Real classifier and storage close only the selected owner's
  suggested series neighbour; exact copies, versions, prior human decisions and the
  foreign owner's pair remain unchanged after normal reopen. Output counts and
  actual events match; source1997/19 indexes and13 evidence files verified.
  Result `474ebaa36f2c7633bc8b5d22f14bfaefef46b17d327eaca58623f9d1811697a7`;
  Lab023 independently ACCEPTed this source and execution; all11 review pins
  verified. This leaf is outside067. Three next-group bindings partition the
  accepted setup6 and series5 nodes4+2+5. Lab024 independently ACCEPTed that
  mapping only; nine file pins and tree identity verified, RESULT acknowledged.
  The plan remains outside067, pending composition into a coherent next group.
  Real archive maintenance FIRST8 then measured6 PASS, one product failure and
  one harness failure: the fault fixture's persistent trigger correctly failed
  normal schema validation before reaching the CLI. A fresh derivative installs
  a connection-local TEMP trigger after normal validation; assertions and all
  other functions remain unchanged. Repaired FIRST8 measures7 PASS/1 PRODUCT FAIL
  (`38e5885da38ffcb1447a1f2ea05c70266f77939420c9a650fc3e216120b55ce2`).
  Reindex preserves existing object/chunk vectors with exact owner/all scope;
  relation-date updates preserve foreign/existing dates and transaction history,
  and injected second-write failure rolls back rows/history/event after reopen.
  Source1997,19 indexes and13 evidence pins verified. Lab025 independently
  ACCEPTed this source, retained executions/classification and three new bindings;
  all17 review pins verified and RESULT acknowledged. Still outside067.
  OPEN `R10-PROBE-CLI-DOCUMENT-DATES-LIMIT`: real
  `backfill-document-dates --user OWNER --batch 200 --limit 1` updates three
  owned documents because the limit is checked only after the fetched batch.
  Keep the required expected-pass oracle and both original/repaired results.
  Fix product logic after the full diagnostic baseline; no waiver or GO credit.
  A four-command real graph CLI package covers entity backfill, entity pruning,
  relation suggestions and global exact-copy resolution. FIRST12 originally gave
  10 PASS, one product failure and one oracle failure: accepted-link insertion
  legitimately also fills the document's legacy primary entity pointer/time.
  A fresh oracle permits exactly that owned pointer/time change and preserves all
  other fields, foreign rows and the owner's rejected link. Repaired FIRST12 is
  11 PASS/1 PRODUCT FAIL (`f1fc2fa8524baa157b94bcde1648ebb848107ee8bf71b30c0ffecc511872cd83`).
  It confirms one stale unreviewed entity is softly deleted with a new version;
  human/reviewed/live/permuted/foreign nodes survive, partial-corpus pruning refuses
  without writes, and relation extraction produces only a sourced review candidate.
  Global duplicate clusters retain an earliest winner separately per owner, preserve
  versions for human review and stay unchanged on repeat. A real second-loser
  write failure rolls back the complete component with a sanitized error.
  Lab026 independently ACCEPTed source/run/oracle repair and four new bindings;
  all18 review pins verified and RESULT acknowledged. Original failures retained.
  OPEN `R10-PROBE-CLI-ENTITY-BACKFILL-LIMIT`: `backfill-entities --apply`
  with `--batch 200 --limit 1` creates three entities instead of one. This is the
  same batch-limit defect class in a separate handler; fix after full baseline.
  Private provisional068 composes the ten accepted new CLI bindings and five
  test modules:202 cases,179 registry entries,2001 raw source files. Old case/rule
  contracts and registry AST are preserved; both limit failures remain required
  expected-pass checks. Dispatcher65 adds regressions without body-coverage credit.
  Lab027 independently ACCEPTed composition and25 inventory policies;30 review
  pins verified and RESULT acknowledged. Canonical collection now
  measures28737 nodes =28641 retained +96 new. The externally staged inventory
  contains13241 functions and preserves all13216 previous semantic policies.
  Source2001,19 indexes and29 evidence pins verified; collection is not execution
  of28737 test bodies. A fresh final068 candidate installs the measured inventory
  and owns its Git/index with nine flattened transitive object stores. Metadata18
  PASS; actual audit validates200 bindings with85 structural gaps,320 execution
  gaps,201 required NOT_RUN and zero unknown entrances. Tree
  `1d838b583426670287d82fdbe67c2823e3f012a2`, source2001+Git15 and21 total protected
  indexes verified through private package preparation. Lab029 independently ACCEPTed
  actual collection/inventory/Git;27 review pins,29 collection evidence pins and21
  indexes verified. Received `4c0b70022185635adc82f5d389f5546d8fdd54b5dad8279b6783e52f9f5f5391`;
  late read-only review accepted without execution-deadline credit, RESULT acknowledged.
  Sol022 independently ACCEPTed metadata18/actual audit/private package;
  15 review pins verified, native RESULT consumed. Checkpoint068 is published
  (`d38eaf3b8046b1bd444bb949bb57af399d7de949c667fc1eea5dd47e6e633a7a`),
  replacing067 as private source authority. Full/live baseline and final acceptance
  remain NOT_RUN; all historical product failures retained. Sol021 source authoring
  is received; Sol023 now maps the four remaining CLI service entry bodies.
  Owner-parallel work respected: no rush, duplicate TASK or wake for lateness alone.
  A separate retag CLI FIRST12 measured8 PASS/4 FAIL, no errors/skips
  (`41067250e5eb9cf95bf2e4e842f21f38d3952209f40b936a25b8c2abd2ff30f7`).
  Passing checks cover owned show/apply/private JSONL, exact document versions,
  repeat safety, foreign document preservation, declared/rejected/new model kinds
  through a scripted chat boundary, real disabled-model refusal and learn/rebuild.
  Actual model/transport/live execution and global dictionary tenant isolation
  are not claimed. Source1997,19 indexes and13 evidence pins verified.
  OPEN `R10-PROBE-CLI-RETAG-LIMIT`: batch200/limit1 retags three owned documents.
  OPEN `R10-PROBE-CLI-RETAG-EXCEPTION-OUTPUT`: the CLI prints a model exception's
  contents verbatim to stderr, including a synthetic credential canary in this
  test; no production-secret exposure has been measured.
  OPEN `R10-PROBE-CLI-RETAG-REPORT-ERROR`, independently confirmed as a product defect:
  symlink/directory report targets raise uncaught OSError/IsADirectoryError instead
  of the asserted CLI input-error exit2. Later preservation assertions were not
  reached; do not claim verified failure cleanup or archive preservation.
  These four failed nodes represent three product roots. Lab028 independently
  ACCEPTed source/FIRST/oracles and the required binding, validating the graceful
  exit2 oracle for both bad report targets. All18 review pins verified and RESULT
  acknowledged. Retag remains outside068;
  preserve expected-pass failures and defer product fixes until full baseline.
  Separate import/purge CLI FIRST18 measured15 PASS/3 FAIL, no errors/skips
  (`46999f6230a285a14cff983e8ef79eedeced91836fcfbeba9420b826fe12d0e3`).
  OPEN `R10-PROBE-CLI-IMPORT-RESUME-PREFIX`: with20 already-imported sorted files,
  adding a new21st file then repeating `import --limit 1` reports success/new0/
  duplicates20 and never reaches the new file. Small2-file prefix resumes correctly.
  The fixed20x plan ceiling prevents the advertised resumable batch from advancing;
  keep expected-pass failure and defer product fix until full diagnostic baseline.
  Two other FIRST failures are audit-oracle errors: purge deliberately redacts
  durable audit payloads, while the test expected the full public receipt.
  Fresh private repair changes only that assertion; original FIRST retained.
  Repaired FIRST18 is17 PASS/1 PRODUCT FAIL, no errors/skips
  (`8c58da82b2a2be51e185e9e4e7ede3da2d05367e766113c71126f52bc3be9369`).
  All nine purge nodes now pass real argv/leases, file/vault/raw/KO/version removal,
  private receipt and redacted audit, explicit/inferred owner, foreign preservation,
  repeat/refusals, shared file lifetime, retention/limit and first committed+audited
  deletion before a scripted second vault cleanup failure. No every-table/full-archive
  purge credit. Import eight other nodes pass forced Inbox, provenance/known or
  unknown author, repeat/dry-run/refusals and per-file failure continuation.
  Source2002,21 indexes and13 evidence pins verified; no product or live effects.
  Lab031 independently ACCEPTed source/FIRST/oracle-repair/two bindings;
  24 review pins verified, RESULT acknowledged, receipt
  `6992c2630e8aa651b05f46b752295e01628b5883286539c9b5afbc8d68352f9d`.
  plan `26e7da6d2e6d5d8aa5e34aa6a8cee448e2c27006499f5dc160b4c86b52918ebd`
  remains outside068 and required expected-pass. Lab030 delivered the two model
  CLI handlers' actual provider contracts; six source pins verified, RESULT
  acknowledged. This source map is not executed-model evidence.
  New model-graph CLI FIRST25 measured21 PASS/4 FAIL, no errors/skips
  (`3db7c21530650c42e2ca45049f89a7c48004309265d4f00b478b192e0fbb83d5`).
  Real CLI/graph/storage uses only a scripted chat boundary. Passing paths cover
  grounded suggested queue/show/apply/repeat, foreign graph preservation,
  malformed/ungrounded response refusal, real disabled routers, counted model
  failure, two-vote confirm/reject/unsure/disagreement, actual accepted relation
  endpoints, terminal repeat, private0600 provenance report, review limit1 and
  structural rejection without a model call. No actual model/transport/live credit.
  OPEN `R10-PROBE-CLI-STRUCTURE-LIMIT`: `extract-structure-relations --batch 50
  --limit 1 --apply` scans three owned documents and calls the arbiter three times;
  batch1 control passes. Foreign/unrelated tables preserved before failed bound.
  OPEN `R10-PROBE-CLI-RELATION-REVIEW-EXCEPTION-REPORT`: on a scripted RuntimeError,
  CLI returns1 and preserves the graph but writes its synthetic credential canary
  verbatim into the private JSONL report. No actual production-secret leak measured.
  OPEN `R10-PROBE-CLI-RELATION-REVIEW-REPORT-ERROR`: symlink/directory targets cause
  uncaught ELOOP/IsADirectoryError instead of required input-error exit2. Two failed
  nodes; later canary/archive/lease assertions are not reached and earn no credit.
  These four failed nodes have three product roots; Lab032 independently ACCEPTed
  the new leaf, FIRST/oracles and two required expected-pass bindings. All22 review
  pins verified; RESULT acknowledged, receipt
  `00b4af588c89a769e0f72cab800bac4dc6c187645c39cf14247a66c3ea9d52d3`.
  Plan `387086cd1b8999e972cc4ee5fc575c77d1da2b1937827c9e91d8231048db9ad8`
  partitions extraction9/review16, remains outside068. Source2002,21 indexes and13
  evidence pins verified. No product fixes before the full diagnostic baseline.
  Eval-bootstrap CLI FIRST12 PASS/0 failures/errors/skips
  (`f7847929e7a9d1095cd8d698d31c45ef9084871e2dad36588b31138e1480d28c`):
  actual argv/proposal audit/save/SQLite, owned provenance/repeat/foreign case
  preservation, rejection/disabled/limit and partial model-failure continuation.
  Scripted chat only; no retrieval-quality/live-model credit. Lab034 independently
  ACCEPTed source/FIRST/one required binding;17 pins verified and RESULT acknowledged.
  Command-store CLI original FIRST9 was1 PASS/7 logging ORACLE failures/1 PRODUCT
  failure (`4390206b204e902673684f85d0c51e23316299c71c5bcf1afa1605a51b5e57ff`).
  Fresh harness repair captures real secure logging and expects safe exception
  class instead of private error codes; original evidence remains immutable.
  Repaired FIRST9 is8 PASS/1 PRODUCT FAIL/0 errors/skips
  (`42e6a097f1d2cb197d847459c1dc096132dc15d4bf2033b2da8b169c2de0e1f7`).
  OPEN `R10-PROBE-CLI-COMMAND-KEY-SYMLINK`: provision with a symlink key raises
  uncaught OSError ELOOP from O_NOFOLLOW open instead of graceful input-error exit2.
  Later no-create/canary/archive assertions are not reached; no cleanup credit.
  Eight passing nodes verify actual CLI provision, private modes, authenticated
  runtime readback, store identity/sequence and committed nonce replay after repeat,
  wrong-key refusal, invalid key/store refusals and real backend/kernel writer leases.
  No command execution/full backup-restore/live credit. Lab035 independently ACCEPTed
  source/FIRST/logging repair/one required expected-pass binding;25 review pins
  verified, RESULT acknowledged. Lab033 source map is separate from this review.
  Eval, command-store and model-graph leaves/bindings remain outside068.
  Sol021 data-source author leaf is received and exercised through actual argv,
  Friday storage and two owner-separated external SQLite databases. Original
  FIRST2 was1 PASS/1 ORACLE failure: Astra incorrectly expected SQLite entry when
  invalid declaration only constructs a lazy storage handle. Fresh repair restores
  the original author's False oracle; all other AST is identical to Sol's leaf.
  Repaired FIRST2 PASS/0 failures/errors/skips
  (`5e5b9dc3f33e32e38d6eb6833e46e160f87ed434bdb45477fd71a66381e5941d`)
  covers all five actions, redeclaration/repeat, exact query/schema outputs,
  success-only last-used metadata, foreign rows/bytes and precise refusal paths.
  A separate pre-execution guard refused three Ruff cache files before any tests;
  owned cache cleanup and both outer receipts retained. Source2002/21 indexes and
  13 evidence pins exact. Lab036 independently ACCEPTed leaf/FIRST/repair/one
  required binding;28 review pins verified, RESULT acknowledged. Its functions=11
  describes helper/fixture definitions; the test denominator remains2.
  All remain outside068. CRITICAL logging is intentional: no default-log privacy,
  network SQL provider, live or full-baseline credit.
  Private provisional069 now combines six reviewed CLI leaves and eight required
  cases:210 cases/187 registry entries,2007 raw source files. All202 previous cases,
  prior surface contracts and179 registry entries preserved; all520 product files
  and runtime fixtures match068, including retag's older067 base. The78 prior FIRST
  nodes include68 PASS/10 retained PRODUCT failures; this is not a combined body run.
  Composition `13a75a4e532ba54673b1755e292c66e3d5b2993603d349053ef91c593031d1a3`,
  plan `06595d26d00f425e12b731723be666d17ee12617c326b67885da3a75d125421d`.
  Canonical collection actually measured28815 nodes =28737 retained +78 new.
  External inventory stage13277 functions preserves all13241 earlier semantic
  policies and declares36 new functions. All29 evidence pins/source2007/21 indexes
  verified; collection result
  `81b67cc25a39870e41d1ba3003bc059d36adfd5fe28b39beffc09700fc2fecb6`.
  Lab037 independently ACCEPTed composition/policies;9 direct and6 retained
  review pins verified and RESULT acknowledged. Measured inventory is installed
  in fresh final069; all2007 raw source files match the approved composition.
  Own Git tree `631d9de4e0b0d0fa525aa37473cd97ecd3aaa012` has16 metadata files,
  ten flattened transitive alternates and exactly9 changed paths. Private package
  protects23 indexes and proves raw/Git identity with11 inherited CRLF mappings.
  Metadata18 PASS; actual audit210cases/187registry/208bindings/77structural/
  320execution gaps/209requiredNOT_RUN/0unknown. Proposal
  `/var/tmp/friday-astra-acceptance-checkpoint-069-knpn69m_/proposed-checkpoint.json`
  (`9caa4c1337ac10feeb5ae91d3809c3b002eec7aaf521dd33dd0a1497e4c03946`)
  received independent Sol025 metadata/package ACCEPT;15 pins verified against
  the exact task/proposal/tree. Lab039 independently ACCEPTed actual collection/
  inventory/Git closure;22 pins and all23 indexes verified. Old nodes preserve
  relative order as a subsequence, not a contiguous prefix. Published private
  checkpoint069 (`66823c42ef938a434d4e481c8cc7dc94dbe57b01e505b1f428cb58250349bf29`)
  now replaces068 as source authority; no repeated metadata/collection or deployment.
  Independently, new backup-verification HTTP FIRST11 initially measured6 PASS/
  5 harness-oracle failures. Four expected a public ok field in privacy-projected
  audit; one assumed the fixture's SQLite context closed and checkpointed its WAL.
  Fresh repair closes owned fixture connections and binds exact private audit
  summary to the pre-call expected receipt. Original evidence retained; no product
  changes. Repaired11 PASS/0 failures/errors/skips
  (`97ed7fbdebdda02e85a684019fa0d1915008ac93ffa98b70140ec66089ab2875`):
  actual app/auth/POST/provider, owner/admin repeat, full exact receipt, three real
  corrupt fixtures returning false, four bad-path404 refusals, anonymous401/user403
  before provider, raw rows and backup bytes preserved. Database-only verification;
  no full archive/restore/live credit. Lab038 independently ACCEPTed source/FIRST/
  repair/one required CAP-BACKUP-RESTORE binding;12 pins verified and acknowledged.
  Root scope clarification: the authorized HTTP receipt intentionally includes
  database filename; filename/path privacy claims apply to audit and unauthorized
  refusals, not every HTTP body. New leaf stays outside069.
  Sol023 delivered a pinned source map for server/bridge/up/TUI startup and cleanup.
  Source risks include pre-yield cleanup, bridge CA failure, partial Supervisor
  startup and TUI failed-autosave-before-exec; these are not executed product findings.
  Actual root-authored server/up FIRST2 PASS/0failures/errors/skips
  (`84d09641d6fc06105d9316317ce0d2b09d3dbbb8499a5891b6f04690943b804f`):
  real isolated CLI processes, loopback health/owner identity, kernel lease/PID and
  supervised ancestry/private logs, same-state/different-port duplicate refusal,
  preserved first-server health, SIGTERM group/listener/lease release. Source2002,
  23 indexes and13 evidence pins unchanged;18.02s outer0 and group absent. No
  model/workers/bridge/production calls, concurrent startup or failure-cleanup
  branch credit. Lab040 independently ACCEPTed source/FIRST/two narrow future
  bindings;14 pins and23 indexes verified, RESULT acknowledged within deadline.
  Sol024 TUI source draft received and8 pins verified; native RESULT consumed.
  Root applied only equivalent lint changes before FIRST, retaining original
  source and normalized-AST proof. FIRST5=4 PASS/1 PRODUCT FAIL/0errors/skips
  (`3013a916b7f1de252389153c5648067cacf641b15ec8c80cdb3f65c6690b425d`).
  OPEN `R10-PROBE-CLI-TUI-FAILED-AUTOSAVE-EXEC`: real atomic save refusal leaves
  settings dirty, but _loop still returns START_BACKEND and actual CLI reaches
  _exec_up/execvp. Test captures forbidden exec; no backend is really launched.
  Keep expected-pass failure until post-baseline repair. Later q/error-display/
  temp/lease assertions are unreached and receive no credit. Four passing nodes
  verify quit wrapper restoration, private dirty-save before exact captured argv,
  safe exec-error class and real owned PTY quit with exact termios restoration.
  Source2002/23 indexes/13 evidence pins exact;2.52s outer0/group absent. Lab041
  independently ACCEPTed source/FIRST/classification/one narrow binding;19 pins
  verified and RESULT acknowledged. Source-only review arrived after its deadline;
  accepted without execution-deadline credit or rerun. Outside069 with server/up.
  Sol025 metadata/package ACCEPT is reconciled; owner-parallel work respected.
  Sol026 authored bounded Supervisor partial-start cleanup tests. Full/live
  baseline remains NOT_RUN; no guard stop or wake inferred from lateness.
  New actual CLI Telegram-bridge FIRST8=7 PASS/1 PRODUCT FAIL/0errors/skips
  (`de76ba6c5d4a3d9e2e63b21cb7be720fe2d5ec976e85926135662b6ea41b5a1f`).
  Real parser/config/bridge.run/private SQLite/kernel lease; actual HTTP clients
  use local MockTransport for getMe/setMyCommands and three endless loops are gated.
  Passing paths cover normal/error loop exit, second-client construction failure,
  client/task/queue/lease cleanup, held-lease refusal and three invalid configurations.
  OPEN `R10-PROBE-CLI-BRIDGE-CA-STARTUP-CLEANUP`: invalid owned CA escapes before
  run's cleanup finally; after actual refusal the opened inbox instance remains.
  Later direct SQLite-close and lease-release/reacquisition assertions are unreached;
  underlying handle/lease retention is source inference, not additional measured
  evidence. Fixture fallback closes/releases owned resources after the failed oracle.
  Preserve expected-pass RED; no product fix before full diagnostic baseline.
  Source2002/23 indexes/13 evidence pins exact;3.27s outer0/group absent. Lab042
  independently ACCEPTed source/FIRST/classification/one narrow required binding;
  18 pins verified and RESULT acknowledged within deadline, outside069.
  No real Telegram/backend/model calls, endless-loop body or live-delivery credit.
  At22:53UTC native Sol turn was inProgress, goal null, no guard errors in the
  bounded window; no wake performed and owner's parallel assignment respected.
  New server startup FIRST3=1 PASS/2 PRODUCT FAIL/0errors/skips
  (`25f06a703ba723907dd20a9972c77a55dc8b5c39e2e8d6ba50dd49e828606d2f`).
  OPEN `R10-PROBE-SERVER-PREYIELD-STORAGE-CLEANUP`: Workers.start or MCP.start
  failure unwinds the backend role before permanently closing storage. Both
  measured failure snapshots show final=False, two registered SQLite connections
  still answering SELECT1, lease_active=False, before any assertion or fallback.
  Normal real-app startup/health/shutdown control passes. This is one cleanup root
  with two failure nodes; subsequent permanent-access-refusal/reacquisition checks
  are unreached in failed nodes. Exact owned fixture fallback closes storage after
  evidence capture. Source2008/23 indexes/13 evidence pins verified;5.73s outer0,
  group absent. Lab043 independently ACCEPTed source/FIRST/classification/one
  additional server startup-body binding;18 pins verified and RESULT acknowledged.
  Actual CLI process coverage remains the separate Lab040 accepted path. No real
  MCP subprocess/model/network/production/fullbaseline credit.
  Sol026 Supervisor source received and9 pins verified; root retained the original
  and changed only two child-command constants before FIRST: bounded SIGALRM and
  normal-control pause until actual Supervisor.stop, avoiding an exit/handle race.
  All other AST exact. Actual Supervisor FIRST3=1 PASS/2 PRODUCT FAIL/0errors/skips
  (`71d7cd928448a34cf3b8bf8f7eea0847c0b2d8c17af34dd2750590ed990884b6`).
  OPEN `R10-PROBE-SUPERVISOR-INITIAL-START-CLEANUP`: first missing Popen leaves
  its log open; second missing Popen leaves the already started child running.
  These are two nodes of one cleanup root. Later second-node group/log assertions
  are unreached. Normal SIGTERM stop passes; exact owned fallback runs only after
  snapshots/oracles. Original-leader PGID checks and nonforking fixtures give no
  arbitrary descendant-tree claim. Source2008/23 indexes/13 evidence pins exact;
  0.616s outer0/group absent. Lab044 independently ACCEPTed source/FIRST/fixture
  adjustment/classification/one additional cli:up binding;21 pins verified, RESULT
  acknowledged within deadline. Product fixes remain deferred until full baseline.
  Group070 now composes all six accepted leaves, seven required expected-pass cases,
  32 prior FIRST nodes=26 PASS/6 PRODUCT FAIL,18 functions. This is retained evidence
  from separate first runs, not a combined execution. Old210 cases/187 registry
  entries and all prior contracts remain exact; new totals217 cases/194 registry.
  Composition `19c4a812792f62dd3494980fdff1b3101ea4a12a0848e989d1c286e3ce6509e6`;
  canonical collection actually measured28847 nodes=28815 retained+32 new, preserving
  old relative order. External inventory13295 functions preserves all13277 old
  semantic policies and adds18 explicit declarations. All29 evidence pins,2013
  source files and23 indexes verified;31.60s outer0/group absent. Result
  `17ba52cf454f1fe0a2722004194b5f649562d9820591f32e9d6cb28e6f4afea3`.
  Lab045 independently ACCEPTed composition/policies;12 direct and6 prior-review
  pins verified, RESULT acknowledged within deadline. Actual measured inventory
  installed in fresh final070. Private tree
  `59bc661a12aedaecbe005b2bbc85a18745b1f68f` has2013 raw files+16 own Git files,
  11 flattened alternates and9 changed harness paths. Package protects25 indexes,
  proves every blob/mode with11 inherited CRLF mappings; original indexes untouched.
  Metadata18 PASS (`337ce317fcd8c213c95aea648509aff2d2e80a7ea27000a1afd95dcb4b58b0f5`),
  outer18.76s0/group absent. Actual audit217cases/194registry/215bindings,
  72structural/320execution gaps/216requiredNOT_RUN/0unknown; no product acceptance.
  Proposal `/var/tmp/friday-astra-acceptance-checkpoint-070-7zh1mn6p/proposed-checkpoint.json`
  (`25889debf13038966ceab090981c26c01bb4c6a74a7cb1c866a608322e6438e6`)
  received independent Sol028 metadata/package ACCEPT and Lab047 collection/
  inventory/Git ACCEPT. Respectively17 and14 pins verified; private2029 files and
  25 indexes exact, native result consumed and lab result acknowledged. Published
  checkpoint070 (`cf22da433d02bc61de8d2abb6c24373854a1375ec1b11656d3aa1dec1e9d3626`)
  replaces069 as source authority. No repeated collection/metadata or deployment.
  At23:38UTC Sol's actual turn was inProgress,
  goal null, no guard errors in bounded1000-event window; no wake. Sol027 authors
  four inbox/enrichment HTTP bodies separately; Sol028 queued after027, both actual
  Sol027 source RESULT has since arrived:14 pins verified, native event consumed.
  Actual28-case FIRST measured24 PASS/3 ORACLE FAIL/1 PRODUCT FAIL. Original IDs
  used inb_ whereas product creates inbox_; audit correctly concealed these IDs.
  TestClient loopback IP was also incorrectly expected empty. Fresh repair of five
  fixture/expected constants yielded26 PASS/1 ORACLE FAIL/1 PRODUCT FAIL. The last
  oracle wrongly retained promotion/quality scores excluded by closed audit schema.
  Fresh repair changes only that audit helper; all other AST, including the product
  assertion, remains exact. Final FIRST28=27 PASS/1 PRODUCT FAIL/0errors/skips
  (`f7b2f2ec0c3fe1a35a1ca0c783849ddffdb1819f55bdc2851735f487600e6099`),
  25.73s outer0/group absent;2008 source files/25 indexes/13 evidence pins verified.
  Original and intermediate results are retained unchanged.
  OPEN `R10-PROBE-REENRICH-RESPONSE-VERSION-LINK-DIVERGENCE`: applying enrichment
  returns item.entity_id=None, while the committed row already contains ent_*.
  Two previews were identical/effect-free; apply returned nonempty graph_links.
  Version/lineage/audit/repeat assertions after the response-equality failure remain
  unreached; newest-version divergence is source inference, not extra measured credit.
  Preserve expected-pass failure until after full diagnostic baseline. Lab048
  independently ACCEPTed source/FIRST/repair/classification/four draft bindings
  (7/6/9/6nodes), outside070;13 pins verified. Review arrived33.50s after deadline,
  accepted as source review with no on-time lab execution credit. Received artifact:
  `/home/jericho/.jericho/grok-takeover/ASTRA-091-inbox-enrichment-lab048-received.json`.
  Sol029 mapped eight existing authenticated product-witness consume/purge nodes:
  HARNESS_PATCH_REQUIRED_NOT_RUN,14 pins verified and native result consumed.
  Sol030 now authors the four finite response/durable/audit/foreign-preservation
  additions identified by that map; TASK enqueued only, delivery unconfirmed.
  Owner-parallel work remains respected; no Sol goal or unsolicited wake.
  Separately, new eval-comparison HTTP FIRST12 PASS/0failures/errors/skips
  (`20e459eecb69ab529837d40ed7c057ff1a29d550f299c84fd66b673d49da2a3a`).
  Actual TestClient/auth/evaluation/search/SQLite: owner/admin selected tenant,
  real FTS ablation recall1 with deduplicated arms and underpowered refusal;
  real one-shot passage indexer and real dense retrieval with scripted topic vectors
  measure baseline recall0 versus chunk recall1, exact delta(1,.1,1). Repeat reports
  and stored summaries match, gold/raw/knowledge rows and foreign baseline remain
  unchanged. Anonymous401/user403 before engine, empty gold/disabled arm preserve
  prior receipts; request-bound actor/target audit with narrow body-canary check.
  Source2008/23 indexes/13 evidence pins verified;13.35s outer0/group absent.
  No large-set statistical verdict, live model, global effects or full privacy claim.
  Lab046 independently ACCEPTed source/FIRST/two draft required bindings, six nodes
  each;9 pins verified, RESULT acknowledged within deadline. Scope excludes full
  ENTANGLED/not_measured schema and a direct foreign-read detector: foreign rows
  and summaries are preserved, while read isolation also relies on product scoping.
  These five new functions and12 nodes remain outside070; baseline/final acceptance
  NOT_RUN. No unchanged reruns to obtain broader credit.
  New document-map witness HTTP FIRST23 PASS/0failures/errors/skips
  (`73132cfcde27d6a9d0803b794d485aeb017db88d86abce76539e1b926eefca5b`),
  21.12s outer0/group absent;2014 source files/25 indexes/13 evidence pins exact.
  Actual routes/auth/one-shot orchestration/private receipt/SQLite consume are
  exercised; model attempt/admission and installed-release identity are fixtures.
  Observe returns the exact canonical response and private receipt; consume binds
  its request and both durable terminal rows. Exact replay preserves rows/receipt;
  a second SQLite connection sees committed state (no process-restart claim).
  Tamper/expiry/process drift/candidate rebinding, four non-owner identities,
  request-body refusals and failed receipt-write attempt burn are covered. Six
  product tables, including owner/foreign raw rows, remain exact; audit assertions
  cover append/action/actor/target and content sentinels, not the full payload.
  No independent cryptographic signature recomputation/live/installed/concurrency
  or global privacy credit. A wrapper hash typo failed before product import or
  pytest; original evidence retained, fresh wrapper then made the sole FIRST run.
  Lab049 independently ACCEPTed source/FIRST and two required expected-pass draft
  cases10/14nodes (one shared node), outside070;10 pins verified within deadline,
  RESULT acknowledged. Plan: `/var/tmp/friday-astra-document-map-http-bindings090-001/plan.json`.
  New semantic-supervisor witness HTTP FIRST28 PASS/0failures/errors/skips
  (`3e245aacf3cf3b353ebeb13222ca7eff7a8b003c7a33a5e53220460873df3770`),
  24.93s outer0/group absent;2014 source files/25 indexes/13 evidence pins exact.
  Actual routes/auth/real full-schema SQLite recompute and issue assist/canary
  witnesses, consume them and preserve the exact byte response/durable row on retry.
  Tests independently recompute attestation/consume HMACs with literal domains and
  the fixture database key. Synthetic trace/event seeds and scripted runtime refresh/
  installed identity are fixtures, with no real production/model/installed credit.
  Stale population/hash/registry/budget, forged/bad binding/expired/process identity,
  consumed rebind, non-owner/body and unavailable-runtime refusals preserve witness
  and five data-table baselines including foreign raw rows. Audit append/actor/target/
  request ID/content sentinels are checked; no full audit-schema/privacy/concurrency
  or restart claim. Lab050 independently ACCEPTed FIRST/source/two future
  bindings14/16nodes with two shared positive nodes;9 evidence pins verified,
  within deadline and acknowledged. These bindings remain outside070.
  Mission start/research-candidate HTTP FIRST26 now has24PASS/2PRODUCT,0errors/skips
  (`76f0fcd82f83fd7c8b9ea8577d3bd4e0cca4a52650dcaa5e626a60c2057f0786`),
  21.77s outer0/group absent;2014 source files/25 indexes/13 evidence pins exact.
  OPEN `R10-PROBE-MISSION-START-SHARED-AUDIT-ACTOR`: in a shared archive, starting
  either person- or agent-authored missions as Alice records the archive owner
  as the audit actor. Exact ready/version/error response and stored transition,
  preserved tasks/data and audit action/target pass before the actor assertion;
  subsequent audit privacy and repeat checks in those two nodes are unreached.
  Personal/owner positive controls and all10 research-queue nodes pass. Research
  exercises actual HTTP/ingestion/storage, exact review-only Raw/Inbox provenance,
  response/repeat and same-owner/foreign decoys; no generated research, mission
  execution, live, concurrency or global privacy claim. Original26=22PASS/2PRODUCT/
  2ORACLE is retained: anonymous401 legitimately appends auth.failed. Fresh harness
  repairs only the two refusal oracles, verifies that one safe auth audit and
  unchanged remaining state; mission actor expectations are unchanged. Lab052
  independently ACCEPTed FIRST, repair and two required expected-pass cases16/10;
  14 evidence pins verified, within deadline and acknowledged.
  Plan: `/var/tmp/friday-astra-mission-research-http-bindings092-001/plan.json`.
  Product fixes remain deferred until full diagnostic baseline.
  Sol030 product-witness oracle draft received and hash-verified. Canonical FIRST8
  gives7PASS/1FAIL (`ab5a55a550b6c642c18d472a27d7405bcddb929e09e7f0533f782aa75e777770`),
  10.59s outer0/group absent;2013 source files/25 indexes/13 evidence pins exact.
  Consume/CAS/replay, genuine unused attestation, tamper/server/expiry refusals and
  delegated/dependency guards pass. Purge target rows disappear and exact-content
  same-owner/foreign decoys survive, with exact cleanup digests/residues and safe
  purge audits. A new blanket backup target-ID byte prohibition then fails;
  prior model-advice audit intentionally retains target_id. Lab053 independently
  confirmed ORACLE;10 evidence pins verified, within deadline and acknowledged.
  The failed backup was removed with the
  isolated scratch, so its exact retained-byte location was not observed. Later
  operator backup/full tombstone/second purge checks are unreached. Preserve the
  original FIRST and repair only in a fresh harness; no product defect or PASS
  inferred from this finding. Evidence/classification:
  `/var/tmp/friday-astra-product-witness-http-first092-001/classification.json`.
  Fresh backup observer uses exact Raw/Inbox/audit/idempotency snapshots plus all
  logical table/cell scans: only one owner model-advice audit target may retain
  the inbox ID; raw ID remains absent from copied bytes. Audit/idempotency cells
  exclude source/body fragments; model-body, tombstone and decoy checks remain.
  Intermediate FIRST7PASS/1HARNESS failed because the observer lacked real SQLite
  functions used by external-content search tables; that evidence is retained.
  Fresh second repair registers the three real backup-reader function groups,
  without changing assertions or product code. FIRST8 now has7PASS/1PRODUCT
  (`ee162d2183513f6f480f53d160d9fb64c2d76d67350e02f2e6e3facdd5808f43`),
  10.69s outer0/group absent;2013 source files/25 indexes/13 evidence pins exact.
  OPEN `R10-PROBE-OPERATOR-BACKUP-WITNESS-DECOY-FALSE-POSITIVE`: actual purge/replay,
  exact ordinary backup rows and safe audit lineage pass, but release operator
  `_exact_sqlite_backup` raises `backup_active_secondary_product_witness` for
  surviving unmarked same-owner manual/foreign API decoys. Its guard matches only
  the source_ref prefix, unlike the ordinary backup/witness predicate. Both decoys
  occur in one fixture; their individual rejection was not separately executed.
  Operator backup and later tombstone/second purge checks remain unreached.
  Keep expected PASS; product repair deferred until full diagnostic baseline.
  Lab054 independently ACCEPTed source/repairs/FIRST, the product classification
  and one required8-node case covering consume and purge;14 evidence pins verified,
  within deadline and acknowledged. Plan:
  `/var/tmp/friday-astra-product-witness-http-bindings093-001/plan.json`.
  Regenerate body source map Lab051 received/verified/acknowledged, no execution
  credit. Sol031's seven real AgentRuntime HTTP lineage/idempotency/ownership
  nodes now pass in canonical isolation
  (`45dafb60c48e764e14b0a96ca0dba6086a698060bdd0ea73fcd49009ece14aa1`),
  8.84s outer0/group absent;2014 source files/25 indexes/13 evidence pins exact.
  Only the final model boundary is scripted. Exact appended source/reply/lineage,
  same-operation replay versus new alternative, personal shared-archive ownership
  and exact auth/explicit-deny/missing/empty refusals pass. Refusals observe no
  model calls or message/idempotency changes; they do not instrument agent entry.
  The ordinary-user403 suggestion was correctly replaced by a real explicit
  chat.use deny; builtin users have chat.use. Original seven PASS had a root
  launcher typo LANG=C.UTF-7; preserved as noncanonical evidence. A fresh launcher
  fixes LANG=C.UTF-8 without changing test/product bytes; this actual environment
  defect is the explicit repeat reason. Lab056 independently ACCEPTs the source,
  retained results, environment repair and one required seven-node binding;
  all20 evidence/source pins verified, result within deadline and acknowledged.
  Coherent071 staging contains seven independently accepted source sets
  and14 new cases;132 prior subset nodes total128PASS/4PRODUCT across three roots.
  Group071 is now composed in a private source:2019 files,231 cases,208 registry
  entries. Canonical full collection actually found28971 nodes; external inventory
  has13332 functions, preserving all13295 prior policies and the28847 old-node
  order as a subsequence. New124 nodes/37 functions; eight changed product-witness
  selectors preserve their old identities. Collection35.10s outer0/group absent,
  source2019/index25/evidence29 exact; Ruff and format checks pass. Result
  `/var/tmp/friday-astra-group071-095-001/collection-001/result.json`
  (`9593382096316859d8f964f9ca357ecd2088cbf0a6457e586fa6b9ef64340885`).
  All required RED retained. Lab057 accepted composition/policy (source-only;
  result3.8236s after its actual deadline, no on-time execution credit). Sol034
  independently accepted actual collection and external inventory. Inventory is
  now installed in a separate immutable071 source; Lab059 accepted its inventory
  and private Git closure:tree a0ad1dddbcc4edca556b20583c47621901726545,
  2019raw+17Git files,27 protected indexes,12 flattened alternates. Actual071
  metadata18PASS; audit231cases/229valid bindings,57structural/320execution gaps,
  230required NOT_RUN,0unknown surfaces. Private freeze proposal exists;
  independent Sol035 metadata/audit/package review ACCEPTs exact21pins.071 is
  now the published private authority:checkpoint SHA
  `6d48296ccc5a7381e01c9d6a6a5297f8863cf4bdbd5cdd4732c2a527f1df2b8d`.
  Four complementary reviews (057source,034collection,059Git,035metadata/freeze)
  satisfy publication; no product/deployment changes or full-baseline credit.
  Collection does not execute test bodies or count as the full diagnostic baseline.
  Coverage denominator clarified from the actual classifier: of070's72 structural
  gaps,54 have empty case links;18 already have valid links but require execution
  receipts (17 browser surfaces and chat's isolated-live layer). Do not create
  redundant chat/UI tests solely because structural classification intentionally
  credits deterministic collection only. Empty links include38 Telegram commands;
  Lab055's source map now covers all38 empty-linked commands (plus already-bound
  reminders). It proposes43 existing nodes for22 reusable surfaces and identifies
  16 remaining partial/missing adapter command checks. All provider/test pins
  verified, result within deadline and acknowledged. Those43 existing literal
  selectors now actually pass in canonical isolation:43PASS/0FAIL/0ERROR/0SKIP,
  4.53s outer0/group absent,2013 unchanged source files/25 indexes/13 evidence pins.
  FIRST `/var/tmp/friday-astra-telegram-existing-first095-001/first-001/result.json`
  (`3fef62dd77fa2d1c585dc52d9b9c613b27a6150722ec0e79c6ec84d16c21b12c`).
  Lab058 accepted exact body-supported mapping; its unrelated stale canon field
  is explicitly excluded. Root plan has15cases/22surfaces/41unique nodes,
  42memberships (research/inbox share a selector). Lab061 independently ACCEPTs
  this concrete plan and actualFIRST43:22pins,2013source/25indexes/13evidence
  exact, within its actual deadline. Root072 composition now has247cases,
  224registry entries and2020source files; old231cases and208bindings unchanged.
  Canonical collection actually found29005nodes (old28971 retained in order,
  34new); external inventory13359functions preserves all13332old policies and
  adds27 declared delivery/change/unit/300s64MiB functions. Ruff/format pass,
  32.85s outer0/group absent,2020source/27indexes/29evidence exact. Result
  `/var/tmp/friday-astra-group072-097-001/collection-001/result.json`
  (`3c881871eb89a58fc31401372210108fae1b0c3458b9bb42092f9601b1613de1`).
  Sol036 independently ACCEPTs072 composition/new policy.072 installation and
  freeze were deliberately superseded by073, retaining all072 evidence without
  repeating Telegram FIRSTs.073 changes only the suite revision and the stale
  model-gap kind/close_with; all247cases,224registry entries,320rules and required
  expected-pass outcomes remain unchanged. Sol037 independently ACCEPTs this
  exact delta, with18 pins/source2020/index27 verified. Receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-099-model-gap073-sol037-received.json`
  (`da349af0079191b7a92474ddd411084558ba1dec8b2a3bc3c36b7738a5557422`).
  Actual073 collection29005/inventory13359 matches072, with unchanged old order
  and policies,32.25s outer0/group absent,29evidence pins. Inventory installed in
  a fresh private073 source; Git tree
  `fe9e04d8044b0bb56dd3b9a308e64467a7d57be1`,2020raw+11Git files,13flattened
  alternates,4changedpaths versus071 and141 cumulative paths. Metadata18PASS,
  20.12s outer0/group absent; audit247cases/245valid bindings,19structural gaps,
  320execution gaps/246required NOT_RUN/0unknown. Source/Git/index hashes remain
  unchanged. Freeze proposal
  `/var/tmp/friday-astra-acceptance-checkpoint-073-p3ugl7ua/proposed-checkpoint.json`
  (`97b5aaaa939302bd3eef206d70e57b238829a64b1272cd7c9023549ef9644468`)
  was independently accepted by Lab064 for actual collection/installed inventory/
  Git closure and by Sol038 for metadata/audit/package.073 is now the published
  private authority, checkpoint SHA
  `8458555a5dc2dcce1c6a0da41dd8d4a0c57361420d68da104403448af6ca0a2b`.
  Lab064 arrived51.46532s after its deadline; retained only as read-only source/
  artifact review, with no on-time execution credit. All four complementary
  reviews036/037/064/038 and their receipt hashes are bound in publication.
  Lab062's current baseline command blueprint is accepted with root corrections:
  19UI/mount/chat execution-receipt gaps are not missing runner code or a reason
  to demand their receipts before the first baseline. Its symbolic commands
  are not admission. Lab063 reconciled the remaining model-live gap label against
  actual implemented native handlers; root073 wording and independent Sol037
  retain the three required actual first observations in an exclusive slot.
  No gap is waived and no synthetic138/126 evidence transfers to live execution.
  Canonical host capacity/package/AppArmor namespace prerequisites actually pass;
  cached Syncthing ELF matches the canonical digest. No slot or live/model claim.
  Frozen543product files still match the original001 diagnostic snapshot;13main
  dirty product files differ and remain untouched for later exact reconciliation.
  Baseline inputs now have a separate private model-only configuration:45selected
  settings reproduce the operator file exactly through the canonical reader;
  secondary remains enabled, numeric model endpoints validate. No inference,
  HTTP, production change or slot admission was performed. Golden immutable root
  `8b6a8c13ce54b8b07192cb6f5b820953da4efcb5`, its documented private observation
  (`207aad4458d048c0abd9c52befd05b7b29bda7988e8c62fa62fb3a5b6eac7f66`), and the
  cached Syncthing archive are available with exact pins. Configuration evidence:
  `/var/tmp/friday-astra-baseline-inputs100-_eex7z1l/inputs.json`
  (`cf25c435b1d88d1a7de37d49503ffdc6fb5a404aaff137dbddcfcae5262e1784`).
  These historical golden prerequisites do not assert current production health.
  Lab065 observed four bounded model-catalog GETs returning200 with each selected
  model advertised, including secondary with its selected CA. No inference was
  performed; workload quality and live user scenarios remain unverified. Receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-101-model-api-lab065-received.json`.
  Diagnostic private commit `fa55f43dcaebc62ff4c49eb3e47330fbb61c0dd3` has the
  exact073 tree and all2020 raw files. Initial carrier preparation stopped at an
  incorrect LF-worktree assertion; shipped nested attributes require11CRLF
  PowerShell files. Finalization verified exact raw bytes and the already-bound
  Git mappings without changing source/Git or rerunning tests.
  First actual canonical exact-release stopped before test bodies after4.78s:
  its private PATH hid the installed pinned Node22.23.2 and unrar7.20. Failure
  `/var/tmp/friday-astra-exact073-_z3tu0vb/after.json`
  (`b17980dbe91f87981733d721bc9a98abbb5a16182aea559552aa8e2058c72141`)
  is retained as an environment prerequisite failure. A private bin exposing
  only those two exact existing binaries passed canonical toolchain preflight.
  Environment-corrected exactR2 passed toolchain and Ruff lint, then stopped at
  formatting in `friday/orchestration/transient_web_comparison.py` and
  `friday/organs/mixed_journey/consume.py`, still before test bodies. R2 result
  `/var/tmp/friday-astra-exact073-r2-ots94img/after.json`
  (`37be6db2549801b7e4fded666a45ca1dd3ecf8efbd45cd19d4b914ec3649b452`):
  5.09s,exit1,owned cleanup safe,source/Git/index unchanged. Neither attempt is
  a full diagnostic baseline or a product behavior failure. Both original
  failures remain recorded; no successful retry may overwrite them.
  Root staged only the two formatter outputs and proved exact AST equality.
  Lab066 independently accepted these two layout changes (source-only report
  arrived20.37s after its actual deadline; no on-time execution credit). Fresh
  private candidate `73cd644d72dc0b39123fd816eae7b890d321eb48`, tree
  `b0feac931586a358f600fd81dc78e21bb7584488`, contains exactly those two changes;
  all2020 raw files and30 protected older indexes were verified. Receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-101-baseline-format-lab066-received.json`.
  No product logic, model, test node,
  expected outcome, suite073 revision or gate threshold changes. The eventual
  baseline must name this formatting prerequisite and new candidate identity.
  Sol039 accepts the original carrier identity but returns REVISE for two P1
  launcher faults: cancellation before child registration and host-lock release
  with surviving descendants. Both actual attempts ended cleanly; these findings
  do not assert an observed leak. Original launchers/evidence remain immutable.
  Further heavy runs require repaired ownership and independent control review.
  Receipt `/home/jericho/.jericho/grok-takeover/ASTRA-102-baseline-carrier-sol039-received.json`.
  Sol040 owns a private controller repair with dummy-process cancellation,
  stubborn-descendant and lock-exclusion tests; Astra owns review/integration.
  ExactR3 actually finished on the formatting-prerequisite candidate73cd644:
  non-UI 28760 passed / 140 failed / 2 skipped in 642.13s. No UI phase started.
  Canonical failure also records a scratch cleanup PermissionError; this is
  distinct from assertion failures. All 67 owned commands and the outer owner
  confirm cleanup; source/Git/protected indexes/toolchain are unchanged.
  Observation `/home/jericho/.jericho/grok-takeover/ASTRA-104-exact073-r3-observed.json`
  binds original logs, failure and terminal evidence. Sol043 independently
  partitioned all140 nodes exactly once: 51 environment/precondition, 27 harness,
  5 oracle and57 product-classified nodes, not140 independent product defects.
  Receipt `/home/jericho/.jericho/grok-takeover/ASTRA-105-baseline-triage-sol043-received.json`.
  Root107 corrected two C04 interpretations without rewriting that triage:
  the coverage CLI still has the case_bindings key, but inherits an installed
  selector while importing source, causing runtime_surface_discovery_failed.
  Two isolated audits reproduce source-only success versus installed-claim
  failure (`/var/tmp/friday-astra-coverage107-z86lcbox/observation.json`).
  Root108 scopes that standalone source-audit test's child environment by
  excluding the outer installed selector. Both source and actual-wheel-parent
  controls pass; parent selector and installed origins remain intact. Wrapper
  and gate identity checks are unchanged. Independent review is still pending.
  The advertised harness command
  actually omits31 existing R10 modules; the expectation was correct. Root107
  restored those explicit selectors; its existing plan test passes. Lab078
  confirmed both legacy pin failures are identity lockstep issues. Root108 read
  the111-line battery lifecycle delta, proposed operator+test repinning and ran
  two existing dependency controls successfully. One fresh model-free dense
  measurement matches all historical metrics/result/case hashes; a separate
  current-identity receipt preserves the original historical file and exact
  quality requirements. The changed dense module passes all3 tests, including
  a fresh measured path. Sol049 R1 verified the four metadata/test pins and15
  proposed inventory declarations, but two negative checks compared one Git
  blob with itself and looked for metrics at the wrong JSON level. Root109
  verified explicit commit:path blobs and actual nested metrics. Corrected
  independent review049#2 is ACCEPT; exact executable comparisons and all four
  source pins were verified without repeating the benchmark. Receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-110-legacy-sol049-g2-received.json`.
  The reviewed legacy manifest is:
  `/var/tmp/friday-astra-harness-composition107-lbugfaap/legacy-pins-review108.json`.
  The scratch-total assertion must be reconciled with the final new inventory.
  Fifteen nested product journeys never dispatched; eleven gate phase probes
  failed import identity before reaching intended checks. Lab075 owns bounded
  unchanged pre-request diagnosis of those fifteen, without child dispatch.
  Lab075 could not recover original context bytes or exception details. Root106
  rebuilt the context from the real retained wheel and frozen source: the
  unchanged prefix reaches request-write successfully, with zero requests
  written or journeys dispatched. This does not explain the old exception.
  Evidence `/var/tmp/friday-astra-journey-prefix106-5hhf1a0p/observation.json`.
  Root107 adds a closed literal ValueError code projection and safe pytest
  failure diagnostics, preserving FAIL/executed0/cleanup fencing. Five focused
  fault/privacy controls pass. Sol048 R1's five adversarial failures arose from
  patching a duplicate bare module instead of the called tools namespace.
  Root's separate copy changes only those two imports and all5 cases pass on
  unchanged source. Its other negative projection check lacked a concrete
  witness. Corrected independent review048#2 is ACCEPT: five actual injected
  faults pass, and exact DictComp projection inspection withdraws the second
  finding. All five source pins match; prior plan/coverage positives remain
  pinned without rerun. Receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-109-diagnostics-sol048-g2-received.json`.
  This instrumentation does not establish the original fifteen failures' cause.
  Sol045 delivered isolated gate scratch cleanup and nested phase import repairs;
  all2020 source pins and its16+3 tests were checked. Root's independent synthetic
  replacement control found that cleanup binds the root inode only after the
  body, deletes a different directory placed at that path, and leaves the real
  mkdtemp tree behind. R1 is REVISE, not integrated. A second root control on the
  real retained wheel found import failure: R1 expects tools inside installed
  site, but wheel packages only product namespaces. Its positive fixture had
  copied tools into a synthetic site. Astra's original instruction was imprecise;
  correct authority is source-owned tools plus installed product. Generation2 for
  both fixes was natively enqueued after046 closure. Receipts:
  `/home/jericho/.jericho/grok-takeover/ASTRA-105-gate-harness-sol045-revise.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-106-gate-harness-sol045-revise-addendum.json`.
  Generation2 received/closed107. Root verified2020 source pins and11 focused
  XML controls, independently proved replacement refusal with both normal and
  exceptional bodies, unchanged contents/mode/closed descriptors, and correct
  imports with an actual product-only wheel. Scoped ACCEPT:
  `/home/jericho/.jericho/grok-takeover/ASTRA-107-gate-harness-sol045-g2-reviewed.json`.
  Eighteen tail/config/gate/diagnostics/plan/coverage/legacy paths are independently
  accepted in the private composition; semantic repairs remain outside it.
  No new candidate/suite/gate. The accepted18-path manifest is:
  `/var/tmp/friday-astra-harness-composition107-lbugfaap/composition-accepted110.json`.
  Two read-only manager
  queries failed without user-bus variables and succeeded with the verified
  own-UID XDG_RUNTIME_DIR/DBUS_SESSION_BUS_ADDRESS. This establishes a launch
  prerequisite. Root106 then allocated one canonical scope with the verified
  own-user bus: controller limits and live anchor PID were proven; scope kill,
  empty tree and unit collection succeeded. Evidence
  `/var/tmp/friday-astra-systemd106-n5rj8xo1/observation.json`. This is a resource
  prerequisite PASS, not a retrospective PASS for51 Engineer tests. The next
  launcher must retain verified own-user bus settings. No product fix or full-gate retry
  before harness prerequisites are handled.
  Full diagnostic baseline remains ATTEMPTED_INCOMPLETE.
  Root prepared the R3 launcher and verified six atomic marker/error controls;
  manifest `/var/tmp/friday-astra-exact073-r3-uokto_da/launcher-prepared103.json`.
  Independent review of Sol040 R1 reproduced another cleanup failure path:
  persistent EMFILE escapes recovery, leaves a live owned child and permits a
  competing lock acquisition without a terminal receipt. The probe reaped its
  own child and restored subreaper state. A separate source finding identifies
  inconsistent persisted/returned STOP_UNCONFIRMED flags after marker retry.
  Root REVISE `/home/jericho/.jericho/grok-takeover/ASTRA-103-outer-owner-sol040-revise.json`;
  repair continues as Sol040 generation2, with R1 retained. No heavy-run credit.
  Sol040 R2 is independently ACCEPTED: new repeated census/pidfd/reap/restore
  fault controls and terminal-retry equality passed. Root reran the four original
  cancellation/descendant controls on changed R2 and verified real canonical
  host-slot exclusion through four injected EMFILE failures, then release only
  after ECHILD/restoration. Receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-103-outer-owner-sol040-reviewed.json`.
  A first root lock probe used the wrong duplicate-module exception class;
  its terminal cleanup was clean. The corrected probe uses the lock module's
  actual exception class; original evidence remains classified as probe error.
  Sol041 independently ACCEPTED the exactR3 launcher integration before its run.
  Lab067 mapped current160/A+B candidate construction: isolated outer imports
  must select the candidate source; explicit private config and external run
  directories prevent operator defaults. Worker snapshots are source evidence,
  not installed-wheel acceptance. Root corrected one truncated reported hash;
  scoped receipt `/home/jericho/.jericho/grok-takeover/ASTRA-103-model-candidate-lab067-received.json`.
  Both isolated model entry import probes now select the exact candidate;
  Lab068 accepts entry/import scope. No readiness/model/case execution credit.
  `/var/tmp/friday-astra-model-baseline104-a4qi1x8h/prepared.json` binds both
  observations. Canonical160/native own their host lock internally; no duplicate
  outer acquisition. Canonical160 actually launched through that reviewed entry
  and returned4 before its run directory/readiness/cases. The unchanged pure
  prefix reproduces `acceptance_profile_alias_conflict` with the same exception
  class hash; all160 case outcomes remain NOT_RUN. Original attempt and diagnosis
  are retained under model-baseline104 `160-observation/after.json`.
  Lab071 independently confirms product-effective FRIDAY-first precedence:
  alias normalization alone still fails the obsolete required profile check.
  Model aliases already equal the required model; no model/deployment change.
  Open prerequisite R10-BASELINE-CONFIG-AUTHORITY: bind160/native to the actual
  frozen owner-native configuration without an obsolete profile requirement,
  preserving model readiness thresholds and exact config identity. Receipt
  `/home/jericho/.jericho/grok-takeover/ASTRA-104-config-lab071-received.json`.
  Private repair105 validates explicit config with FRIDAY-first precedence and
  the real registered profiles. A broader regression caught an ambient-env
  selector error (61PASS/1FAIL, retained); the actual CLI now passes the explicit
  selection flag. Legacy direct callers retain the frozen-dispatcher check.
  Config selection5, remaining config/teardown72 and native setup12 pass;
  these overlap earlier controls and are not aggregate unique coverage.
  SGLang readiness now reuses the existing product deployment witness sampler,
  preserving stable epoch, idle/quiet intervals, four classifiers and deadlines.
  Fourteen new controls and47 existing readiness tests pass. Five-file manifest
  `/var/tmp/friday-astra-harness-repair105-pzd15h87/config-readiness-review.json`;
  Sol046 independently ACCEPTED the exact five files with25 focused controls;
  root verified18 evidence pins and closed both assignment profiles. Receipt
  `/home/jericho/.jericho/grok-takeover/ASTRA-106-config-readiness-sol046-received.json`.
  No composition or
  repeated live run credit. Lab074's fresh witness/metrics/server-info/witness
  quartet was independently hash-checked and parsed by root against original
  product code; composition, nonce and seed match. No model restart/change is
  needed for this mechanism. Receipt
  `/home/jericho/.jericho/grok-takeover/ASTRA-105-sglang-witness-lab074-received.json`.
  This is one metadata sample, not full readiness or a restart experiment.
  Lab073 confirms startup duration is not an epoch; its retained bodies are
  verified, but publication was61.789s late. Lab072's missing raw bodies remain
  attributed-only evidence. Original failed160/native outcomes stay unchanged.
  A+B v1 was rejected after an
  actual late-SIGTERM probe disproved Lab069's cancellation rationale. Fresh v2
  adds explicit no-cancel/no-timeout guards, with three exact-predicate controls;
  Lab070 independently ACCEPTED the tiny delta and corrected its069 rationale.
  Official A+B completed through v2: A0PASS/200FAIL, B NOT_RUN after redA.
  Outer ECHILD/restoration and unchanged source/index/toolchain confirmed.
  All200 rows share pass_lifecycle_unreconciled; all10 tails fail database_exact.
  Open prerequisite R10-BASELINE-TAIL-SQL-FUNCTIONS: the bare read-only SQLite
  tail connection lacks required canonical SQL functions. Actual retained DB
  probe fails with missing friday_conversation_passage_anchor_valid, then the
  same digest reader completes after registering the three existing function
  families used by backup verification. DB bytes are unchanged. This proves
  an unreadable tail oracle, not retrospective pre/post equality or PASS.
  Evidence model-baseline104 `ab-control/tail-read-probe.json`; original raw
  attempts remain intact. Root106 reconciled18 additional failing cases:
  two exact CSV requests missed code-owned office routing; thirteen attachment
  marker requests had readable files but not_found/zero model attempts, with
  the same pass07 HTTP reconciliation failure. Root110 reproduced all20 A07
  projection statuses on original73cd with actual generated synthetic file
  bytes and the existing owned-carrier projection. The diagnostic removal of
  a trailing case-control ID restores marker access for6 of13 misses; five
  retain false NOT_FOUND from adjective/surname or TXT qualifier anchors, and
  two become UNKNOWN. Source qualifiers such as test/synthetic/text are being
  treated as required body targets. Preserve real missing-literal refusal;
  classify control-ID interference separately, without changing fixtures or
  product before the corrected baseline. Required subsequent regression scope
  is actual field-target binding, ordinary paraphrases, absent literal controls
  and complete readable attachment delivery, not a larger stopword list.
  Evidence `/home/jericho/.jericho/grok-takeover/ASTRA-110-attachment-anchor-probe.json`.
  Lab081 independently confirms adjective/surname and hyphenated-TXT qualifier
  confusion while retaining genuine surname and missing-quoted-literal controls.
  Its six original CSV classifier samples isolate missing count-action wording
  for A03-07/15 (soschitay/count-of-positions): both lack action/raw-kind matches,
  unlike four controls. Closed office kind is empty in all six and is not itself
  the cause. Add ordinary count wording at the owned count-request boundary
  after baseline, preserving ambiguity, source and exact-output checks. The full
  CSV evaluator was not run by this diagnosis. Lab081 published after deadline;
  its body-prefix retention field is weaker than root110's exact-marker check.
  Receipt `/home/jericho/.jericho/grok-takeover/ASTRA-111-lab081-received.json`.
  Three fallback answers require
  independent semantic-oracle adjudication. These are candidate clusters, not18
  confirmed independent product bugs. Lab076 and root independently adjudicated
  all three fallback answers as responsive; their predicates demand unasked
  concepts or reject harmless explanation syntax. Lab077 delivered its private
  repair on time; root verified source/delta/result pins, but reported339+19
  controls lack retained JUnit pins. Sol047 found12 mismatches in17 independent
  messages: unasked component/user-data conjunctions, negation/contradiction
  leaks and responsive paraphrases rejected. Root accepts these concrete
  findings; Lab077 remains unintegrated. Lab079 returned a three-file repair
  after its deadline; its264 JUnit cases pass, while the separate44 leaf claim
  lacks retained evidence. Root109 reproduced four mismatches in five actual
  evaluate_case controls: a genuine service-collapse negative became positive,
  negated purpose still passes, and a harmless parenthetical user explanation
  fails. Lab079 is REVISE, unintegrated. Lab080 returned its bounded correction
  on time;50 JUnit cases pass, while a separate53-leaf claim lacks retained
  evidence. Sol050 reviewed the final three-file semantic delta:25 required
  cases pass, but3 of8 fresh cases fail. A negated collapse in one clause masks
  a separate affirmative collapse, and a protection claim accepts later
  mutation of the same protected object. Lab080 remains REVISE, unintegrated.
  Sol051 R1 implements these scopes and its actual68 leaf cases pass; the
  manifest mistakenly reports0 because its evidence counter is wrong. Root111
  verified the allowed two-file delta and ran8 independent cases:6 pass, while
  explicit repeated-object mutation and a following-sentence mutation still
  pass incorrectly. A greedy protection/object match consumes the first
  contradiction; sentence-local tracking misses the second. Sol051 generation2
  owns these remaining04 fixes, preserving06/14 and different-object/negated
  mutation explanations. Independent receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-111-sol051-received.json`.
  Sol051 generation2 fixes both prior root failures; root verifies84 existing
  leaf/root controls PASS, but three additional controls regress against080:
  negated isolation becomes positive, the prior protection synonym is lost,
  and an unrelated second-sentence hedge rejects a valid protection claim.
  Actual independent result87=84PASS/3FAIL; REVISE, generation3 assigned.
  Root freezes G2 separately because Sol reused the R1 source path; pinned
  prior patches/receipts remain. Final native metadata has correct source-only
  scope; an earlier unpublished draft was rejected on a changed hash.
  `/home/jericho/.jericho/grok-takeover/ASTRA-112-sol051-g2-received.json`.
  Generation3 repairs these regressions but omits the restored synonym from
  its negated-protection guard. Root verifies the paired counterexample, closes
  G3 REVISE and corrects one guard alternative plus one leaf parameter in a new
  private source; independent Sol052 review is pending. Changed main/leaf and
  root controls:1686 tests,1679PASS/7 environment failures; exact seven pass
  after explicit asyncio plugin and private Git metadata, raw source unchanged.
  Original command failures remain recorded; no full-baseline credit. Receipt
  `/home/jericho/.jericho/grok-takeover/ASTRA-112-sol051-g3-received.json`.
  Sol052 independently ACCEPTED the final root guard/counterexample correction;
  root verified all three cumulative semantic files and composed them with the
  18 accepted harness paths (20 unique changed paths, product unchanged).
  Actual combined main/integrity/leaf checks:1737PASS. A caller placed its own
  selected-collection JSON at untracked source/None; all original file bytes
  and indexes stayed unchanged, and the verified artifact was relocated.
  `/home/jericho/.jericho/grok-takeover/ASTRA-113-semantic-sol052-received.json`.
  One actual full collection found29123 nodes/13391 functions/32 additions.
  Wrapper policy first missed the accepted ambient-env parameterization; exact
  retained-collection reconciliation includes five old parameter changes,
  preserves all13359 old resource policies and all other node rules/order.
  No collection rerun, dropped case or overwritten original failure.
  Private suite074 now installs the measured TSV, corrects stale scratch total
  to39285 MiB, and changes only matrix revision073->074. Metadata19PASS;
  actual audit preserves247 cases/245 valid bindings/19 structural gaps/
  320 execution gaps/246 required NOT_RUN/0 unknown surfaces. Sol053 ACCEPTED
  the exact metadata/inventory transition, independently repeating19 metadata
  checks. Lab084 accepted the private sealer; root actually froze074 candidate
  `31b20769f2e5e87e7196a2818628db7aa36f2760` (2023 files,23 harness changes,
  34 protected indexes; product520 unchanged). Checkpoint:
  `/home/jericho/.jericho/grok-takeover/ASTRA-ACCEPTANCE-074-checkpoint.json`.
  New exact admission binds accepted harness changes instead of old format-only
  assumptions. Historical parent has321 separately pinned preexisting bytecode
  files; original source/Git unchanged, new074 inventory remains strict.
  Positive read-only admission and both160/AB import controls pass; the actual
  frozen model config passes current configured-model validation while the old
  dispatcher checker reproduces its alias refusal. No model/service claim.
  Sol054 ACCEPTED exact admission and five own-copy controls. Lab086 ACCEPTED
  current160/AB entry bindings; existing owner/lock/terminal logic unchanged.
  Root independently accepted Lab085's three exact canonical TXT/DOCX fixture
  plans with fresh identities, isolated HOME and user bus. Exact074 completed
  RED:28945PASS/73FAIL/2SKIP; UI_NOT_RUN. All67 command cleanups and outer cleanup
  confirmed; candidate/source/Git/indexes unchanged. Exact-node comparison:
  72 repeated failures,1 new,68 old no longer failed. Sol055 independently
  classifies57 product-test failures and16 harness failures (15 installed-runtime
  context refusals,1 parallel file-lock collision); two LibreOffice prerequisites
  were skipped. These counts are not independent product root-cause counts.
  Canonical160 completed157PASS/3FAIL with4/4 readiness probes,8 complete passes,
  private evidence and clean reconciliation. Sol056 independently confirms2
  semantic oracle false negatives and1 response missing requested greater-than
  symbol. OfficialA completed177PASS/23FAIL; B NOT_RUN_AFTER_RED_A.
  All10 shutdown reconciliations and outer cleanup clear; A-P07 HTTP-attempt
  reconciliation failed:13 direct attachment/model-route failures propagate
  pass-level failure to7 otherwise successful cases. Two exact-count Office
  requests miss their code-owned route despite correct integer answers; A09-14
  repeats the semantic oracle false negative. Native three-document run completed
  1PASS/2FAIL: first-gen and named-version produced no required artifact;
  named-Cyrillic delivered a DOCX whose3 source lines root independently read.
  Actual native wheel e7344528802b3ed0ce52d56980a23be917adf188fa4cacc5d31845ab449a66e3;
  all3 cleanups/runtime bindings and source/index integrity verified. Lab088 and
  root independently identify the shared product cause: input TXT names become
  extra output candidates, rejecting both the model carrier and fallback. Varied
  fixture wording stays unchanged; missing-file checks must not be weakened.
  Supplemental legacy UI31 and remaining63 now both completed PASS: all94
  current inventory browser nodes, disjoint and exact. The103-node difference
  from non-UI is94 browser plus9 nightly nodes, not103 browser tests. These are
  source diagnostics; original exact-release UI_NOT_RUN and final receipt/GO
  obligations remain. Root receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-119-ui074-all94-observed.json`.
  Corrected diagnostic snapshot is retained separately:
  `/home/jericho/.jericho/grok-takeover/ASTRA-119-baseline074-checkpoint.json`.
  C11 helper repair now independently ACCEPTED by Sol058 in a private leaf:
  both socket protocol and file-lock path are isolated per test; held fake-host
  lock reproduces the old collision and repaired test reaches exact readiness.
  Staged120 in a private mutable075 copy; no prior FAIL rewritten. Sol060's
  repair of the new A09-04 later-contradiction false positive is independently
  accepted and staged121 with the A09 fixes: three accepted changed files total.
  Root checked nine controls through the predicate and public evaluator plus
  both actual074 responses. The alleged A09-14 subject gap was an overstrict
  root negative control: its first sentence already answers the actual question.
  Independent Sol061 agrees; original control retained, correct expectation TRUE.
  `/home/jericho/.jericho/grok-takeover/ASTRA-121-sol060-received.json` and
  `/home/jericho/.jericho/grok-takeover/ASTRA-121-sol061-received.json`.
  Lab089 did not reproduce C02 in isolated serial/two-worker entry captures;
  no original guard/body executed. Lab090's focused15 launcher needed fixes for
  plugin lookup under isolated Python, bounded owned pip and cleanup fencing.
  Root121 corrected those; actual bootstrap plugin lookup passed. Lab091 accepted
  the launcher. Root122 actually ran the unchanged guards/bodies: all15 PASS,
  all15 execution observed,30 matching identity captures and clear cleanup.
  Lab092 independently ACCEPTED this focused installed-wheel diagnostic.
  Original full-group C02 remains unresolved; no exact-release PASS transfer.
  Actual receipt `/home/jericho/.jericho/grok-takeover/ASTRA-122-C02-focused15-observed.json`.
  Earlier investigation receipts:
  `/home/jericho/.jericho/grok-takeover/ASTRA-120-sol059-received.json` and
  `/home/jericho/.jericho/grok-takeover/ASTRA-120-C02-lab089-received.json`.
  One unexpected derived Python cache appeared in frozen074 after Lab089's
  result. Root preserved its exact bytes outside the source, proved its code
  equals the pinned source, and restored strict admission. Sol060 identified
  his old-predicate probe without -I -B as the creator; subsequent imports fixed.
  `/home/jericho/.jericho/grok-takeover/ASTRA-120-derived-cache-recovery.json`.
  Read-only C05 source analysis identifies the two causes behind ten recorded
  HTTP failures: graph-path treats an alias-result list as a dictionary and
  lacks canonical-name lookup; admin reject expects False from a resolver that
  raises ValueError on missing/foreign candidates. The original product stays frozen.
  `/home/jericho/.jericho/grok-takeover/ASTRA-121-C05-source-causes.json`.
  Diagnostic checkpoint122 now records every available contour, including the
  previously preempted15 bodies; separate private product repair has begun.
  `/home/jericho/.jericho/grok-takeover/ASTRA-122-baseline-checkpoint.json`.
  C05 repairs now pass30 source HTTP tests, including all10 prior failed nodes
  and3 new ambiguity/internal-error controls; independent Sol063 ACCEPTED.
  `/home/jericho/.jericho/grok-takeover/ASTRA-122-C05-tests-observed.json`.
  Sol062 maps12 C06 failures into6 roots (11 source-proven nodes,1 hypothesis).
  Origin-preservation repair and three ingestion callers now pass101 focused
  source tests, including3 prior C06 failures and8 new controls. The initial79
  run exposed a linked-Telegram origin overwrite (78PASS/1FAIL), now fixed;
  that source and its failed result remain preserved. Sol064 ACCEPTED source.
  Private Git-index bytes changed during the101 run/review interval; all2023
  indexed paths/stages/modes/blob IDs/flags and source bytes match. The outer
  guard refusal is retained separately; Lab093 independently ACCEPTED the actual
  source-test evidence with this explicit limitation.
  `/home/jericho/.jericho/grok-takeover/ASTRA-122-C06-origin-tests-observed.json`.
  Sol065 delivered the remaining9 C06 repairs; root independent143 source tests
  observed142PASS/1FAIL. Eight prior failures and both new transaction controls
  pass; reenrichment still had a stale version snapshot after graph linking.
  Root transaction/order repair fixes that and passes rollback controls, but
  exposed a previously masked replay refusal (33PASS/1FAIL in focused34).
  Instrumentation identifies an existing-link UPSERT invalidating dependency
  authority until outer commit. Root fixes metadata-only replay separately;
  the final private source passes48 focused tests (43 retained nodes,5 new
  rollback/replay controls), with exact collection, unchanged source/Git/tools
  and clean process ownership. A prior48 run had3 new fixture-ID errors, now
  corrected; its45PASS/3FAIL remains preserved. Independent Lab097 ACCEPTED
  the exact source delta and48-test evidence; no installed/full-gate credit.
  Original failures retained.
  `/home/jericho/.jericho/grok-takeover/ASTRA-124-lab097-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-123-C06-reenrich-v3-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-123-C06-remainder-tests-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-123-C06-reenrich-first-observed.json`.
  Root's separate DOCX input-role fix recognizes the attached-input adjective
  without relaxing output multiplicity/format; all145 focused source checks pass,
  including all139 existing nodes and6 new controls. Lab094 independently ACCEPTED;
  native DOCX must still run on the final installed artifact.
  `/home/jericho/.jericho/grok-takeover/ASTRA-122-WORD-input-tests-observed.json`.
  A07 private query-intent repair now passes298 source checks (259 existing,
  39 new), including all20 original battery questions/files through owned
  synthesis/verifier and no action/web schemas. Request-correlation metadata
  and generic carrier descriptors no longer prove false absence; strong
  literal/name targets remain conclusive, weak misses preserve UNKNOWN and
  bounded evidence. Verbatim output modifiers preserve real cross-file scope.
  Prior290=277PASS/13FAIL and294=292PASS/2FAIL remain preserved. Lab100
  independently ACCEPTED; original live A07 failures require a new run on
  the final candidate, not source-test credit.
  `/home/jericho/.jericho/grok-takeover/ASTRA-124-A07-v3-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-124-lab100-received.json`.
  Lab098/099 supplied pinned source contracts for four CLI limit defects and
  five report/error-handling failures. Root's initial limit repair measured
  53PASS/6FAIL in full four-module57+2 source checks: all4 limit defects and
  both pagination/restart controls pass, six report/secret failures retained.
  The combined private correction now passes138 source checks (136 existing,
  2 new), including all10 repaired C07 nodes and graph-review/CLI-dispatch
  regressions. Invalid report targets refuse before work and close storage;
  exception text cannot enter retag stderr or graph-review reports. Lab101
  independently ACCEPTED; no gate credit.
  `/home/jericho/.jericho/grok-takeover/ASTRA-124-C07-reports-v2-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-125-lab101-received.json`.
  The next private patch fixes three more C07 failures: decryption publishes
  completed plaintext atomically and CLI refuses a concurrently created target;
  invalid key opens produce a domain refusal; provisioning secures SQLite before
  its first connection. All168 source tests pass (163 existing,5 new failure,
  publication-race and permissive-umask controls). Lab103 independently ACCEPTED.
  `/home/jericho/.jericho/grok-takeover/ASTRA-125-C07-private-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-126-lab103-received.json`.
  Import's work limit no longer truncates the scan to its first limit*20 files.
  The existing full read-only plan and ingested-only work counter now resume
  beyond already-imported prefixes; all42 unchanged import/purge/bulk tests pass.
  Lab104 independently ACCEPTED. These separate source patches cover the original14
  C07 failed nodes; composition and final installed gates remain required.
  `/home/jericho/.jericho/grok-takeover/ASTRA-125-C07-import-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-126-lab104-received.json`.
  Lab101's follow-up structure-CLI disclosure is reproduced: both dry-run/apply
  controls exposed raw exception text and incorrectly returned success (0PASS/2FAIL).
  A separate private repair redacts the body, counts object failures separately,
  continues valid later objects and returns a nonzero partial-batch result.
  All140 source checks pass (138 accepted parent nodes+2 controls); Sol069
  independently ACCEPTED. This patch still needs composition and installed gates.
  `/home/jericho/.jericho/grok-takeover/ASTRA-127-C07-structure-repro-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-C07-structure-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-sol069-received.json`.
  C08 chronicle source analysis proves two window mismatches: local-offset ISO
  compared lexically to UTC knowledge timestamps, and an inclusive date-only
  upper bound excluding timed events on that same day. Private repair now passes
  138 source checks (131 existing,7 new): normalize the locally subtracted cutoff
  to UTC; include a full calendar end day in both event page and count. Explicit
  timestamp precision, exact/not-before filters, privacy, ordering and limits
  remain covered, including negative offsets, DST, leap/year/max-date boundaries.
  First137PASS/1FAIL is retained: one new control incorrectly excluded a date-only
  document event after noon; its corrected expectation preserves existing behavior.
  Private draft index stat-cache changes were reconciled against2023 identical
  indexed entries and pinned before tests; source/Git/tools/protected checks and
  cleanup then remained exact. Sol066 independently ACCEPTED this chronicle patch.
  `/home/jericho/.jericho/grok-takeover/ASTRA-125-C08-chronicle-source-causes.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-126-C08-observed.json`.
  Lab105 mapped the remaining seven C08 reminder failures to five source causes:
  dismissed rows consume the visible limit, delivery states restrict dismissal,
  exact clocks disappear from HTTP cards, Telegram callbacks exceed64 bytes,
  and a chat change bypasses a personal dismissal tombstone. Root's runtime repair
  passed159 checks (148 existing+11 race/state/compatibility controls), independently
  ACCEPTED by Lab107. Reminder-only person/key INSERT guards preserve shared-chat
  dedup; cancellation covers pending/sent/uncertain/failed without late-ACK revival.
  Sol067 supplied the list/clock/button repair; root's combined chronicle/runtime/
  views candidate passes295 checks (274 existing+21 new), including all9 original
  C08 failures. Lab108 independently ACCEPTED the view delta and composition.
  Source/Git/tools/protected checks and owned cleanup are exact. Source-only;
  no installed or release-gate credit is transferred from these checks.
  `/home/jericho/.jericho/grok-takeover/ASTRA-126-lab105-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-127-sol066-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-127-C08-runtime-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-lab107-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-127-C08-composed-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-lab108-received.json`.
  C09: the private known-name punctuation repair passes63 checks (55 existing+8
  identifier/alias/person controls), including both original HTTP queue failures.
  Root combined its two leaves with Sol068's five TTL/style leaves on fresh074:
  all134 checks pass (122 existing+12 new), covering four original C09 failures.
  Lab109 independently accepted style/entity changes and the134 proof. Its TTL
  acceptance includes a compatibility flag; root requires a correction preserving
  finite integral HTTP floats and numeric strings while refusing fractional TTLs
  before mint/audit. Sol071's refinement now passes141 checks (122 original+19
  new) on a fresh074 reconstruction; Lab113 independently ACCEPTED. The134
  strict-TTL snapshot remains historical. Integral floats/numeric strings keep
  their HTTP compatibility; fractional/nonfinite/bool inputs cannot mint/audit.
  Parent128 private index drift was preserved; only pinned source leaves moved.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-C09-compat-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-lab113-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-C09-parent-drift.json`.
  Lab109's note that old HTTP accepted bool is incorrect: it already refused bool.
  Sol's private Git-index/cache drift is retained and recorded; only the five
  verified product/test leaves were transferred, without his Git/cache metadata.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-sol068-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-C09-entities-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-C09-combined-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-lab109-received.json`.
  The fifth C09 failure is independently classified as an OCR fixture/environment
  mismatch: the privacy oracle assumes an available optional Tesseract although
  isolated toolchain PATH has none. A separate test-only patch explicitly binds
  the existing bounded synthetic blank-OCR executable, retains the original
  empty-page/privacy assertions, and adds an unavailable-provider control.
  All45 checks pass (44 original+1 new), product bytes unchanged; Sol070 ACCEPTED.
  Actual native OCR remains unproven by synthetic-provider tests;
  the correction requires the next harness revision and final candidate gates.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-C09-vision-fixture-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-sol070-received.json`.
  C10: the private launcher fix keeps the TUI loop and error after failed dirty
  autosave instead of executing backend `up` with stale settings. All27 unchanged
  CLI/TUI checks pass; Lab110 independently ACCEPTED. All seven original C10
  failure nodes now have passing focused source evidence and independent review;
  composition, descendant followup and final installed acceptance remain open.
  Server startup retirement passes18 (12 original+6 new), including actual
  repeated task cancellation with held physical SQLite readers in worker and
  both restart-reconciliation paths. Lab112 found cancellation/premature-close
  defects in the earlier15-pass draft; that draft is retained. The revised owner
  defers cancellation until tracked work drains, final-closes storage before
  releasing the role, and removes both premature nonfinal closes. Lab114 ACCEPTED.
  Bridge37 (36 original+1 new) moves TLS setup into existing queue/lease cleanup
  and clears running state. Cause was run() before its finally, not constructor.
  Earlier36P1F is retained: only the new test's unsupported SystemExit expectation
  changed to the actual OSError contract; original36/product bytes unchanged.
  Supervisor19 (18 original+1 new) checks first/later Popen refusal, all-exit
  process/log cleanup, signals, restart/rotation and private permissions. Lab115
  ACCEPTED bridge37 and direct-child supervisor19, without a no-orphans claim.
  Root's three real inherited-group grandchild controls then all failed after
  normal stop, later spawn refusal and leader crash; original0P3F is preserved.
  A separate repair retires the exact group before forgetting a crashed leader
  and at all-exit cleanup; bounded physical-death checks precede return. All22
  checks pass (19 retained+3 unchanged failing controls), with exact pidfd fixture
  fallback and outer cleanup. Root REVISES Lab117's ACCEPT: its zombie-held
  identity claim contradicts actual Popen.poll()/wait() reaping before killpg.
  The22 result proves descendant death, not safe PID lifetime. Sol075 delivered
  a fresh two-leaf correction: waitid(WNOWAIT) observes without reaping; exact
  group TERM/KILL and physical death precede the retirement marker and final
  Popen.wait. Root's22 checks pass (18 original+4 later controls), including a
  real crashed leader still waitable at SIGKILL and retired before reap. Source,
  Git, tools, protected indexes and owned cleanup are exact. Lab120 ACCEPTED.
  A valid non-UTF8 Linux process name then exposed UnicodeDecodeError in the
  held-leader stop path (0 PASS/1 FAIL). Reading proc stat as bytes fixes it;
  all23 controls pass and Lab121 ACCEPTED the narrow change. Escaped sessions
  remain outside this evidence. Prior nine-leaf
  C10 composition166 remains NOT_RUN and must be rebuilt after that correction.
  The original witness failure was an operator prefix-only backup
  false positive after successful purge, not retained witness material. Lab111
  identified the complete five-field predicate. Sol073's two-leaf repair and
  strengthened true-witness/decoy fixture pass62 checks (all30 original witness
  oracles,30 existing exact-backup/restore checks,2 new controls); Lab116 ACCEPTED.
  The helper's local Friday import runs in the host operator, matching existing
  writer-lease imports; source tests do not prove installed candidate provenance.
  Sol074's A10 correction carries an explicit RU/EN one-sentence less/greater
  pair through the existing single tool-free regeneration and validator. All
  1647 checks pass (1640 original+7 new), including actual scripted chat/Telegram
  rendering and authority negatives; Lab118 independently ACCEPTED. Root fixed
  only the new test's reserved pytest parameter and stable IDs before execution.
  The original160 missing-greater-than failure remains immutable; final live
  model acceptance is still due. No benchmark branch or extra retry was added.
  A pinned17-package map includes full inherited C06/C07 deltas. A private55-leaf
  composition preserves all edits across six shared files; ruff check/format
  pass. Three formatting corrections preserve full Python AST/string values.
  Lab119 ACCEPTED the six-file composition; the49 exclusive leaves comprise47
  byte-identical leaves and2 independently checked AST-identical test formats.
  The updated57-leaf composition ran3121 focused nodes:3120 PASS/1 FAIL,
  cleanup and source/tool/protected integrity clear. The remaining static
  callback scanner did not follow the reminder payload's local variable.
  A test-only correction follows callback assignments in their lexical scope;
  the command oracle now follows the unique awaited F.6 dispatcher with exact
  forwarded arguments. All76 focused checks pass, including9 scanner controls.
  Product source92 is unchanged by this oracle fix; Lab128 accepted the oracle.
  Saved N4/F.6/F.7 work excluded from diagnostic074 is restored in a private
  92-leaf union. Lab124 accepted composition and module-qualified inventory
  accounting: retain13391 baseline policies, add53 saved rules, admit two saved
  parameter expansions and retain074 labels for three consume rules.
  OUTPUT005 REVISE remains open. On unchanged saved92,25 controls returned
  2 PASS/23 FAIL (13 incorrect file-authority flags without actual files in that
  short fixture,10 trusted-table promotions). A first repair then ran797 nodes:
  772 PASS/25 FAIL. Three stronger indentation fixtures produced actual PDFs;
  other failures exposed technical-colon misclassification, legitimate output
  regressions, one obsolete detector assertion and one current-source lookup.
  Post-run admission also found the shared Chromium cache missing; source/Git,
  tool/protected hashes and owned cleanup remained clear. The same browser
  revision was restored and admission passes; Lab126 initially found no actor.
  This797 run remains diagnostic, without gate credit. A revised private source
  preserves raw indentation, distinguishes technical atoms from reporting
  colons and restores contrast/direct-filename/ordinary-modifier output routes.
  Its806-node run returned805 PASS/1 FAIL, with exact selection/JUnit, intact
  source/Git/tool/protected hashes, restored admission and clear owned cleanup.
  All previous F.7 failures pass; the remaining A07 current-attachment lookup
  omits its marker from first synthesis. Two bounded one-node traces confirm
  that a generic noun-relative selector replaces the authorized current upload
  with an empty historical source set. Lab127's static selector finding was
  accepted; its speculative downstream explanation and incorrect JUnit pin
  were not. A private repair preserves current-owned relative references while
  retaining explicit filename/ordinal/among/comparison selector priority.
  Five new real current/history source fixtures accompany unchanged A07 tests.
  Lab129 accepted the exact F.7/current-source code and prospective116 policies;
  installed/live signoff remains open. Exact887-node collection
  completed; the first body launch stopped before execution when a historical
  admission artifact disappeared. Owner relayed Sol's confirmation that his
  cleanup removed both that directory and Chromium; cleanup was stopped.
  Sol restored the observation, and root verified its original SHA-256,
  0700/0400 modes and unchanged full admission. All887 tests then passed in371s;
  exact selection/JUnit, source/Git/tool/protected integrity and cleanup clear.
  Full collection completed29828 nodes. Its parent metadata guard correctly
  rejected one historical F.6 test replacement omitted by the prospective
  no-removal policy. The actual collection was retained without rerun; the
  explicit old-fast-no-status to initial-status-plus-cancel-before-final
  transition preserves its prior policy and single-final/cancellation checks.
  Lab131 independently reproduced the exact staged inventory:13506 functions,
  115 additions plus1 replacement, all13391 prior policy fields preserved,
  only the two admitted saved parameter expansions and journal514→519,
  total41885MiB non-nightly scratch. Lab130's initial driver review is retained.
  A private metadata075 derivative preserves product bytes. Its20 checks pass
  after correcting the historical large-family count27→30 for three declared
  A10 controls (the first19PASS/1FAIL remains recorded). Actual audit verifies
  all245 bindings but discovers one unclassified saved public route:
  `GET /api/me/mixed-deliveries/{message_id}`. Required coverage/case binding
  was mapped by Lab132 into required `R10-MIXED-DELIVERY-HTTP` under
  `CAP-MIXED-JOURNEY`. The actual HTTP suite now passes26 nodes (5 existing,
  21 new): owner/bridge projection,12 refusals and8 oracle controls. The first
  25PASS/1FAIL was a write-injection error: immutable-message UPDATE correctly
  failed; an unexpected owned append now verifies the unchanged-state oracle.
  No product protection changed. Provider fixtures and selected content-state
  coverage remain explicit. Full collection29849/13509 functions preserves
  all13506 prior rules and node order, adding3 declarations/24MiB (41909 total).
  Metadata075v3 passes20 checks; its audit verifies246 bindings across248 cases,
  with0 unknown surfaces,19 structural/321 execution gaps and247 NOT_RUN.
  Lab133 accepted the HTTP oracle, exact case binding and prospective policies.
  Final075 composition is pinned:2037 files,98 changes vs074 (42 product,
  51 test and5 tool files),14 additions/no removals. Product bytes equal the
  887PASS snapshot; only five declared test/metadata files changed since it.
  Lab134 accepted final inventory/composition and the private freeze driver.
  Candidate075 is now frozen as `9c4a5e7a12850e47599f19e108825e720d0f8046`,
  tree `3d12c06d1caa9935ca5650558c5ba80b2870f7e2`: all2037 raw/blob/mode checks,
  11 inherited CRLF mappings and36 protected indexes verified; private only.
  Its first combined change/exact-release execution passed toolchain/Ruff and
  stopped at one mypy error before test bodies. A private one-variable rename
  produces candidate `3a05c390decb65d2f926752df0cae517348aec5a`; canonical
  mypy passes544 source files. Lab137 accepted the static typing repair/rebind.
  Before the full-gate retry, Sol076 independently found two P1
  delivery defects, confirmed by Astra: projection metadata can differ from
  finally authorized sources, and chat.use can use a stale request preset.
  The private repair binds metadata/message/chat.use/source authority to one
  final snapshot and reconstructs closed facts/web fields. New HTTP regression
  controls exercise metadata replacement and late status/preset/grant revocation.
  Focused owned validation passes159 nodes, including24 new HTTP regressions,
  and canonical mypy544. Lab139 accepts the product repair source. The full
  collection29873/13513 preserves all prior13509 rules and node order. Four
  new8MiB declarations raise the exact total to41941; the aggregate assertion
  was updated after the preserved metadata19PASS/1stale-total failure. v2 now
  passes20 metadata checks; audit248cases/246bindings/0unknown still reports
  247 required NOT_RUN. Mandatory mixed-delivery binding grows26->50 nodes.
  Lab140 confirmed the stale aggregate; Lab141 accepted corrected composition
  and private freeze. Candidate076 is now `1b9632aa917fa9a04b2416e707bd4f08527f154e`,
  tree `6e7057f81f6121b8a266b2a3ff8f9e04938b5925`:2037raw/blob/mode checks,
  11CRLF/41protected indexes and no shared source/parent inodes verified.
  Lab142 accepted the actual Exact076 combined-gate binding; all review pins
  were verified. The canonical change/exact-release run finished RED9594e6:
  29737PASS/31FAIL/2SKIP in non-UI; UI NOT_RUN. Toolchain/Ruff/mypy544/wheel
  build, verification and install passed. Cleanup and source/Git/tools/index
  integrity are clear; evidence root `/var/tmp/friday-astra-exact076-ondj5e8e`.
  Fifteen failures share deterministic_gate_runtime_mismatch. Lab145 located
  the controller fence but did not identify the differing runtime field;
  Astra classified that result as partial. Lab147 accepted an owned diagnostic
  that captures real installed-wheel fixture identity before any journey body.
  The corrected diagnostic completed: all15 fixture identities match with two
  workers, installed site present and parent runtime stable. Full-suite
  interaction remains unresolved; these15 diagnostic skips earn no test credit.
  A private coordinated-summary-to-file repair passes191 checks including both
  failed hierarchy cases. Three exact metadata repairs pass: intentional202nd
  mixed-delivery GET, planned mixed-delivery harness module, and a genuinely local
  account in the deletion fixture. Lab146 accepted all four leaves and the
  AST-identical formatting overlay. They are composed into a fresh private
  derivative with a sixth repair: the stale Telegram source-string assertion
  now checks actual /approvals delivery with empty and nonempty pending lists.
  All14 tests of that module pass; Lab148 accepted this replacement oracle.
  The preparation writer-deadline failure also reproduces alone (about2s vs
  a50ms preparation budget); it is not dismissed as full-suite load noise.
  Lab149 confirmed cold connection initialization precedes the reader deadline.
  A private repair bounds connection opening, schema locking and SQLite waits
  with that same deadline, retaining privacy repair and normal source authority.
  It passes160 source regression tests and mypy544; the original50ms/<0.5s
  timing assertion stays unchanged, failed opens leave no registered connection,
  and the following preparation succeeds. Lab151 accepted the three-leaf repair.
  Output-carrier grammar now distinguishes future filenames after "в MCP outbox"
  and "по нему" from source identities. All103 focused checks pass; Lab150
  accepted this repair and its AST-equivalent test formatting overlay. A further
  original reply-document failure also passes. The owned-inbox replay fixture now persists a
  real owned conversation and assistant answer; all12 module checks pass without
  weakening product replay authorization. Lab152 accepted the fixture repair.
  A further private derivative now passes93 checks across six modules. It admits
  comma-coordinated web requests only after a complete direct file-read clause,
  retains quote/reporter/negation controls, and recognizes filename output after
  "по нему/ней/ним" and an explicit file object in MCP outbox. The whole original
  compound-file test now passes. The Telegram retry fixture now serves digest-
  and conversation-bound delivery authority and counts answer chunks separately
  from status messages; disconnect/restart still yields exactly one full answer
  with one chat call and fresh replay authorization. Product transport is unchanged.
  Lab154 accepted these four leaves. A further private repair now recognizes
  complete owned output clauses with the table/document/format before the action,
  chooses xlsx/docx/pdf consistently, and admits generic forward document carriers.
  An extended lineage check exposed a missing word boundary: the filename selector
  treated the initial "о" in "оформить" as the preposition "о". The corrected
  boundary retains genuine descriptive filenames and exact source/deletion guards.
  All551 selected checks pass without skips, plus mypy544; original cases9/10
  pass with additional speaker, filename, lineage and exact outage controls.
  Lab156 accepted the four new leaves. Sixteen of the original31 failures now
  have focused repairs, including the freshly measured current dense receipt.
  Fifteen journey identity failures form one runtime mutation family. The actual
  exact077 run and clean-install reproduction below now identify its cause.
  Lab155 found no proven full-suite mutator; it accepted a private hash-only
  diagnostic design, not a root-cause or repair verdict. A separate harness
  derivative now records case/nonce-bound identity hashes before the unchanged
  mismatch fence. All325 checks across three harness modules pass without skips,
  including absent-site/root/field differences and failed diagnostic writes.
  The public error and stop-before-worker behavior stay unchanged. Lab157 accepted
  these two harness leaves; subsequent exact077 evidence identifies the mutation.
  Case4's current dense receipt predated076 and hashes every Friday Python file.
  After freezing repaired source42c6d063, the actual owned offline measurement
  reproduced all historical metrics:24/24 recall at10/20,0foreign sources and
  unchanged result digest. Only the current receipt changed in final private077
  commit a3cfd7a8aaa377c2304b87b49fe5c8690399ba60, tree6200de0edad4724361de75c064adf275844c007f.
  All3 unchanged receipt tests pass, including independent measurement in a clean
  clone of final HEAD. Historical evidence and quality thresholds are unchanged.
  This measures synthetic archive ranking, not production embedding-model quality.
  Focused tests are not a replacement full gate; Model160 remains NOT_RUN.
  New private candidate exists; source integration remains due.
  Both skipped LibreOffice cases now pass with the existing rootless converter's
  exact bin directory added to the isolated PATH; the next full-gate toolchain
  must bind these runtime inputs. No package install or broad PATH inheritance.
  Lab143 accepted the Model160/A+B/owned import inspector source rebind;
  actual owned import checks now pass for both entries; model execution remains due.
  Three native Word fixtures were generated by the real canonical constructor
  under owned isolation; native plan exists. Lab144 confirmed a missing ownership
  seam: the native child's internal lock cannot protect outer emergency cleanup.
  A private source repair now separates unchanged preflight from the native body;
  the public entry holds its real host lock through failure and final summary
  publication. All228 native/acceptance checks pass without skips, including a
  real kernel-lock contender during publication and the private body under an
  already-held parent lease. Lab158 accepted these two leaves. Two earlier RED
  profiles are retained: a test exception-class mismatch and overlong isolated
  TMPDIR caused their failures; shortening pytest basetemp alone was insufficient.
  The new owning controller now binds native and160 private bodies while retaining
  the real parent lease through cleanup. Actual non-model controls passed:
  import origins, SIGKILL of its own child controller, adoption and reaping of a
  detached TERM-ignoring descendant, lease contention after child death and lease
  reacquisition after cleanup. Lab159 accepted this CONTROLS_ONLY design; actual
  frozen live-mode plan/review and native/model execution remain required.
  Suite revision076 preparation now composes20 changed leaves:4Friday,12tests,
  4tools. Nineteen code/test leaves match existing accepted hashes; the remaining
  matrix change is only075->076. Actual full collection remains identical at
  29873nodes/13513functions; metadata20PASS. Canonical audit retains248cases,
  246bindings,247required NOT_RUN,19structural/321execution gaps and0unknown.
  Lab160 accepted the composition and source-freeze script. Actual two private
  freezes preserved2037raw files,11explicit CRLF/blob mappings,modes,parent Git,
  no remotes/alternates/shared inodes and protected indexes. Final077 differs
  from076 in21leaves:20accepted repairs/metadata plus the observed current receipt.
  Lab161 accepted measured077 and gate integration. Canonical exact077 ran once:
  29754 passed,16 failed,0 skipped; UI did not run after non-UI failure. Static,
  wheel build/verification/install and collection passed. Outer cleanup completed
  with ECHILD and restored ownership; source/Git/tools/Office/indexes were intact.
  Both original076RED and exact077RED remain immutable; their markers are used.
  Exact077 retained evidence isolates20 generated bytecode files in Friday and
  package_broker:547 wheel namespace files unchanged,0missing,20extra. These
  additions exactly explain all15 runtime identity mismatches. The approval-key
  test launched a Python child with a minimal environment that dropped bytecode
  suppression. A private one-leaf repair adds -B to that child and preserves its
  key-loading, dumpability and descriptor-closing assertions. Actual clean-wheel
  comparison reproduced the same20 paths and a following journey's identity
  failure before the repair. With the repair, that test and all16 exact077 failed
  cases pass without skips; runtime identity stays unchanged. The hung-child test
  passes unchanged; full-suite replay is still required to confirm its behavior
  under concurrent load. Strict identity fences and timeout assertions remain.
  Lab163 accepted the cause and one-leaf repair. New private078 is frozen at
  717661f317d81aa96aecb6019d4b8d4253dd1ed7/tree82b6cddca81a698bc3f6a770d71e15149cb7ef5a:
  only the reviewed -B test change and suite revision076->077 differ from077.
  Friday product bytes and measured current dense receipt stay unchanged.
  The freeze preserves2037raw files,11CRLF/blob mappings,modes and46protected
  indexes. Its collection reference is explicitly historical; fresh canonical
  collection is part of the new gate. Lab164 accepted freeze and launch binding;
  admission passed and exact078 completed:29769PASS,1FAIL,0SKIP; UI did not
  run after non-UI failure. All16 exact077 failures now pass. The remaining
  user-deletion oracle compared acting-owner auth timestamps as account effects.
  A forced advancing-clock probe reproduces the original failure; reusing the
  existing all-row snapshot fixes it while retaining HTTP/identity/effect checks.
  The fixed module passes16tests, including the actual write-after403 negative
  control. Lab167 accepted the private test-only repair; it is not yet composed
  into a new candidate. Original078RED remains immutable.
  Supplemental UI execution on the unchanged078 wheel now passes94/94 with
  zero skips. Selection comes from a fresh full29873-node collection and the
  canonical browser inventory (16modules,4loadscope workers), not the legacy
  UI module list. Selection/JUnit and installed namespace match. This separate
  result does not turn the original combined exact gate green or claim its
  parent deadline-ledger evidence.
  Lab162 accepted frozen077 model-entry/AB/inspector binding. Actual owned import
  checks pass for160 and AB with0 model calls; three fresh native077 Word fixtures
  were generated. Actual077 native/160/AB cases were not run. Frozen078 now has
  native/160 execution-mode bindings plus officialAB and import inspector ready
  for Lab165 review. The three previously generated inputs use an unchanged
  constructor and never-executed run ID; no prior live result is transferred.
  Lab165 accepted the078 execution bindings, and actual078 import checks pass
  with0model calls. First native Word3 execution completed1PASS2FAIL with safe
  cleanup. Lab166 independently confirmed the passing DOCX and two product
  defects: the spoken Cyrillic output name was not honored, and a single-file
  generation request wrongly triggered the two-document completeness refusal.
  The latter's automatic harness classification is also incorrect; preserve its
  observed symptoms separately from the product cause. A three-leaf private
  repair now preserves quoted output names, separates all rows from all sources,
  and requires a real command after a named-source lead. Its initialized kernel
  fixture reproduces the original1PASS2FAIL and passes all3 actual DOCX outcomes;
  the full four-module profile passes642/642 with no skips and clean integrity.
  The16 targeted controls overlap that profile and are not additional coverage.
  Lab173 accepted the reviewed core; Lab174 mapped natural-language variants.
  Private native156R3 restores all-my/all-three document sets and English plural
  summaries, admits bounded read-filename-then-create requests, and preserves
  input versus destination names after read+noun prefixes. Reported past/future
  actions no longer authorize a new file; ordinary command and nominal forms
  remain admitted. Retaining all source rows in an output is distinct from an
  explicit numbered-record selection before creation. All5 actual DOCX outputs,
  real catalog selection and the explicit ambiguous-selection negative pass.
  The full four-module profile passes679/679 with exact collection, no skips,
  and clean source/Git/tool/Office/index integrity and owned cleanup. Focused17
  overlap that profile. Earlier676P2F and15P2F repair results remain retained.
  Lab178 independently ACCEPTed the immutable three-leaf repair. Sol review and
  actual native rerun on the final candidate remain required; no integration.
  Lab172 returned late; its versioned-suffix diagnosis is contradicted by runtime
  evidence and corrected fixture results. A separate unquoted read-then-create
  prefix (возьми source.txt и создай...) had a source-level grammar gap; native156R3
  now closes it with real output evidence, without changing all sentence rules.
  A pinned seven-case helper probe confirms the Cyrillic filename is recognized
  in the original request and lost in its file-create projection. In the versioned
  case file_create is unproved, the future output becomes a second source, and
  the all-source detector matches a phrase about all rows inside one document.
  Explicit two-document and quoted-command controls retain their boundaries.
  Official A completed196PASS4FAIL across200cases; B correctly remains NOT_RUN
  after red A. Failures SYN-A03-07, SYN-A03-15, SYN-A09-14 and SYN-A10-07 require
  causal classification; privacy canaries and cleanup are clear. Lab169's bound
  observations confirm two CSV exact-count path defects, including an erroneous
  non-exhaustive refusal. Astra accepted those product findings. Lab177 identified
  missing сосчитай, imperative точный счёт позиций and explicit ведомость target.
  Private CSV157R2 restores these ordinary forms on the existing bounded intent
  arbiter path; trusted complete structure still owns every returned count.
  No battery-control suffix is removed and no source completeness guard changed.
  Six real runtime cases prove16/14/3 with and without control suffixes, including
  durable office_exact verdict and passed verification. Source/quote/account and
  local/filter controls remain; all236 tests across3 modules pass with no skips,
  exact collection and clear source/Git/tools/Office/index and cleanup fences.
  R1 retained230PASS6FAIL: its new Артикул/Количество fixture lacked an authoritative
  known-schema header. R2 uses the actual A03 ID/Статус schema and observes owned
  verdict in durable metadata; product bytes are unchanged betweenR1/R2.
  Lab180 independently ACCEPTed product/source boundaries; Lab181 independently
  ACCEPTed the corrected final fixture and236-test evidence. Final actual configured-model
  rerun and Sol acceptance remain open. A private ten-leaf composition now combines
  native3+CSV2+harness4+authclock1 while retaining all2037 source files. Its first
  combined native/CSV two-module runtime profile passes339/339, no skips, exact
  collection and clear owned execution/source fences; counts overlap prior profiles.
  Lab182 accepted byte provenance. Its final-candidate freezer remains unexecuted:
  the archive-search current receipt hashes all519 Friday Python sources, and the
  product changes require a fresh actual measurement. Prepared R2 freezes only
  premeasurement source079 (final_acceptance_candidate=false), then the bounded
  instrument will emit a separate current receipt before final freeze/gates.
  Lab183 independently ACCEPTed this two-phase procedure. Private premeasurement079
  is now committed as d83271b9f5e0bbdb9226b846d18d98bf45f93920, tree9d8bbde1,
  with2037files/10changed leaves and final_acceptance_candidate=false. The actual
  owned offline dense instrument completed successfully in31.834s on that commit:
  recall@10 and@20 are24/24, MRR916667ppm/NDCG938488ppm,24current-revision and
  dense-evidence cases, zero authorized foreign sources returned. New source digest
  8019af02 is measured, corpus/metrics/results match unchanged historical evidence,
  and all source/Git/tools/index/parentOffice fences and process cleanup are clear.
  This measures synthetic archive ranking/reauthorization, not production embedding
  quality or model-visible output. The separate measured current receipt is ready.
  Lab184 independently ACCEPTed the measured receipt and final freezer. Final079
  is now frozen as7245fe5012643cb91a4bcb5c74ed333996abaad7, tree728e19e4,
  with2037files/53protected indexes and only the measured receipt added to the
  premeasurement source (11total changed leaves versus078). Current admission
  passed all inherited and current source/Git/tool/Office/index bindings. One
  combined exact-release plan (including change tier,20nonUI/4UI workers) is
  prepared and independently ACCEPTed by Lab185. Actual079 exact-release ran
  for70.256s, completed static/build/clean-install and fullcollection, then stopped
  before test execution: `collected function membership differs from inventory`.
  Source/Git/tool/Office/index integrity and process cleanup are confirmed clear.
  This is a harness registration defect, not152 independent product failures.
  Authoritative collection30030 reveals22 newfunctions/152cases plus five added
  parameter cases in two existing functions. Private081 changes only inventory
  and matrix revision:13511 oldrules semantically exact, two parameter hashes/counts
  expanded. Canonical P-table labels were regenerated; F rows are not byte-identical.
  All prior semantic policies are preserved, no scenario removed. The old unparameterized
  exclusion scenario is retained as the first parameter with an identical AST body.
  Canonical inventory maintenance and a fresh private081 fullcollection verify
  all30030 nodes/13535functions and42205MiB declared nonnightly scratch. Lab188
  independently ACCEPTed the repair and freezer. First freeze guard stopped before
  candidate creation because the private index stat cache changed; all2037 semantic
  entries/extensions and all source/other Git bytes remained exact. Both index
  versions were preserved and original pinned bytes restored under index.lock;
  no protection rebind. Lab189 independently reviewed this restoration and current
  freeze/launch bindings. Final080 is frozen as4dbe4a1a66353d7fd04c542f39419b207ff6bab9,
  treefd11a967, suite081,2037files/55protected indexes, only2 metadata leaves changed
  from079. Current dense measurement is retained because all Friday Python bytes
  are unchanged. Actual current admission passed. The independently accepted080
  combined exact-release completed RED:29925PASS/2FAIL/0skip, UI not run after
  non-UI failure. Static/build/wheel/clean install/full collection passed; all
  source/Git/tools/Office/index fences and owned process cleanup stayed clear.
  Two roots: stale41941 scratch assertion versus actual42205, and product selector
  omitted the found-file adjective in “все найденные файлы”. Private suite082
  changes only that allowlist, exact scratch assertion/comment, and suite revision.
  All207 tests across the three relevant modules pass with exact collection and
  clean fences; independent Sol078 review ACCEPTed the repair. Lab193 accepted
  source freeze and current dense-measurement bindings. Actual private source081
  is frozen as0fcaaefc/treec6aa4e4b, suite082,2037files/57protected indexes.
  Fresh synthetic offline dense measurement passed24/24 recall with0 foreign
  sources in33.54s, current Friday source digest8e0c8e51. Source/Git/tools/index/
  Office fences and owned cleanup are clear. This does not measure production
  embedding quality. Lab195 ACCEPTed actual measurement and one-leaf receipt
  freezing. Measured private081 is now frozen as f642da36/treef4cbc46a,
  suite082,2037files/58 protected indexes. It contains the actual receipt;
  full canonical gate/model160/A+B/native3 remain NOT_RUN. Current exact and
  model/native plans received independent Lab198 binding ACCEPT. Actual owned
  inspection verified both160/A+B import origins without case/model execution;
  new app/Telegram integration and the complete runs remain pending. Original
  failed baselines remain red.
  Lab191 resolved Lab190 checker mistakes and ACCEPTed the unchanged080 model
  bindings. Actual owned inspection verified both160/A+B import origins without
  model calls. The all160/A+B/native3 executions remain NOT_RUN, held for the
  known product repair. B still requires green A. No old result or retry credit.
  Required dedicated Telegram round-trip has a new Sol080 source executor, still
  requiring verification/integration and its separate test contour. The owner
  config-path question is pending, no credentials in chat or
  external messages sent. Soak duration/load are now frozen by Astra in private
  `ASTRA-R10-SOAK-LOAD-001.json`:3600s, four distinct accounts,15s cadence,
 1024-request cap (minimum admission981),3720s deadline and3780s outer cap.
  Resource and first-failure limits are explicit. Execution candidate/config
  binding and the actual run remain pending; read-only continuation never proves
  the missing mixed/reminder branch.
  Root now has one pinned child for explicit short/frozen profiles; full mode
  requires actual candidate commit/tree/suite identity. Three actual CLI probes
  rejected detached/full, source mismatch and digest mismatch before runtime
  creation, with clear owned cleanup. This is admission proof, not a soak run.
  END170: Lab201 ACCEPTed the child/load boundary, without approving an absent
  hour wrapper. Sol081 delivered the outcome package and its assignment is closed.
  Root actual short run completed30.02s with37/37 requests,13PASS/1FAIL/7NOT_RUN,
  functionalFAIL(reminder_creation_not_observed) and EVIDENCE_COMPLETE. Independent
  read-only duration continued safely; no mixed-soak or enabled-model credit.
  All source/Git/tool/index/Office fences and owned cleanup stayed clear.
  Root fixed one missing hashlib import in tests;120 focused tests passed with
  exact collection and zero failures/errors/skips in13.81s outer time. Mandatory
  formatting preserves exact AST; these results retain their original byte pins,
  and the final integrated candidate still requires its full canonical gate.
  END172: root connected the actual app runner to a separate isolated-live driver,
  required case, canonical control node, independent receipt audit and frozen load.
  Initial broader profile410PASS/1FAIL found four new modules omitted from the
  final wrapper command. Root fixed the command and retained the completeness
  assertion; focused15PASS (including a new nonfinite-JSON control), no overlap sum.
  A separate root/child run-ID gap was found before hour execution. New child and
  wrapper share a pre-run external expected policy/configuration context. Actual
 30.02s confirmed identical run/policy and retained13PASS/1FAIL/7NOT_RUN with complete
  raw evidence. Four real CLI refusals reject missing/foreign/inside-run context and
  mismatched attempt IDs before application initialization. All owned/source/tool/
  index/Office fences are clear. Lab204 ACCEPTed the coherent integration/context
  source; full successful frozen receipt ingestion, final candidate/inventory/suite,
  concrete reviewed hour plan and actual hour remain unproved.
  Sol082 source guards passed79 offline dispatch/bootstrap/ledger controls; after
  required lint/format corrections the clean snapshot also passed79, exact/no skips.
  These runs overlap and prove no real Telegram activity. Lab205 reviews the guard
  package. Sol082 was consumed/closed; actual Sol083 now implements Telegram registry
  and independent receipt ingestion on the app-integrated base, preserving transport.
  One-character082manifest input-hash typo was retained and independently resolved
  against the actual immutable effects scope, without rewriting the original packet.
  Lab202 ACCEPTed the coherent source+actual evidence package. Root171 prepared
  the frozen-hour runner with canonical candidate admission, concrete independent
  review bindings, one-attempt directory, outer3780/cleanup30 and truthful RED
  completion. Four actual CLI refusals passed before candidate-admission import;
  source/Git/tools/index/Office fences and owned cleanup stayed clear. Lab203
  reviews this new runner. Final hour plan/candidate/receipt and actual hour remain
  absent. Standalone executors do not close battery integration: current081 still
  needs their callable registration, precise case/layer/node bindings and independent
  receipt ingestion; Telegram remains unimplemented in its matrix. The frozen load
  must replace the stale unfrozen scope question without claiming run completion.
  Sol082 immediately
  took the independent Telegram pre-dispatch budget implementation; actual native
  turn is inProgress, no Sol goal. The late081 result arrived normally; no guard
  wake or duplicate task was needed. New private SoakSession signs actual JSON/multipart
  wire bytes, saves bounded private durable attempts, prevents implicit retries
  and observes four distinct actual identities. Four owner chat aliases resolve
  to one user and are rejected. Lab192 accepts the bounded ingress source;
  all48 focused tests pass, including real signature verifier/MockTransport
  negatives, exact collection and clean owned cleanup. No persistent soak credit.
  Sol079 delivered the private app-soak driver and tests; its source assignment
  is received/closed, source acceptance pending. Initial offline mechanics and
  ingress profile passed90 tests. First real short app execution found a false
  positive: the foreign-source oracle treated the exact query echo as leaked
  source content despite count0/items[]. Astra corrected only that verified echo
  exception and added leak/shape controls:99 tests pass, exact collection and
  clean source/index/Office/owned cleanup; counts overlap, do not sum them.
  Actual R2 passed tenant bindings, owned upload/source query/artifact readback
  and foreign refusals, then stopped: public reminder creation with model disabled
  produced no scheduled event. This conflicts with the driver policy requiring
  llm_enabled=False; actual reminder creation/publication and duration soak remain
  open. Both first failed attempts are retained; neither is whole-soak proof.
  Lab196 source ACCEPT missed the two actual failures and was rejected as whole
  executor authority. Lab197 accepts the query repair and confirms the public
  reminder/model-disabled incompatibility. Its proposed product prefetch change
  is deferred until baseline. Sol081's delivered package repairs first-failure evidence and safe
  independent duration continuation, retaining dependent NOT_RUN and no full-soak
  credit from read-only workload. Six real API response fault controls passed
  within105 focused tests; the changed formatted ingress profile passed48.
  These overlapping counts are separate observations, not combined acceptance.
  Root owns actual containment and current candidate/harness integration.
  Lab194 supplied the real Telegram four-leg contract, including USER ingress
  and independent destination readback; a bot token alone cannot supply those.
  Sol080 delivered the source executor/tests. Lab199 source ACCEPT was rejected
  as live safety authority: actual post-spawn fault controls found unowned children;
  call budgets and effective inbox path were also not enforced as claimed. Root
  corrected failed-start/adapter-interrupt cleanup and the effective FRIDAY/legacy
  inbox-path binding. Current57 focused tests pass, exact collection and clear
  owned cleanup; original50-test run retains2 failures. These counts overlap48/52.
  No actual Telegram/network/model calls occurred. Remaining pre-dispatch method/
  call-count limits and startup-effect inventory are required harness gaps;
  Lab200 independently ACCEPTed the52-test startup repair and REVISEd the
  remaining transport limits. Root57 inbox delta remains for final package review.
  `ASTRA-R10-TELEGRAM-EFFECT-SCOPE-001.json` now freezes role/method/chat limits,
  including one declared dedicated-bot command-menu startup effect, typing and
  status edits. No extra/unknown methods or post-hoc call-count enforcement.
  Sol082 coherent implementation task was sent once after Sol081 receipt and is
  actually running; root actual app verification proceeded concurrently.
  Owner dedicated contour path and manual-user/session observer choice remain
  pending. No whole-executor/live acceptance from these local checks.
  Owner asks adequate tooling before the
  global baseline, without multiday polish; improvements unrelated to truthful
  results are deferred. Full scope remains incomplete.
  Lab182 also found a changed CSV private Git index. Its2037 semantic entries and
  extensions were identical; only2035 stat-cache entries changed. Astra preserved
  both versions and restored exact pinned bytes under the standard index.lock,
  without rebinding protection or altering source/staging. All51 composition input
  indexes again match. Writer attribution is unknown; no reviewer is blamed from
  timing alone. The current source identity changed9188276a→8019af02; old dense
  measurements remain historical references, not fresh coverage.
  Lab169 product labels for A09-14 and A10-07 are not accepted: the original questions do not
  by themselves require one particular paraphrase or an angle-wrapped marker
  without code formatting. Lab171 independently confirmed an A10-07 oracle
  contract mismatch: visible safe symbols and marker are preserved exactly in
  one delivered sentence. The comparator adds an unrequested no-code/tag-shape
  restriction. Private harness repair now accepts either glyph order with safe
  code spans only on the glyphs, preserving exact source/render/style, marker
  and single-delivery checks. All1691 offline profile tests pass; Lab175 accepts
  the correction independently. B07/A17 contracts and product are unchanged.
  The original baseline stays red; this is no new full/native acceptance.
  The first160 attempt was rejected before any cases/model calls by an invalid
  plan artifact ID. Lab168 accepted a fresh160-only plan with the canonical ID.
  Its actual run completed158PASS2FAIL/160, exact-once dispatches and4workers;
  readiness probes passed4/4. P06 tenant isolation passed40/40 with2880/2880
  expected zero observations. SYN-A09-06 and SYN-A09-14 failed semantic checks;
  Lab170 source review confirmed false failures on both actual160 responses:
  equivalent no-data-loss/no-prior-run-influence wording is missing from the
  comparator. Private suite079 adds bounded survival wording and single-word
  emphasis normalization, retaining quoted-statement, polarity, source-role and
  positive-leak negatives. Three exact first responses (including distinct
  officialA14) fail the original comparator and pass the repaired comparator.
  The1728-test offline profile passes without skips, with exact collection and
  clean source/Git/tool/Office/index integrity. Lab176 independently accepted the
  actual responses and distinct officialA14 contract. Astra rejected its dismissal
  of truthful hypothetical carryover wording. Suite080 now distinguishes a bounded
  complete если бы/past-бы example from an actual data-leak assertion. The real
  fresh-database claim must remain outside; factual/mixed/old-database claims stay
  red. Original079 rejects base+counterfactual,080 accepts it and retains all three
  first-response repairs. All1748 profile tests pass with exact collection and
  clean fences; Lab179 independently ACCEPTed this bounded correction. Scope is
  single-clause consequence with comma, not unrestricted language parsing; masculine
  reflexive просачивался remains a known unsupported hypothetical variant. Earlier
  R1 reflexive-consequence failures are retained. Profile counts overlap; no final gate credit.
  Cleanup/lease reacquisition and all pinned
  identities remain clear. The original rejected attempt is retained separately.
  Final Sol review, canonical gates and live acceptance remain required.
  Sol076 RESULT was consumed and its peer assignment CLOSED. The old deferred
  TASK must not be restored or executed again. Owner explicitly confirmed Sol is
  free on2026-09-11. Reservation cleared; Sol078 ACCEPT was consumed and closed.
  Sol079 source RESULT is hash-verified and both native peer records closed.
  Sol080 source result is hash-verified, consumed and closed in both profiles.
  Sol081 app-soak outcome package was sent once after receipt; no Sol goal.
  Actual Sol081 native turn is inProgress, queue empty, no Sol goal; receipt
  is confirmed from turn state. Root Telegram offline profile passed48 tests
  in3.56s with exact collection and clear owned cleanup; no live/process-main proof.
  The prescribed baseline runs and supplemental UI results are retained at
  `/home/jericho/.jericho/grok-takeover/ASTRA-153-frozen078-baseline.json`.
  Baseline verdict is NO-GO with failures; overlapping cases must not be summed
  or substituted across runs. Product/harness repairs and final acceptance remain
  open; old57 collectors omit saved work. No integration or GO. Current evidence:
  `/home/jericho/.jericho/grok-takeover/ASTRA-169-checkpoint.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-139-sol076-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-139-mypy-typing-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-131-lab117-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-131-A10-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-131-lab118-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-131-integration-input-map.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-131-composed-prepared.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-132-sol075-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-132-C10-identity-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-132-lab119-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-132-composed57-prepared.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-132-inventory-policy-draft.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-lab114-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-lab115-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-C10-witness-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-lab116-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-C10-descendant-repro-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-C10-supervisor-groups-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-130-C10-composed-prepared.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-C10-server-v2-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-lab112-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-C10-bridge-v2-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-C10-supervisor-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-129-lab111-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-C10-tui-observed.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-lab110-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-128-C10-source-notes.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-124-lab098-received.json`.
  `/home/jericho/.jericho/grok-takeover/ASTRA-124-lab099-received.json`.
  No product integration/deployment or final acceptance yet.
  Original104/074 outcomes remain immutable. Finish focused instrument repairs;
  full canonical gates and live acceptance must run on the final artifact.
  `/home/jericho/.jericho/grok-takeover/ASTRA-114-exact074-prepared.json`.
  Collection preparation reconciles all22 parameter payloads moved across four
  old and two new semantic test functions; no payload is lost. Only those four
  existing functions may refresh parameter count/node hash; all old resource
  policies and other exact rules remain. This is prospective policy, not an
  actual new collection or semantic acceptance:
  `/home/jericho/.jericho/grok-takeover/ASTRA-112-collection-policy.json`.
  Lab082 independently maps old launcher pins and required new candidate/suite/
  admission bindings; read-only preparation, no repeated baseline or model run.
  The previous final-review receipt is:
  `/home/jericho/.jericho/grok-takeover/ASTRA-110-semantic-sol050-received.json`.
  The original Lab080 receipt is:
  `/home/jericho/.jericho/grok-takeover/ASTRA-110-lab080-received.json`.
  Original REVISE receipt:
  `/home/jericho/.jericho/grok-takeover/ASTRA-109-lab079-received.json`. Keep
  meaningful negation/contradiction controls and the actual questions; no answer
  whitelist or automatic original PASS. Adjudication:
  `/home/jericho/.jericho/grok-takeover/ASTRA-106-ab-semantics-lab076-received.json`.
  Original extra-failure evidence:
  `/home/jericho/.jericho/grok-takeover/ASTRA-106-ab-extra-findings.json`.
  Repair105 adds the three canonical connection-function registrations while
  keeping the entire logical digest and read-only/error/close behavior. Four
  current-schema controls pass (unchanged; post-close row mutation; missing;
  corrupt), as do58 existing integrity tests. Sol044 independently ACCEPTED
  exactly the two changed files. Manifest
  `/var/tmp/friday-astra-harness-repair105-pzd15h87/tail-repair-review.json`;
  root receipt `/home/jericho/.jericho/grok-takeover/ASTRA-105-tail-repair-sol044-received.json`.
  The combined candidate, new test inventory and suite revision remain pending.
  Native Word's three original TXT inputs, expected DOCX names/content and all
  run-id/base/context arguments are frozen in
  `/var/tmp/friday-astra-native104-suyq9jn9/plan.json`; Sol042 independently
  ACCEPTED that concrete plan. Actual native CLI returned5/native_setup_failed
  before run directory, wheel, model calls or context creation. Its unchanged
  pre-directory AST prefix reproduces acceptance_profile_alias_conflict. All3
  remain expectedPASS/observedNOT_RUN. Native104 `observation/after.json` binds
  the attempt and diagnosis. Full baseline and final acceptance are INCOMPLETE.
  Two selected partial approvals/help controls do not close their missing positive
  contracts. This is command/callback adapter evidence using scripted backend and
  transport fixtures; actual backend HTTP/storage/model/live behavior is not proved.
  Source map: `/home/jericho/.jericho/grok-takeover/ASTRA-094-telegram-map-lab055-received.json`.
  Sol033 delivered34new literal nodes for those16 missing/partial command paths.
  Root read the source and applied only AST-equivalent formatting before FIRST;
  new34 plus unchanged approvals-group control all passed:35PASS,3.23s outer0,
  group absent,2014source/27indexes/13evidence exact. Lab060 independently ACCEPTs
  source/FIRST/format proof and one120s required expected-pass case. Evidence:
  `/home/jericho/.jericho/grok-takeover/ASTRA-096-telegram-gaps-lab060-received.json`.
  Signature checks are shape-only; outgoing chat destination is not asserted;
  reset knowledge preservation is response text only. Paired help flags do not
  cover every combination. This is adapter evidence, not HTTP/storage/live proof.
  Combined future Telegram batch:16cases/38surfaces/76unique bound nodes;
  existing helper-only status test remains in the full inventory. No product
  repair precedes the full diagnostic baseline, which remains NOT_RUN.
  Sol032's source map confirms the actual create_app mount is traversed by the
  existing dashboard Chromium fixture and owned cleanup. Reuse R10-UI-DASHBOARD
  for api:MOUNT /admin by extending rule/case/registry surfaces, with no new node.
  Mapping accepted for next composition; actual user-ui receipt still required.
  Its two reported function hashes are inventory parameter-set hashes, verified
  under that actual algorithm; source bytes were pinned separately.
  No live/browser receipt or underlying product credit follows from this mapping.
  Full/live baseline and final acceptance remain NOT_RUN; no unchanged reruns.
- Source-discovery repair is required: the first APIRouter prefix was incorrectly
  applied to every router in a module, inventing user mission/cancel. Sol009's
  draft corrects that identity (+37/-1 source routes), but independent review
  found remaining conditional-body, alias/shadow/rebinding and **kwargs gaps
  despite17 focused PASS. Sol010 fixed those cases but independent source review
  found three further concrete stale-prefix omissions: assignment RHS walrus,
  partial try-body changes before except, and loop-carried router changes.
  Root reproduced all three with extracted stdlib-only scanner helpers; no
  product snippets were executed. Exact current-source228 identities remain
  unchanged. Sol011 closes R4/R5/R6 independently, but new source-bound probes
  find R7 definition-time default walrus and R8 break/continue tail fallthrough
  still invent old/hidden and omit the new receiver prefix. Root review
  `dd0651d4ffa29923b02fc16c828fdac51124dbcd6ead4417573adec6004fe771`.
  Sol012 gets exact snippets with a small conservative rejection scope.
  Sol010/Sol011 FIRST remain NOT_RUN after concrete pre-test source blockers.
  Sol012 closes the simple boundaries but still skips transfers in nested loop
  `else` blocks and evaluated lambda defaults. Astra's private derivative changes
  two helpers, adds three regressions and preserves all prior test ASTs, current
  ordered228 routes and15 protected indexes. Proposal
  `9e76c313c277efd9c2bd3387d835349ad4c250c83a35b4055ae1c67f48507490`;
  Sol013 independently ACCEPTed the root source change, review
  `400d6d142cee109a6251dc829b81f45878c96e9ffa63e51763eaa707d59f47fe`.
  The first attempt stopped before pytest on Ruff SIM114; that failure is retained.
  A one-function merge of identical assignment branches was independently ACCEPTed
  by Sol014. Changed FIRST32 PASS, zero errors/skips, result
  `0fa6fd2529a4aff2aaee07b3f750be089440829f2c87a8c1985da3ee526508f5`.
  The scanner is composed in a separate prospective066 source with unchanged
  registry154/cases177. Sol015 ACCEPTed execution evidence; lab013 accepted35 new
  inventory declarations and lab014 the fresh installed source/Git closure.
  A private Git write-tree failure from nested alternates was retained; flattening
  only the new store dependencies repaired it without touching older stores.
  Final candidate tree `36197ecd83c0f2170110324f3c017071049b4481`; metadata18 PASS
  gives175 valid bindings,101 classified structural gaps,299 execution gaps and
  176 required NOT_RUN. Sol016 ACCEPTed the final package and metadata; together
  with lab014 ACCEPT this permits the published private checkpoint066. Its final
  report explicitly retains21 additional unknown Obsidian API entrances.
  Fullcollection28550 does not execute test bodies or establish a full baseline.
  Source and15 older protected indexes stayed unchanged.
  No real surface or required coverage is waived;
  066 was published with these findings;067 now supersedes it. The battery remains INCOMPLETE.
- `R10-PROBE-OBSIDIAN-SOURCE-SURFACES` OPEN: corrected source discovery exposes
  21 unclassified Obsidian API entrances, additional to101 classified gaps.
  All21 match real router registrations. A private mapping identifies20 existing
  router/server test nodes; lab015 accepted this partial source index. The isolated
  FIRST executed all20:20PASS/0FAIL/0ERROR/0SKIP, outer6.883s, source and17 protected
  indexes unchanged (`70412bd45f676ac17f8e0287a1447c21d3555b5d272d015d58e4e74e2029d3ef`).
  Lab016 independently ACCEPTed this limited execution evidence; its report
  preserves a stale pre-run labeling note without overriding measured20 PASS.
  Sol017 supplied an additive leaf for malformed-input, owner/auth refusal and
  all21 enabled/disabled registrations. Astra independently reviewed its source;
  isolated FIRST66 passed66/66, zero errors/skips, outer12.900s, source1993 and17
  indexes unchanged (`0a0abc5c4df9943dfb1d588bef8952c529aa88738127dfc0eae19c748604951a`).
  Lab019 independently ACCEPTed FIRST66 and nine cases partitioning86 measured
  nodes (existing20+new66), binding all21 previously unknown routes and the existing
  health projection. Candidate067 metadata now validates these bindings and finds
  zero unknown entrances. This boundary coverage is independently accepted in
  published067; full/live execution remains separate. Rules retain existing
  CAP-OBSIDIAN beta obligation; every new deterministic case stays release_required.
  Controlled runtime/auth fixtures prove routing/owner/body boundaries,
  not durable vault effects; HTML/JS assertions do not prove browser/Android launch.
  Preserve CAP-OBSIDIAN beta and owner-parked physical Android scope.
- Required baseline edge probes remain queued, not waived by narrow positive
  cases: `R10-PROBE-CONTAINER-PARENT` (parent validation follows entity creation)
  and `R10-PROBE-GRAPH-PATH-ALIAS` (route treats alias lookup list as one entity).
  The parent-validation probe is still NOT_RUN. Graph-path now has a retained
  isolated first diagnostic slice; product changes remain deferred. Details and
  exact source hashes: private `astra-acceptance-086-baseline-edge-probes.json`.
  Grok source-map job `job_6cae6c1e0d4d44eda9ae5d9f9316a8e6` completed
  within its actual acknowledged window; source hashes independently checked.
  Exact mounted entrance is `GET /api/kg/graph-path`; no existing test hits
  that HTTP route. A new private HTTP file now exercises 17 exact nodes.
- `R10-PRODUCT-GRAPH-PATH-RESOLVER` — confirmed HTTP defect, OPEN. First
  isolated slice on unchanged061 product: **9 PASS / 8 FAIL**, zero errors/skips,
  pytest15.24s. Four name/alias cases returned500 instead of200; four missing/
  foreign endpoint cases returned500 instead of404. ID paths/directions/depth,
  disconnected results, validation and authentication passed. This is one
  resolver failure cluster, not eight independent bugs. Source shows alias
  lists used as entity dictionaries and no name lookup; the exact exception
  was not captured, so that root-cause attribution remains source-inferred.
  Selected entity/relation rows stayed unchanged. Preserve this first result
  before a separate product repair; no xfail, expected500 or ID-only waiver.
  Evidence: private `friday-astra-graph-first087-drys1xqm/graph-first-result-087-001.json`
  SHA `69a59ed4ce2fb3958417981028905713ef0fae1e6ba3ac40b508e2281c19aacb`.
  New tests and their binding are frozen in063; full baseline remains NOT_RUN.

- `R10-PRODUCT-ADMIN-RESOLUTION-REJECT` — confirmed HTTP defect, OPEN.
  Corrected-fixture diagnostics002 and003 returned500 instead of404 for missing and
  foreign resolution candidates. These are two manifestations of one failure
  cluster. Source shows an uncaught resolver ValueError; no exception trace was
  captured from the HTTP500 body, so this cause remains source-correlated.
  Product unchanged; ordinary404 assertions remain required. Full baseline NOT_RUN.
  Evidence: private `friday-astra-admin-links-reject-first088-tf1pw4_8/first-result-088-002.json`
  SHA `0e60ab503c4b37e9b1be1065b5d9b14392a0d9bf508ecdbfdb43b010493c6ae3`.
  Final fixture003 result `friday-astra-admin-links-reject-first088-loqcapf_/first-result-088-003.json`
  SHA `2e7319b8f3c98b80acf9e47fafa8b3122afa96260c142d17c408948bd8d28142`:8P2F,
  zero errors/skips,10.87s pytest,15.907s actual runner; source/cleanup preserved.

- Canonical exact-release now binds the fifteen journey children to a fresh run
  context: base/candidate/tree/wheel/inventory, Python and actual package/suite
  digests. The registered journey node seals this context into the request and
  receipt. Before temporary JUnit/site cleanup, the gate verifies each child and
  stores private receipt references, hashes and observed timings in its summary.
  The acceptance reader reopens the receipt and full-settings request; missing,
  changed, public/nonregular or wrong-run evidence prevents execution credit.
  Attempts, cleanup, complete node/case membership and deadlines remain strict.
- The isolated gate loads an explicit candidate tools namespace and aliases its
  own gate module; it adds no product checkout to sys.path and refuses foreign
  authority. An actual cold controller→child→JUnit→collector→reader diagnostic
  passed with suite hashes equal to frozen017. These probes use owned copied
  packages and synthetic release IDs: no built wheel/fullgate/release credit.
- Independent017 child-provenance/cold-loader review is scoped ACCEPT; private
  actual-child receipt/request hashes reopened. Lab JUnit was synthesized from
  that child, and writer order checked by AST. Its fabricated aggregate-budget
  comparison supplies no independent aggregate enforcement proof. Full canonical
  gate and live remain NOT_RUN; active aggregate enforcement is now wired and independently accepted within its stated scope.
- Sol's supplemental graph/Inbox oracles integrated after Astra read all1554 lines,
  verified seven secondary evidence refs and independently replayed the controls.
  Ten old nodes retained; ten new nodes strengthen six cases: exact temporal
  topology/members, persisted relation tuples/replay/audit, bulk terminal/refusal
  state, directory membership/truncation/privacy and literal signed-tenant Inbox
  ownership/replay rows. All19 private product-path mutants fail the expected
  assertions with zero setup errors; product source is unchanged.20combined nodes
  plus coverage/acceptance/inventory:167PASS30.40s. Canonical execution credit is
  still pending. Sol supplied the bounded first-attempt ledger; Astra integrated
  it with the existing parent and corrected two safe-path producer tests.
- Integrated private OMP_NUM_THREADS=1 and OPENBLAS_NUM_THREADS=1 before product
  imports; full worker environment digest binds them. Native census is unchanged.
  Actual runtime probe→worker_main→app startup/shutdown verifies identity and clean
  leave. A surviving thread still fails census/containment, with outer child/PG
  cleanup verified. Both probes make zero model HTTP calls and deliberately report
  primary_http_not_observed; neither is live PASS. Before-fix startup rejection
  retained. 1701 native/battery/driver checks PASS38.57s; final14 actual lifecycle,
  inventory and acceptance checks PASS14.05s; Ruff/format/diff PASS. Independent018
  lab review is scoped ACCEPT for pool policy, actual lifespan and survivor/env
  tamper refusals. Full Word/model/native acceptance remains NOT_RUN.
- All300 surfaces classified;147bindings verify;134structural gaps remain.
  All300execution gaps and148required NOT_RUN cases remain explicit. Collection
  28,257 nodes; non-nightly scratch24,610MiB. Revision045 integrates independently
  accepted lifecycle/conflict oracles:25nodes/20actualcontrols and eight routes,
  including exact retained positive nodes. Sol25PASS21.79s;2unchanged prior probes
  now fail at intended audit assertions3.16s. Full rows, history and audits bind.
  Frozen045SHA `8ca21976f8112241b5bcc4716fca97bc18509f07b10f01898edd21b093b957ef`,
  tree `75850d648121917ce46834439c503cc0f62ee476` (54harnesspaths).
  Metadata168PASS1FAIL45.65s: printed harness command omitted newmodule; repaired
  affected plan1PASS0.19s. Ruff/format/diffPASS; audit exit2 retains actual gaps.
  Revision046 adds independently accepted quality23nodes/21controls plus one
  retained private-quarantine usage node. Sol002:10affectedPASS10.09s; both
  unchanged escape probes fail at intended assertions3.19s. Requested-person
  refusal privacy and404audit provenance bind; no product fix. Three new inventory
  rules add184MiB; all prior rules remain exact. Metadata169PASS46.64s; Ruff,
  format and diffPASS; audit exit2 retains actual gaps without binding complaints.
  Frozen046SHA `c78520089db039da83ea86a2028c1d3c4da10e9e0b3b6c036447c3c6f3dd6017`,
  tree `c5a2ae1449e0aa2d3b1f641e1e148a340e638d8b` (55harnesspaths).
  Revision047 integrates settings11nodes/9controls and diagnostics15nodes/12controls.
  Sol settings002:6affectedPASS6.12s0other5/old; diagnostics002:15PASS13.07s
  plus2independent privacy probesPASS3.13s. Leaf bytes and shared fixture helpers
  remain exact. Seven inventory rules add208MiB; all previous rules stay exact.
  Metadata169PASS47.73s; Ruff/format/diffPASS; audit exit2 retains actual gaps.
  Frozen047SHA `05b73897ef84810ec746afc727e43171d12b20a238be4956f96965cfd1738058`,
  tree `9a25c6fba0ad46e26bb447c22bd80a42cd99ebf8` (57harnesspaths).
  API metadata001 independent14PASS exposed raw-credential and duplicate-parameter
  oracle escapes. Repair002: all5issued bearer secrets and parameter uniqueness;
  5affectedPASS6.42s, both actual escapes now fail3.54s (credential probe removes
  only its obsolete marker-absence precondition).17nodes; Sol002 SCOPED_ACCEPT:
  5affectedPASS6.44s0prior12/old and2disjoint sensitivity probesPASS3.51s.
  Inbox001 independent21PASS21.05s/2escape probes required credential canaries and
  closed admin response/item keys: excluded private-row ID escaped in an extra
  field. Repair002: all5issued RAWtokens, exact admin envelope/item keys and3new
  controls;7affectedPASS8.18s, both escape probes now expectedFAIL3.44s (token
  derivative changes only obsolete precondition).24nodes; Sol002 SCOPED_ACCEPT:
  7affectedPASS8.02s0prior17/old/retry and2disjoint sensitivity probesPASS3.41s.
  Health002 fixes independent alias controls and nonempty prioraudit:26PASS.
  Health003 added full-header canaries; Astra004 corrects RAWtoken detection and
  actual owner/invalid response controls:8affectedPASS7.45s,0other22. Sol001:
  30PASS23.79s,2probesPASS3.14s; REVISE because configured home could leak.
  Astra005 adds actual home/vault/independent Obsidian-root canaries in bodies and
  audits, with6per-alias path controls:10affectedPASS8.83s,0prior26controls.
  Original unchanged home-leak probe now expectedFAIL2.60s at privacy assertion.
  36nodes; Sol002 SCOPED_ACCEPT:10affectedPASS8.74s0prior26/old/retry and
  2disjoint path-leak sensitivity probesPASS3.05s. Prior source/results preserved.
  Revision048 integrates these accepted API17/Inbox24/health36 nodes with exact
  leaf and helper bytes. Four cases bind seven surfaces, six newly covered;
  existing health journey bindings remain. Twelve inventory rules add616MiB;
  all previous rules remain exact. Collection adds77nodes and removes0.
  Metadata169PASS48.77s; Ruff/format/diffPASS; audit exit2 retains actual gaps.
  Frozen048SHA `956d81beec3129c07638e7f0c35525a31eae8c68a8c9196eea6814e4a9f5d3c3`,
  tree `bf0abcf3b6d6483a14eb118a5d075510381023fd` (60harnesspaths).
  Existing CRUD candidate strengthens TWO existing tests for7entity/knowledge
  routes;9existing IDs proposed,0newnodes. First1PASS1FAIL3.37s exposed timestamp
  ties in the new audit-order oracle; ordered rowid observation fixes both.
  Final2PASS3.88s;2actual persisted-effect corruption controlsPASS3.24s. No productfix.
  Sol CRUD001:9PASS8.29s;2false-negative probesPASS3.15s found filtered audit
  deltas ignored extra persisted admin actions. Repair002 preserves the complete
  prior audit prefix and requires all new rows to match exactly five/two actions.
  2affectedPASS3.37s; unchanged Sol escape probes now expectedFAIL3.20s at the
  intended sequence assertions. Sol002 SCOPED_ACCEPT:2affectedPASS3.27s and
  2disjoint prefix/missing-audit probesPASS3.03s; no prior7 or product replay.
  Eval/explain candidate strengthens two existing tests for6routes/10existingIDs:
  actual seeded-object selection, stored gold case/baseline and per-case metrics,
  returned/discarded trace identity and counts. First1P1F3.24s: stale ingestion
  receipt field; affected retrieval-onlyPASS2.51s.2actual HTTP/SQL corruption
  probesPASS3.42s. Sol eval001 SCOPED_ACCEPT:10PASS6.70s and2actual baseline/
  discard-reason sensitivity probesPASS3.15s. Revision049 integrates the four
  exact accepted CRUD/eval files:19existingIDs, fourcases,13newlybound routes.
  Collection unchanged28,257; no inventory rules/scratch added. Metadata169PASS
  49.62s; Ruff/format/diffPASS, audit exit2 retains actual gaps.
  Frozen049SHA `7f4967876920e47ed02ccecaf148581eb33c638069948b8a01ecf133fb771a44`,
  tree `876cbb859dd2ba6e0bcbe545e5edaabd7ce17a73` (64harnesspaths).
  Container/event browse candidate strengthens3existingfunctions/8existingIDs
  for8routes, with5storage-only supplements. Exact tag/filter membership, created
  container fields/parent edge/list/search/stats, full audit deltas, persisted
  event dates/refusals and mixed timeline identities now bind. Initial3PASS3.86s;
  inspection adds invalid-date refusal row equality, affected1PASS2.34s.
  3actual HTTP/SQL corruption controlsPASS3.74s; first source preserved, no
  retained-node replay. Sol001 SCOPED_ACCEPT:8PASS6.01s and2disjoint real audit/
  refusal-state probesPASS3.09s; seven refs verified, integrated051.
  Export/files candidate strengthens2existingfunctions for3routes/8existingIDs:
  real export artifact/download bytes and literal tenant/raw/knowledge contents;
  file200 bytes/MIME/name then quarantine404; persisted audit fields checked.
  First2FAIL3.30s at new expectations of unsanitized audit targets/filename;
  corrected for opaque references and filename length, final2PASS3.30s.
  2original-positive-call artifact/HTTP corruption controlsPASS3.17s;6retained
  nodes not rerun. Export audit reference shape is checked, not cryptographic
  filename binding. Selected storage privacy supplements HTTP evidence.
  Sol export001 SCOPED_ACCEPT:8PASS6.50s+2disjoint audit probesPASS3.29s;
  sixrefs verified. Compact/export accepted leaves integrated in revision050.
  Five cases bind12routes,18uniqueexistingIDs (19case memberships). Inventory
  and collection28,257 unchanged. Metadata169PASS50.72s; Ruff/format/diffPASS;
  audit exit2 retains actual gaps.
  Frozen050SHA `c2631d6aa6d1ae3c35da2a586d4ae5596de0a03dc1d8bec245e3750c618a662f`,
  tree `5004cb3dbe9af833ddbc687f16a3b8aa51726963` (69harnesspaths).
  Revision051 integrates notification and graph/browse candidates:5testfiles,
  3cases/19existingIDs/12newlyboundroutes. Three files match reviewed bytes;
  two only reorder/coalesce existing imports and format whitespace, preserving
  imported symbols and other AST. Two accepted async-handler positives became
  sync signed-HTTP positives; names and parameter IDs remain unchanged.
  Fresh collection28,257 unchanged; no new inventory rules/scratch. Metadata
  169PASS55.32s; Ruff/format/inventory/private-index diffPASS. Audit exit2 retains
  remaining gaps without binding complaints. Frozen051SHA
  `97baff0d6452670f950705474349874fd54073e0a45a46b62aa2385093dd4312`,
  tree `3c577107c93fddba820e9b3fc5d21df85c814c9d` (74harnesspaths).
  Revision052 integrates accepted Knowledge and Monitor/reflection candidates:
  5testfiles/5cases/8uniqueexistingIDs/8routes, six newly bound. Four files match
  reviewed bytes; shared-archive only formats a nested signature, full AST exact.
  Retained CRUD assertions and prior graph/tag bindings remain. Metadata
  169PASS56.86s once; Ruff/format/inventory/private-index diffPASS. Collection
  28,257 and inventory24,610MiB unchanged; no new nodes or inventory rules.
  Frozen052SHA `20404b04f25d23df6de0f3d6f5a992a47125f419f00da49abf81e51ab3ada0b9`,
  tree `25fccb23ddbff645aeef9daa19e1b4f6c86be01d` (79harnesspaths).
  Bridge/approval/cleanup candidates remain outside052.
  Revision053 integrates the exact independently accepted Bridge-event leaf:
  one case/nine existing HTTP nodes/one newly bound route,80harnesspaths.
  Initial metadata168PASS1FAIL58.98s exposed nine missing selectors in the
  printed plan command; corrected command, affected plan1PASS0.20s only.
  The first failure remains recorded; no replay of the other168 tests.
  Ruff/format/inventory/private-index diffPASS. Collection28,257 and inventory
  24,610MiB unchanged; audit exit2, bindings valid, no execution/live credit.
  Frozen053SHA `e2a98d9d9e352600802271d48ea53932f7870e266366dc563ba63c3b142acd6c`,
  tree `9dab37ca55cdaf7353768606fcee34130e0a68f7`.
  Approval is independently accepted and integrated054; Cleanup integrated055.
  Revision054 adds two approval cases/six unique existing HTTP nodes/two routes,
  with ten case memberships. Both reviewed leaves and retained notification
  helpers are exact; no new nodes or inventory rules. Collection28,257 unchanged.
  Initial metadata37FAIL27PASS105ERROR6.26s: Astra mistakenly passed the JSON
  matrix to the Python formatter, which added invalid trailing commas. Restored
  JSON serialization from053 plus exact054 registrations; complete AST equal.
  Only142 failed/error nodes rerun:142PASS54.35s; prior27PASS not replayed.
  Python Ruff/format, JSON registration, inventory and private-index diffPASS.
  Audit bindings valid, exit2 retains all execution and remaining structural gaps.
  Frozen054SHA `6a81b9d9027d4f094073b3aa3b7809f6edde51e01e9e2e0d5d080acbbf45e0bb`,
  tree `20ab493a322d4f8fd1eb41d2cbb0702503a69885` (82harnesspaths).
  Revision055 registers the exact accepted cleanup leaf:one existing node,
  one case/two routes; retained helpers and direct handler match reviewed bytes.
  Metadata169PASS59.15s once. Python Ruff/format, JSON registration, inventory
  and private-index diffPASS; audit bindings valid, exit2 keeps actual gaps.
  Collection28,257/inventory24,610MiB unchanged; no new nodes/rules/scratch.
  Frozen055SHA `d9965e68fad47dc10cc6f2e36566ec4e057734fe2d9c10fa5c060860cfef1e9e`,
  tree `e8f02d5616347dc52d80f5495f98b3f03c9e23b3` (83harnesspaths).
  Revision056 registers the exact accepted relation-candidate leaf:one case,
  four existing HTTP nodes/two routes; ten direct dependencies match reviewed
  bytes. Metadata169PASS57.63s once; Python Ruff/format, JSON registration,
  inventory/private-index diffPASS. Collection28,257 and24,610MiB unchanged.
  Frozen056SHA `43746aaa291e9727fdf8bd3d606223facb212d5a644e5d1dbe97f6470aee87ab`,
  tree `4be515c290ead72b293e43a9a72a444a83321122` (84harnesspaths).
  Revision057 registers the exact accepted conflict-list leaf:one case,
  one existing HTTP node/GET route; twenty-one direct dependencies match reviewed
  bytes. Metadata169PASS57.79s once; Python Ruff/format, JSON registration,
  inventory/private-index diffPASS. Collection28,257 and24,610MiB unchanged.
  Frozen057SHA `fb45a58c42dc0161904181129c37545035c1846bdab63b4c0c24f3e408ae9ffc`,
  tree `cc8698d2a10cd7c21366f4a05731a011638c04eb` (85harnesspaths).
  Sol's earlier post-test report-write failure remains in the review record;
  registration validation does not turn that process into a clean gate PASS.
  Historical047 lookup found direct-HTTP candidates for85
  of its112 remaining API gaps; this is a candidate map, not current coverage.
  Grok strengthened3existing compact/mission functions: first2PASS1FAIL then
  affected compact retries, final3PASS5.38s;7others not rerun. Positive HTTP checks
  are useful, but separate copied comparators did not prove their fault sensitivity.
  Repair003 now uses exact cardinality:2affectedPASS4.70s0other8, and2actual
  injected faults call original modified positives:PASS1.46s. Prior copied probe
  credit withdrawn. Sol001 SCOPED_ACCEPT:10PASS8.43s and2disjoint actual
  POST/list cardinality probesPASS1.28s; sixrefs verified; integrated050.
  Notification reuse identified direct-handler tests missing HTTP/signature
  coverage. Grok converted2existing positives and extended1bearer refusal test
  across pending/artifact/claim/ACK; synthetic signed bridge, no live Telegram.
  First2FAIL1PASS5.51s missing local Engineer key; repaired3PASS3.91s and2actual
  original-positive-call artifact/ACK persistence probesPASS3.20s. Astra verified
  15refs; helper source review agrees: advertised artifact fields/pointer, whole
  pending membership and returned claim identity were unbound. Lab002 repairs
  these:2affectedPASS3.33s0C/other8 and2actual pending-path/claim-chat controls
  PASS3.09s. Astra verified15refs; exact047+three-file candidate ready for FIRST
  Sol review, no integration/full/live credit. Prior attempts preserved.
  Sol notification001 SCOPED_ACCEPT:11PASS7.86s+2disjoint actual artifact/ACK
  probesPASS3.17s, sixrefs verified. Synthetic signed HTTP plus six direct
  revocation supplements; no realTelegram/ZIP-validity/fullprivacy credit.
  Notifications and Graph-browse integrated051; Knowledge and Monitor/reflection
  integrated052; Bridge integrated053; Approval integrated054; Cleanup integrated055.
  Relation-candidate integrated056; Conflict-queue integrated057. Sol text/import
  source-plan corrections received; next bounded conflict-dismiss source-plan
  review sent once. These plans provide no execution or acceptance credit.
  Knowledge lab001 replaces the vacuous POST with real dual-person seeds and
  strengthens3existing source/date/isolation tests:3PASS5.53s,2actual probes
  PASS3.04s;23refs verified. Source review requires repair: no-mutation checks
  compare only counts/titles; refusal privacy/excerpts are weak; source queries
  and date windows do not overlap, so their dual-person isolation is unproven.
  Probe artifacts omit the asserted traceback. Lab002 tasked with exact seed-row
  preservation, private canaries, overlapping fixtures and captured original
  assertion evidence; same3functions, no newnodes/productfix/Solcredit.
  Lab002 source repairs supported:3affectedPASS3.86s reported; final2actual
  original-call probesPASS2.97s. First probe1PASS1FAIL was a meta-guard miss;
  first probe source unavailable and old001 bytes mislabeled first-tested.
  These provenance limits remain explicit. Sol knowledge001 SCOPED_ACCEPT:
  3PASS3.99s+2disjoint actual excerpt/refusal-state probesPASS3.19s; eightrefs
  verified. Initial launcher failed before pytest import,0tests; preserved.
  Integrated052 within its accepted scope; no full/live/model credit.
  New monitor/reflection candidate strengthens2existingfunctions/5existingIDs
  for4routes: exact create/list/stop persistence, audit/refusal preservation and
  own/foreign digest/message/seed-state checks. First2PASS3.06s;2actual SQL/HTTP
  corruption controlsPASS2.99s;3retained not rerun. Private049+two-file package
  Sol SCOPED_ACCEPT:5PASS4.14s+2independent actual post404 monitor persistence/
  raw-reflection leak probesPASS2.94s; seven refs verified. Integrated052.
  Two HTTP nodes plus three DIRECT supplements; no worker/allroles/model/live credit.
  Bridge-event candidate strengthens3existing functions:7affectedPASS7.92s and
  2actual signed-HTTP/persisted-row corruption controlsPASS3.61s.9existing HTTP
  nodes/1route; exact journal append/prefix, literal payload bounds, selected
  refusal state/privacy. Sol SCOPED_ACCEPT:9PASS9.47s+2independent actual
  detached-response-ID/post400 journal probesPASS3.82s; seven refs verified.
  Integrated053 within this selected scope; no full/live credit.
  Approval lab001: first5PASS1FAIL7.93s accidentally used12workers; corrected
  harness error expectation, final6PASS6.05s and2original-call controlsPASS1.53s.
  Eighteen refs verified. Audit any-match, incomplete list membership and invalid
  merge topology remain oracle gaps; repair002 prepared before FIRST Sol review.
  First-tested source is unavailable; saved first_source was frozen049, while
  attempt2/final identities match. Preserve history; no product or live credit.
  Grok cleanup001 returned one strengthened workflow for two routes; three
  harness audit-expectation failures retained, final1PASS2.66s and2actual
  original-positive corruption controlsPASS3.65s. Twenty-four refs verified;
  C1 filtered audit/C2 derived risk/C3 incomplete page/C4 weak state required
  harness repair. Private repair002 pins full audit prefix/delta, literal risks
  and whole pages, selected four-table refusal/replay snapshots and concrete
  versions/inbox fields. Three new harness failures preserved, final1PASS4.55s
  and2actual extra-audit/duplicate-page probesPASS5.90s;47refs rehashed.
  Sol Cleanup SCOPED_ACCEPT:sole testcase1PASS JUnit4.657s; process exit1 after
  completion because its selected-node path collided with sessionfinish O_EXCL.
  This is testcase evidence, not a clean gate process; the node was not rerun.
  Two disjoint actual replay raw-provenance/risk-swap controlsPASS5.57s;
  59refs parent-verified. Integrated055 within the scoped review; no full/live credit.
  Approval002 repaired A1-A3:6PASS5.88s+2actual audit/list controlsPASS1.52s;
  actual first/final raw sources match,21refs verified. Final derivative changes
  only contextlib.suppress/import/format; exact transformed AST and Ruff PASS.
  Sol executed the final derivative once:6PASS6.23s, SCOPED_ACCEPT integrated054. Two disjoint actual original-positive controls detect a self-link
  after genuine approve200 and an unexpected audit append after foreign404.
  Two setup-only failures of the refusal control remain recorded; its valid
  final attempt1PASS0.78s. Empty audit0-to-1 proves append detection, not changed
  nonempty history. Twelve refs verified; prior001 limitations remain explicit.
  Grok relation-candidate001 strengthens two existing list/review functions:
  first/final2PASS5.68s;2actual original-positive controlsPASS5.28s. Literal
  ordered pages, known cards, selected state, edge/reviewer and full audit bind.
  The combined edge/reviewer control fails first on reviewer, so only reviewer
  sensitivity is credited. Write checks cover selected candidate/own relations,
  not every endpoint entity or tenant. Missing separate probe argv/environment
  and pre-run Ruff evidence remain explicit; later packaging checks are PASS.
  Sol Relation001 SCOPED_ACCEPT:4PASS JUnit6.173s/exit0 with exact ordered gate
  list;2disjoint actual controlsPASS4.56s detect edge-only SQL endpoint swap and
  extra audit row retaining the expected row. Candidate control preserves six
  selected fields/reviewer, not a complete candidate-row probe snapshot.
  Forty-four refs parent-verified; exact leaf integrated056 without transplant.
  Two retained HTTP identity/write-only supplements ran only in Sol review.
  Gate output was absent and selected metadata separate; no path collision.
  Grok conflict-list task on frozen052 covered one existing HTTP body/one route:
  full literal list/hints and selected state/audit/refusals.
  Conflict001 review verified26refs: first/final1PASS4.58s and2actual page/triage
  controlsPASS5.30s. C1 requires a narrow oracle repair: huge-limit422 can carry
  an extra raw body because its envelope and private-text guards are incomplete.
  This is a source-level test gap, not an observed product leak. Private helper
  repair002 pins full huge-limit422 JSON and actual raw/private/owner-token
  refusal canaries. Exact source1PASS4.39s; two initial control wrapper failures
  preceded injection and receive zero credit; repaired controls2PASS4.58s detect
  raw-body/token exposure. Parent verified50repair refs, then51package refs.
  Sol Conflict SCOPED_ACCEPT:1PASS JUnit3.852s/exit0; two disjoint actual raw
  source_ref SQL drift/extra anonymous401 audit controls2PASS4.58s. Complete
  prefix/auth.failed retained;68refs parent-verified. Exact leaf integrated057;
  original provenance/privacy/single-GET limits retained.
  Grok Resolution001 returned first/final2PASS6.182s and2controls5.939s.
  Exact two-body patch supported, but successful merge/undo bypasses audit
  prefix checks, lacks full selected state/restoration, and leaves success/audit
  privacy projections open. Joint SQL/audit control proves only audit sensitivity.
  Twenty-six refs bound; two diagnostic hashes have65characters and mismatch
  actual files. Original claims preserved; actual bytes separately hash-bound.
  Repair002 received;41refs and both patch-derived trees verified. Positive A
  first1FAIL5.179s, repair1PASS5.226s; first controls1FAIL1PASS6.206s, final2PASS6.265s.
  Trigger-rejected first control has no mutation credit; replayed green control
  earns no extra credit. Full audit prefix/opaque targets and selected privacy
  improved, but target aliases/admin undo restoration/timestamps remain unbound.
  Controls use uncommitted same-connection writes and partial row projections;
  original assertion sensitivity is supported, complete committed evidence is not.
  Source review requires repair before FIRST Sol; B bytes unchanged/no replay.
  Repair003 sent once to Grok: same A body, literal aliases/full restored rows,
  bounded distinct SQL timestamps and two new committed admin-undo controls.
  Two-route text/import Sol REVISE_PLAN concretizes shared-person/tenant proof,
  assessed pending Inbox/KO linkage, literal receipts and imported members,
  exact sanitized audit targets/deltas, replay and refusal preservation. Keep
  three existing bodies/five selectors; no test or coverage credit from mapping.
  Text/import lab request now sealed against057 after14-ref/AST/scope checks
  and bounded draft review; next admin-link source plan prepared. Neither is
  dispatched yet; current Resolution repair and conflict-dismiss review continue.
  Existing UI backup-verify evidence does not close
  its distinct deterministic API gap. No new nodes/productfix authorized here.
  No full/live/model replay. Laptop available again per owner; instrument
  acceptance still precedes first full baseline.
- The exact-release parent now enforces node/case active deadlines through atomic
  worker START/FINISH events around full setup/call/teardown. Parallel nodes charge
  at rate N; shared nodes execute once per declaring case, with completed totals
  carried between non-UI/UI. Equality and numeric ceilings remain unchanged;
  controller ceiling3600s, canonical20/4/load/loadscope, worker restart0.
  Subreaper ownership/pidfd cleanup includes midphase adopted-zombie reaping.
  Catchable cancellation is recorded through launch; no blocked child mask.
  Uncertain cleanup retains nested homes/root and fences subsequent commands.
  Summary v2 and its reader require exact event-order/digest replay, derived
  first-attempt counts, JUnit/wall interval bounds and each measured command's
  cleanup proof. Legacy v1 cannot satisfy this revised instrument.
  Eleven actual parent/pytest probes plus12 receipt mutations cover expiry,
  setup/teardown, missing/duplicate events, crash, cancellation, pre-runtest hang
  and denied-kill cleanup retention.309 checks PASS56.47s; final gate52PASS3.57s;
  Ruff/format/diff/mypy PASS. Actual ordinary-host admission plus13 native checks
  pass with the parent/hooks. Cold plan seals27719 selected nodes/46cases without
  sys.path changes/product imports. A newly added parent registry-audit call was
  removed before dispatch; instrument/reader registry audit stays authoritative.
  Independent023 wired review is scoped ACCEPT:59 focused nodes reproduced;
  actual outer topology was xdist despite the report's sequential label. Two
  controls cover synthetic receipt rejection and guarded clone-like Popen cleanup,
  not two actual canonical integration mutations. Parent SIGKILL stays outside
  the guarantee. No full canonical/live/baseline execution credit.
- Ownership024 starts before initial Git/host admission and candidate cloning.
  Three fixed bounded source helpers load from actual bytes into a private
  bootstrap namespace; owned admission verifies their loaded hashes before clone.
  Projected candidate tools keep strict authority without product sys.path changes.
  Inventory Git reads and bounded output capture share the owner. Cancellation
  reaps descendants before scratch deletion; uncertain cleanup retains partial
  copies. V2 auxiliary records require successful early/late cleanup, exact clone
  count and no overlap with measured phases. Inventory source is suite-hash bound.
  Ten guarded real-Git/cache/cancellation/overflow probes and ten reader mutations
  pass within329 focused checks (72.08s). Final metadata checks62PASS; one inventory
  check raced registration, then passed sequentially against19,050MiB. Ruff/format/
  diff/mypy5source PASS. Actual ordinary-host early admission plus13 native/hook
  checks PASS1.08s; cold plan27739 nodes/46cases preserves import authority.
  Independent024 early-ownership review is scoped ACCEPT:20 original probes and
  2 actual wrapper/helper cancellation/overflow controls. Its inner controller
  was substituted, so this is not a full canonical run. Comparison-cardinality
  recommendation is rejected: the release reader intentionally refuses any
  comparison wheel before auxiliary validation; that writer is non-certifying.
- Sol reminder002 is independently reviewed and integrated unchanged. Astra
  reproduced 29 PASS / 7 plain FAIL across 28 new and 8 existing reminder nodes;
  all eight actual corrupted HTTP/transport/persistence controls pass. Four cases
  bind three exact entrances: GET reminders, POST dismiss and Telegram reminders.
  Separately allowlisted chat5003 migration has no covered_surfaces: storage
  robustness only. Coverage/acceptance/inventory: 169 PASS (22.54s); Ruff/format/diff PASS.
  Current server/storage contracts and the passing existing after-delivery HTTP
  test explicitly supersede historical pending-only cancellation. The legacy
  sent queue-ID button remains supported; its failure is retained, not waived.
  Seven failed scenarios: dismissed head consumes active GET limit; uncertain and
  failed current event IDs cannot be dismissed; old sent queue-ID button fails;
  delivery-chat migration resurrects a dismissed event; exact personal clock is
  lost; Telegram callback exceeds 64 bytes. These may share product roots. No
  skips, softened assertions or product repair before FIRST full baseline.
- User create/PATCH and actual GET deletion/DELETE now have four exact bindings.
  Final 16 new nodes: 15 PASS / 1 plain FAIL (12.79s), including ten actual output,
  persistence or audit controls. Nine unchanged deletion nodes PASS (7.81s);
  169 binding/acceptance/inventory checks PASS (24.51s); Ruff/format/diff PASS.
  Observations cover requested fields, metadata/upsert, all non-target accounts
  including actor authority, audit, and actual disable/reactivate token/role effects.
  Only acting-account updated/last-seen timestamps are normalized. Direct GET and
  DELETE authority refusals preserve accounts/tombstones; the old protection test
  exercised GET only. Default code-owned 503 remains distinct from explicitly
  test-only enabled deletion positives; no live deletion or maintenance credit.
  New plain defect: explicit source=admin is overwritten as local by role
  assignment's ensure_user default. Its expected value remains required, unskipped;
  product repair is deferred until after FIRST full baseline. Initial control
  setup attempted to remove immutable audit rows; corrected to omit the actual
  writer call without bypassing triggers. This setup error supplies no credit.
  Independent user028 review is scoped ACCEPT: 16 new nodes reproduced with the
  retained source failure, 9 old nodes PASS, and an actual sibling promotion after
  real POST is caught. Hashes/source/JUnit verified; the outside-root mutant has
  an empty JUnit classname, so Astra reran its exact immutable file/node (1 PASS,
  3.80s) before granting scope credit. No canonical or live credit is inferred.
- Grok's user-management and adjacent eight-entrance mappings are scoped accepted
  with verified source/receipt hashes (11 and 7 focused existing nodes PASS).
  Sol retains the three identity entrances in his existing native TUI. Astra's
  five account entrances now bind actual resolve/activity/preset/supervisor/
  permission HTTP. Literal people/ambiguity, shared author pagination and privacy,
  persistence, other-account and same-person override preservation, actual role/
  permission/hierarchy access and refusal effects are observed. Route before/after
  audit inputs are captured by a pass-through recorder; stored privacy projections
  are separately checked for history/count/person and private-note exclusion.
  21new+4oldHTTP:23PASS2plainFAIL19.73s; all13actual controls PASS. Nonlocal origin
  resetslocal on preset/override: same ensure_user root as user028, retained before
  FIRSTbaseline. Initial matching-text/metadata/transaction/raw-audit expectations
  were harness errors corrected before freeze.169metadataPASS26.35s; lint/format/
  diff PASS. Grok independent030 review submitted, receiptQUEUED only. No whole
  instrument, fullbaseline or release credit from these focused runs.
- Account030 independent Grok review scoped ACCEPT:25nodes23PASS2retained source
  FAIL20.76s; two independent actual recorder/same-person-override mutants PASS
  4.46s. Source/fixture/report hashes and exact node identities verified.
- Identity031 adds real GET/POST/objectDELETE bindings from Sol's697line file.
  Astra24new+9old30PASS3FAIL23.27s. Plain observations retained: owner-person audit
  collapses into shared scope, linked_by uses tenant, delegatedadmin can rebind an
  already owner-bound identity toanotheraccount. This does not prove owner-data
  access. Astra actual403thenBpromotion escaped the original refusal oracle;
  repaired allaccount/override preservation+3controls. Signedbridge explicit new
  chat/language metadata preserves original values. Final27nodes24PASS3FAIL20.63s,
  18actualcontrolsPASS,169metadataPASS27.62s; lint/format/diffPASS. Independent
  repair scoped ACCEPT by Sol PERSONAL001. Revision036 closes its three standalone
  preservation gaps: all account/override rows checked immediately after HTTP,
  before retained source failures; owner-filter GET/delegated link preserve other
  identities and exact link cardinality. Eight actual post-request corruptions
  each fail the intended preservation assertion, never a known source assertion.
  First3changed+8new:8PASS3plainFAIL9.23s;169metadataPASS33.53s; lint/format/diffPASS.
  Sol IDENTITY-REMAINDER001 independently scoped ACCEPT:11nodes8PASS3known
  sourceFAIL4.62s,0old/0extra mutants, one reviewer within8min. Hashes verified;
  all eight controls target the intended failure. No productfix/fullbaseline credit.
- Profile032 adds real personal-instructions PATCH, positive own-account GET and
  derived-profile GET bindings. Exact normalized/cleared metadata, guest/denied
  authority, unrelated account/override/identity/knowledge/graph preservation,
  literal corpus counts/membership/visible facts and bounded optional synthesis
  are observed. Nine controls corrupt real response/persistence after execution.
  Final19new18PASS1plainFAIL16.03s; seven old checks passed;169metadataPASS28.13s.
  The retained source defect: HTTP saves personal style to actor.own_id, but the
  actual runtime prompt builder reads tenant context.user_id in a shared archive,
  losing the saved preference. This is input-delivery evidence, not live model
  obedience or deployed behavior. Initial overly precise timestamp expectation
  was corrected to the documented whole-second storage clock; all other fields
  remain exact. Sol PERSONAL001 independently scoped ACCEPT,18PASS1plainFAIL;
  product unchanged before FIRST.
- Chronicle033 observes real HTTP shared history, each person's private calendar,
  exact ordering/empty/50visible-row limits and five local-day anniversaries.
  Six real corruption/effect controls pass; denied/invalid reads preserve account,
  knowledge, graph, time, privacy-owner and notification rows. First12new+5old:
  15PASS2plainFAIL14.24s; added50limit node1PASS2.61s;169metadataPASS30.16s.
  Two retained source defects: UTC records at the local seven-day boundary are
  omitted by string comparison across offsets; a valid persisted timed event
  earlier today is excluded by a date-only upper bound. Official storage with
  explicit minute precision confirms the latter1FAIL2.54s. No product repairs,
  live worker/delivery or FIRSTfullbaseline credit. Sol PERSONAL001 independently
  scoped ACCEPT,11PASS2plainFAIL; stored-timestamp scope explicitly confirmed.
- Knowledge034 binds eight actual admin read surfaces: filtered list, tags,
  corpus timeline, inspection, version diff, confirmed mentions, source-text
  search and containers. Eighteen existing behavioral nodes reused;23new add
  exact content/source/history/membership/limits, actor/target audit and per-route
  permission/refusal/no-business-write observations. Seven actual response/writer/
  persistence controls pass. First41nodes40PASS1oracleFAIL36.77s: expected map
  discriminator was misnamed mapping. Corrected per source contract; diffpositive
  pluswrongdiff control2PASS3.43s. Strengthened name canaries;23newPASS24.69s and
  169metadataPASS32.88s; lint/format/diffPASS. Sol found an actual post-GET relation
  creation escaped this state snapshot. Revision037 adds relations, candidates,
  revision history and transaction context; only the documented logical clock
  observed_at is normalized because normal auth/audit transactions advance it.
  Two real GET/403 relation-write controls now assert the intended preservation
  failure;25read nodes total. Sol LIFECYCLE001 independently scoped ACCEPT.
- Preset035 binds the three actual capability/preset GET/POST routes. Astra
  independently reproduced three source failures: update audit omits before;
  delegated creator is recorded as tenant; delegated update changes custom policy
  assigned to canonical owner. The latter is stored-policy mutation, not proof
  of owner-data access. Initial Astra task's DELETE was an unsupported prospective
  entrance; original proposal preserved, no existing required scope waived.
  Original create oracle accepted Astra's actual post-HTTP sibling privilege
  grant. Repaired fullaccount/override/identity/rawcapability preservation, audit
  history and timestamp bounds; four new real controls. Supported first21+probe:
  19PASS3FAIL17.53s; repaired25:22PASS3plainFAIL19.08s incl16controls. Metadata169
  plus8old nodes177PASS38.00s; lint/format/diffPASS. Sol KNOWLEDGE-PRESET001
  independently scoped ACCEPT for preset035: same three source failures retained;
  original DELETE scope corrected. No product fixes/baseline credit.
- Knowledge037 binds edit/restore/soft-delete/purge/purgeable through30new HTTP
  nodes and13actual writer/response/persistence/audit controls;12existing file,
  vault, FTS, alias, dedup and audit-failure tests reused. Exact intended rows,
  history/version increments, actor, privacy projection, purge affected count,
  protected/foreign/invalid/repeated requests and neighboring business/graph state
  are checked. Initial oracle failures and one accidental unchanged replay are
  retained in the attempt ledger. Final55knowledge nodes54PASS1oracleFAIL53.26s;
  corrected only restored-version durable privacy expectation, affected restore
  plus12old13PASS10.26s. Raw/HTTP version stays exact. Metadata169PASS36.10s;
  Ruff/format/diffPASS. Sol LIFECYCLE001 independently scoped ACCEPT:55PASS9.70s,
  no retry/old-node replay,13bindings22actualcontrols. Result/evidence/four source
  hashes verified. Raw test-log digest is supplied in hash-bound evidence; no
  separately named raw execution log was reopened. No fullbaseline/live credit.
- Entity review038 binds three GET routes: candidate queue, grouped suggestions
  and document suggestions.31nodes11actualcontrols plus six old behaviors check
  exact membership/windows/estimates, literal name/ID/type/confidence/order,
  decided statuses, empty/invalid/authority/target boundaries and unchanged
  business/graph/history/audit rows. Initial35nodes15PASS20oracleFAIL32.10s:
  empty audit payload is NULL; append-only audit requires writer fault injection.
  Repaired29nodes23PASS6FAIL28.57s exposed a sentence-final dot hiding exact names.
  Keep two separate required period scenarios plain FAIL; semicolon positive
  fixtures let controls prove sensitivity. Final31:29PASS2plain sourceFAIL30.36s;
  six old tests passed first run. Metadata169PASS42.65s; Ruff/format/diffPASS.
  Source matcher excludes following dot although phrase-scanner contract strips
  sentence punctuation. No product fix before FIRST. Sol independently scoped
  ACCEPT:31nodes29PASS2plain sourceFAIL7.41s,0old/probes/retries. Result, evidence
  and raw execution log hashes verified. Both period failures retained;
  suggestion_foreign is a same-person wrong-container identity control and earns
  no independent foreign-tenant privacy credit.
- Sol PERSONAL001 review exact033 is SCOPED_ACCEPT_WITH_GAPS,53PASS6plainFAIL.
  Evidence and all3module hashes verified. Identity standalone preservation gap
  repaired in036 and independently accepted by IDENTITY-REMAINDER001. Nine old
  tests exceeded8 by one, recorded without extra replay. KNOWLEDGE-PRESET001
  completed48nodes45PASS3plainFAIL7.84s plus one actual relation-write escape
  probe2.53s;0old/one reviewer/within15min. Result/evidence hashes verified.
  Formal queued duplicates of both results require no repeat review or ACK.
- Grok UI001 completed: reported four new-suite attempts24setupERROR;
  15PASS9FAIL;22PASS2FAIL;23PASS1retained deleted-inspectFAIL, plus10oldPASS.
  Patch/twofiles/proposal/firstattempt logs/JUnits hashes verified. Published
  07:37Z after07:26 deadline following watch restoration; no matching browser/
  uvicorn process observed at Astra census. No UI files integrated or accepted.
  Astra read bothfiles and issued REVISE: setup cleanup misses failures before
  yield; archiveflag queried but never asserted; backup verify accepts any toast,
  backup create never checks artifact; audit/dashboard/quality oracles too weak;
  missing six actual fault-injection controls. Existing negatives do not prove
  sensitivity. A new explicit bounded harness-repair job (not retryfullbattery)
  job7ef56742b0d74b43999420843bdd5025 QUEUED07:44:59Z,25min/2continuations/0Friday
  calls, <=32new/changed10affectedold nodes, same two private files. UIslot reserved
  exclusively; Astra/Sol nonUI. Keep product failures plain; independent review
  still required. UI002 DONE: first31nodes28PASS3FAIL68.55s; final30PASS1FAIL68.83s,
  six fault controls and setup-cleanup case pass; ten unchanged old nodes not
  rerun. Patch040b7bc3/twofiles/bindings/confirm/both logs+JUnits hashes verified;
  Astra read all457+735lines and independent Sol UI001 static review returned
  SCOPED_REVISE: soft-deleted admin inspection200 is the intended history contract;
  cleanup hooks were artificial and missed mixed BaseException failures. Original
  404-oracle failure is not a product defect. Both files repaired privately in039:
  exact own soft-deleted history/content/version2+1 and refreshed-list exclusion;
  real seed/launch/goto failures, mixed-error preservation and observed owned
  socket/thread/server/browser/driver/descendant closure. Astra35PASS71.43s;
  strengthened compact HTTP/DB/UI counters and empty-person cleanup plus two real
  result faults4PASS8.02s; final5cleanup nodes PASS7.64s. Host census empty.
  Twelve tab bindings contain37nodes/eight behavior faults and five cleanup nodes;
  activity/chats/graph/sources/timeline remain unbound. No sidebar-only credit.
  Metadata168PASS1oracleFAIL40.28s due old deterministic-only registry assumption;
  fixed layer selection explicitly excludes browser results,1PASS0.43s. Other
  168checks retain first PASS. Ruff/format/diff PASS; no product/fullbaseline credit.
  Sol UI-REPAIR-REVIEW002 independently SCOPED_ACCEPT:37PASS25.37s,0old/probes/
  retries and clean owned census; result/evidence/rawlog hashes verified. Guarantee
  concerns observed owned resources, not arbitrary host processes. Prior findings closed.
  Actual RESULT08:26:42Z is after08:23:07Z deadline, contrary to payload flag;
  do not infer model completion time from receipt. Grok reports MANUAL owner poke
  after monitor delivery, so automatic idle wake remains unproven. No substitute.
  A separate eight-entrance lifecycle/conflict mapping jobfba3f727369f4b89bb52b8b5a7ba8ece
  submitted08:44:34Z, FAILED08:59:35Z deadline_passed_before_claim: no claim,
  no work or RESULT credit. No blind resubmit or hidden TUI substitute. Owner
  reported watcher repaired. Durable idle probe job2ba7032ca9f44b9aace55f8b2947489c
  has matching submitted nonce, claim09:20:34Z and DONE/RESULT09:21:07Z; Grok reports
  no user Enter via persistent TUI monitor. New exact039 mapping request002 sent
  once09:23:28Z as job9916ae706c804a8398a578cb487ea541, claimed09:24:06Z and RESULT
  09:33:33Z. Mapping8routes/6modules,10PASS9.28s; mapping/confirm/log/JUnit/source
  hashes and exact actual10JUnit node IDs verified. Five primary HTTP gaps plus
  authority/refusal/audit/state boundaries remain. Normal work through repaired
  route is observed; failed001 was never retried. New nonUI harness implementation
  job656a550412804095b8390b8f74e37e45 RESULT10:00:11Z: one new test file,
  12nodes/7faults; first11PASS1harness-locatorFAIL, final12PASS12.57s. New patch,
  source, first/final logs/JUnits and exact declared12IDs hash-verified; ACK sent.
  Sol LIFECYCLE-CONFLICT-REVIEW001 REVISE:12PASS11.20s plus two real false-negative
  probes2PASS3.10s. Missing conflict-read/bulk audits and unchecked changed-row
  corruption reproduced. Six report/evidence/task/probe/log refs hash-verified.
  Grok repair002 jobffe987df946943c1a49509331158042c returned22nodes/17controls:
  first21PASS1harnessFAIL21.03s (auth.failed refusal audit), final22PASS20.84s.
  Both original Sol probes now fail named audit assertions; patch/source/first+final
  logs/JUnits22IDs/product/probe hashes verified, ACK sent. Still unintegrated:
  Astra found read snapshots omit own knowledge/accounts/links/revision/candidate
  state and new-history snapshot checks only5fields. One retained node ID's Cyrillic
  parameter spelling corrected to the collected literal escapes in a separate
  proposal; all8 arrays then match actual IDs. Repair003 joba321c4edb3494e9e8f1748e656a0776d
  QUEUED10:43:51Z: <=27new0old/1worker25min2continuations0Fridaycalls, full snapshots
  plus <=3 actual gap controls; no UI/laptop/sharedsource/index/product writes.
  Grok self-probe jobd251caa076d2456ab25ac1904611ede9 nonce/claim/result matched;
  its monitor reprint-cap removal/selftest16/16 are producer reports, no independent
  monitor/live credit. Cursor msg_1f336870968244abb82225acd8219a1a; ACK sent.
- UI040 adds Sources7 and Chats6 nodes with seven actual response/storage faults.
  Exact source membership/environment/secret absence, declaration/forgetting and
  same-named foreign preservation; missing-env schema409 without external DB.
  Exact feed/thread and repeated labelled queue intent for selected recipient,
  no transport reply box and automatic refresh preserving an unsent draft.
  Initial13=7PASS6oracleFAIL111.82s: decoded versus percent-encoded person path.
  Matcher/interception corrected; affected6PASS15.76s, Sources7 not repeated.
  Metadata169PASS41.88s; lint/format/diffPASS; postrun census empty. Sol independently
  SCOPED_ACCEPT040:13PASS10.42s,0old/probes/retries and clean owned census; exact
  result/evidence/rawlog hashes reopened. No Telegram delivery/created_at audit credit.
- UI041 completes representative bindings on17tabs with12Activity/Graph/Timeline
  nodes and9actual response/foreign-write/read-audit faults. Exact selected-person
  period/preview, graph membership/edges/typefilter/namedfocus, chronology year/month
  drilldown/reset and read-state/audit checks. Initial12fixtureERROR19.50s (positional
  KO content populated entity_id); second9PASS3graphFAIL24.79s (unlinked peer).
  Third graph1PASS3FAIL11.10s (total is unfiltered archive); fourth1PASS2FAIL9.49s
  plus diagnostic1FAIL4.27s (legacy-owner authentication activity timestamps).
  Corrected only test expectations/seed; owner updated_at/last_seen_at now paired
  and monotonically bounded by server time, every other owner/foreign field exact.
  Shared final12PASS25.32s; metadata169PASS41.43s; Ruff/format/diffPASS, census empty.
  Sol independent UI-THREE-REVIEW001 SCOPED_ACCEPT:12PASS24.97s,0old/probes/retries,
  clean census; exact artifact/evidence/log/task/source hashes verified. Reconciled
  via filesystem and hash-bound sender ENQUEUED receipt because its formal event
  was absent from current Astra queue view; Sol idle, task no longer pending.
  Same event/hash now observed in native queue; no resend/replay or ACK inference.
  Representative scope only: cooccurrence
  weight/inspect-filter forwarding, extended activity/graph/timeline features,
  supported browsers and live/full acceptance unclaimed. All attempt logs retained.
- Source HTTP042:13nodes/10actual controls bind4routes. Exact list/default-person,
  real ownedSQLite schema and own/foreign database bytes, explicit self creation
  followed by different-owner replace/forget. Closed target/foreign/sibling and
  business/graph/history rows, preserved creator/time, exact source audit actor,
  body-free payload and independent HMAC target, prior audit prefix; denial and
  invalid/missing boundaries. Broad HTTP privacy claim was rejected by Sol below.
  First12=6PASS6oracleFAIL11.45s (explicitdata.read grant/privacy projection);
  second13PASS12.46s. Stronger actualforeign row control1PASS2.50s; creator/operator
  separation affected7writesPASS7.37s. Metadata169PASS41.03s; Ruff/format/diffPASS.
  No old/fullgate/UI/model replay, PostgreSQL/MySQL or cross-person initial creator
  attribution claim. Sol independently13PASS12.21s, REVISE: actual invalid DSN
  reflected into the real400 body escaped the positive oracle (1PASS2.39s).
  All result/evidence/task/test/probe/log hashes reopened; initial probe setupERROR
  retained. Product returns generic safe error; this is an instrument defect.
  Revision044 adds direct-input canary, closed400 and privacy inspection of every
  refused body; three actual400/401/403reflection controls. Affected positive+3
  controls4PASS4.83s; original Sol probe nowFAIL2.43s at source_connection_secret.
  Registered16module nodes/3newparameters, then Sol DATA-SOURCES-REVIEW002 scoped
  ACCEPT:4affectedPASS4.86s; same original probe assertionFAIL2.40s, no setuperror.
  Zero unchanged12/extra probes/retries. Native event+sender receipt bind result;
  result/evidence/task/runtime/probe hashes verified. Source false-negative closed
  within stated deterministic SQLite scope. No product change. Owner gaming laptop
  gpt-oss-20b unavailable; advance notice/resource coordination before secondary/live.
- Audit HTTP043:12nodes/10actual controls bind exact actor-filtered whole stored
  rows, tie order/count/total/pages/anchors and a real newer insertion between
  requests. Exact read audit actor/target/payload/request HMAC, full old prefix
  and relevant business/graph/history intact. Anonymous/denied/revoked/invalid
  requests preserve state. Initial12setupERROR11.71s from projected fixtureIDs;
  then6PASS6oracleFAIL12.89s timestamp precision; third11PASS1oracleFAIL12.75s.
  Corrected missing-account target HMAC; affected positive1PASS2.55s. Metadata
  169PASS41.30s; Ruff/format/diffPASS. Equal-time concurrent insertions/global
  first-page races and storage-writer-wide privacy unclaimed. Sol AUDIT-REVIEW001
  scoped ACCEPT:12PASS12.75s,0old/probes/retries; five exact source/task/result/
  evidence/runtime references rehashed. Invalid before syntax and exhaustive denial
  audit/partial-field confidentiality remain outside this bounded case.
- Grok lifecycle003: three actual pre-fix state/history corruption probes escaped
  002 (3PASS5.54s); repaired25nodes/20controls PASS23.30s. Source/patch/bindings,
  full JUnit membership, stdout and product hashes verified; result ACKed.
  Astra read002->003 diff: full READ/REFUSAL state plus conflicts, scoped owner,
  full decoded new snapshot and complementary mutation checks. Not integrated
  or independently accepted. Cleanup004 completed20affectedcontrolsPASS19.30s,
  first=final; explicit selected-fault proof and SQL value readback, Ruff/format
  PASS in intended source context. AST: only fault-test plus two fault helpers
  changed; five positives unchanged. All20JUnit IDs, eight arrays/25new+retained
  IDs and source/product hashes verified; Astra read changed function/helpers.
  Prepared exact044+replacement review tree d86c65ebd5d1e9a3364d4da9a121d2a168e82851;
  source/index untouched, no lifecycle registration. Sol LIFECYCLE-CONFLICT-REVIEW002
  onceENQUEUED25new0old<=2probes/15min; receipt unconfirmed. Grok now maps only
  admin settings/diagnostics/quality against existing assertions; static mapping
  returned,0runtime, source/registry hashes verified. Allthree lack completeHTTP
  proof; unit/collector/mixedLLM aliases rejected. Grok now implements ONLYsettings
  with independent expectedprojection/fullstate/actualfaults,<=14nodes25min.
  Astra owns diagnostics/quality next. Separate static metadata/health map saved;
  no mapping result earns binding or execution credit.
- Source-index reconciliation:
  Initial freeze stopped before writes because source index now stages exact039;
  HEAD and product unchanged. Index preserved; complete staged+unstaged+untracked
  allowlist verified50paths then51for041,52for042 and53for043/044. Sol reports no shared index writes;
  Grok reports an earlier failed direct-source apply attempt, without a successful
  live --index record. Origin unproven; no reset. Stage-agnostic full-base allowlist
  and separate private index used for040/041/042/043/044; real index unchanged by each freeze.
- Token revision029 corrects Sol's independently reproduced REVISE findings.
  Mint now compares full existing token rows including last_used_at. Auth checks
  permit a timestamp change only on the exact authenticated credential and bound
  it to the request at storage's second precision. Every other row/field remains
  exact. Revocation preserves all raw target fields except revoked_at and compares
  the entire revoke-audit delta across all IDs; repeat/missing 404 adds no record.
  Delegated-admin GET now checks the complete safe metadata/filter/revoked contract.
  Its expected data comes from pre-request rows plus only the validated auth touch.
  Seven added real controls catch collateral mint/ordinary/admin timestamp writes,
  missing-ID extra audit, delegated hash/future-time corruption and real revoked
  auth admission. All 14 earlier token nodes remain; 22 new + 12 old API-token
  nodes: 33 PASS / 1 plain FAIL (23.41s), with all 16 actual controls PASS.
  Initial raw-versus-redacted target comparison was a harness error; corrected to
  raw SQL comparison without dropping hash preservation.169 binding/acceptance/
  inventory checks PASS (24.59s); Ruff/format/diff PASS. The fractional TTL1.5
  product defect remains required and unskipped; no product repair before FIRST.
  Corrected029 independent Grok review is scoped ACCEPT:34nodes33PASS1retained
  TTLFAIL24.76s; two independent real-boundary mutantsPASS4.44s. Full source/report
  hashes and exact JUnit identities checked; outside-root classname limitation
  explicitly retained. Earlier026 REVISE is not retroactively accepted.
  Source integration and focused greens are not release GO.
- Owner-reported runtime inference outage 2026-09-08 investigated read-only.
  Backend/bridge PIDs6350/6367 run installed5c3ce13c since Sep07 21:39MSK. In
  retained last3h logs,78 embedding and78 reranker ConnectErrors at06:36–06:43MSK.
  At09:13MSK both configured192.168.1.78:8002/8003 fail synthetic authenticated
  HTTP from the backend's same network namespace; TCP errno111 Connection refused.
  VM192.168.12.128 routes through reachable192.168.12.2. Container liveness on
  another host does not establish published-port reachability. Host currentLAN-IP/
  published ports requested; root cause not yet established. No restart/config/
  product changes; not a battery baseline finding. Owner restarted both model
  and APIcontainers; at09:20MSK same-namespace authenticated synthetic calls recover:
  embeddings200 one finite1024D vector47ms; reranker200 two correct indices/scores
  857ms. Exact individual failing container/root not established by restart alone.
  Private incident receipts:
  /var/tmp/astra-embedding-incident-20260908/.
- Eight administrative conversation HTTP bindings added from the laboratory's
  audit: feed/cursor/person transcript/reply queue; conversation list/transcript/
  archive/delete. Exact memberships/order/pagination, author attribution, private
  projections, actual persisted state/history/channel changes and audit are checked.
  Two intentional identical replies yield two distinct owned pending queue items;
  sending is disabled. Fourteen altered HTTP/persistence/audit controls go red at
  their exact assertions.23 nodes PASS18.83s;170 focused checks PASS32.26s.
  Independent021 lab review is scoped ACCEPT:23 nodes plus two independent
  wrong-queued-body/skipped-unarchive mutations verify the actual assertions.
  No canonical/browser/Telegram/model or active-work-item retirement credit.
- Nine self-service conversation bindings now cover exact own/foreign listing,
  history, export bytes/window, search filters, channel reset, latest-assistant
  diagnostics and rename/archive/delete state under shared archive. Signed
  current-reference tests bind literal personal tenant/channel through history,
  rename, export, archive/unarchive/delete; owner API/ordinary/other bridge refuse.
  Thirteen altered HTTP/persistence controls trigger their exact assertions.
 24 nodes PASS19.13s;194 self/admin/coverage/acceptance/inventory PASS51.63s.
  Independent022 review is scoped ACCEPT:24 nodes plus2 actual product-boundary
  mutations (wrong export window and cross-person channel reset).025 additionally
  seeds distinct channel IDs and channel kinds for one person. Reset/repeat must
  preserve exact non-target sessions, conversation rows and history. Three actual
  SQL-write controls independently drop user_id/channel/channel_id; each is caught.
  All24 old nodes remain;27 self checks PASS20.93s and169 binding/acceptance/inventory
  checks PASS20.37s; Ruff/format/diff PASS. Independent025 reset review is now
  scoped ACCEPT from Grok:4 baseline nodes plus an actual extra same-owner sibling
  deletion after a correct reset are verified. The unsent Sol self-review task
  was superseded before dispatch; no duplicate review was requested.
  All17 conversation entrances have source bindings; canonical execution is NOT_RUN. Audit expectations
  preserve the private schema; persisted state is checked independently.
- Current remaining at frozen057:145 reported surface gaps =127 entrances with
  no linked case plus18 verified executable bindings awaiting their actual layer
  proof (17 UI tabs and POST /api/chat). This is138 required and7 enabled-only
  obligations; it is not145 new test bodies. Read-only decomposition:
  `/var/tmp/astra-acceptance-069-gap-decomposition.json`. No proof/GO credit added.
  The legacy structural classifier excludes live/browser bindings even when a
  case exists; zero of that aggregate is not the sole first-baseline admission.
  Independent instrument acceptance and frozen identities/scope remain required;
  final GO still requires the actual required execution proofs, with no bypass.
  Revision058 integrates independently accepted text/import002 and Resolution004
  source oracles in a separate checkout, preserving frozen057 for the laboratory.
  Two cases bind eight existing selectors to11 routes; no new tests or inventory
  rules. Metadata169PASS, Ruff/checkformat/inventory/bindings/private diff PASS.
  Frozen058 tree `0feafa6619ad81945f7a0858bc2e202805de9513`;
  full patch SHA `21b6af652ae7767d82a46c887e0ed997eb8df04f374d804962017bb82c9e2871`.
  Collection28257 reused because all declarations/inventory bytes are unchanged.
  Resolution004 source/3positives PASS5.965s retains five-table preservation and
  exact target/audit checks. Controls007 verifies two committed non-target faults
  with full selected snapshots, fresh readonly equality and original assertions;
  personal5.279s/admin5.479s, both before deadline.005 no-run/006 Ruff I001 and
  prior publication/source limits remain. External probe JUnit omits module path;
  actual absolute argv and frozen probe hash bind it. No broader fault credit.
  Text/import002 source SCOPED_ACCEPT5PASS7.27s; optional controls0/2 NOT_RUN.
  Missing separate pre-run conftest/import/runner identities remains a limit to
  runtime attestation. Historical sanitizer/control/publication failures remain.
  Admin-link002 tree5c8df262f1bac6ba647915fe2ee90633ff2947c2: FIRST Sol
  SCOPED_ACCEPT2PASS4.99s/process5.697s,36indexed artifacts verified within900s.
  Integrated in revision059: exact accepted leaf and one admin PATCH route bound
  to two existing selectors; no new nodes/inventory, all prior058 bindings kept.
  Frozen059 tree98ea1b18cf76650c5895d3b88cd6e0a7f4ac14cf;
  full patch SHA4a4f3138bd3e3159435bc8b7c98a62cd3815a26103b2b840d44fac002a4872ef.
  Metadata169PASS63.38s/process64.24s, Ruff/checkformat/inventory/audit/private
  tree and independent bounded registration review PASS. Collection28257 reused.
  Current148verified bindings;133structural gaps(115unbound+18actual-layer proof),
  300execution gaps;149requiredNOT_RUN. Raw observed_at equality remains excluded;
  old controls stay unaccepted and future suggestions add no scope automatically.
  Conflict-dismiss lab002 tree54dace94a1861ddbe6244abd721732d96ef8aa00:
  Sol REVISE despite1positivePASS. Repair audit-ID regex to exactly16hex and
  require original audit timestamp tzinfo/utcoffset in both success/anonymous
  paths; never normalize a naive timestamp into a passing oracle. Repaired003
  tree5e405a7d660e9132a800a0654aa964aa68a7c338: one old selector1PASS3.87s,
  process4.627s; Ruff/checkformat and outside-body AST PASS. Exact one-file narrow
  Sol003 SCOPED_ACCEPT1PASS/process4.618s:35indexed artifacts, actual pre/post
  source/conftest/runner/env, Ruff/format/cleanup and600swindow verified. Ready for
  next combined integration; no new ID/time fault sensitivity, C1/C2 limits kept.
  Feedback lab001 reports2positives+2controls but is
  REVISE: successful POST full feedback/state/usage/message rows and generated
  timestamp bounds are incomplete. LAB002 submitted for those original-scope
  repairs and fresh before/after evidence;001 receives no acceptance upgrade.
  LAB002 source9822b6bd5fa7bb96404ba0788c8a7e66f63a1c37 closes those POST gaps;
  GET unchanged. Actual first1F1P (product second-precision clock) then2P and two
  original faults observed. Firstsource bytes unavailable in inspected evidence;
  conftest/runner/after bindings still incomplete. Source FIRST Sol reports2P
  but REVISE: second precision unasserted/GET normalizes naive audit time;
  anonymous audit rows open; GET current expectation partly uses production
  get_feedback_state, permitting correlated false greens. Exact result bound,
  full evidence check now binds50indexedrefs/69paths,2P4.528s/process5.258s,
  source/conftest/runner before/after and900swindow. Separate feedback003 fixes
  allthree findings inside the same two bodies: strict second-format timestamps,
  exact11-column anonymous audits and fixture-literal9-field GET current row.
  Root focused2P4.54s/process5.278s, Ruff/format and independent narrow source
  review pass. Frozen feedback003tree881664c4b2fabb7ba9dde6efada3e4744e71bc06;
  FIRST002 independently SCOPED_ACCEPT2PASS4.56s/process5.302s:56indexedrefs,
  84parentrefs/149checkedpaths exact;11actualsource/helpers+runner beforeafter,
  env0/all3processes, orderedcollection/JUnit/window/cleanup verified.
  Oldfailed shell-launch before any governed check stays narrative-only with
  no separate stderr artifact.
  No old lab runtime/control attestation or integration credit.
  Future lab wrapper now archives actual source tests/conftest independently of
  probe conftest and hashes declared source/probe/runner before/after. Seven
  helper checks and one synthetic main SIGTERM during source-preflight (0Popen,
  interrupted/NOT_LAUNCHED) pass; independent v2 narrow ACCEPT. Original v1 signal
  gap and old071 wrapper/evidence retained. No full wrapper process/live claim.
  Grok admin entity-list GET job3757e94f9d9c4c2ea9ab4464e785e57d submitted once:
  one existing body, one original membership fault, new v2 source wrapper; no
  POST/PATCH/DELETE, product or model scope. ExecutorACK01:11:53Z, deadline
  01:41:53Z; result delivered01:39:05Z, first=final1PASS3.08s verified. Source
  REVISE: old strict truncated boolean check weakened by Python True==1; exact
  JSON scalar typing missing. Control first injection missed; unpermitted retry
  reached original assertion but swapped order, preserving membership. Original
  argv/env pointers overwritten by retry (embedded before values remain); no
  retry-control acceptance. Lab002 job5510a2bf4afc454f994c8e03996db5ee submitted
  for narrow strict-type repair/one positive/zero controls; preserve001 evidence.
  Independent disjoint admin-entity write candidate
  adds three HTTP bodies for create/patch/delete with exact cards, selected
  entity/version history and ordered audit rows plus refusal/no-effect checks.
  Initial2P1F exposed an oracle mismatch: durable audit sanitizes organization
  to entity_type_chars12. Repaired literal expectation and strict canonical
  version time/order: final3P5.31s/process6.03s, Ruff/format PASS; narrow source
  review ACCEPT. Frozen one-file tree5e4347249ec6dd8ff2b133b7ac75158fb0d3c0de;
  first failure/readback retained. Sol FIRST SCOPED_ACCEPT verified3PASS with
  actualenv0/fullbeforeafter15files/runner/cleanup;68indexrefs/92paths exact,
  process6.136s and taskwindow checked.
  Prospective060 now registers the exact accepted leaf and three nodes against
  POST/PATCH/DELETE admin entities; no frozen integration yet.
  No product/live or extra control credit.
  Native secondary002 treef0b9f03ebdd4c6b92bee5f1d6c92382e60d2b090 has scoped
  Sol review and standalone native003138PASS14.92s/process15.685s. Historical
  119P19F AF_UNIX path-length failures,113(104P9F)+19P, Sol001 configuration and
  parent cleanup failures remain. Short runtime paths fixed the launcher.
  Writer21P/reader28P/CLI first37P22F then22P histories remain separate.
  Exclusive-create candidate5c696a2eb53e69065978c806c09996d2c3645b5f fixes
  deletion on O_EXCL failure and closes descriptors on cleanup interruption:
  final13PASS0.35s, RuffPASS. Existing other entries survive; unknown-inode
  empty paths remain safely retained. No atomic lstat/unlink race claim.
  Context candidate0a7ece07f3aab34557438407a4093c1b53496920 predeclares run/base,
  freezes actual seven identities/ordered selection and canonical context+digest
  before readiness, rechecks before workers, preserves signal/FD/file ownership.
  Final40PASS0.48s; first35P4F wrong test-module alias retained. These synthetic
  tests establish neither actual model execution nor independent authorship.
  Composed candidate4b0376742b102793ef85a6f3771efd37849dfe88 FIRST001 REVISE
  before any tests: delegated git status refreshed private index, violating the
  bound no-write setup. Current1982 staged entries/8ownedleaves remain exact;
  no staged-blob drift. Zero combined execution credit; oldindex not repaired.
  FIRST002 observed exact126PASS/process18.11s, but REVISE: all three actual
  process environments omitted mandatory GIT_OPTIONAL_LOCKS=0, stripped by
  canonical isolation.84 indexed artifacts, source/privateGit and deadline
  verified; no conforming combined FIRST credit. Prior source scopes remain.
  FIRST003 now SCOPED_ACCEPT: exact126PASS with mandatoryenv0 in all three
  processes, exactprovidedrunner,86indexrefs/117paths,19source/support/Git
  beforeafter plusrunner/inputs/logs/JUnit/orderedcollection/window/cleanup.
  Combined FIRST execution accepted;001NO-RUN/002invalidenv remain unchanged.
  No new architecture review or candidate change was needed for the defect.
  Native composition remains outside059; code patch applies mechanically, docs
  append conflict prepared separately preserving058/059 text. Prospective060
  private checkout composes the prior14 paths plus accepted entity-write leaf,
  conflict003, entity-write, two feedback and observer registrations:155cases/132registry
  entries; exact accepted feedback leaf preserves its two old declarations,
  retaining all059 bindings. It is not a frozen/accepted integration. Inventory,
  full collection and metadata await combined admission; no new run on059.
  Static inventory reconciliation identifies45 new functions, one obsolete
  secondary-unimplemented function replaced by the accepted enabled-secondary
  test, and4 expanded parameter sets. Projected28453nodes is not collection
  evidence. The3 UIreader functions/11instances are already included in that
  delta. Reviewed staging must remove only the obsolete rule, preserve retained
  policies and refresh exact counts/digests from the eventual real collection.
  Observer-snapshot one existing HTTPbody now checks literal9-field typedowner
  loopback response with physicalprivateoutbound1/bridgepending1/deadletter1,
  selectedoutbound/audit/updates equality, rawcanaryabsence and actual delegated
  admin/remoteowner403. First1FAIL503 was a testreadback defect: mode=ro opened
  WAL sidecars that the stopped-queue contract correctly rejects. Preserved
  source/log/files; testonly no-sidecars precondition + immutable read repair
  gives1PASS4.52s/process5.229, Ruff and independent narrowreviewPASS. Frozen
  tree50f7e901429741dccd23b20faa06c39b16346016; FIRSTSol SCOPED_ACCEPT1PASS,
  process5.242s,65indexedartifacts/193hashedrefs verified with14actualfiles/runner,
  env0/orderedcollection/window/cleanup. Exact leaf and one existing-node binding
  now in prospective060; no frozen/metadata/full/live/GO yet. Prior failure and
  lease-race/wholeprivacy limits retained.
  Production-read-only-observation existing HTTPbody now uses literal7-key
  response and all nested counts, pinned schema digest and independent rawOS
  process epoch. Nonzero other-tenant scheduled work and unrelated notification/
  worker entries check aggregate filters. Full7selectedtable rows and seeded
  audit remain exact after real200 and missingchallenge400. First1FAIL3.81s
  beforeHTTP: fixture audit action mission.created is sanitized to audit.unknown.
  Originalsource/log/DB-WAL-SHM retained unchanged; corrected literal guard
  yields1PASS3.83s/process4.627. Ruff/final narrowreview pass;18actualfiles+runner
  beforeafter/env0/collection/JUnit/cleanup bound. Frozen one-leaf candidate
  69196a631b0af109f4cc7a386947f0ca5beb967a; independent FIRSTSol SCOPED_ACCEPT:
  one PASS3.80s/process4.502s, 236 evidence refs and 18 source files/runner
  before-after verified. Exact leaf and one existing-node GET binding are now
  in frozen060. Parent mutating Ruff-format stage remains in history; FIRST
  used check-only Ruff. No product fix, controls, transient-write/sidecar/race/
  all-role/deployed-observation or full acceptance credit.
  Lab entity-list002 exceeded actualACK deadline02:07:08Z without RESULT.
  At02:11:46Z Astra requested CANCEL of further governedruns and preservation/
  boundedpublication of available evidence. Delivery/state RUNNING is not
  process-stop confirmation; no taskwindowextension or testacceptance inferred.
  Read-only process check finds the original Grok TUI and attached monitor live;
  no job-specific test argv found. Cancel_requested is true, RESULT absent.
  Process-name absence does not prove all work stopped; no hidden replacement.
  Retained controller cleanup/time assertions and runtime validators define
  supported proof; separate process-stage/staged-CA artifacts are not claimed.
  UI reader treed6159c01bd510f3401f95d7a500da60a35017d54:15PASS16.19s;
  FIRST Sol SCOPED_ACCEPT15PASS16.40s verified:42refs/32indexed artifacts;
  Source reconciliation confirms five UI code/test segments and three docs
  passages already exactly included through prospective060 native composition.
  No second patch needed; three inventory rows/11 new instances are included
  in frozen060 and its actual collection/inventory admission. Prior candidate Git-object proof was not newly
  established; retained source/patch hashes bind this comparison. Actual
  UI/native/device executions remain NOT_RUN.
  Positive enabled Engineer/Coding/secondary/mixed/table, scope/browser/config/
  soak freeze and independent instrument acceptance remain outstanding.
  Earlier038/UI002 pending labels above are historical, not current work requests.
  Telegram needs a dedicated contour; physical Android/Pandora remain parked.

**Diagnostic baseline: NOT_RUN. Final acceptance: NOT_RUN.** Expected Word/DOCX
outcome stays PASS; freeze a fixture naming its source TXT and record the actual
candidate outcome. A historical WORD003 failure does not preassign the result.
Product/model/deployment changes must not erase the first baseline.

After independent instrument acceptance: freeze candidate/wheel/runtime/model/
config/suite identities and scope, run canonical exact-release and 160-case
acceptance plus official A+B (B only after green A), then all required additional
journeys. Preserve the diagnostic baseline separately; after repairs repeat the
full process on the final artifact. No mixed-candidate GO or automatic 1.0 tag.
Browser support, enabled configurations and a real bounded soak still require
concrete scope freeze with the owner. Dedicated Telegram test contour is required;
no second production consumer. Android/Pandora and other owner-parked actions
remain parked, never PASS.

## Operating rules

- Release small independently reversible packages to `main` and production.
- Do not use Docker to certify primary Friday. The laptop inference node keeps
  its separate Docker contour.
- Do not touch the Obsidian companion plugin without a separate owner request.
- Preserve the primary-only path whenever the laptop or secondary runtime is
  unavailable.
- Measure progress by complete recoverable user journeys, not by another
  adapter, organ, evidence ceremony or store. One turn has one effect owner,
  one final publisher, one inherited deadline and one authorized source set.
- Secondary and Semantic Supervisor components are advisory. They may deepen
  analysis or propose a plan, but never own tools, effects or publication.
- Authorization, provenance, ingestion review, privacy, effect fencing,
  citation/coverage, cancellation, honest `UNKNOWN` and one-publication are
  product invariants; cognition profiles may never disable them.
- The owner-only autonomous Engineer Mode deliberately has no per-command HITL
  or isolated-workspace policy rail. Its hard boundary is instead fresh owner
  Telegram provenance and capability authorization; every other surface keeps
  its existing controls.
- Do not merge old feature branches wholesale. Re-audit and port only exact
  useful commits onto current `main`.
- Never overlap our full native/UI gate with SolGoodman's full gate. Check the
  active gate processes first; while his gate owns the machine, continue useful
  implementation, review or focused tests and wait for the full-gate slot.
- During implementation use focused tests plus static/change evidence. Run the
  full exact-release gate only at a clean release boundary or after a shared
  release/schema/runtime contract changes; reuse immutable commit-bound evidence
  when the certified product artifact is unchanged.
- Run every full gate from a short private `mktemp` directory under disk-backed
  `/var/tmp`, never quota-limited `/tmp`; remove that exact directory as soon as
  its evidence is captured. Keep the path short enough for Unix sockets.

## Priority order

### C1 — source correctness, containment and import-order stability

Status: implemented in this source revision; independent review, exact-release
certification and deployment remain pending. Version/schema are deliberately
unchanged: this is not another sealed `0.208.55` artifact. The release captain
must choose the next free version and follow the existing activation procedure.

- S3 charges final normalized output using the exact nested verifier JSON cost.
  The same affordable output budget is reserved and accepted. The fitting Q38
  projection remains covered, while escape-heavy output cannot consume an
  unreserved verifier budget. Repeated owned citations remain valid; unfinished
  bracketed qualifiers are rejected rather than deleted. Length-limited synthesis
  is honestly partial; a length-limited verifier cannot certify an answer.
- Research merging uses the shared public URL identity, retaining meaningful
  query parameters, scheme and non-default ports. Malformed existing sources
  remain visible to later anomaly handling without crashing the merge.
- Supervisor baseline counts malformed joined JSON and malformed assistant
  trace metadata instead of hiding rows. Duplicate JSON keys and unhashable
  task fields cannot silently pass or crash the diagnostic path. Historical
  promoted events and the 05:25Z S3 witness are not removed or recreated.
- The default Coding runner mounts only the current operation's workspace/export,
  binds a probe to one admission and directory identity, applies admitted hard
  memory/CPU limits to trusted probe/compilation, discards unbounded process
  output, and kills/reaps its supervisor on cancellation/timeout. Default TEST
  additionally creates a user-systemd transient unit, reads `memory.max`,
  `memory.swap.max=0`, `pids.max` and `cpu.max` back from cgroupfs, then joins
  the bubblewrap supervisor into that unit before wait(). TEST source/export
  mounts are read-only; `/tmp` is a sized tmpfs. A confirmed namespace probe is
  not this proof. If the tree cannot be proved, TEST stays blocked.
  Discovery finding zero tests does not claim that uploaded modules were never
  imported. Execute/run stay fail-closed. Engineer Mode's separate
  owner-authorized boundary is unchanged and is not the Coding TEST runner.
- Orchestration contracts no longer eagerly import the complete router. This
  removes a real Coding↔orchestration import cycle; fresh-interpreter tests also
  preserve the public router exports and clean-wheel import origin.

Verification at the 2026-09-06 source checkpoint: locked Python 3.13.5 author
checks ran all 1322 affected change-tier cases: **1318 passed, 4 failed**.
The four retained native bubblewrap tests fail because `/usr/bin/bwrap` is
unavailable in this author environment; they are neither skipped nor certified.
A 135-case source regression subset also passes. Complete collection before the
two added receipt-root tests contained 26733 nodes; both new tests pass. The
Python 3.14 inventory additionally retains its two native Unicode cases.
Repository Ruff lint, the canonical 1564-file formatting scope and Mypy over
528 source files pass. These are author checks, not independent review, the
canonical hosted change gate, exact-release certification or production acceptance.
The historical receipt-root parser was updated together with the documentation:
a newer production summary cannot silently rebind old golden-journey receipts.

Next actionable work, in order:

1. Obtain native boundary results and independent review of this exact source
   range. Execute/run of uploaded programs stay fail-closed. Default TEST is
   admitted only inside the proved aggregate tree; never confuse a probe with
   that proof.
2. Verify the C2 shared carrier/export implementation below and continue the
   remaining input/extraction transaction boundary. Source export is not
   functional create/modify acceptance.
3. N2 requesting-answer consumption is on this source tree. Remaining N2 live
   configured-model acceptance, then a functional N3 Python
   create/modify/test/repair/persistent-revision journey, and measured N1/N4 mixed
   journeys. I/O observers and empty claim sets are not completion of live N2.
4. Revisit S3 minimum-tier selection and the extra-hop activation lifecycle only
   with compatibility/restart/replay tests and independent review. No gate bypass.

### C2 — coherent final carriers and bounded immutable source export

Status: implemented in this source revision; author-tested, not independently
accepted, release-certified or deployed. No new store, publisher, execution
owner, model authority, version, schema or Pandora work is introduced.

- Generated-file publication binds the one FILE carrier to the selected user
  path, not index zero of the unfiltered input. Explicit internal metadata and
  internal filenames are filtered before decoding; malformed flags fail closed.
  This repairs a reproduced case that delivered a receipt instead of the report.
- The existing shared ZIP writer is also the Coding source packer. Both consume
  the same admitted bytes, preserve empty Python package members, use fixed
  timestamps/permissions and reject portable duplicate or non-UTF-8 names.
  Final archive allocation is bounded before ZIP creation, including UTF-8
  headers; aggregate input count and decoded bytes are bounded before decoding.
- Coding source export obtains one bounded descriptor-relative snapshot. Aliases,
  nonregular files, outside hardlinks, concurrent inode/name/content changes,
  excessive depth/count/size and overlapping source/export roots fail closed.
  VCS metadata, compiler caches and secret/internal paths are not source results.
  Digests, manifest, exported file and returned attachment use exactly those
  captured bytes rather than separately reopening mutable paths.
- Export publication is private, fsynced and atomic without replacement. A retry
  accepts only byte-identical prior output; a different revision, alias or racing
  writer is never overwritten. Failures do not publish a partial temporary file.
  This is source export only, not a claim that create/modify implements a task.

Verification at this source checkpoint: **125 author tests passed**, including
41 new carrier/export cases and existing Engineer publication, Telegram progress,
Coding mode and archive-plan tests. The initial export regressions reproduced
18 failures in 22 cases before the repair; four additional internal-metadata
regressions also failed before their correction. All original test policies and
Python 3.14 Unicode parameter sets remain unchanged in the canonical inventory.
Whole-project Ruff, the 1565-file canonical formatting scope and Mypy over 528
source files pass. Hosted change, native exact-release, independent review and
real production/Telegram observations remain separate pending evidence.

Remaining coherence work: atomic authorized archive extraction, N2 live
configured-model acceptance and N3 implementation/revision continuity
(functional create with an independent oracle, then real edits/repair),
then mixed-journey publication/restart checks. A user project file named
`manifest.json` or `cache.py` still intersects the inherited internal-name policy;
resolve that by explicit source provenance, not by globally disabling filtering.

### C3 — no-clobber archive input and shared descriptor-relative I/O

Status: implemented and author-tested in this source revision; independent
review, hosted change evidence, native release certification and deployment
remain pending. The workspace inode stays stable for the existing worker scope.

- Extraction preflight does not follow root/ancestor/member aliases or silently
  ignore broken destination links. Existing owner files and their permissions
  are preserved on collision. The actual file creation uses exclusive no-follow
  descriptors, so a new file appearing after preflight cannot be overwritten.
- Controlled write, sync and interruption failures remove only newly created
  members whose device/inode still belongs to this invocation. A replacement
  created by another writer is not deleted. Short writes are handled explicitly;
  completed files and directories are synced before reporting EXTRACTED.
- Compressed input and base64 allocation are bounded by the existing archive
  limit before parsing/decoding; member decompression has an explicit declared
  size bound. All member bytes are validated before the first output file write.
- Input and output share one narrow `workspace_io` module for no-alias directory
  traversal and inode identity. This replaces duplicated path semantics, not the
  operation owner, admission contracts, runtime or storage architecture.

Verification: **226 author tests passed** across C2/C3, existing extraction and
publication/admission contracts, Telegram/Coding surfaces and fresh-interpreter
import boundaries. C3 includes 26 new cases for collisions, aliases, bounded input,
partial writes, sync errors, interrupts and ownership-preserving cleanup. Initial
C3 tests reproduced the overwrite/ancestor-alias defects and missing input/read
bounds. Whole-project Ruff, canonical formatting (1567 files) and Mypy (529 source
files) pass. No test inventory policy or native/external case was removed.

Not claimed: atomic publication of an entire directory or power-loss rollback.
A killed process can leave a private incomplete workspace; it has no EXTRACTED
return/execute grant, and existing paths fail the next same-workspace extraction
closed. Durable recovery/revision continuation still needs its real acceptance
slice. Aggregate untrusted execution, real N2/N3 outcomes and final release gates
remain open. Pandora and the separate Engineer authority boundary are unchanged.

### C4/C5 — one source revision, durable bytes and exact restoration

Status: author-tested source correction, not independently accepted, released
or deployed. Reconstructed on `6ad812559f53466b405f88195121857dd69445cd`;
prior local-only `c983137d` / `3129accd` are NOT claimed recovered or integrated.
The implementation below replaces their unavailable local checkpoints.

- Creation, extraction and restoration share the existing descriptor-relative
  no-clobber writer. Existing files, symlinks and hardlinks are never overwritten;
  controlled failures remove only this invocation's surviving new inodes.
- Carrier bytes, the source card and revision hashes consume one export snapshot.
  Creation checks that export still matches its prepared revision before output.
  Removed the unbounded second workspace read and silent 32-file truncation.
- Assistant metadata stores a bounded source-hash manifest and exact carrier
  digest. The existing generated-file publisher stores the bytes and descriptor;
  no additional database, table, migration or source-body field was introduced.
  An interrupted/missing publication does not create a usable revision.
- `restore <message_id> <sha256>` (also `восстанови`) in the same owner's private
  Coding chat restores exactly that response's sources into a new admitted work
  directory. It requires an exact lowercase digest, the same person/tenant/chat,
  matching durable descriptor and file bytes, then validates every ZIP member
  before writes. No latest/HEAD fallback, source execution, applied edit, or
  independent acceptance is implied. New attachments cannot be mixed into it.
- The restore entry point uses the exact `message_id` in the prior API response
  and `context.coding_source_revision.revision_sha256`. Natural-language/reply
  selection and UI copy affordances remain follow-up work, not secretly fuzzy
  resolution. Functional create/modify/test/repair remains open in N3.

Evidence: 45 new regression cases; 167 affected source/export/generated-file,
Coding-surface and import-order tests passed on isolated Python 3.13.5. A wider
pre-final-additions run passed 547/551; four native bwrap tests failed because
this execution environment does not provide `/usr/bin/bwrap`. They remain
unchanged and mandatory. Exact Ubuntu 26.04 host evidence also remains external
to this environment. Source Ruff/type checks pass; canonical inventory retains
all old function/node declarations and adds the exact new cases.

Two test-accounting repairs are explicit: the mandatory scratch-total assertion
now includes C1-C3 and the new revision fixtures; descriptor-leak testing drains
unrelated collectable API resources BEFORE its baseline and disables GC only
for its probe. The same exact FD equality assertion is retained. No acceptance,
parameter, native host requirement, or adversarial check is removed.

Next: qualified review, natural source-revision selection, actual model-backed
application implementation and bounded upload edits/repair, followed by exact
native/UI release evidence. Do not infer that a restored scaffold satisfies N3.
Pandora, primary/secondary authority and production are unchanged.

### C6 — real immutable revision edits and publication-time source authority

Status: implemented and author-tested on the C4/C5 integrated source
`26076c10250b14cf8122f4d81e86cef8aa8f8804`. This is a source package, not an
independently accepted release or a production deployment.

- The existing owner-only Coding turn now accepts an exact source-edit request:
  `edit <message_id> <sha256>` (also `modify` / `измени`), followed by a newline
  and a JSON object. `replace` maps existing relative filenames to complete
  UTF-8 text, `add` maps new names to text, and `delete` is a list of existing
  filenames. Example body: `{"replace":{"main.py":"answer = 42\n"}}`.
  Attachments cannot be mixed with this exact revision operation.
- The parent is loaded through the existing same-person/tenant/conversation
  durable revision reader. Its complete source digest is the precondition.
  No latest selection, fuzzy patching, inferred filenames or source execution
  occurs. The source-body text cannot become an execute/build/test command.
- Validate the entire batch before writing: at most 16 operations and 1 MiB
  of request JSON, with the existing 32-member / 36 MiB export limits. Reject
  duplicate keys/paths, incompatible operations, missing replace/delete targets,
  existing add targets, unsafe/internal/secret paths, case collisions, file vs
  directory collisions, unchanged results and unpublishable empty inventories.
- Apply the prepared bytes only after the existing modification and worker
  admission gates pass. Use the existing exclusive descriptor-relative writer
  in a fresh per-operation workspace, never mutate the selected source blob or
  its former worktree. Packing must match the complete prepared candidate hash.
- Preserve project identity and add a new content-based revision. The ordinary
  final publisher remains the sole durable writer; its existing transaction
  now reauthorizes the stored Coding parent binding before artifact handles
  commit. Revocation between preparation and publication rolls the new artifact
  back. This check also covers newly requested restores. No new store, table,
  schema, final publisher, interpreter, or generic orchestration layer is added.
- Parent message/digest is body-free metadata. Exact new and old sources remain
  independently recoverable after database reopen and working-directory loss.
  A failed/cancelled write or a changed output snapshot cannot be delivered as
  an accepted revision; original sources remain untouched.

Author evidence: the real turn previously failed the new functional revision
edit regression; 54 new cases and 221 focused Coding/source/publication/inventory
cases now pass locally on Python 3.13.5. All prior inventory policies and exact
parameter declarations are preserved; the new fixtures add 296 MiB of declared
scratch and the existing strict scratch-total assertion is updated accordingly.
Whole-source Ruff passes; 1570 files pass the canonical formatting scope; Mypy
passes on 530 files. An expanded 306-case local run passed 279; its 27 failures
all report `bubblewrap_unavailable` in existing Engineer startup preflight.
Those tests and their host requirements are unchanged, not skipped or waived.

Remaining N3 work: this is an explicit source-edit primitive, NOT yet a model
implementing an application from a natural-language request. Natural revision
selection, model-backed create/modify, behavioral verification and bounded
repair remain open, as do qualified independent review and exact-host/native/UI
release acceptance. Pandora and deployed production remain unchanged.

### C7 — primary-model proposals for exact revision edits

Status: implemented and author-tested against the integrated C6 tree
`77240ae1aa68e0e7bd89d39387fcbcf80132bf0b` at main
`c976ff5ff712a8c13ab5880bc3f9fb3242cdec88`. Not an independently accepted
release, deployment, or live-model quality claim.

- The real Coding route accepts `revise <message_id> <sha256>` (Russian alias
  `доработай`), followed by a newline and an ordinary textual change request.
  Selection remains exact and same-person/tenant/Coding-conversation, with no
  mixed uploads, implicit latest, second model, history replay or new store.
  Existing `edit` JSON and `restore` commands remain model-free and unchanged.
- The existing configured primary `LLMRouter` receives one effect-free request.
  It keeps its own semaphore, credentials, endpoint and transport. Tools are
  explicitly empty, retries are disabled, thinking is disabled for this bounded
  code-output call, and the earlier of the inherited deadline and a 90-second
  stage limit is passed through. Require complete context; never silently trim
  source files or forward work to a secondary/cloud provider.
- Send only the task and complete UTF-8 source members, not storage paths,
  person/tenant/message identifiers, source handles or conversation history.
  Enforce 16 KiB task, 128 KiB serialized input, 64 KiB output and 8192 output
  tokens. Reuse the existing stronger credential predicate on raw source text
  as well as its serialized projection. Binary/oversized/secret-bearing source
  input is blocked rather than guessed, decoded lossily, or partially forwarded.
- A complete model answer is a proposal, never authority: validate its strict
  replace/add/delete JSON using the C6 preparer, including path, count, digest,
  no-op, collision and export rules. Reject truncation, effect/tool calls,
  duplicate keys, prose, invalid encodings and secret-bearing candidates. Only
  an exact standalone JSON fence may be removed; do not repair malformed JSON.
- The existing synchronous Coding owner reloads the exact authorized parent
  after the model await, then applies the validated proposal to a new workspace.
  Cancellation propagates before turn/workspace writes. Timeouts and transport
  failures yield a blocked result with no retry/fallback. A late valid response
  is not applied, even when a transport suppresses cancellation at its deadline.
- Preserve the original human task in the normal conversation, not a synthetic
  assistant-written edit command. Add only a closed outcome and model invocation
  count to diagnostics. No response body, private exception or reasoning trace
  enters diagnostic metadata. The unchanged durable publisher reauthorizes the
  parent in its existing transaction before committing result handles.
- Both thin AgentRuntime entry points and the orchestration Coding route call
  the same organ helper. No new product logic is added to the giant runtime.

Author evidence: the new real-route regression failed on C6 (`editing` remained
empty; no proposal was requested). 49 new cases now exercise model proposals,
publication/reopen, both AgentRuntime modes, static-path parity, scope refusal,
credential hygiene, invalid/truncated outputs, cancellation, deadline expiry,
revocation during the await and revocation at final publication. The primary
transport is simulated in these deterministic tests, not a live model witness.
270 focused Coding/source/publication/inventory cases and 276 existing
turn-context/primary-transport/pending-durable cases pass locally. Whole-source
Ruff, canonical formatting and Mypy pass. Inventory adds only the 16 new test
functions (49 exact cases), reuses existing policies and adds 225 MiB of declared
scratch; every prior test policy, parameter declaration and native gate remains
unchanged. Pandora content and production are unchanged.

Still open: natural/reply-based selection UX, full model-backed creation,
behavioral build/test verification, bounded repair, real configured-model
acceptance, independent review and exact-host/native/UI release certification.
Generated edits explicitly remain unexecuted and `verified=false`; do not call
this a passing program, a completed N3 milestone, or a safe executable.

### C8 — repair hosted CI prerequisites and source-sweep inventory

Status: source/CI correction on the C7 tree `8e34e49a5e2dbd23667147f2c2890eb359cc062c`.
Not independent acceptance, release certification or production activation.

- The C6 hosted log failed at the closed collection: the journal privacy sweep
  expected 503 modules but current source has 506. This is distinct from the
  missing Bubblewrap observed in the author environment. Use source-relative
  test IDs so repeated module basenames cannot renumber unrelated cases.
- Add a focused regression against the real sweep parametrization. It fails
  on the stale declaration and passes after the explicit inventory correction.
  Preserve every existing function, tier, policy, resource allowance and both
  Python-3.14 Unicode-derived cases. No runtime inventory regeneration in CI.
- Hosted smoke reproduced a second prerequisite: Bubblewrap alone hits
  `Failed RTM_NEWADDR: Operation not permitted`. Load the distribution's
  package-verified restrictive AppArmor profile, confirm userns hardening stays
  enabled and verify the enforced child stack as the ordinary runner.
  Keep the existing Coding native cases in the change tier. Do not disable
  AppArmor/userns hardening, run test payloads as root, or grant shared networking.
- Preserve failing gate console output with an always-run artifact upload;
  pipefail keeps nonzero status. Failure logs are diagnostics, never a passing
  summary or a substitute for the exact-host release evidence.

Local Python 3.13 author checkpoint: 562 of 563 selected inventory, controller
and journal cases pass; the retained exact-host case requires provisioned Ubuntu
26.04 and fails here. The complete local collection also lacks the two expected
Python-3.14 Unicode cases; their declarations are deliberately retained. Hosted
remeasurement, independent review and exact-release/native acceptance are separate.
No product source, version, schema, Pandora content or production state changes.

### C9 — primary-model source creation through the existing Coding boundary

Status: implemented on the integrated C8 source tree
`579cbfc941beec168f44571a3e42d481d0a0a13c` (`main` `ff646e6078a814ff8f5316bf4e718c9f648f208c`).
This is author implementation, not live-model quality evidence, independent
acceptance, release certification or production activation.

- An ordinary Coding creation request (`создай`, `напиши`, `сделай проект`,
  `новый проект`, `create`, `generate`) now asks the existing primary model for
  complete new source files instead of silently substituting a fixed scaffold.
  Explicit `scaffold` and the lower-level static scaffold API remain model-free.
  Empty tasks, mixed uploads and unavailable or archived conversations fail
  before model dispatch; no implicit modification of uploaded projects occurs.
- Creation and exact revision editing share one primary-call budget, complete
  context admission, cancellation/deadline handling and strict response checks.
  The full user task (up to 16 KiB), including its tail and newlines, reaches the
  primary. The short title/goal remains only a planning/display projection;
  truncation ending at whitespace must not invalidate that projection.
- Output is a closed `files` JSON object with at most sixteen complete UTF-8
  files. The existing source snapshot, export and inspect validator now also
  validates creation. Duplicate/unsafe/colliding paths, excluded artifacts,
  invalid encodings, credentials, oversized and truncated responses are blocked
  before writes. Failed generation never falls back to a successful placeholder.
- The synchronous Coding owner revalidates the proposal and uses the same
  exclusive descriptor-relative writer and exact-digest exporter. The ordinary
  final publisher retains durable source bytes and revision identity. No new
  store, runtime, provider, permission, schema or effect owner is introduced.
- Archiving a conversation during a model await prevents new creation and edit
  writes. Previously published revisions remain readable under their existing
  authorization. Cancellation propagates without a new persisted turn; code or
  task words such as `run` and `pytest` do not grant execution authority.
- New and edited revisions use the same durable publisher and survive database
  reopen and loss of their workspaces. Output remains `verified=false` and the
  response explicitly states that build and behavior tests have not run.

Author regressions first reproduced that creation never called the primary,
then exercised 57 new cases, including full-task preservation, strict malformed
outputs, cancellation, late replies, failed writes, changed snapshots, archived
conversations, both AgentRuntime entrances and create-to-revise persistence.
The primary is controlled in these tests; no live quality witness is invented.
Inventory adds sixteen functions using existing policies (280 MiB of explicit
scratch) while preserving all prior exact parameter sets and Python-3.14 cases.
Hosted validation and final change-gate results are separate, commit-bound
records, not evidence that this entire N3 milestone is closed.

Still open: behavioral build/test execution and bounded repair, natural/reply
revision selection, real configured-model acceptance, qualified independent
review and native/exact-host/UI release certification. Pandora, the Obsidian
companion plugin and deployed production remain untouched.

### C10: exact saved-revision Python syntax checks

Status: author implementation on C9 `fc2ff1cd08921fe9ce20d88ba3b366e8b9bb5ea3`
(source tree `f533b77752bd263ff52d490736e3efc07f2347fd`). This package closes
the missing source-revision-to-compiler path, not behavioral verification or N3.

- `check <message_id> <sha256>` / `проверь <message_id> <sha256>` selects only
  that published revision in the owner's private Coding conversation. No latest
  selection, upload mixing, model call, workspace recovery or source write occurs.
  Ordinary upload-only `check`/`проверь` requests retain their existing static
  inspection path; only explicit saved-message references reject mixed uploads.
- Python bytes come from the existing authenticated durable revision reader and
  travel through bounded stdin to one code-owned compiler program. No project
  module is imported or executed; no source filename/body appears in process
  arguments. The same Bubblewrap/prlimit toolchain hides the host, network,
  database and owner roots. There is no direct-host compiler fallback.
- The compiler accepts at most 256 Python files / 1 MiB source bytes, inherits
  the remaining turn deadline (at most 30 seconds), and has hard 128 MiB address
  space, 10-second CPU, descriptor, zero regular-file-write and core-dump limits.
  These limits apply to trusted compilation only, not an arbitrary process tree.
  Cancellation/timeout/overflow kill and reap the compiler before completion.
- A bounded JSON report is bound to the exact stdin digest and revision. It
  counts all syntax failures, exposes at most sixteen path/line/column locations
  without source excerpts, and records the actual `/usr/bin/python3` version.
  Empty or non-Python projects cannot silently pass. `verified=false` and
  `behavior_tested=false` are explicit even after successful compilation.
- Source and check replies share the existing message writer. The final file
  publisher persists the report normally and reauthorizes its stored source
  binding in the publication transaction. A revoke/archive during compilation
  or before publication prevents the new report from becoming a durable handle.
  Reports are not source revisions and cannot be restored as project code.

Author tests cover exact selection rather than newest, restart without the
workspace, invalid reports, deadlines, cancellation during process creation,
bounded stdout, process reaping and two reauthorization boundaries. The native
case uses the real declared Bubblewrap path and remains mandatory. Focused local
results and hosted validation are recorded against the final source identity;
local compilation-only tests do not attest native isolation.

Still open: EXECUTE/RUN of uploaded programs stay fail-closed. Default TEST is
admitted only inside the proved aggregate tree on this source tree, not on live
`0.208.55`. Bounded repair, ordinary reply-based revision selection, live
configured-model quality, independent review, and full native/exact-host release
acceptance remain open. No schema, version, production or Pandora changes.

### C11: composable privacy SQL on the hosted SQLite runtime

Status: source correction on C10 `394ac7e121ed0203724f41783760ca583d86d822`
(tree `c5476be235d72350afb1c7f8a7f493824c1698be`). Not a release or independent
acceptance. Full-gate failures take priority over adding more Coding features.

The C8 full change log (run `34056278405`) recorded 24,115 passes and 214
failures. Its 149 rows containing `parser stack overflow` share deeply nested
privacy predicates composed inside reviewed and superseding graph relations.
Reproduction against the package-owned SQLite 3.45.1 library from the Ubuntu
24.04 runner fails seven of eight existing temporal graph cases; the same
source passes all eight on local SQLite 3.46.1. The latter runtime can grow its
parser stack, so earlier local passes did not establish hosted compatibility.

Replace nested CASE guards in the existing JSON/relation predicate builders
with ordered, flat CASE arms. Size still precedes JSON validity, validity
precedes type/JSON traversal, and NULL remains denied. Candidate provenance,
private entity/knowledge dependencies, tenant checks and the exact own-reminder
exception remain unchanged. Do not catch SQL failure and return empty graphs,
relax a privacy predicate, change the schema, or require a newer SQLite merely
to hide this reproduction. No new view, cache, store or dependency is added.

All 149 recorded parser-failure node IDs passed locally using that exact 3.45.1
library after the change. Added 41 cases cover lazy bounded JSON guards, NULL
and malformed shapes, review intent, limits and a genuinely reviewed replacement
whose evidence later becomes private. The new replacement case fails against
the old implementation on the same library. All 946 cases in 35 affected modules
then passed locally on that library, including 178 of the 214 recorded failing
node IDs (the extra failures were downstream symptoms). Forty-three focused
privacy cases also pass on SQLite 3.46.1. Hosted and full canonical-gate results
remain separate records bound to the final source tree.
The remaining failure groups are still open, including FTS backup integrity,
Engineer host preflight and unrelated runtime/expectation failures. Do not infer
that 149 corrected recorded failures make the entire current suite green.

No deployed runtime, version, schema, Pandora item or companion plugin changes.

### F.0–F.11 apply-and-release (2026-09-07)

Status: F.0–F.6 source is on `origin/main` at
`20311f324614ba8a792dc1f7e1d96e6a1d27f22a` (tree
`82b8515b1a2e1f40b95d77e032c42e58bdcb89a4`). Change-gate versus intake
`7be8d7f9303fdc5acb28c5fb24f218ee2afc5bcd` completed with
`Quality gate (change): PASSED (NON-CERTIFYING)` (`result=passed`,
`certification_eligible=false`, evidence
`/var/tmp/friday-change-evidence.2aom2zP5`, inventory
`28433dd8f2f4333cb260d9affed5a56b90fcea3107072ebe2d6464fbdc13fe05`,
topology 20/4). This is not exact-release certification and not a
deployed release. Production is `0.208.57` /
`5c3ce13c1e486e6512d64e62a898a2ba84d76f93`. Version and schema are
`0.208.57` / 50. Luna-1 independently ACCEPTed
`7be8d7f9..20311f32` (HANDOFF 2026-09-07T05:16:33Z, worktree clean,
no product edits). Mixed-journey consume provider identity landed on
source `c8e245ea`; production mixed-journey dispatch remains
fail-closed without injected prepared evidence.
Sol independently reviewed `908e56ce..5c3ce13c`: REVISE, because transient
evidence drops fallback provenance and mixed consumption reports it as primary.
M1 is repaired in `98dab89af89df911cf1946c19d131d304fb5f7dd`, independently
ACCEPTed by Astra and integrated locally. Primary/fallback provenance now
survives projection and common consumption; legacy or malformed facts do not
invent a primary decision. Exact-commit review ran 74 tests; integration with
Astra's source/privacy/cancellation candidate passed 153 focused tests and
Ruff. This source acceptance does not certify or change the installed release.

Landed F.0–F.6 commits: `aa28cedd` timezone pins and dense rebind;
`f9deec53` FTS rebuild after secure-delete purge; `1f592c51` inventory
declare; `6536a2a8` ruff-format; `f9f7a656` isolated import probes;
`2e642cd0` requesting-answer consume; `805c50c9` TEST cgroup admission;
`7ac90584` independent create oracle; `304c41f0` reply-bound edits;
`032bfb7a` mixed file+archive+web consume; `60b08f2a` mypy cancel bind;
`20311f32` closed non-nightly scratch sum 16_535.

- [x] F.0/F.1 stabilization source on this tree (clock/window pins,
      FTS rebuild after purge, inventory, isolated import probes).
      Hosted Ubuntu 24.04 SQLite 3.45.1 remains a distinct record from
      local 3.46.1. Not a deployed release.
- [ ] F.2–F.6 product acceptance: source on this tree (calendar fixtures, research
      consume, TEST cgroup, coding oracle and reply-bound edits, mixed
      consume). F.6 still needs real evidence preparation and durable
      consumption through the common authorized publication path; its
      current ledger is process-local. Live configured-model N2 acceptance is not
      fabricated. Live on `0.208.57` including mixed-journey provider
      identity (`c8e245ea`).
- [ ] F.7 real document delivery and recovery acceptance reopened by
      LIVE_OWNER API lab on unchanged `0.208.57`. The Markdown checklist
      request promised a downloadable file but returned only fenced text
      (`job_astra_live_document_002`; artifact SHA-256
      `6951e1e30dc287fff1863cf4845a62bcba01e835b1fb72940232b9d624576184`).
      Markdown is outside the current output-format contract; this proves a
      false artifact promise, not failure of all supported formats. A separate
      supported Word request reported model-generation failure; its one
      same-conversation follow-up returned attachment no-match and no artifact
      (`job_astra_live_document_word_003`; artifact SHA-256
      `1f267a278cb0afc0ce74e9eb6d6aabfd9111964360ef2de87354ca0a39f7c82c`).
      Grok's `job_astra_live_word_diagnosis_004` localizes a source-name
      rejection before rendering: "я загрузил файл <name>.txt" was mistaken
      for an unsupported output filename. Six real-runtime regressions fail
      before the new source-role repair. The original journal records two
      rejected native carriers but omits their exact tool names; that part
      of the attribution remains an inference. Diagnosis artifact SHA-256
      `b6ec1f2b862ca6fee23798c4cfad5b1173c1d9c6a7a8a87893f0f94041d01bcd`.
      The follow-up separately misrouted "верни ... Word-файлом" as a body
      query and "по тому же файлу <name>" as an output name. Local source
      repairs distinguish these inputs from an authorized output request.
      Sol F7 R1 REVISE of `d7481cb7` then found neighboring narrated "верни
      ... PDF" overriding an active Word request. Six actual-runtime cases
      reproduce it. The revised candidate removes return verbs from the
      global format matcher. Sol R2 still finds a P1 on unlisted narration
      ("В документе написано", "Автор пишет", "Это цитата") and a P2 on
      "верни в виде Word-файла"; receipt SHA-256
      `5b829c7766d06201e90f6b918869d262303b75dd6ec3b2a8af9566c1200c19d7`.
      The next local repair accounts for the entire active clause prefix;
      colon/comma alone cannot discard an unknown speaker. It preserves
      explicit source-navigation, polite and read-and-return leads, and
      removes quoted/code/Markdown-block commands before shared admission
      and format resolution. Closed inline format literals remain data.
      Genitive Word/Excel/PDF output relations now work; a later proved
      imperative is not suppressed by a preceding reported instruction.
      A 42-case baseline has 36 real-runtime failures before repair.
      1583 routing/query/shape checks and 101 privacy checks pass, followed
      by focused validation of bounded polite-prefix parsing. The 153 owned
      cases cover actual current/restored/conversation sources, three binary
      formats, reported/quoted commands, genitives and active follow-ups.
      Actual files preserve a source-only fact and have one creation effect;
      source questions, negation and standalone reported commands create none.
      Models and context retrieval are fixtures; source reading, permissions,
      rendering and conversation restoration use the real local components.
      Sol R3 REVISE on `e3c6f800` finds a reporting colon followed by a blank
      paragraph still overriding the output format, and ordinary direct-source
      and comma-separated read-and-return requests failing. Receipt SHA-256
      `26d375350f68bf074e51f97645de0db15604e9084d8e7909efb2cde11857a464`.
      The new local repair shares lexical clause boundaries with N4 tables:
      whitespace after a colon keeps its introducer. Bounded direct source
      prefixes and punctuation before the read-and-return conjunction work.
      A further four-case baseline proves ungrounded generation when "this
      file" has no prior authoritative used-source lineage. The existing
      source-restoration path now reports the missing required source instead
      of falling through to file generation. Positive conversation cases use
      a real preceding runtime turn, clear its model recordings, then verify
      source evidence in the follow-up and actual rendered bytes. 836 focused
      routing/source/privacy/runtime/HTTP/bridge checks pass. Independent
      review of this combined F7/N4 repair and installed acceptance remain open.
      Lab004 also downloaded one genuine DOCX on unchanged installed `0.208.57`:
      SHA-256 `83be33c367424594654ab9e856f525ddb6d2c8269e5e35cdc8fde98a5e3d613b`.
      Decoded date `12.09.2026`, count `4`, venue `кухня` preserve the requested
      facts despite literal-string checker misses. The unrequested source-only
      canary is absent; this probe alone does not prove source-only grounding
      or acceptance of the dirty candidate. Own lab conversations are archived;
      synthetic raw/inbox/output residuals remain recorded in lab results.
      Independent review and installed/live F.7 acceptance remain open.
      Existing document/memory/graph contracts remain on this tree
      without new product logic: office formula-cache/hidden/merged
      incomplete (`tests/test_office_structure_completeness.py`),
      OCR/vision no-exception-body (`tests/test_local_ocr_fallback.py`,
      `tests/test_multipage_scan_vision.py`), graph snapshot reaching
      the agent (`tests/test_graph_snapshot_reaches_agent.py`), sole
      ordinary facade `archive_search`, reversible entity edits
      (`tests/test_entity_edits_are_reversible.py`). Installed-package
      golden journeys remain on the installed `0.208.57` artifact. Do
      not rebind historical `0.208.1` registry receipts from this note.
- [ ] F.8 existing cancellation/restart/idempotency contracts remain
      on this tree (`tests/test_turn_deadline_reliability.py`,
      mixed-journey consume on `032bfb7a`/`60b08f2a`). Mixed consume
      caller cancellation leaves a comparison task running on the inherited
      source; event cancellation and success do not drain their children.
      Astra's local repair requires independent review and release acceptance.
      The process-local ledger is not durable restart proof. Existing installed
      Telegram/HTTP restart evidence does not close this mixed-journey gap.
- [x] F.9 N5 maintainability ratchet remains standing.
      `friday/agent_runtime/__init__.py` is 76_572 lines. F.6 added only
      thin dispatch there. No wholesale rewrite.
- [x] F.10 S3 historical consumed witness on live `0.208.51`
      2026-09-06T05:25Z is preserved. Do not fabricate traffic. Do not
      re-activate `5c3ce13c`, `908e56ce`, `e60860ea` or `9c8a4eed`.
      Compact health `requested_mode=assist` `effective_mode=off`
      after the `0.208.57` cutover.
- [x] F.11 independent review of `7be8d7f9..20311f32` by Luna-1
      ACCEPT (HANDOFF 2026-09-07T05:16:33Z, no product edits,
      worktree clean at `20311f32`).
- [x] F.11 version `0.208.56`, exact-release, sealed
      `semantic_supervisor_assist_to_shadow`. Trusted-CA
      `https://127.0.0.1:8000/api/health` shows `version=0.208.56`
      `status=ok` `requested_mode=shadow`. Owner-smoke
      `friday.s6-r3-signed-owner-smoke.v1` verdict=`passed` (11/11).
      Observation `status=clear`. DR lifecycle published revision 272
      after activate enroll race (`dr_authentication_input_changed`);
      did not rollback; did not re-activate `908e56ce`. Pandora stays
      excluded.
- [x] Version `0.208.57`, exact-release, sealed
      `semantic_supervisor_shadow_to_assist`. Trusted-CA
      `https://127.0.0.1:8000/api/health` shows `version=0.208.57`
      `status=ok` `requested_mode=assist`. Owner-smoke
      `friday.s6-r3-signed-owner-smoke.v1` verdict=`passed` (11/11).
      Observation `status=clear` (`hard_contradictions=0`). DR
      lifecycle published revision 276. Mixed-journey consume carries
      selected provider from evidence or fail-closed; it does not
      invent `yandex`. Do not re-activate `5c3ce13c`. Do not issue a
      no-product `0.208.58`. Pandora stays excluded.

### P0G — canonical Gate Diet

Status: complete on `main` through implementation head
`31c48d2541ccfce1c932f63ece28536c9fc346c3`; it had no standalone activation
and is now included in production `0.207.90`.

Evidence: certifying head `60ce37191bce3fd311b617bf9b7ae3e885dda9ac`
passed 24,313 exact nodes with zero skips/retries in 473.833 seconds; summary
SHA-256 `34351e090e1aeb3a7d1ee3d0544f07c2669a819e48c7842b32845de0ff884cc6`.
The non-certifying same-wheel projection
`3da073dc93dfdbf6d8e59a3294f7c830b4e96a5a` then ran 24,315 nodes against the
exact `0.207.84` wheel `954641e3…` in 475.033 seconds at topology 20/4, versus
744.736 seconds at 12/1: 36.2% lower wall time with zero retries. Measurement
summary SHA-256 `043b473402d0f2970b2341f1af2f0d6910f4cb90a2a8a1447ba34a95fc6a94bb`.

Goal: reduce mandatory gate time, disk churn and maintenance surface without
removing any unique release-blocking invariant. Regression testing, exact
release certification and live/physical observation must no longer run as one
ever-growing ceremony.

1. Record the current executable nodeid inventory, wall time, CPU/RSS, scratch
   bytes and flaky/retry history; map every mandatory node to one named product
   invariant and one authority tier.
2. Keep a compact change gate for static, contract and unique deterministic
   integration coverage. Move wheel/install, schema, backup/restore, rollback
   and owner smoke to one exact-candidate release gate. Move physical devices,
   live network/providers, large artifacts and long fault batteries to
   nightly/on-demand gates.
3. Delete or merge duplicate tests only when another authoritative node covers
   the same failure boundary. Preserve unique adversarial, restart, privacy,
   effect-fence and fail-closed coverage.
4. Forbid permanent nested release factories, mutable-host self-attestation and
   repeated execution of the same closed inventory in the normal gate. Expensive
   one-off release rehearsals use private disk-backed scratch and are removed
   immediately.
5. Require every new gate node to declare its invariant, tier, maximum runtime
   and scratch budget. A new duplicate must replace an older node or justify its
   additional fault boundary.

Acceptance: all current release-blocking invariants remain mapped and green;
the exact-candidate release result is unchanged; before/after measurements are
published; no hidden skip or permissive fallback is introduced. Target at
least 30% lower mandatory wall time, with larger cuts accepted only when the
invariant map proves equivalent coverage.

### P0H — bounded release-artifact retention

Status: reader and pair-bearing writer deployed through `0.207.92`. Exact-release
gate passed 24,729 nodes with evidence SHA-256
`518ad092aadf2ffe1b350754bd3388df5e0a19de56322ab98ef791415659942e`.
Two independently authenticated v2 generations, durable fallback, exact scope
and privileged no-delete probe are live. Because Linux still provides no
boot-start/quiescence authority proving global open-reference absence, every
candidate remains referenced and no retention apply or deletion is authorized.
The reviewed R3 boot proposal is explicitly rejected and not merged: `/init`
does not prove first userspace on every admitted kernel, its installer has an
authenticated-source TOCTOU, its uninstall marker is not crash-atomic, and its
persistent system-wide `kernel.io_uring_disabled=2` policy violates the ordinary
installed-software contract of Engineer Mode. Any successor must be an explicit
one-shot maintenance boot/transaction which returns the machine to its ordinary
kernel profile before Friday or owner software resumes.

Goal: replace the unbounded release polygon with a small recoverable set. The
current inventory is roughly 54 GB of backup data, 29 GB of wheel-only releases
and 14 GB of legacy releases; deletion is forbidden until every retained and
retired object is classified by code-owned identity.

1. Resolve current, previous, schema-capable fallback, active/staged activation
   journals, canonical evidence roots and the newest independently verified
   database/inbox/Engineer/Obsidian backup before proposing any deletion.
2. Keep all identities needed for immediate rollback and one older verified
   disaster-recovery generation. Keep an unfinished transaction and every
   object it references regardless of age. Never infer authority from a glob,
   directory mtime or a mutable symlink alone.
3. Add a deterministic dry-run manifest with exact paths, commit/tree/wheel or
   backup receipt hashes, byte counts and one closed retention reason per
   object. Ambiguous, malformed, referenced or open objects fail closed into
   `retain`, never `delete`.
4. Delete only an exact reviewed manifest through descriptor-relative,
   no-follow traversal, then prove retained releases still authenticate and a
   production-copy restore plus immediate rollback remain possible.
5. Apply the same bounded policy to obsolete gate/release scratch while keeping
   canonical release receipts and measurements outside disposable roots.

Acceptance: production is untouched during classification; dry-run and apply
manifests agree exactly; current/previous/fallback and both retained backup
generations reauthenticate; no open or journal-referenced path is removed; disk
recovery is measured and the normal release path enforces retention thereafter.

### P0 — production message stability

Status: deployed in `0.207.52`.

- Keep `AgentRuntime.chat` and every orchestration wrapper structurally
  signature-compatible.
- Preserve authenticated Telegram carrier identity through every legacy,
  fallback and canary branch.
- Keep signed `/api/chat` and signature-parity regressions release-blocking.

### P1 — make the current Engineer Mode a complete user workflow

Status: deployed in `0.207.57`.

Goal: close `authenticated owner request → autonomous plan/tool loop → host-user
run → progress → result files/archive` for arbitrary software installed in the
Friday VM. No `/approvals` callback is part of this mode. Friday chooses and
chains its own commands, sees their real output, may use the VM filesystem and
network as the Friday service user, and keeps durable cancellation/progress and
artifact delivery.

1. Preserve truthful trusted-output refusal receipts and independently port the
   isolated fix from commit `81998fd29b38adcace8dbfe717a4d74bed4d32f3`.
2. Read only sealed job outputs, revalidate path/type/size/SHA, persist them as
   generated Raw objects and deliver files or a deterministic archive exactly
   once through Telegram.
3. Add durable terminal notification and sparse fact-based progress after
   restart. Never invent percentages or ETA.
4. Resolve natural “status/cancel current task” through an exact
   actor/conversation binding; fail closed when more than one job is plausible.
5. Add bounded retention that never removes pending, uncertain or unpublished
   output.
6. Grant exact current-message Telegram files to a job as immutable read-only
   input snapshots; bind every digest to the request/receipt and re-authorize
   immediately before execution.
7. Wire the already-present bundle/publication seams to command, Java and patch
   flows so sources, binary/output and receipts arrive atomically.
8. Gate traversal, symlink, hardlink, race, tamper, restart, duplicate callback,
   cancellation/cgroup and nmap-route isolation; then perform one benign live
   Telegram smoke.

Items 1–2 and their traversal/symlink/hardlink/race/tamper/legacy/UNKNOWN
gates are deployed in `0.207.53`. Item 4 is deployed in `0.207.54` with durable
exact-scope focus and fail-closed ambiguity handling. Item 3 and the workspace
portion of item 5 are delivered in `0.207.56`: terminal publication is
automatic, progress is durable and fact-only, and only old proven-sent
workspaces are retired while canonical archives remain. The preceding
owner-confirmed isolated command admission is now a deployed predecessor, not
the target product contract. Autonomous owner-only host-user admission,
iterative planning, current Telegram inputs, lifecycle closure and items 6–8
are deployed in `0.207.57`. The live signed owner smoke executed an installed
command and returned exact stdout without an approval row. The P1 contour is
closed; current continuation belongs only to S1 and further toolchain expansion
is demand-driven.

Production regressions found after rollout are closed by `0.207.58`: composite
systemd time budgets are parsed exactly, current Telegram uploads reach the
autonomous service, an identical failed step executes once, model-authored fake
terminal messages are rejected, and terminal delivery no longer creates empty
archives. A real uploaded PE completed through the live command kernel with the
requested 300-second timeout. `0.207.59`/`0.207.60` restore stable V12 restart
attestation by widening only bounded SGLang observation budgets; same-epoch
identity, exact-zero drain and fail-closed semantics remain intact. Production
is `canary_ready` with `archive_read` and `file_read` live.

`0.207.62` removes model-selected deadlines from the arbitrary-command schema,
stops same-turn polling after durable admission, recovers terminal command truth
across provider failures and returns no-file stdout/stderr as bounded Telegram
text instead of an empty archive. Progress now exposes measured stage, elapsed
time, output byte counts and only a real hard-deadline remainder. `0.207.63`
deploys the reusable edited Telegram status surface and selective bounded
reasoning for complex plan/replan turns. Progress delivery is advisory and
cannot duplicate task execution; execution/status/final phases remain
no-thinking, and no approval rail was reintroduced.

P1B `EngineerWorkItem v1` is live in `0.207.67`: restart-safe continuation,
independent-ledger reconciliation, code-owned dependent-command source slots,
exact Work Item/fence/job/terminal-receipt binding, bounded replan and atomic
final Work Item CAS publication are active. Durable Work Items contain no
prompt, CoT, argv, output or path bodies; `RUNNING`/`UNKNOWN` is never replayed
blindly. Backup/restore and schema-capable fallback were admitted before the
signed owner smoke.

### S0 — freeze the constructor

Status: enforced architectural ratchet; exact-release evidence defect closed in
`0.207.67`.

- Do not start another journey-specific store, generic WorkGraph, Host Control
  expansion, closed compiler catalog, MCP platform, sensitive-data plane,
  Decision store, material-quarantine path or heuristic-retirement system.
  Existing bug fixes remain allowed.
- Keep the shipped Supervisor foundation and accepted
  `gptoss20b-2335df…` profile; do not build a second release PKI, effect owner
  or post-commit observer around them.
- Keep the released exact-release validator bound to named executable tests and
  an authenticated owner Telegram smoke where an external edge exists. A
  mutable or self-declared receipt is never `VERIFIED`.
- Do not salvage stale feature branches wholesale.

### S1 — finish Engineer continuation

Status: deployed in `0.207.67`.

- Preserve the released ledger-loss/rollback, source-slot, restart,
  publication and backup/restore invariants.
- Retain the distinct schema-capable fallback and keep the focused/native gates
  plus one benign signed owner smoke release-blocking.
- Do not add a generic graph, model-authored persistence or a new approval rail.

Acceptance: 1,652 focused tests; native gate 23,021 Python plus 31 UI tests;
static gate clear; two independently built wheels were byte-identical; immutable
activation, authenticated backup boundary, schema-capable fallback and signed
private-owner command smoke all cleared.

### S2 — one turn nervous system

Status: complete. Foundation deployed in `0.207.68`, scalar runtime propagation
in `0.207.69`, and strict tenant-bound file/attachment/V12 adoption in
`0.207.70`.

- Extend or wrap the existing `orchestration.contracts.TurnInput`; do not add a
  third unrelated DTO. Build one immutable authenticated turn contract after
  ingest/admission with actor, tenant, person, conversation, mode, turn ID,
  inherited deadline/budgets, authorized source identities, turn policy,
  effect fence and exactly one pending-work owner.
- Router, legacy runtime, Engineer, secondary and Supervisor consume the same
  contract. `AgentContext` is derived from it. Fallback may change strategy but
  may not discard the plan/source identities or classify the raw message again
  from zero.
- Carry one turn ID through ingest, route, tools and final publication. Land the
  current small-model behavior first with no user-visible change.

Foundation acceptance: 86 focused and 677 expanded contract tests; native gate
23,108 Python plus 31 UI tests; static gate clear; three independent reviews
accepted; two clean wheels were byte-identical; immutable activation and the
schema-46 backup/fallback boundary cleared.

First runtime-slice acceptance: 1,081 focused/related tests; independent review
accepted 922 exact S2 tests; native gate 23,230 Python plus 31 UI tests; static
gate clear; two wheels were byte-identical. Production activation created and
verified one SQLite/inbox/Obsidian/Engineer recovery set, retained the distinct
schema-46 fallback and reached a terminal `clear` receipt. Signed bridge-owner
identity returned `200`; an isolated authenticated two-turn owner chat returned
`200/200` on one conversation. Canonical rollback/restore paths passed the
release gate; no destructive production rollback was manufactured.

Completion acceptance: 860 changed-suite and 167 focused integration tests plus
72 signed-Telegram activation tests passed; independent integration, release,
security and whole-diff reviews were clean. The canonical native gate passed
23,306 Python plus 31 UI tests with no skips, including pinned Syncthing and
native PowerShell boundaries; static checks were clear and two clean wheels
were byte-identical. Immutable activation retained the distinct schema-46
fallback, produced verified SQLite/inbox/Obsidian/Engineer recovery receipts,
and ended `clear`; signed private-owner identity returned `200`. No synthetic
attachment traffic was inserted into the live owner conversation.

### S3 — bounded second hemisphere

Status: readiness and authenticated bounded-advisor path deployed in `0.207.71`;
on-demand runtime admission refresh deployed in `0.207.82`; assist-controller
cutover deployed in `0.208.11`. Observation-bound consumed witness landed
2026-09-06T05:25Z on live `0.208.51` assist. No-product extra-hop restore
after the historical promoted row landed on live `0.208.55` assist.
Compact health after a cutover process epoch is `requested_mode=assist`,
`effective_mode=off` (`material_loaded_not_accepted`); that is
process-epoch compact, not a missing witness. Do not reopen S3.

- Keep the primary as sole tool caller, effect owner and publisher. Secondary
  and Semantic Supervisor receive the shared turn contract and return bounded
  structured advice only; their absence is exactly the primary-only path.
- Let joined shadow observations accumulate from real eligible traffic; target
  20 for a useful sample, never fabricate traffic and never make the count a
  product-availability gate.
- Promote only the current-file-plus-current-public-web journey to bounded
  assist after release-bound evidence, latency evidence and assist-to-shadow
  rollback. Keep effect planning and heuristic retirement deferred.

Acceptance: focused authenticated-turn/advisory regressions passed; the native
gate passed 23,350 Python plus 31 UI tests with zero skips; static checks were
clear and two clean wheels were byte-identical. Immutable activation retained
schema 46 and the distinct schema-capable fallback, created verified
SQLite/inbox/Obsidian/Engineer recovery receipts and ended `clear`; signed
private-owner identity returned `200`. A real consumed representative-window
witness now exists on live `0.208.51` (see Closed on production `0.208.51`).
Do not fabricate additional traffic. Assist still does not change the sealed
query, does not own synthesis, and does not expand into coding or research
as a new journey. Honest residual of that witness is `partial_evidence`
(`web_source_truncated`, `local_context_truncated`) — an audit finding,
not an S3 reopen.

The primary keeps files local and sends only an independent sealed public-web
topic. Current files (one or several), a restored prior file, and an unused
current upload no longer veto an independent public-web clause. Same-sentence
summarize without a sealable public topic stays local and skips web instead of
refusing the whole turn. File-as-query and leftover file nouns still fail
closed for the outbound query. The genuine consumed representative-window
witness exists; do not fabricate additional traffic.

Demand-refresh acceptance: every static eligibility, evidence, actor and
capability gate is evaluated before secondary traffic; only a genuine eligible
assist turn may perform one bounded content-free runtime refresh, after which
the capability snapshot, authenticated authority and root deadline are checked
again. Ineligible turns and laptop absence retain the primary-only path. The
focused union passed 393 tests; the exact native gate passed 24,154 Python plus
31 UI tests with zero skips and static checks clear. Two clean wheels were
byte-identical. Same-schema production-copy acceptance passed 35/35 checks and
rollback passed 12/12. Immutable activation retained exact `0.207.81` as both
predecessor and schema-capable fallback and ended `clear`; trusted-CA health
reports `0.207.82`/`ok`, and signed private-owner chat returned exact `OK`.

### S4 — one search facade with passage memory

Status: R0 measured recall and R1 corpus-backed lexical gap closure deployed in
`0.207.72`; the reader-first schema/coverage contract is deployed in `0.207.73`.
The bounded writer and restart-safe backfill are deployed in `0.207.74` and are
converging in production. R2c v3 passage topology and authenticated stored
locators are deployed in `0.207.76`. R3 measured search-facade parity is
deployed in `0.207.78`; R4a reader-first conversation passages are deployed in
`0.207.79`; R4b schema-50 writer/lexical activation is deployed in `0.207.80`;
R5 measured conversation recall is deployed in `0.207.83`; R6 bounded lexical
refill and R7 five-contour document recall are deployed in `0.207.93`; the R8A
compatibility/model-control slice is deployed in `0.207.95`; fail-soft dense
document passage recall is deployed in `0.207.96`; the R8B internal retrieval
foundation is deployed in `0.207.98`; R8C exact focused-source parity is
deployed in `0.207.99`; and R8D authenticated exact-message windows are
deployed in `0.208.0`. Local `main` additionally carries the accepted R8E
bitemporal/graph internal lane, the archive composite seam, a dispatch owner
that expresses exact window / `as_of` / `known_at` / graph, and a sole-facade
measurement with `cutover_ready` true. R8 sole dialogue-facade retirement is
not yet a production release.

1. Add reader-first `document_passages` with a schema-capable fallback, bounded
   writer, restart-safe resumable backfill and honest `index_incomplete`.
2. Make `archive_search` reach parity with document, promoted-knowledge,
   bitemporal/graph and message semantics. Only after regression and shadow
   recall measurements may `memory_search`, `source_search` and
   `message_search` disappear from the dialogue model catalog; they remain
   internal adapters.
3. Add conversation passages and adjacent context as a separate reversible
   release.
4. Reconstruct a current measurable recall set before changing ranking or
   embeddings. Close filename, alias, format, date and truncation holes only
   from corpus-backed failures; do not revive `raw_fts.metadata_json`.

R0/R1 acceptance: the real-path benchmark has 21 closed cases across all ten
classes, body-free deterministic reports and exact release/origin binding. The
only accepted runtime change raised measured positive recall from 14/20 to
15/20 by repairing one reproduced capped lexical miss without changing
candidate membership, passage evidence, cursor or coverage truth. The focused
archive/benchmark suite passed 1,026 tests; the native gate passed 23,512 Python
plus 31 UI tests with zero skips; static checks, two-run benchmark identity and
two byte-identical wheels cleared. Immutable activation retained schema 46 and
the distinct fallback, produced a verified recovery set and ended `clear`;
signed private-owner identity returned `200`.

R2a acceptance: the schema/reader, archive, lifecycle and migration suites
passed 399, 899, 323 and 142 tests respectively; the exact final native gate
passed 23,540 Python plus 31 UI tests with zero skips and static checks clear.
Independent schema/lifecycle and reader reviews were clean; readiness over
100k/500k stored child rows measured 0.158/0.516 ms median without rechunking.
Two wheels per sealed sibling were byte-identical. A production-copy 46→47
migration preserved all 4,475 Raw identities, created 1,991 explicit-incomplete
parents and zero children, reopened identically under both stable and rc0, and
passed integrity/FK checks. Immutable activation retained the distinct
schema-47 rc0 fallback, produced verified SQLite/inbox/Obsidian/Engineer recovery
receipts and ended `clear`; the signed private-owner smoke returned exact `OK`.

R2b acceptance: focused writer/backfill coverage passed 333 tests and three
independent reviews were clean. The final native gate passed 23,564 Python plus
31 UI tests with zero skips; static checks were clear and two sealed wheels were
byte-identical. Production-copy acceptance proved restart continuation every
three pages, idempotent replay, backup/restore identity, fallback reopen and an
honest zero-missing/zero-stale audit. Immutable activation produced verified
SQLite/inbox/Obsidian/Engineer recovery receipts and ended `clear`; trusted-CA
health reported `0.207.74`/`ok`, and the signed private-owner Telegram smoke
returned exact `OK`. The two detected sparse-text topology failures remain
explicitly incomplete rather than being falsely marked current.

R2c acceptance: 323 combined focused tests and an independent 88-test audit
passed; the six adversarial topology/dependency blockers were clean. The native
gate passed 23,795 Python plus 31 UI tests with zero skips; static checks were
clear. Both stable and rc0 were built twice byte-identically. A sealed-wheel
production-copy migration carried 1,718 exact-current parents, retained two
honest pending parents, repaired both through the bounded worker, and reopened
under stable and the distinct rc0 with integrity/FK clean; the schema-47
predecessor rejected schema 48. Immutable activation produced verified
SQLite/inbox/Obsidian/Engineer recovery receipts and ended `clear`. Production
now has 1,720 current parents, 16,359 child passages and no pending v3 backfill;
trusted-CA health reports `0.207.77`/`ok`, V12 is `canary_ready`, and the signed
private-owner smoke returned exact `OK`.

R3 acceptance: facade materialization now preserves authorized candidate tails,
conversation-first diversity and one-shot keyboard-layout repair without
changing public limits, final reauthorization, cursor or exact replay. The
focused suite passed 396 tests. Deterministic body-free evidence kept recall at
15/20 with zero false absence, matched candidate membership 6/6 and recorded the
single known order mismatch honestly at 5/6. The full native gate passed 23,945
Python plus the same 31 UI node IDs with zero skips; UI used the gate's serial
safe fallback after parallel Chromium workers hit a host SIGTRAP flake. Static
checks were clear and two wheels were byte-identical. The operator-independent
schema-48 backup verified; immutable activation ended `clear` with exact
`0.207.77` as predecessor/fallback. Trusted-CA health reports `0.207.78`/`ok`,
V12 is `canary_ready`, and the signed private-owner smoke returned exact `OK`.

R4a acceptance: schema, lifecycle, reader, privacy and performance reviews were
clean. The exact native gate passed 24,013 Python plus 31 UI tests with zero
skips; static checks were clear. Stable and rc0 were each built twice as
byte-identical wheels. Production-copy acceptance passed 17/17 checks: the
48→49 migration preserved all 1,407 conversation identities, created one
body-free `backfill_pending` projection per conversation and zero child/FTS
rows; stable and the distinct rc0 reopened schema 49, while `0.207.78` rejected
it without changing bytes. Backup/restore preserved the exact sidecar and FTS
receipt. Immutable activation produced verified SQLite/inbox/Obsidian/Engineer
recovery receipts and ended `clear`; trusted-CA health reports
`0.207.79`/`ok`, and the signed private-owner smoke returned exact `OK`.

R4b schema decision: the released schema-49 insertion/update guards authenticate
the complete prior prefix and therefore cannot provide a prefix-independent
writer bound. R4b may migrate 49→50 only to replace those guards with exact
incremental next-source/one-anchor CAS proofs and the supporting index. It may
not add an authoritative store or change R4a's public/body-free contracts. The
request path remains bounded and fails soft to complete legacy message history;
schema 49 is the fail-closed predecessor and a distinct schema-50 rc is the
fallback.

R4b acceptance: schema, writer, reader, provenance and privacy reviews were
clean. The exact native gate passed 24,128 Python plus 31 UI tests with zero
skips; static checks were clear, and stable/rc0 wheels were each reproduced
byte-identically. Production-copy acceptance passed 35/35 checks across the
49→50 migration, two reopens, all 85 authoritative tables, bounded writer and
backup/restore. The 12/12 rollback rehearsal selected the never-activated
schema-50 rc0 after an injected post-migration health failure and rejected stale
tree/backup identities before mutation. Immutable activation produced verified
SQLite/inbox/Obsidian/Engineer recovery receipts and ended `clear`; trusted-CA
health reports `0.207.80`/`ok`, and signed private-owner chat returned exact
`OK`. The bounded production backfill is active and remains honestly partial.

R5 acceptance: the exact conversation package retained a matched message in
bounded excerpts and added deterministic authenticated conversation-journey
measurement without changing storage authority. Independent review was clean;
31 focused tests, six compatibility tests and six restart/replay parameter
cases passed. The native gate passed 24,175 Python plus 31 UI tests with zero
skips; static checks were clear and two wheels were byte-identical.
Same-schema production-copy acceptance passed 35/35 checks and rollback passed
12/12. Immutable activation retained exact `0.207.82` as predecessor and
schema-capable fallback and ended `clear`; trusted-CA loopback/LAN health report
`0.207.83`/`ok`, and the signed private-owner smoke passed all 11 checks.

R6/R7 acceptance: one conditional bounded lexical refill closes both reproduced
conversation channel gaps without changing the complete-history absence
authority. The document harness measures filename, alias, MIME format, legacy
calendar date and bounded truncation through the real archive path. The focused
regression union passed 1,197 tests. The exact release gate passed 24,739 Python
plus 32 UI tests with zero failures or skips; summary SHA-256
`f584a21d4684aebb2600f2dba3a20c26870d5bb2c409534b523bfb1217cdd2c1`.
Two independent builds and a clean-unpacked-ZIP build reproduced the exact wheel.
Immutable activation ended `clear`; DR index revision 20 retains `0.207.93`
current and `0.207.92` older with retention `review_required`. Trusted-CA health
reports `0.207.93`/`ok`, and signed private-owner smoke passed all 11 checks.
The broader R8 candidate failed 60 existing routing/tool-call regressions and
was excluded before release; legacy dialogue contracts remain until real parity.

Inbox-fairness acceptance: selection now filters current-policy advice,
exhausted attempts, private dependencies and secondary-product witnesses before
the bounded worker page, then orders immutable creation identity oldest-first.
Focused worker/storage coverage passed 69 tests and the explicit tenant-scope
contract passed six. The exact release gate passed 24,739 Python plus 32 UI
tests with zero failures or skips; summary SHA-256
`81ac61657ea7f68d601fcfc4e688577e674fbfb91e188db84ef4e1b731e25ce1`.
An independent private-clone build and clean-unpacked-ZIP build reproduced the
exact wheel. Immutable activation ended `clear`; four backup surfaces were
receipted, DR index revision 24 retains `0.207.94` current and `0.207.93` older,
trusted-CA health reports `0.207.94`/`ok`, and signed private-owner smoke passed
all 11 checks.

R8A compatibility acceptance: the released legacy textual tool-call and facade
surface remains intact while malformed late-start carriers fail closed and
quoted prose plus labelled non-JSON fences cannot become executable control.
The exact release gate passed 24,758 Python plus 32 UI tests with zero failures
or skips; summary SHA-256
`b41d058539e71f58645450e17c0c96f75a54fa9aafd394bd16b6aec7e941e523`.
Independent clean-checkout and Git-archive builds reproduced wheel
`fc2b002fc742164554e2e01cfe5d0678182741f70c223fb1b6fe00e5fe8ac266`.
All four clean-artifact journey bundles are release-bound and verified under
binding `95800fe23d86e497faf83ed6d6578edaa5ffb9fbe48c4d64bffc131ea0298335`.
Immutable activation ended `clear`; DR lifecycle revision 28 retains
`0.207.95` current and `0.207.94` older with retention `review_required`.
Trusted-CA health reports `0.207.95`/`ok`, and signed private-owner smoke passed
all 11 checks.

Dense document recall acceptance: `archive_search` now admits bounded dense
passage candidates without replacing its complete lexical/message tails, then
re-authorizes and re-scores exact-current sources before returning provenance.
Missing, stale or failed dense state fails soft to the released non-dense path.
The frozen body-free 140-document/24-qrel synthetic ranking corpus improved
recall@10 from 12/24 to 24/24 and recall@20 from 13/24 to 24/24 with zero
foreign-authority results; this evidence deliberately makes no claim about the
private production corpus or production embedding-model quality. The exact
release gate passed 24,772 non-UI plus 32 UI tests with zero failures, skips or
retries; summary SHA-256
`ae22e5d4b2689bfdf927c8638b7cc1e43e009651c0261e00ae0c9a46a3fe3a88`.
Independent clean-checkout and clean Git-archive builds reproduced release wheel
`bd7645b2f9120ab3c51acbe82d83269dd8cd0d86f30af31d9b112539d221be3a`.
Immutable activation ended `clear`; DR lifecycle revision 32 retains
`0.207.96` current and `0.207.95` older with retention `review_required`.
Trusted-CA health reports `0.207.96`/`ok`, and signed private-owner smoke passed
all 11 checks.

R8B acceptance: code-owned memory, message and source retrieval now uses one
explicit internal execution scope with fresh account, preset and override
authorization; message retrieval additionally requires `conversations.read`.
Model-selected message search has a closed argument projection, and final
publication rechecks both capabilities so a late revoke produces a source-free
denial without result bodies, evidence, files, voice, citations or continuation
state. The six benchmark compatibility calls use the same internal lane. The
legacy dialogue tools remain visible because `archive_search` does not yet
match memory `as_of`/`known_at` and graph context, source focus, or message
empty-query/full-window/full-content semantics. The exact release gate passed
24,881 non-UI plus 32 UI tests with zero failures or skips; summary SHA-256
`25f85373fab4073c0e6c9704d9acec600619d472a7b33123db249ef2ca1ce3f0`.
Two independent private-clone builds reproduced exact wheel
`878ed0a100e8bfbfb56e2bb75f992e35706557b22f5525e93ef1caad910a7632`.
Immutable activation ended `clear`; DR lifecycle revision 40 retains
`0.207.98` current and `0.207.97` older with retention `review_required`.
Trusted-CA health reports `0.207.98`/`ok`; the read-only production observation
found schema 50, zero foreign-key violations and zero hard contradictions, and
the signed private-owner smoke passed all 11 checks.

R8C acceptance: query plus source focus now has one exact v2 contract across
authorized lexical and dense selection, body-free rowid lead selection with
exact authorization joins, stable replay and benchmark parity. Legacy passage
v1 remains compatible; multiline v2 is conditional, and stale or inauthentic
dense state still fails closed or soft to the released lexical path. The exact
release gate passed 25,258 non-UI plus 32 UI tests with zero failures or skips;
summary SHA-256
`4f89cbd32d8a3594714df80f124683c72779d80940981691d1d2a3dd668ea4ed`.
The private-clone and clean-unpacked-source builds reproduced wheel
`3ce9144b544d99a7c50671f7ca7a239d128b2eb8c0f0ef46c9c2f3d0fc9c6dd4`.
Immutable activation ended `clear`; DR lifecycle revision 44 retains
`0.207.99` current and `0.207.98` older with retention `review_required`.
Trusted-CA health reports `0.207.99`/`ok`; the authenticated production
read-only bundle is verified under binding
`d7c01df8270985f13a0b50932cedfa9e5e20a1e0bef5ca9ed0f0a10d20022af7`,
and the signed private-owner smoke passed all 11 checks.

R8D acceptance: queryless exact conversation windows now enter through one
authenticated code-owned lane with durable ingress bounds, stable page-chain
identity, full source reauthorization and atomic final publication. Partial
page failure retains only the accepted witnessed prefix; carrier loss, replay,
late permission drift and SQLite snapshot drift fail closed without exposing
message bodies to the dialogue tool catalog, TTS or durable Work Items. The
exact release gate passed 25,324 non-UI plus 32 UI tests with zero failures or
skips; summary SHA-256
`4194a2cf0499099ebccc47d567857fc6d92099024a75f537bcbeaf51d6dd1c2a`.
Two independent private-clone builds and a clean source-ZIP rebuild reproduced
wheel
`1354792971eb50b792c6a65ea7f86db47f0b9a643b7fb113dbbaddd733078490`.
Immutable activation ended `clear`; DR lifecycle revision 48 retains `0.208.0`
current and `0.207.99` older with retention `review_required`. Trusted-CA
health reports `0.208.0`/`ok` with the V12 gate `canary_ready`; the authenticated
production read-only bundle is verified under binding
`b02177bb427c5ff5aa537ffb7460a69aec897a8ac9eb573f5a272eb0105c68e8`,
and the signed private-owner smoke passed all 11 checks.

Estimate: 4–10 clean-work days across the remaining separately reversible releases.

### S5 — measured cognition and installation budgets

Status: R0/R1 deployed in `0.207.77`; schema remains 48.

- Extend the existing attested `V12ModelProfileSpec`/lease instead of creating a
  competing environment-only model profile. Measured capabilities control
  lexical routing aids, verifier policy, history, tool rounds/calls, prepared
  evidence, native tools and context use.
- Preserve a baseline equivalent to today's 27–35B behavior. More capable
  leases may remove cognition crutches only after probes; authorization,
  provenance, review, privacy, citation/coverage, effect fencing and
  publication invariants never vary by model.
- Separate safety deadlines from model anti-loop limits and installation
  resource budgets. Every nested stage inherits the parent deadline.
- Remove the artificial V12 8k ceiling where the exact runtime is attested for
  40k; use `min(attested runtime, installation cap)` rather than a model-name
  guess.

Acceptance: the 1,347-node focused union and the complete native gate (23,926
Python plus 31 UI tests, zero skips) passed. Exact live q38/SGLang attestation
proved verified, installation and effective capacity of `40960` tokens with the
leased capacity authoritative through planner, file, archive, document and
current-file/public-web journeys. Two deterministic wheel builds were
byte-identical. The operator-independent schema-48 backup verified, immutable
activation ended `clear`, exact `0.207.76` remains the sealed fallback, and the
signed private-owner smoke returned exact `OK`.

### S6 — journey proof and recovery, not new organs

Status: current mission/reminder and Telegram ingress/delivery recovery audit
deployed in `0.207.75`; R2 Telegram browse-to-full-document closure is deployed
in `0.207.81`; R3 exact journey evidence is deployed in `0.207.84`; R4
candidate-bound restart/fault evidence and authenticated production observation
are deployed in `0.207.97`. Physical Android acceptance remains owner-bound.

- Keep core Telegram, web, file/Office, Obsidian, Engineer and reminder paths
  release-blocking with named deterministic tests and exact-release evidence.
- Audit scheduled work for at-most-once delivery, restart/cancel/expiry and
  uncertain-effect recovery. Change the design only where a current journey is
  actually broken.
- Prove release-level clean-home backup/restore, deterministic index rebuild,
  ENOSPC, clock skew and duplicate inbound behavior without duplicating generic
  operator evidence into every journey row.
- Re-measure review queues before adding bounded aging/observability. Keep
  export-before-purge deferred until a real retention journey requires it.
- Close the reproduced Telegram `/browse` and profile-document dead end by
  reusing the existing tenant-gated `doc:show` callback path. Preserve empty or
  malformed-result behavior and do not add another document API or store.
- Perform Obsidian Android round-trip, reconnect and conflict acceptance only
  with the owner/device present; server-side work is otherwise complete and the
  companion remains excluded.

R0/R1 acceptance: mission execution now has exact-attempt claims, completion
fences and compensation CAS; reminder payloads cross only a reauthorized
send-edge claim and ambiguous acceptance is never replayed; scans are bounded
and clock skew cannot wedge recovery. Telegram poll admission, callbacks,
commands, cached answers, chunks and terminal notices converge across commit
faults, restart, duplicate pages and ENOSPC without duplicating an admitted
effect or answer. The final native gate passed 23,774 Python plus 31 UI tests
with no skips; static checks were clear and two sealed wheels were byte-identical.
Immutable activation retained schema 47 and the distinct schema-capable
fallback, created verified SQLite/inbox/Obsidian/Engineer recovery receipts and
ended `clear`. A copy-only rehearsal rehashed the complete recovery set and
reopened its main/inbox stores twice under both candidate and fallback with
schema 47 and clean integrity/FK checks. Trusted-CA health reported
`0.207.75`/`ok`, and the signed private-owner chat returned exact `OK`.

R2 acceptance: every eligible browse, tag, entity, namesake and profile-document
result now reaches the existing tenant-gated full-document callback, and every
derived callback remains within Telegram's 64-byte limit; malformed or
overlong identities are omitted honestly. The focused suite passed 154 tests,
and the exact native gate passed 24,137 Python plus 31 UI tests with zero skips;
static checks were clear and the wheel build was deterministic. Same-schema
production-copy acceptance passed 35/35 logical/schema checks, rollback passed
12/12, and all private disk-backed temporary trees were removed. Immutable
activation retained schema 50 and exact `0.207.80` as predecessor/fallback,
ended `clear`, and produced verified SQLite/inbox/Obsidian/Engineer recovery
receipts. Trusted-CA health reports `0.207.81`/`ok`; signed private-owner chat
returned exact `OK`.

R3 acceptance: four exact clean-artifact journeys — conversation recall,
document recall and answer, durable scheduled work and honest degradation —
have deterministic privacy-safe manifests and receipts bound to the deployed
commit, tree, wheel, schema and closed executable node IDs. The focused union
passed 131 tests; the native gate passed 24,279 non-UI plus 31 UI tests with no
skips and all static checks clear. Two wheels were byte-identical;
production-copy acceptance passed 35/35 and rollback rehearsal passed 12/12.
Immutable activation retained exact `0.207.83` as predecessor and
schema-capable fallback, ended `clear`, and both trusted-CA health and the
11-check signed private-owner smoke passed on `0.207.84`.

R4 acceptance: the exact release gate passed 24,871 non-UI plus 32 UI tests
with static checks clear; summary SHA-256
`44486c6fc358d821376e80aec5f5942d7a0dedb16c9ce1bc3bbddeba536e278b`.
Two independent gate-profile builds reproduced wheel
`a0925c0399637499b2eb6b027ff495f173b618ed1b378950781b5e551fc97e62`.
Six current candidate-bound clean/restart bundles and one externally
authenticated production read-only bundle are verified under release binding
`f7a7ab8311b413d9beb6b349144a589d560b5a49c61c0892ebbb333c5284042b`;
their public evidence contains no private observation path or body. Immutable
activation ended `clear`; the DR lifecycle published revision 36 with
`0.207.97` current, `0.207.96` older and retention `review_required`. Trusted-CA
health reports `0.207.97`/`ok`, and signed private-owner smoke passed all 11
checks.

### N0 — Post-backlog baseline reconciliation

Status: live identity reconciled 2026-09-04. Production, `main` and the
sealed sibling were `0.208.11` / `6b61987a` at that reconciliation. S3
assist-controller consume is done. The observation-bound promotion
witness closed on live `0.208.51` (2026-09-06T05:25Z). Assist restored
on live `0.208.55`. N0 does not
block N1.

- [x] Reconcile source candidate, production, fallback, journals and evidence.
- [x] Deploy or explicitly supersede the `0.208.2`–`0.208.10` candidate chain.
- [x] Record exact live identity in this register (`0.208.11`, schema 50,
      predecessor `0.208.10`, activation `clear`).
- [x] S3 advice on a genuine eligible turn — observation-bound consumed
      witness on live `0.208.51` assist, 2026-09-06T05:25Z. Independent
      classification, not owner appearance. Do not fabricate traffic.

### N1 — Universal Operation Progress and Two-Message UX

Status: contract live on `0.208.18`. Ordinary `/chat` status, Engineer
Telegram status, file-album DOCUMENT status, Engineer FILE/ARCHIVE
carrier, observed web/archive chat status after `/api/chat`,
restart/edit-reject/send-fence/cancel Telegram method counts, and one
final carrier packing are live. Backend wait stays CHAT and does not
mint SEARCHING_SOURCES. Mandatory owner contract: one user message →
one editable Friday status → one final result carrier. Reuse
`TelegramStatusMessageManager`. Do not add a second execution engine.

- [x] Shared `OperationProgressProjectionV1` and code-owned Russian renderer
      (`friday/orchestration/operation_progress.py`). Truthful measured
      progress only; at most one current-focus step; no fabricated percent
      or ETA.
- [x] Migrate ordinary `/chat`, files, archive and web onto that projection
      with one status created at the start of an interactive operation.
      Ordinary `/chat` status rendering is on the projection; file-album
      status uses the DOCUMENT files projection. After `/api/chat` returns,
      observed web sources use FORMULATING_ANSWER and generated
      archives/files use archive or DOCUMENT delivering. Backend wait
      stays CHAT. Live on `0.208.18`.
- [x] Migrate `/engineer` user carrier: policy lives in
      `friday/orchestration/engineer_result_carrier.py`; Telegram publication
      sends TEXT, one ordinary FILE, or a user ARCHIVE without receipts/logs.
      Status() diagnostic ZIP is unchanged. Live on `0.208.18`.
- [x] Migrate `/engineer` Telegram status: coalesces to one editable
      message and uses the shared renderer. Live on `0.208.18`.
- [x] One final carrier: text, one file with caption, or one deterministic
      archive. Chat `make_file` packing (one file or one ZIP) is live on
      `0.208.18` (`friday/orchestration/operation_result_carrier.py`).
      Delivery uncertainty edits status and never duplicates.
- [x] Restart, edit-reject, send-fence and cancel/timeout/`UNKNOWN` proof
      with actual Telegram message counts. Live on `0.208.18`.

### N2 — Deep Web Research and Automatic Knowledge-Gap Search

Status: research I/O/observers are reported live on `0.208.26`; requesting-answer
consumption is on this source tree, not a deployed release. N2 live
configured-model acceptance remains open. `_web_research` plans a 2–8
complementary public-query mission, executes those queries on fact-bearing
public sources, and observes remaining N2 gates on the already-admitted
report. Empty research does not attach observation keys.
`SEARCH_BLOCKED_PRIVATE` never becomes a query plan. The observer
does not search, fetch, or invent claims. The requesting answer consumes
the admitted bundle. Live `_web_research` still
refuses private observed URLs, empty-after-outbound
(`no_admitted_sources`) and invalid provider facts. `_web_search`
and `_web_fetch` refuse `BLOCKED_PRIVATE`. File+web comparison and
`POST /api/ingest/url` consume `BLOCKED_PRIVATE`. Private N2
self-score is Friday's own gates, not Gemini parity. Do not claim
Gemini parity without a paired scored set. Do not fabricate
witnesses. F.2 calendar/window/document-date contracts remain in
existing tests; F.0 pinned frozen Moscow fixtures. Do not hardcode Dest's
location for every tenant.

The checkboxes below acknowledge those specific contracts/observers only.
- [x] The requesting answer consumes an exact admitted evidence bundle on
      `origin/main` (`friday/orchestration/web_answer_evidence_consumption.py`,
      kernel `consume_kernel_web_answer`, runtime after
      `_strip_model_authored_web_urls`). Claims/citations come from the actual
      answer and admitted ledger, not an empty observer claim set.
      Unsupported/current-sensitive/contradictory/private evidence holds or
      blocks publication. Restored source-followup, isolated current-file+web
      compare and empty model bodies skip consume so code-owned answers stay
      intact. Not a deployed release.
- [x] Automatic currentness and configured-provider fallback reach the
      requesting-answer consumer on `origin/main`, without private queries or
      new provider credentials. Prefetch refuses `SEARCH_BLOCKED_PRIVATE`.
      `web_surfer.search` consults `select_web_provider`. `FALLBACK_USED` is
      `admitted_degraded`; `UNAVAILABLE` holds. I/O observers stay empty-claim.
      Not a deployed release.

- [x] Automatic currentness / knowledge-gap policy module on `origin/main`
      (`friday/orchestration/web_currentness_policy.py`). Prefetch and the
      requesting-answer consumer consult it. Private filenames, paths and
      deictics stay local.
- [x] Multi-query research mission planner live on `0.208.26`
      (`friday/orchestration/web_research_mission.py`). Emits 2–8
      complementary public queries; `SEARCH_BLOCKED_PRIVATE` never becomes a
      query plan. Kernel `_web_research` plans and executes complementary
      queries on fact-bearing public sources.
- [x] Provider selection and honest degraded-fallback policy on
      `origin/main` (`friday/orchestration/web_provider_policy.py`).
      `web_surfer.search` and the requesting-answer consumer consult it.
      PRIMARY_OK / FALLBACK_USED / DEGRADED_PARTIAL / UNAVAILABLE; empty
      success is refused. Not a deployed release.
- [x] `WebEvidenceBundleV1` contract on `origin/main`
      (`friday/orchestration/web_evidence_bundle.py`). The requesting-answer
      consumer reuses its claim/source facts. I/O observers still do not
      invent claims.
- [x] Public-web source diversity on `origin/main`
      (`friday/orchestration/web_source_diversity.py`). Lexical hostname
      only; EMPTY / SINGLE_HOST / CONCENTRATED / DIVERSE. Observed live
      on `0.208.26`.
- [x] Research consumption gate on `origin/main`
      (`friday/orchestration/web_research_consumption.py`). CONSUMABLE /
      CONSUMABLE_DEGRADED / BLOCKED_PRIVATE / UNAVAILABLE. Live kernel
      `_web_research`/`_web_search`/`_web_fetch`, file+web comparison and
      `POST /api/ingest/url` refuse `BLOCKED_PRIVATE`; kernel research also
      refuses empty-after-outbound. The requesting-answer consumer uses the
      selected provider decision; I/O observers still do not invent claims.
- [x] Research readiness composition observed live on `0.208.26`
      (`friday/orchestration/web_research_readiness.py`). READY /
      READY_DEGRADED / NOT_READY from mission, diversity and consumption.
- [x] Citation host coverage on `origin/main`
      (`friday/orchestration/web_citation_coverage.py`). COMPLETE / PARTIAL /
      EMPTY / BLOCKED_PRIVATE; lexical hostname only. Observed live
      on `0.208.26`.
- [x] Claim-support contract on `origin/main`
      (`friday/orchestration/web_claim_support.py`). COMPLETE / PARTIAL /
      EMPTY / UNSUPPORTED / BLOCKED. Contradicting-only and unknown source
      ids are not support. Observed live on `0.208.26`.
- [x] Answer-admission gate observed live on `0.208.26`
      (`friday/orchestration/web_research_answer_gate.py`). ADMITTED /
      ADMITTED_DEGRADED / HOLD / BLOCKED from readiness and citation
      coverage. Does not invent claims.
- [x] Contradiction coverage on `origin/main`
      (`friday/orchestration/web_contradiction_coverage.py`). EMPTY / NONE /
      PRESENT / UNIVERSAL / BLOCKED. Supporting-only is not contradiction.
      Observed live on `0.208.26`.
- [x] Exact mission coverage observed live on `0.208.26`
      (`friday/orchestration/web_mission_coverage.py`). COMPLETE / PARTIAL /
      EMPTY / BLOCKED. Extra executed queries do not complete coverage.
      Coverage input is only planned queries that actually ran.
- [x] Evidence grounding on `origin/main`
      (`friday/orchestration/web_evidence_grounding.py`). EMPTY / GROUNDED /
      PARTIAL / UNGROUNDED / BLOCKED. A claim is grounded iff an admitted
      supporting or contradicting source id is present. Observed live
      on `0.208.26`.
- [x] Source date coverage on `origin/main`
      (`friday/orchestration/web_source_date_coverage.py`). EMPTY / DATED /
      PARTIAL / UNDATED / BLOCKED. `retrieved_at` alone is not dating.
      Observed live on `0.208.26`.
- [x] Claim currentness admission on `origin/main`
      (`friday/orchestration/web_claim_currentness.py`). EMPTY / ADMITTED /
      HOLD / BLOCKED. `SEARCH_NOT_REQUIRED` does not admit current-sensitive
      claims. Observed live on `0.208.26`.
- [x] Passage-reference coverage on `origin/main`
      (`friday/orchestration/web_passage_reference_coverage.py`). EMPTY /
      REFERENCED / PARTIAL / BARE / BLOCKED. Title, digest and `retrieved_at`
      are not references. Observed live on `0.208.26`.
- [x] Remaining N2 contracts observed live on `0.208.26` by
      `friday/execution_kernel/web_research_gates.py` on an already-admitted
      research report. Compact keys only; fail-closed on TypeError/ValueError.
      Do not fabricate witnesses.
- [x] Private representative N2 self-score on `0.208.26`
      (`test_n2_private_self_score_is_friday_gates_not_gemini_parity`).
      Body-free compact dict of Friday's own gates. Explicitly not Gemini
      parity.
- [ ] Gemini parity needs a paired scored set. Do not claim it.
      Owner-parked into Pandora 2026-09-05 (P0W cancelled; owner will not
      enable or pay a Google billing account; Gemini API Search grounding
      unavailable). Evidence:
      `handoffs/SolGoodman/P0W-CANCELLED-BY-USER-001.md`. Not `[x]`.

### N3 — Coding Mode

Status: N3 product completion remains open. Previously deployed `/coding`
static inspect, isolated-worker boundary, admitted
archive extract, isolated untrusted `py_compile` / unittest,
prompt-to-small-project, one-final `friday-source.zip` pack, digest
and overwrite observation on extract, Coding Mode
view/plan-gate/carrier, and upload-modification EMPTY observer remain
live through `0.208.28`. Owner private Telegram only; execute/run of
uploaded programs stay fail-closed. Engineer bubblewrap is not a
Coding Worker. This is not a safety certification.
Upload-modification apply never rewrites uploaded project files. Do
not claim safe build/test of untrusted uploads.

- [x] Bare-source inspection contracts on `origin/main`
      (`coding_source_member.py`, `coding_source_tree.py`,
      `coding_source_inspect.py`, `coding_inspect_hazards.py`,
      `coding_toolchain_hint.py`, `coding_inspect_report.py`).
      EMPTY / MAPPED|INSPECTED|HINTED / BLOCKED. No execute, no
      rebuild, no file I/O, filename-suffix hints only. Live as
      `/coding` static inspect on `0.208.19`. Isolated-worker boundary
      live on `0.208.20`.
- [x] Prompt-to-scaffold groundwork reported live on `0.208.26`
      (`friday/organs/coding/create.py`). This writes a fixed no-op template,
      not an implementation of the requested application. No execute.
- [x] Functional application creation with an independent behavior oracle
      (`friday/organs/coding/behavior_oracle.py`, `verify.py`). Frozen
      `csv_summary_v1` family; BUILD then ORACLE inside the proved
      user-systemd cgroup tree. Bounded repair (1 initial + 2). Calculator
      create stays unverified. Execute/run stay fail-closed. Not live on
      `0.208.55`. Not a safety certification.
- [x] Real edits to an immutable published revision, bounded tests/repair,
      durable accepted revision and exact follow-up/rollback. Frozen
      `csv_summary_v1` is inherited from the parent publication; BUILD then
      ORACLE run on the new revision; parent bytes stay unchanged. Reply
      `доработай этот проект` / `восстанови этот проект` / `проверь этот проект`
      resolve that exact assistant publication, never latest/HEAD. Explicit
      `msg_` + digest remains the expert path; conflicting reply+ids fail closed.
      The EMPTY upload-modification observer is unchanged. Not live on
      `0.208.55`. Not a safety certification.
- [x] Aggregate resource enforcement for uploaded tests on this source tree
      (`friday/organs/coding/worker_cgroup.py`). User-systemd cgroup-v2
      `memory.max` / `memory.swap.max=0` / `pids.max` / `cpu.max` are
      installed and read back before the bubblewrap TEST supervisor is
      joined into that unit. Native tests prove a passing unittest, a
      forking memory tree is not TEST_OK, and a pid tree is not TEST_OK.
      Execute/run stay fail-closed. Not live on `0.208.55`. Not a safety
      certification. Engineer is not the Coding TEST runner.
- [x] Upload-modification EMPTY observer live on `0.208.27`
      (`friday/organs/coding/modify.py`). Composes
      `coding_upload_modification_admission` from inspect, isolation,
      identity and edit-plan facts. Never applies file rewrites of
      uploaded projects. Execute/run stay fail-closed.
- [x] Persistent project identity contract on `origin/main`
      (`friday/orchestration/coding_project_identity.py`). EMPTY /
      IDENTIFIED / BLOCKED. Exact revision only; `latest`/`HEAD`/
      `newest`/`current` fail closed. Not wired to git or a worker.
- [x] Isolated coding worker boundary live on `0.208.20`: dedicated
      Coding bwrap profile, no host secrets, no Docker socket, no
      production database, bounded network. Spawn only after
      admission; probe-only, never executes uploaded project code.
      Isolated untrusted build/test of extracted uploads is a second
      bwrap live on `0.208.24`. Execute/run of uploaded programs stay
      fail-closed.
- [x] Safe archive extract admission on `origin/main`
      (`friday/orchestration/coding_archive_extract_admission.py`).
      EMPTY / ADMITTED / BLOCKED from member metadata. Traversal,
      absolute paths, symlink/hardlink, device, size, bomb ratio,
      file count, nesting and case-fold collisions fail closed.
      In-memory zip extract observer consumes this gate live on
      `0.208.22`; host paths are never opened.
- [x] Archive extract-plan family on `origin/main`
      (`coding_archive_member_catalog.py`,
      `coding_archive_extract_plan.py`,
      `coding_archive_digest_facts.py`,
      `coding_project_isolation_admission.py`,
      `coding_archive_overwrite_plan.py`). Catalog, relative
      destinations, project-root isolation are consumed by the
      extract observer live on `0.208.22`. Digest facts and
      overwrite/collision are observed on extract live on `0.208.26`.
      Untrusted execute remains fail-closed.
- [x] One final source archive and restart/rollback contract groundwork.
      TEXT/FILE/ARCHIVE plan, manifest, pack admission, publication,
      restart, rollback and uncertainty contracts are on `origin/main`
      (`coding_result_archive_plan.py`,
      `coding_result_archive_manifest.py`,
      `coding_result_archive_pack_admission.py`,
      `coding_result_publication_admission.py`,
      `coding_result_restart_admission.py`,
      `coding_result_rollback_admission.py`,
      `coding_result_uncertainty.py`). One-final `friday-source.zip`
      pack, restart/rollback EMPTY observation and uncertainty are
      live on `0.208.26`. Never restart/rollback I/O. Execute/run
      stay fail-closed.
- [x] Coding Mode situation composition on `origin/main`
      (`coding_mode_intent.py`, `coding_mode_snapshot.py`,
      `coding_mode_execute_claim.py`, `coding_mode_plan_gate.py`,
      `coding_mode_carrier.py`, `coding_mode_view.py`). EMPTY /
      PROJECTED / BLOCKED from landed inspect/admission facts.
      Untrusted execute without an admitted worker fails closed.
      Execute-claim is composed by `/coding` static_turn live on
      `0.208.22`; isolated BUILD/TEST loop is live on `0.208.24`;
      plan-gate, carrier and view are composed live on `0.208.26`;
      the process worker remains probe-only; execute/run stay
      fail-closed.

Do not prebuild a compiler catalogue. Do not weaken Engineer Mode or
primary release certification to create the worker.

### N4 — Whole-Organism Coherence

Status: mixed-journey projections are reported live on `0.208.21`; mixed
file+archive+web consume is deployed on `0.208.57` but only injected prepared
evidence can reach comparison. The ordinary AgentRuntime dispatch provides
none of the three prepared evidence lanes and returns unavailable. Real
end-to-end acceptance remains open; existing projections remain preserved.
Local source preparation now reads a genuine current upload and an exact owned
archive filename/raw revision through the shared reader. Complete selector
parsing, quoted spaced filenames and invalid-reference rejection are tested at
the source, web adapter and AgentRuntime boundaries. The next local candidate
connects these readers, the existing transient web adapter and attested model
through `mixed_journey/runtime.py`. It commits both messages with final source,
process-lease and parent-context guards in one common transaction. Real
`/api/chat` upload tests prove one authenticated publication pair, idempotent
replay after application recreation and refusal after archive revocation;
73 focused integration cases passed. A subsequent disposable bridge -> signed
HTTP -> real runtime test found cached Telegram delivery bypassing source
revocation after reconnect. The local candidate adds a server-owned,
answer-digest-bound source authorization read before initial/cached mixed
delivery; denial uses existing durable termination/send fences. 66 focused
HTTP/replay/Telegram/status regressions pass, including rejection after revoke,
pre-accept retry, accepted-send timeout without duplicate, and one editable
status on a delayed comparison. External model/web/Telegram are fixtures;
this is not LIVE_OWNER evidence. This candidate is not independently accepted
or deployed. Live Telegram, process-kill recovery, immediate status for fast
turns and complete failure/coverage acceptance remain open.
Sol's preliminary runtime/status finding is reproduced and repaired locally:
authorized file/archive facts and common web consumption are stored with the
actual turn identity. The signed delivery authorization read supplies those
body-free facts to the existing mixed observer; ordinary API responses strip
them. A delayed real-runtime bridge turn now edits one status into MIXED with
file+archive+conversation+web, preserving document mode for media groups.
Prose emits no table fact. An unavailable web projection cannot veto a useful,
source-authorized partial answer. 106 focused runtime/HTTP/Telegram/privacy
cases and two explicit album checks pass; independent review remains pending.
Source preparation also reproduced an unintended relation-history write and
waiting behind an unrelated writer before request-effect admission. The local
reader now uses the shared rollback-only read snapshot with the inherited
deadline; bounded reads reject uncommitted source authority. 121 focused
source/runtime/HTTP/Telegram cases pass, including concurrent committed reads
and unchanged relation clock. This additional delta is not yet Sol-reviewed
or deployed.
Sol's formal review of the prior `905ddd8` candidate accepts source ownership,
final atomic reauthorization, rollback, cancellation and the privacy repair,
but reproduces a cache-discriminator bypass: removing cached `context` lets an
old mixed answer replay after archive revocation. This remained in `f7ea9e96`.
The next local delta reads the owned durable assistant before passthrough,
rejects missing/mismatched mixed cache context and binds missing-identity
refusal to the original authenticated request at both HTTP replay entries.
An ordinary message cannot lend its identity to different cached content.
The actual API reproduction failed before repair; 18 final source/cache/HTTP
cases pass, including `/api/chat` and `/api/me/regenerate`, with no model/web
rerun. Earlier 28 replay/Telegram/regenerate and 24 effect-fence checks pass.
This additional repair still needs independent review and release acceptance.
Sol R2 accepts the `f7ea9e96` projection delta; R3 accepts the `1fd208c0`
HTTP-cache and rollback-only reader deltas. R3 retains a distinct Telegram
cache-discriminator P1. A real signed-backend/reconnect test reproduces an
answer delivered after archive revoke when cached context disappears. A
separate `/retry` cache reproduction finds the same missing delivery gate.
The new local delta discards cache-supplied internal facts before every exit,
classifies identified cached bodies from owned durable server metadata, and
uses fresh source authorization for actual mixed replies in both normal chat
and `/retry`. Ordinary identified replies require the exact owned body and
conversation; classification does not grant mixed source rights. Unidentified
responses with mixed cues or internal facts refuse. Genuine ordinary legacy
notices without those claims keep their existing path.
33 final signed transport/API/replay cases pass; earlier 150 bridge/profile/
uncertainty/completion checks and 90 cache-recovery checks pass (overlapping
scopes). Four final unidentified-cache cases also pass. External Telegram,
model and web remain fixtures. Independent review and installed/live
acceptance of this Telegram delta remain pending.
Sol R4 accepts identified cached-body classification and fresh mixed authority
on `dd769fea`, but finds `/retry` can lose every cached identity and source
cue. A genuine stored-answer/revoked-source/bridge-reopen regression confirms
the leak. The local fix requires durable identity for every cached `/retry`;
unknown bodies terminate through the existing delivery fences. 161 focused
Telegram checks pass, including owned ordinary replies, foreign/body drift,
and file-suffix resume without repeated generation or duplicate delivery.
The suffix fixture now supplies durable identity and the signed classifier
contract; its publication assertions are unchanged. Sol R5 independently
ACCEPTS `ac779832` (tree `dcd16a7d`), including all preceding N4 repairs:
161 focused checks plus an independent unidentified-cache leak probe pass.
Receipt: `handoffs/SolGoodman/ASTRA-N4-RUNTIME-ATOMIC-001-RESULT-005.md`,
SHA256 `603be72de4e6bf5946eb616ff12f4737134354b2a6c34ebc892e9ebb9e079ac0`.
No installed/live acceptance is claimed.
The next local F.6 delta reproduces fast mixed turns creating no status under
the default 12-second schedule. Normal chat/media admission now starts the
existing fenced status immediately, with a one-second advisory wait cap;
cached delivery reuses the persisted status or starts one after authorization.
Timeout/cancellation drains the initial send and retains unknown-send fences.
368 Telegram checks pass, including default-schedule signed mixed runtime,
fast text, initial connect/accepted-timeout/stall, cancellation and reopened
delivery without backend replay. Old final-send fault fixtures now address
answer chunks separately from status; their no-duplicate assertions remain.
The cached reply-lineage fixture also supplies the accepted N4 classifier.
Independent review of this status delta and installed/live acceptance remain
open, along with complete table/coverage and process-kill journeys.
Sol F.6 R1 independently reproduces a P1: cancellation during Telegram's
proven `429 retry_after=5` backoff leaves a permanent false UNKNOWN status
fence. His 368 Telegram checks pass but this independent probe fails.
Receipt `handoffs/SolGoodman/ASTRA-F6-IMMEDIATE-STATUS-001-RESULT-001.md`,
SHA256 `c8e043bf976e6475bb3ecbd3a0a28a35d723327d4471044ed861a92e1a56606f`;
candidate `3e14e93c` stays REVISE. Astra owns the repair; Sol now reviews
the separate immutable F.7 R2 repair (`35752513`).
The local F.6 repair reproduces both fast-turn/reopened-delivery failures
with a real `429 retry_after=5` beyond the one-second cap. The existing
status sender now distinguishes cancellation during proven-rejection
backoff from cancellation during an HTTP attempt: only the former clears
its exact revision fence, while cancellation still propagates. An invalid
429 body cannot authorize another send. 381 Telegram checks pass, including
explicit cancellation before/after retry dispatch, preserved in-flight
UNKNOWN, malformed rejection bodies, and one backend/final after reopening.
Sol F.6 R2 independently ACCEPTS this repair on `b6b00e11` / tree `f0d918dc`:
his original rate-limit probe passes, and independent replacement-status
probes distinguish backoff cancellation from in-flight uncertainty.
Receipt `handoffs/SolGoodman/ASTRA-F6-IMMEDIATE-STATUS-001-RESULT-002.md`,
SHA256 `ce1aa9c10194d37ee83f8a640b60219618e9989de75ee0b314d9f62d64012c4a`.
Installed/full-F.6/custom-command/process-kill acceptance remains open.

Sol's separate F.6 coalescing probe returns REVISE: a blocked status edit
plus 32 refreshes creates 33 pending tasks; a stalled advisory edit after
confirmed final delivery retains the inbound row and blocks its chat queue.
Receipt `handoffs/SolGoodman/ASTRA-F6-COALESCE-001-RESULT-001.md`, SHA256
`31fb544dd3dc797581c73d098ef39ac8c8d3d5675aeb72900131224709d140dd`.
The local repair keeps one in-flight publication and one replaceable latest
snapshot per operation. A queued terminal snapshot absorbs later refreshes.
Final advisory drain is bounded to one second; every status child belongs
to the enclosing update and is cancelled/drained when that update exits.
The existing status manager retains publication authority and unknown-send
fences; timeout never fabricates terminal acknowledgement. Actual manager,
HTTP and inbox probes reproduce both findings and an additional orphan edit
on parent cancellation. All 389 Telegram checks pass, including burst
coalescing, independent operations, cancelled source/final work, and a stalled
edit or replacement send after confirmed delivery followed by inbox reopen.
Unsent snapshots may spend revisions: persisted revision matches the exact
last offered snapshot; offered revisions remain strictly increasing. This
supersedes tests equating every revision with a network call, while retaining
one-send, edit-only, terminal and delivery-ownership assertions. Independent
repair review and installed/live acceptance remain open.
Sol independently PASSES the six-path coalescing repair on `98c1e81d` /
tree `5409f1ef`: all 389 Telegram and three inventory checks pass, with
independent burst, terminal-priority, cancellation, replacement and 429
falsification. Receipt
`handoffs/SolGoodman/ASTRA-F6-COALESCE-002-RESULT-001.md`, SHA256
`d5f6fa8e8b15ac689ac90a6cddd633a99f7bbc3267a469c70456ee5d9ea46c01`.
A focused decompile-notifier timing flake also reproduces on the preceding
base; the complete suite passes. This accepts the source repair only.

The next local N4 table delta reproduces two real-runtime failures: an
explicit table request accepted prose, and a valid table returned `plain`
format. The existing two-call comparison now receives a code-owned shape
constraint, rejects missing/ragged/fenced tables and citations only outside
the table, and verifies the exact accepted answer before atomic publication.
The shape is intended to come only from an active request. A verified pipe table
uses Markdown through both entry points; durable metadata and fresh signed
delivery authority bind its format, so reconnect cannot change rendering
under a chunk cursor. The prepared-evidence entry no longer invents a
`tbl_*` knowledge object or table-organ fact from every prose answer.
572 focused checks pass, including actual runtime -> signed HTTP -> Telegram
table delivery, safe reconnect without model/web replay, rejected format
drift, honest web-timeout partial table, and HTTP application reconstruction.
The model/web/Telegram services are fixtures; this does not certify live
model quality, installed acceptance, full F.6 or a persisted table organ.
Sol R1 returns REVISE on `eadb689e` / tree `216fd4c`: a line wrap discarded
the reporting prefix, `без лишнего текста` wrongly negated a table request,
and four-space rows passed validation but remained raw pipes in Telegram.
Receipt `handoffs/SolGoodman/ASTRA-N4-TABLE-001-RESULT-001.md`, SHA256
`dd153652f86c91f17b60867688c5d06d41c7af9dfa41973f73d43efc85295761`.
The local R2 repair proves the entire active clause prefix, retains reported
scope across line wraps, masks quoted/code/Markdown paragraphs, and binds
negation to the requested table or action. A noun reporting another person's
request cannot substitute for an imperative. Accepted rows use the renderer's
physical newline and bounded indentation grammar; incompatible whitespace
is rejected before verification/publication. Valid indented tables reach the
actual signed HTTP/Telegram path, including requests without extra prose.
617 focused checks pass. Four quoted/code inputs still refuse before web
acquisition under the existing public-query privacy grammar; their shape
semantics are tested separately with prepared fixture evidence. These checks
do not broaden source authority or certify installed/live acceptance.
Before R2 dispatch, Sol's separate F7 R3 report exposed the same blank-line
gap here. File and table presentation now share a small lexical prefix helper;
a reporting colon retains scope across blank whitespace, while an explicit
request lead still works. The 836-check combined packet includes these table
cases. R2 was not dispatched; review will cover the coupled repairs together.
Accepted F.6 and original N4 scopes stand.
Sol's combined output review on `d451cdb9` / tree `a6b6f057` remains REVISE:
older create/format paths still admit reported commands; numbered lists,
subsequent reported sentences and indented code can choose output shape.
Common attachment modifiers and polite return requests can lose the required
source or explicit carrier. Receipt
`handoffs/SolGoodman/ASTRA-OUTPUT-AUTHORITY-004-RESULT-001.md`, SHA256
`6a3b740de36acd0911047ebd1ead2903ce00e8235aa55d064c40b987e8ccfc6d`.
297 independent checks pass, but the adversarial real-runtime probes fail;
output authority and required-source restoration remain open for repair.
The next local output repair applies positive clause authority to creation,
return, conversion, contrast and carrier selection. Action and file output
must belong to the same active clause; obsolete global narrator/colon
exceptions are removed. Reported multiline paragraphs retain scope across
list punctuation and subsequent sentences; indented code and struck text
are masked before visible projection. File-effect projection preserves these
boundaries. Ordinary read/return, source-first, polite and coordinated
requests retain their independently proved source and output requirements.
The existing exact filename path now recognizes bounded unquoted spaced
names after a source lead. Attachment modifiers reuse the existing noun
grammar; no source reader, permission bypass or latest-file fallback is added.
A missing named source also exposed a late generation attempt after the
structural refusal. The late builder now respects that refusal before model
or renderer work. Empty history, unproved lineage and an unrelated current
carrier are negative controls. These remain local repairs pending review;
installed/live acceptance is separate.
Durable organs, the shared view, the
store-shaped projection and the store observer derive mixed journeys
from already-durable identities. Telegram MIXED is PROJECTED-only.
Primary and secondary share one operation identity. New modes compose
existing primitives. Mixed-journey is not a registered organ.

- [ ] A real file + authorized archive + public web task produces a grounded
      comparison/table through the existing runtime and one final TEXT
      publisher (`friday/orchestration/mixed_file_archive_web_query.py`,
      `mixed_file_archive_web_comparison.py`,
      `friday/organs/mixed_journey/consume.py`). Two-call tools-disabled
      synthesizer cites F1+A1+W*; PREPARED_EVIDENCE_2. Production without
      injected evidence fail-closes. The local runtime candidate now performs
      real acquisition and final source reauthorization through the common
      transaction; independent review and installed/live acceptance remain.
- [ ] Duplicate identity, cancellation, restart and one-publication acceptance
      for the real mixed journey. Current duplicate/SEND_UNKNOWN checks work
      within one process; the ledger does not survive a process restart.
      Caller cancellation, child cleanup and cancellation during cleanup are
      the first Astra repair slice (`tests/test_mixed_journey_consume.py`),
      pending independent review and release. Baseline regression reproduces
      six failures. No durable or live acceptance is implied by injected tests.

- [x] Read-only `SharedOperationViewV1` / `AgentSituationProjectionV1`
      on `origin/main` from already-supplied facts. No new execution
      owner.
- [x] Mixed-journey view contracts on `origin/main`
      (`mixed_journey_identity.py`, `mixed_journey_organs.py`,
      `mixed_journey_coverage.py`, `mixed_journey_revoke.py`,
      `mixed_journey_restart.py`, `mixed_journey_view.py`). EMPTY /
      PROJECTED / BLOCKED from already-supplied facts.
- [x] Mixed-journey store-shaped projection on `origin/main`
      (`mixed_journey_file_facts.py`,
      `mixed_journey_archive_facts.py`,
      `mixed_journey_conversation_facts.py`,
      `mixed_journey_web_facts.py`,
      `mixed_journey_table_facts.py`,
      `mixed_journey_store_projection.py`). Maps already-supplied
      organ facts into MixedJourneyViewV1.
- [x] Mixed journeys live on `0.208.21`: file+archive+conversation+web+table
      identities project through `friday/organs/mixed_journey/observe.py`;
      Telegram MIXED when PROJECTED (`admit.py`,
      `mixed_journey_progress.py`, `_status.py`). Exclusive
      DOCUMENT/ARCHIVE/RESEARCH remain otherwise. Inbound album keeps
      DOCUMENT. Filenames without ids stay EMPTY. Private
      paths/filenames BLOCK without payload. Restart, revoke-before-publish
      and secondary-absent primary-only are observed, not invented.
- [ ] Prove one turn, one operation, one status, one result, one effect owner,
      one publisher on the real composed mixed journey, including honest
      `UNKNOWN` and primary-only operation when secondary is absent.

### N5 — Maintainability Ratchet

Status: standing rule, not a rewrite. `friday/agent_runtime/__init__.py` is
~76k lines / 3.81 MiB. No new product logic in that module unless no narrow
seam exists. Extract only a touched seam with exact parity tests. Do not
begin a clean-architecture rewrite. Kernel web-consumption helpers now live
in `friday/execution_kernel/web_consumption.py`; `_web_search`/`_web_fetch`/
`_web_research` still own quota and adapter I/O. Inventory of live
`0.208.24` N3 loop vs `0.208.23` found no dump into `agent_runtime`
(thin `handle_coding_static_turn` dispatch only). The implementable
checkbox is closed on `0.208.25`; this chapter stays in force.

### N6 — TinyFish Search through the existing web contour

Status: **planned; design/intake complete, implementation and live NOT_RUN**.
Owner request: 2026-09-08; source requirement
[FRIDAY_TINYFISH_SEARCH_ASTRA_DIRECTIVE_RU.md](../FRIDAY_TINYFISH_SEARCH_ASTRA_DIRECTIVE_RU.md).
This section is the accepted design and the only mutable register for N6.
The directive's TF labels map to N6.0–N6.5 and optional N6.F; they are not a second queue.
Astra owns design, integration and release; Sol provides independent review;
Grok's existing visible lab performs bounded evidence tasks when assigned.

Execution window: **after the current R10 RC/1.0 acceptance package**, including
its first full baseline, prioritized findings and release obligations. N6 is
queued as a separate reversible N2 extension; it does not change the current RC
scope, frozen052 instrument, sealed A/B sets or release gates. The present order
is intake only. Code, owner-key use and rollout start when this package reaches
that window. N6.1–N6.5 remain required queued work; optional N6.F does not block
Search or reopen Pandora. Current production identity is not re-certified here.

**Design and change boundaries.** Inspected main HEAD is
`1bfd839c7906d2ff055437f64755b4601baf101d`. Reconcile current HEAD and occupied
files again at implementation; preserve ongoing R10 and unrelated dirty work.
Reuse `friday/web_surfer/__init__.py` search/provider chain, pinned HTTP transport,
normalizers, local domain/source-class admission, URL validation, deduplication,
`SearchResult` and attested results. A small proposed `friday/web_surfer/_tinyfish.py`
may hold the pure parser/wire adapter; it is a planned file, not existing code.
No new public model tool, SDK requirement, service, crawler, web corpus, cloud
answer model or orchestrator. Existing direct data sources, reader and local
primary/secondary answer ownership remain. Agent/Browser/Research/Mako, other
provider integrations and automatic archival of search/page content are excluded.

Provider integration must cover the closed enum, observation validators and
consumers in `friday/orchestration/web_provider_policy.py`, existing tool-result
serialization, research reports, chat/mixed and stored evidence compatibility.
The three closed registries are `WebProviderId`, WebSurfer's
`_ADMITTED_WEB_PROVIDER_IDS` and `mixed_journey_web_facts._PROVIDER_IDS`.
Trace existing `execution_kernel/web_consumption.py`, `web_research_gates.py`,
`orchestration/web_research_consumption.py`, `web_research_readiness.py`,
`transient_web_comparison.py`, `organs/mixed_journey/consume.py` and existing
agent-runtime projection/recording; avoid new logic in the large agent runtime.
Core WebSurfer/config/provider-policy were clean at intake; transient/mixed
comparison and query, mixed consume/observe, agent runtime, server/storage core
and inventory contain unrelated dirty work that must be preserved.
Keep the existing source-attempt contract in `friday/web_research_contract.py`:
query200 characters, ordinary8 + direct3 source rows,16 attempts and20,000 text
characters per source. API calls are separately counted and never relabeled as
read pages. Preserve N2 multi-query research and N4 composed outcomes.

**Wire and filter decisions.** Fixed HTTPS Search endpoint, GET, request-local
`X-API-Key`; use the existing client parameter encoder and pinned transport,
no redirects, ambient SDK settings or credential on the shared client. Search
uses page0 only in v1; no automatic page1, invented count parameter or result
padding. Parser validates object/results-array and row types, bounds decoded
bytes/text, admits public credential-free HTTP(S) URLs, preserves order and
existing deduplication/limits. Valid empty, all locally filtered, mixed malformed,
all malformed and error envelope remain distinguishable; all malformed is a
protocol failure. `source=provider_id=tinyfish` reaches the actual consumer;
snippets are not fetched pages, provider dates are not independently proved dates.

Map normalized site/include to include_domains; keep exclude_domains local and
apply exact/subdomain/source-class checks again after response. Send language
and uppercase market only from normalized inputs. Proposed owner defaults when
both are absent are explicit ru/RU; one explicit locale field is sent alone,
with provider auto-resolution recorded without claiming independently verified
locale. Foreign source class remains an independent admission constraint, never
an inferred user location. Unsupported combinations raise the existing typed
filter failure before fallback can widen them. HEAD uses **month31 days**, so
recency_minutes is day1440/week10080/month44640/year525600, derived from its one
existing mapping. No purpose/history, date-window mixing, news/paper mode or new
tool arguments in v1. Unknown/invalid/stale dates never acquire today's timestamp.
The fixed endpoint/parameter and published free-access basis were checked against
[Search reference](https://docs.tinyfish.ai/search-api/reference) and
[pricing](https://www.tinyfish.ai/pricing) on 2026-09-08; account access, actual
billing and RU quality remain unobserved and must be checked before promotion.

**Configuration, credential and money.** Planned settings:
`FRIDAY_TINYFISH_SEARCH_ENABLED=0`, `FRIDAY_TINYFISH_API_KEY_FILE=`, and one
`FRIDAY_WEB_SEARCH_COST_POLICY=legacy|free_only` (default legacy preserves
existing installations; owner's accepted N6 configuration is explicitly free_only).
TinyFish admission in v1 requires enabled + free_only; legacy keeps the existing
chain and reports an incompatible enabled TinyFish setting without admitting it.
Keep the credential file-only in v1: no value-variable/SDK fallback or LLM-settable
path/endpoint. Explicit owner default locale belongs to the TinyFish config,
using planned `FRIDAY_TINYFISH_SEARCH_LANGUAGE=ru` and
`FRIDAY_TINYFISH_SEARCH_REGION=RU`. HEAD's public locale defaults are empty;
this is an explicit TinyFish owner profile, not existing global behavior.
`friday/config/__init__.py`, `.env.example`, `docker-compose.yml` and installed
systemd configuration forwarding must agree; Telegram gets no provider credential.
Strict finite/valid admission must not rely on the loader's generic clamp/default
helpers. Fetch stays absent/
disabled; reserve `FRIDAY_TINYFISH_FETCH_ENABLED=0` only if N6.F is admitted.

In free_only, TinyFish is first only after N6.4 ACCEPT, followed by at most one
explicitly admitted existing free reserve. Initial reserve allowlist is EMPTY;
adding a reserve requires cost/filter admission and evidence, not another provider
integration. Existing Yandex/Brave/Tavily/Serper keys or bonuses grant no spending
permission. Unknown cost is denied. No parallel fanout, fallback after private
policy denial/cancellation, upsell, balance top-up or automatic paid operation.
Insufficient permitted sources produce honest unavailability/partiality. Before
promotion recheck account Search entitlement, applicable terms and auto-payment
settings; documentation is not a measured zero charge or perpetual-price promise.

Owner path is `~/jericho/tinyfish_api.txt` on the owner's machine. During intake
its contents and access are NOT inspected. N6.1 checks regular small owner-only
file/no unexpected symlink, bounded read, newline/header-injection rules, and
runtime-user access; no key/prefix in logs, argv, settings repr, API/UI, audit,
backup or evidence. Resolve host/container paths explicitly through existing
systemd secret/config forwarding or a single read-only mount, never whole HOME.
Use metadata/tracked-name checks and targeted Git/build/Docker/report exclusions;
if already tracked/published, stop package publication and record exposure under
the existing rotation procedure, without unilateral revocation/history rewriting.
Missing/invalid credential disables the adapter without breaking Friday startup.

**Failure, shared quotas and deadlines.** Closed bounded reasons: local policy/
invalid filters terminal before any network;400 contract failure without repeats;
401 suspended until credential correction;402 access failure without payment;
403 service refusal distinct from local denial;404 endpoint drift without probing
other hosts;429 validated bounded cooldown;500/503/network/TLS/timeout transient;
200 malformed/error protocol failure. Do not execute arbitrary error messages or
persist raw bodies/URLs. Cancellation propagates and closes streams before any
fallback. Diagnostics use existing status/evidence with configured vs verified
access, actual provider/fallback, reason, attempts/results, elapsed time and
cooldown; sanitized bounded request-ID only, no query or ordinary query hash.

Initial local Search ceilings: rolling20/minute and400/hour, concurrency2 for all
runtime tasks sharing a credential, including retry/research. Reuse runtime_kv
and storage transactions for a bounded atomic reservation/cooldown record under
an opaque installation-scoped credential identity; no query/key in that record,
no per-instance counters, Redis or new daemon/schema by default. Extend a narrow
storage seam if needed; existing daily per-user counters are not this contract.
`WebSurfer` currently takes only settings: explicitly wire the shared reservation
through existing runtime construction (`friday/server.py`) and private test/bench
construction. Keep the current per-person daily web quota independent. A local
reservation refusal means outbound_attempted=false; do not reuse the existing
AllProvidersRefusedError path that implies a network attempt. No ready minute/hour
reservation currently exists; check/reserve both windows in one transaction,
not kv_get followed by kv_set. Reservations survive restart, concurrent processes share the same storage owner,
leases expire safely after crash; corrupt state/clock regression refuses admission
until safe recovery. Other programs' usage is unknown; server429 is authoritative.

Per Search attempt min(10s, remaining overall deadline), connect min(3s, remaining),
decoded response min(256KiB, existing configured web byte limit); string limits
reuse the existing contract. Default one attempt; at most one transient retry
only within remaining deadline and both quotas. Search/retry/free reserve together
fit the current caller budget; no stage gets a fresh full budget. Long Retry-After
is recorded as cooldown without holding the chat. Verify slow continuous streams,
compression expansion, cancellation and resource closure with controlled clocks.

**Work packages and evidence.** All execution rows below are NOT_RUN. Each row
is conditional on its dependencies, not a claim of a present external blocker.
Use existing inventory/invariant IDs, adding only necessary tests in the touched
modules. Store dated hash-bound, secret-free evidence under the existing
`handoffs/SolGoodman/N6-*` review and private lab artifact mechanisms; record actual
paths/commands/candidate identities/verdicts here when observed. No new status log.

| ID / owner | Dependencies and implementation seams | Checks and completion criterion | Stop condition / rollback |
|---|---|---|---|
| N6.0 / Astra | Owner intake; current HEAD/R10; design in this section | Directive mapped to one canonical package with contracts/queue/acceptance; documentation-only static inspection complete | Reconcile new source conflicts before implementation; revert only this intake if owner changes scope |
| N6.1 / Astra; Sol review | N6.0 + R10 window; config loader/validation, deploy forwarding, examples/exclusions, WebSurfer and proposed narrow parser | Default-off/file secret/wire/filter/parser: T01–T12,T24–T29; fake transport and startup reachability; no real key in CI | Unsafe secret/build exposure or incompatible filter blocks candidate publication; keep default-off and previous config |
| N6.2 / Astra; Sol review | N6.1; provider policy, attestation, tool-result/research/chat/mixed consumers and existing runtime construction | T19–T22,T30–T32: exact tinyfish provenance, closed compatibility and zero prohibited endpoint calls with paid keys present | Cost/privacy/provenance mismatch blocks integration; disable TinyFish while preserving free_only |
| N6.3 / Astra; Sol review | N6.2; runtime_kv transaction seam, shared deadline/transport cleanup, diagnostics, current test inventory | T13–T18,T23 plus full T01–T32 traceability; parallel/restart/fault probes; existing off/no-key/legacy/direct/lookup/research/chat/mixed/primary-only regressions | Quota race, deadline/cancellation/resource leak or missing gate binding blocks live; default-off, retain safe budget/cooldown state |
| N6.4 / Grok lab; Astra verdict; Sol evidence review | N6.3 and actual local credential/access/cost confirmation; extend existing web_search_bench isolation, dated quality fixture, ordinary chat | TinyFish-only smoke, repeated frozen20 series, separate40-question quality set, actual sources/required reads/local answer/citations; numeric gate below, NOT_RUN until measured | Inaccessible/paid/unverified terms or failed quality/safety yields no primary promotion; leave off or only separately accepted free reserve; no paid A/B without owner budget |
| N6.5 / Astra; Grok observation; Sol review | N6.4 ACCEPT + ordinary exact-release/install gates; owner config and runbook | Explicit TinyFish-first/free_only activation, installed/live ordinary and mixed/primary-only scenarios, compatible stored reports, verified rollback/observation | No activation before product evidence; rollback only to cost-compatible artifact/config, never silently restore paid Yandex |

N6.0 is the completed current design action; N6.1–N6.5 are open. The requirements
T01–T32 mean the complete numbered matrix in directive §13, including field types,
all filtered vs empty, mirrors, freshness/locale, short page, all error statuses,
429 variants, streaming/compression, shared quota/restart, cancellation, injection,
SSRF/redirect/header isolation, secret-free diagnostics and conserved provenance.
In N6.3 map every requirement to actual existing/new invariant and inventory node;
no unchecked requirement inherits PASS from another provider or a mocked live test.
Extend the existing `test_web_search_filters`, `test_web_provider_policy`,
`test_kernel_web_consumption`, `test_web_research_consumption`,
`test_mixed_journey_web_facts`, `test_web_research_context_budget`,
`test_the_web_has_a_budget_and_manners`, `test_compose_env_contract` and
`test_inert_settings` families as appropriate; the sequential daily-counter test
is not proof of concurrent credential reservations.
No unit/CI calls the real API or reads the owner's key; live is explicitly isolated.

**Predeclared N6.4 gate.** Before the first request freeze configuration, queries,
expected evidence/rubric, exclusions and measurement method. Three separate
quota-respecting runs of the unchanged20 FROZEN_QUERIES: at least18/20 usable
nonempty results EACH. Separate40 public questions frozen before measurement:
at least36/40 score2 on the directive's0/1/2 rubric, at least18/20 in its declared
20-question RU subset. Cases span RU detail, official technical docs/exact errors,
current facts, English primary sources, domain filters and false premises.
Score2 requires a source supporting the asked fact; label false-premise/absence
correctly rather than accepting fabricated evidence. Report all attempts,
denominators/exclusions and nearest-rank p50/p95 for API separately from full chat;
API p95 <=10s and within inherited stage budget. Live compatibility checks must
pass; zero tolerance for cost/privacy/authority/provenance/freshness/fake-read/
quota/cancellation defects. No unreviewed LLM self-grading, permanent probe loop or
historical Yandex median vs new TinyFish p95 comparison. New Yandex A/B requires a
separate owner budget; historical evidence is dated context only. A request for
links does not force reading every page. Actual account charges remain unknown
unless independently evidenced; configured never means healthy/accepted.

**Rollback and separate Fetch decision.** Keep Yandex implementation and historical
provider IDs. In free_only, disable TinyFish and keep only an admitted free reserve
or honest unavailability. A predecessor that does not enforce free_only is NOT a
safe rollback with paid keys still present: choose a compatible artifact or remove
paid-provider credential forwarding from that rollback configuration and prove
zero calls before activation. No implicit spending authorization on rollback.

N6.F is **optional, deferred for separate justification after N6.4**, owner Astra;
not required for N6.1–N6.5 acceptance. Admit only if measured native reader failures
justify it. Reuse normal reader first; never escalate policy/robots/login/paywall,
private/signed URL denials to external Fetch. A later bounded package must define
its own default-off flag, endpoint-only secret, original/final URL public checks,
exact requested-URL association, missing/duplicate/conflicting success+error and
HTTP200 partial errors, target-status honesty, TTL/freshness distinctions,
per-URL quotas/deadlines and preservation of successful partial reads. No unasked
URL, fabricated target200, bulk auto-fetch, advanced extraction or Agent/Browser.
Its first evidence must show a correct Search source whose native reading failed;
repeating search is not Fetch acceptance. N6.F rollout/rollback remains independent.

### N7 — Optional DeepSeek Harness executor and laboratory fault server

Status: **candidate; owner directive registered, implementation/tests NOT_RUN**.
Owner request: 2026-09-11; authoritative requirements:
[FRIDAY_ASTRA_DEEPSEEK_HARNESS_ADOPTION_DIRECTIVE_RU.md](../FRIDAY_ASTRA_DEEPSEEK_HARNESS_ADOPTION_DIRECTIVE_RU.md).
This is one bounded post-release epic, not a separate mutable backlog. E0–E6
below preserve the directive's identifiers. Astra owns design/integration/release,
Sol independent review, Grok laboratory preparation and evidence.

**Release boundary:** functional adoption follows accepted Friday1.0; it adds
zero functional GO requirements to the current RC. E0 and isolated E1 may run
earlier only with capacity independent of the R10 critical path. Existing N6
TinyFish priority and scope remain. E0–E5 are required adoption work; E6 is
conditional on demonstrated benefit after E5. E7 options are not authorized
implementation commitments. Do not reopen Pandora or replace the development TUIs.

Intake observed local main HEAD `1bfd839c7906d2ff055437f64755b4601baf101d` on
2026-09-11, with ongoing private R10 candidates and fixes; main is not the latest
accepted product or live identity. Reconcile actual source and ownership again
at E0. The directive's upstream candidate is DSH commit
`c291e7961a515f6d7af9304e7fd1d257929aef26`; its cited prerelease
`dsh-v0.1.5-rc.2` resolves to a different commit
`fb2c4b9e698e30edb738bca4cf0618587db7d203` (public upstream metadata observed
2026-09-11). Package/runtime equivalence and transport compatibility remain
unverified; source SDK versions do not establish installed artifact identity.
Evidence: `~/.jericho/grok-takeover/ASTRA-187-N7-E0-upstream-metadata.json`.
**Owner clarification, 2026-09-11:** immediately before actual DeepSeek adoption,
recheck the then-current repository, releases, APIs, dependencies and licenses;
select and pin the appropriate snapshot after that review. Today's metadata and
the directive's versions are preliminary references, not a locked implementation
choice. Preserve this refresh in E0 and revise the adoption matrix accordingly.
No runtime, SDK, image, dependency or model endpoint is installed/activated by intake.
Bounded E0 local owner mapping is prepared in
`~/.jericho/grok-takeover/ASTRA-186-N7-E0-local-owner-map.json`: existing
WorkItem reconciliation/completion and result-carrier boundaries were inspected.
The current coordinator is command-specific and the stored channel is Telegram;
DSH integration is not a drop-in ledger/session substitution. E0 remains open;
upstream transport compatibility and functional adoption have not run.
Grok's bounded E1 coverage map is received in
`~/.jericho/grok-takeover/ASTRA-188-lab223-received.json` (11 inspected files).
The shipping completion client requests non-streaming JSON; observed unit tests
use patched HTTP clients or MockTransport. Prepare real HTTP fault controls for
that path; SSE-specific controls apply to DSH or an actually streaming client.
This is a scoped source map, not exhaustive absence proof or executed E1 work.

**First exemplar:** an authenticated owner Engineer request to correct a small
bug in a dedicated workspace snapshot, produce a patch, pass independently held
tests on the exact output bytes, and publish the verified artifact through Friday.
Reuse existing `orchestration/engineer_work_item_coordinator.py`,
`interaction_control_plane/engineer_work_item.py`, execution kernel/Engineer
authority and sandbox, `orchestration/engineer_result_carrier.py`, existing artifact
and publication admission, and V12 model admission/transport. These are existing
ownership seams, not claims that an ACP bridge or model gateway already exists.

| Item / owner | Depends on / scope | Required result and acceptance |
|---|---|---|
| [ ] **N7.E0 — delivery and transport decision / Astra, Sol review** | Isolated preparation permitted; preserve R10 capacity. | Map existing owners/gaps and all directive§4 families as reuse/already-covered/configure/defer/reject. Bound ACP compatibility spike: pinned official Python ACP client + `dsh --profile acp`; verify handshake/capabilities, new/prompt/updates/cancel/close/permission callbacks and actual local model route via existing admission. Do not guess protocol version or substitute load for resume. If incompatible within spike, choose official SDK with one isolated process per Work Item; do not implement both transports or a new RPC library. ADR, adoption matrix, exemplar and revised estimate with exact candidate versions; no production dependency while disabled. |
| [ ] **N7.E1 — complete HTTP/SSE fault server / Grok, Sol review** | Independent of E2 after pinned upstream selection; dev/test only. | Adopt upstream `packages/test-support/llm-mock-server` with its build/CLI/tests/licenses in pinned dev image; no invented installable binary. Exercise shipping Friday adapter and DSH over actual HTTP/SSE: connection refusal, partial disconnect, hang, empty/malformed response,429/5xx/auth/overflow/tool/max-token/success. Fail on exhausted sequence; separate servers/dynamic ports or deterministic schedules for global FIFO. DSH replay/test-support stays for DSH. Reproducible runner/fixtures/terminal assertions; mock PASS supplies no live-model credit. |
| [ ] **N7.E2 — immutable runtime and local model profile / Astra** | Accepted1.0, E0; E1 preparation can run in parallel. | Reuse verified official artifact or upstream build from commit+lockfile; distinguish official SDK/runtime-bin/npm identities. Manifest binds repository/commit/package/platform/dependencies/image hashes/licenses/notices/build recipe/profile digest. Minimal composition on existing bundles; inspect actual tools, not assume sdk-minimal is full coding. Immutable runtime/config; OS isolation, job HOME/workspace/output, no host homes/secrets/Docker socket/Pandora. Local admission covers tools/compaction/children; no direct lease bypass/cloud fallback/telemetry/self-modification/unhandled ask_user. Reproducible build, offline/egress proof, local smoke and clear incompatible-provider refusal. |
| [ ] **N7.E3 — one executor adapter / Astra, Sol review** | E0/E2; current Work Item remains sole task owner. | Bind authenticated owner/capability/snapshot/runtime/profile/model/budget to item+attempt+generation outside model text. Async preflight/dispatch/observation, startup cleanup and durable cursor. Quarantine and verify output provenance/path/type/size/digest with no-follow safe materialization; immutable independent acceptance specification and exact publication bytes. Model text/end_turn/SDK idle/exit0 are not success. Cancellation fences effects/publication before bounded transport/OS-tree cleanup; host-owned pause, late-event rejection, crash reconciliation and no blind replay of RUNNING/UNKNOWN/partial effects. Prove one real user-entry-to-publication scenario and safe pre-dispatch fallback. |
| [ ] **N7.E4 — ready tools and context / Astra, Grok evidence** | E2/E3; configure within the one runtime, no Python ports. | FS observation policy/stale edits/Unicode/line endings; one installed LSP and its four supported operations; real PTY input/wait/exit/cleanup, no fictitious crash continuity. Job-scoped spill including middle-content read/expiry/disk failure and no replay of successful command for output recovery. Compaction/pruning retain original evidence and tool pairing; measured token effects. Trusted skill loader and bounded workspace instructions; advisory repeat/timeout guards plus Friday hard budgets. No automatic migration of these mechanisms into ordinary Friday chat. |
| [ ] **N7.E5 — independent acceptance and rollout / Sol review, Astra release, Grok lab** | E1/E3/E4 plus A01–A24 applicable evidence. | Default off; owner canary of accepted image/profile/model only. Compare ordinary Friday and DSH on equal inputs/model/resources/criteria: small bug, long-log middle evidence, interactive CLI, concurrent file edit, verified artifact, descendant cancel, model failure and crashes around effects; Russian prompts/names. Record quality/verifier/tool/model calls, measured versus estimated tokens, cold/warm time/memory/retry amplification/cleanup. Rollback blocks new jobs and drains owned processes, preserves Friday data/publication; separate DSH data generations prevent old image reading migrated home. Accepted only on actual evidence; uninstall restores default route. |
| [ ] **N7.E6 — PTC and bounded workflows / Astra, conditional Sol acceptance** | E5 and comparative evidence; conditional, no automatic activation. | Native-vs-PTC evaluation on equal local model/workspace data; retain only measured useful classes without quality loss. OS/resource/intermediate-value limits; no raw Friday storage/credentials or host-effect bypass, reauthorize bridge calls, preserve partial outcomes and forbid whole-program replay. Reuse engine with1–2 verified templates under existing Work Item and real descendant stop. Ralph remains hidden/default off absent explicit human opt-in; worker complete is not verifier evidence. Record accepted classes or evidence-backed deferred decision. |

**Cross-cutting acceptance ledger:** all directive§8 IDs stay required for their
applicable feature; conditional PTC/workflow checks are not falsely marked passed
when disabled. Each needs a concrete command, result, artifact and limitations.
A01–A03: disabled/uninstalled default behavior, exact handshake, local-only provider
refusal before effects. A04–A07: tenant isolation, path/symlink/traversal refusal,
Pandora excluded before snapshot delivery, instruction injection cannot grant rights.
A08–A11: cancel during admission and actual model/tool/PTY/workflow work, pause and
late generation, crash/unknown-effect reconciliation. A12–A14: partial/malformed
stream honesty, retry without duplicate effects, exact artifact TOCTOU protection.
A15–A18: disk/spill failure, concurrent edit, critical long-output/compaction evidence,
PTC intermediate overflow and partial failure. A19–A22: admitted local budgets,
equal-resource comparison, no external content/feedback traffic, no unsupported
permission/UI wait. A23–A24: upgrade/rollback data generations and independently
verified final publication. Include upstream corrupted-state and cleanup tests.

Retain body-free Work Item control state; protect contentful sessions/spill with
tenant permissions, retention/deletion/backup and no automatic archive ingestion.
SourceRef/PassageRef/SearchCoverage, reauthorization, secondary's advisory-only
role and owner Engineer policy remain. No cloud runtime or automatic cloud route;
no model-visible credentials. Upstream MIT/third-party notices and all local patches
must be verified and preserved at adoption; license facts here are directive inputs,
not a completed local dependency audit. Updates never download at task startup.

Deliverables: one ADR and adoption matrix using existing conventions, pinned
upstream/build manifest, one optional adapter, separate production/dev profiles,
fault/contract tests, real scenario evidence and enable/rollback runbook. Statuses
advance candidate→implemented→tested→accepted only with corresponding evidence;
conditional work records deferred with reason. Directive estimates10–18 engineer
days for E0–E5 and14–26 including E6 are unmeasured planning inputs, not release ETA;
replace after E0 with exemplar measurements, avoid summing runtime internals twice.

**Not added to required implementation:** E7 owner Web workbench, generalized MCP
bridge/host-effects bridge, extension workshop, ordinary-runtime context migration,
shared Web/core, teams, webhook/schedule, cloud adapters or Cordis/Typert migration.
They require separate scope/benefit decisions. No live core self-modification,
second global task engine, new watcher/Dispatcher or changes to Astra/Sol/Grok TUI
coordination, reasoning or available parallelism. Broad old removed/deferred items
below are not reopened by this bounded owner-authorized executor epic.

### Removed from the active queue

- Supervisor effect observation and heuristic retirement, Package 6 trusted
  attestation-root construction, closed compiler profiles, indexed Ghidra as a
  product, generic Active Frames/WorkGraphs, Executive unification, Host
  Capability growth, MCP generalization, sensitive-data/Decision stores and a
  material-quarantine release path have no current end-to-end product gap.
- Engineer's owner shell is the toolchain. Add an installed compiler, analyzer
  or decomposer only when a real request proves it absent; do not rebuild an
  allowlisted compiler zoo around arbitrary command execution.
- Existing dormant code is not authority to resume these items. Re-entry needs
  a current owner request or a corpus-backed broken journey.

## Explicitly deferred or closed

- Obsidian companion, shared/multi-device vault, desktop control and remote
  agents: deferred.
- Local coding-agent orchestrator: development tooling, not current Friday
  product convergence.
- Old WIP blobs, old V12 model-first branch and PLAN-002/004: superseded.
- Broad exploit validation: no work without separate scope and safe target.
- Owner-parked 2026-09-03 (Pandora; not the live queue). Re-entry needs an
  explicit owner request. Named evidence is still required before any of these
  can be marked complete:
  - P0H reviewed bounded deletion — one-shot maintenance authority; no
    apply/delete while Linux cannot prove global open-reference absence.
  - S6 physical Android / Obsidian round-trip — owner and device. Do not
    touch the companion plugin without a separate request.
  - Off-machine backup/file mirror — implementation present, target empty.
  - External web-search credential rotation at the provider, then the single
    protected runtime secret.
  - Parked Sol R8I worktree/patch under `~/.jericho/runtime/friday-s4-r8i-exact-runtime.worktree`
    and `~/.jericho/grok-takeover/patches/s4-r8i-exact-runtime.unstaged.diff`.
    Do not merge it; it fights the landed dispatch owner.
  - Gemini parity / P0W Gemini-first web search — cancelled before
    implementation. Owner will not enable or pay a Google billing account.
    Keep Friday's Yandex path. Do not claim Gemini parity. Do not resurrect
    P0W. Evidence: `handoffs/SolGoodman/P0W-CANCELLED-BY-USER-001.md`.

## Owner/external actions

- S3 consumed witness is closed. Live assist is restored on
  `0.208.55`. Remaining owner-end is the Pandora box (P0H
  apply/delete, Android/Obsidian device, off-machine backup,
  provider-credential rotation). Gemini parity stays parked inside
  Pandora, visible as parked, not `[x]`.

## Standing lead-architect checklist

This is the live remaining-work register. Owner/external and observation-bound
rows stay open until their named evidence exists. Do not invent a second
checklist elsewhere. The Ctrl+T view is a compact projection of this list.

### Closed on production `0.208.11`

- [x] P0G Gate Diet
- [x] P0 production message stability
- [x] P1 Engineer Mode complete user workflow, including P1B `EngineerWorkItem v1`
- [x] S0 constructor freeze
- [x] S1 Engineer continuation
- [x] S2 one-turn nervous system
- [x] S4-R0..R8F sole `archive_search` facade, exact windows, bitemporal/graph
- [x] S4-R8I unique remainder: generic continuation cannot start a fresh
      exact selector (on `main` since `a914944f`, live since `0.208.11`)
- [x] S5 measured cognition and exact 40k lease
- [x] S6-R0..R4 mission/reminder/Telegram recovery, browse-to-document, journey evidence
- [x] S3 assist-controller cutover on `0.208.11` (`requested=assist`,
      `effective=off` until the consumed witness; witness later closed
      on live `0.208.51`)
- [x] N0 identity reconciliation: `main` = production = `6b61987a` /
      `0.208.11`; candidate chain `0.208.2`–`0.208.10` superseded

### Closed on production `0.208.18`

- [x] N1 two-message Telegram UX: `/chat`, Engineer, file-album,
      FILE/ARCHIVE carrier, observed web/archive status and one-final-carrier
      packing live on `0.208.18`

### Closed on production `0.208.19`

- [x] N3 `/coding` static inspect live on `0.208.19` (owner-private
      Telegram; execute fail-closed). Isolated worker remains the
      open N3 MVP checkbox.

### Closed on production `0.208.20`

- [x] N3 isolated `/coding` worker boundary live on `0.208.20`
      (dedicated bwrap probe, no Docker, no host secrets, no prod
      DB). Untrusted execute remains fail-closed. N3 MVP
      implement/build/test loop of untrusted uploads remains open.

### Closed on production `0.208.21`

- [x] N4 store-backed mixed journeys live on `0.208.21` (Telegram
      MIXED when PROJECTED; exclusive DOCUMENT/ARCHIVE/RESEARCH
      otherwise). One turn / one operation / one status / one result /
      one effect owner / one publisher; honest `UNKNOWN`; primary-only
      when secondary is absent. Mixed-journey is not a registered organ.

### Closed on production `0.208.22`

- [x] N3 admitted in-memory archive extract into the isolated
      `/coding` workspace live on `0.208.22` (catalog + extract
      admission + extract plan + project-root isolation; host paths
      never opened; `ZipFile.extract` unused). Untrusted execute
      remains fail-closed. Extract-only does not close the MVP
      implement/build/test loop of untrusted uploads.

### Closed on production `0.208.23`

- [x] No-product `semantic_supervisor_shadow_to_assist` restore after
      live `0.208.22`. Trusted-CA health is `0.208.23` with
      `requested_mode=assist` and `effective_mode=off`
      (`material_loaded_not_accepted`). N3 extract remains live.
      Untrusted execute remains fail-closed. Next product sibling is
      `assist_to_shadow`.

### Closed on production `0.208.24`

- [x] N3 isolated untrusted build/test loop of extracted uploads live
      on `0.208.24` (`py_compile` and stdlib `unittest.discover` of
      `test*.py` inside a second bwrap after worker admission and a
      confirmed isolation probe). Execute/run of uploaded programs
      stay fail-closed. This is not a safety certification. Trusted-CA
      health is `0.208.24` with `requested_mode=shadow` and
      `effective_mode=shadow` (`default_off`). Next product sibling
      that changes friday sources is `shadow_to_assist`.

### Closed on production `0.208.25`

- [x] No-product `semantic_supervisor_shadow_to_assist` restore after
      live `0.208.24`. Trusted-CA health is `0.208.25` with
      `requested_mode=assist` and `effective_mode=off`
      (`material_loaded_not_accepted`). N3 isolated build/test loop
      remains live and did not dump into `friday/agent_runtime`.
      Execute/run of uploaded programs stay fail-closed. Next product
      sibling that changes friday sources is `assist_to_shadow`.
- [x] N5 implementable seam extract: no giant-runtime dump from the
      `0.208.24` N3 loop (two thin `handle_coding_static_turn`
      dispatch sites). Kernel web-consumption helpers remain on
      `0.208.18` in `friday/execution_kernel/web_consumption.py`.
      The N5 chapter stays a standing ratchet.

### Closed on production `0.208.26`

- [x] N2 live research mission/gates and N3 `/coding` create, digest/
      overwrite extract observation, one-final `friday-source.zip`
      pack, restart/rollback EMPTY observation, and Coding Mode
      view/plan-gate/carrier live on `0.208.26` at
      `9316a5e6cc2e16992a4eafaab04f3fc9f66ff01a` (tree-file
      `4368e94d92a1bb5b41e81f98706fa6e1338b9a5e6c19f654efc9f14c0be8607e`,
      wheel `f98055784f1948335da9cf332b15596936a693f47b506eefc8026e8ea5c45915`).
      Trusted-CA health is `0.208.26` with `requested_mode=shadow` and
      `effective_mode=shadow` (`default_off`). Secondary after cutover
      restart: `mode=assist`, `state=cooldown`, `available=false`.
      DR published index_revision 152; retention remains
      `review_required`. Upload-modification apply stays source-only.
      Execute/run of uploaded programs stay fail-closed. Gemini parity
      is not claimed. Do not re-activate `9316a5e6`. Next product
      sibling that changes friday sources is `shadow_to_assist`.

### Closed on production `0.208.27`

- [x] N3 upload-modification EMPTY observer live on `0.208.27` at
      `e5a25c4a9b988f5f4959b3c69eca4358a0bb6770` (tree-file
      `60ea6f01ff06233e4a7965daa792d0f7fef1b16c916b22de1ed191522f84494e`,
      wheel `9e9731acd8ba5c9e3e8657d78a01baf9f806f81ee6108bddf07ae91c4d1e109d`).
      Trusted-CA health is `0.208.27` with `requested_mode=assist` and
      `effective_mode=off` (`material_loaded_not_accepted`;
      `source_revision_loaded` and `representative_window_verified`
      true). Secondary after cutover: `mode=assist`, `state=healthy`,
      `available=false` until the process-epoch health window is
      fresh. DR published index_revision 156; retention remains
      `review_required`. Apply never rewrites uploaded project files.
      Execute/run of uploaded programs stay fail-closed. Gemini
      parity is not claimed. Do not re-activate `e5a25c4a`. Next
      product sibling that changes friday sources is
      `assist_to_shadow`. Public `/api/health` `cooldown` is the
      in-process circuit, not laptop Docker liveness; laptop-local
      generation does not refresh Friday's process-epoch window.

### Closed on production `0.208.28`

- [x] Delayed secondary startup re-probe after cooldown live on
      `0.208.28` at `71833028e735e94b9020a1aa2a92c97ca141f0bf`
      (tree-file
      `ec18064695bd5116c052539907721e7e8c188535c26feb8c23575d36479a38d9`,
      wheel
      `f81cce01960bf1508eaa673e3076469197e0f479cd5cf5db9506f15c041c98fe`).
      One detached `_startup_probe` still; after the first epoch probe
      if the circuit is `COOLDOWN` it sleeps `cooldown_retry_after_sec`
      and retries inventory/canary once. Trusted-CA health is
      `0.208.28` with `requested_mode=shadow` and
      `effective_mode=shadow` (`default_off`). Secondary after cutover
      restart: `mode=assist`, `state=cooldown`, `available=false`.
      DR published index_revision 160; retention remains
      `review_required`. Execute/run of uploaded programs stay
      fail-closed. Gemini parity is not claimed. Do not re-activate
      `71833028`. Next product sibling that changes friday sources is
      `shadow_to_assist`. Compact `/api/health` `cooldown` remains the
      in-process circuit, not laptop Docker liveness.

### Closed on production `0.208.51`

- [x] S3 Supervisor advice on a genuine eligible current-file-plus-public-web
      Telegram turn. Independent classification of live `0.208.51` assist
      2026-09-06T05:25Z: user `msg_d145ce5796b54ae1`, assistant
      `msg_df97d09e4d2b4a2f` (2452 chars, not the canned 58-char
      `step_failed`), graph `graph_866d37c2794dcc1a`. Canonical file
      sha256 `d8b64d6c1152058e715d9d0f13f7e16f5c0887c4bd506f9d223697c98a93f42b`,
      caption 73 chars / 134 UTF-8. `answer_mode=semantic_supervisor_assist`,
      plan `82d3ddce1177e39c43f40f70bb07ab4fcd891f8af5641653bca9c65ecf291f8b`
      admitted, `read_current_file` complete+verified,
      `read_current_web` complete+verified, `primary_synthesis`
      partial+verified, graph `partial_evidence`
      (`web_source_truncated`, `local_context_truncated`).
      `verified=true`, `verification.status=passed`, `score=1.0`,
      citations `[F1] [W1] [W2] [W3]`. Controller compact health:
      `invoked_total=1`, `publication_total=1`,
      `event_success_total=1`, `last_promotion_reason=admitted`,
      `fallback_total=0`. Owner appearance is not this close.
      Live identity `1b01a59ceb59180abbff9ab4cc48453a770db652`
      (tree-file
      `10f4be3b6d5d1667ef09ded4b7ebb56f89c26552e26e9e11e93fab767cf5bd6d`,
      wheel
      `426695fc04df01870e55f88d7a08aff6b0f34d8dac8f1b7cbeb165823db9cf5b`),
      predecessor `0.208.50` /
      `705dbe20a664a449f00e23c45ab25921e82b607c`, journal `clear`,
      DR index_revision 252. Do not re-activate `1b01a59c` or
      `705dbe20`. Next product sibling that changes friday sources is
      `assist_to_shadow`. Honest residual `partial_evidence` stays an
      audit finding, not an S3 reopen.

Decision chain of this attempt (body-free; this is the success-path
record the owner required in the audit commit, not a failure dump):

1. `0.208.47` 02:54Z (`msg_0f4fe2d233b44903` /
   `msg_dcae55874ecc4762`): `plan_not_admitted`,
   `answer_mode=general_conversation`. GPT-OSS invented a
   length-legal `manifest_id`; parser/policy fail-closed.
2. Pin owned `manifest_id` + `budget_sha256` as schema enum.
   Product `0.208.48` (`f7e74e9a` / dense `34b3cdbc`); extra-hop
   `0.208.49` (`86f94cc5`).
3. `0.208.49` 04:06Z (`msg_889d6a8978dc4da0` /
   `msg_ad5db81a23e745a5` / `graph_65a66eeeffd06bf7`): plan
   admitted, file+web complete+verified, `_validate_answer`
   `_AnswerRejected(code=citation_labels)` after a 506-token
   `stop`, canned 58-char `step_failed`.
4. Local repro of that reject class. Product `0.208.50`
   (`ce6b0a49` / dense `705dbe20`, `assist_to_shadow`); extra-hop
   `0.208.51` (`1b01a59c`, `shadow_to_assist`).
5. `0.208.51` 05:25Z consume witness above. Independent
   classification, not owner appearance. S3 `[x]`. Gemini stays
   parked in Pandora. Remaining owner-end: Pandora.

### Closed on production `0.208.55`

- [x] No-product `semantic_supervisor_shadow_to_assist` restore after
      live `0.208.54` keep-shadow. Trusted-CA health is `0.208.55` /
      `ok` with `requested_mode=assist` and `effective_mode=off`
      (`material_loaded_not_accepted`). Live identity
      `e60860eaa89827bc5fc58d7c4f1c47514caaf2d4` (tree-file
      `706aee22e4b1a435e6ae4969d41936f603cd9863ac1e2c2535e1cfe878bb80b7`,
      wheel
      `c834a45cc3159ea082cf002ec1dd6e0b263c87047ea1b54fe6e29683633daf1a`),
      predecessor `0.208.54` /
      `9c8a4eed2858abd50bdcf27d7aab1b75f687f61c` (tree
      `c81fae29e659db22090559cde0352f37bc8413029e47109c4b552557146bf51e`,
      wheel
      `547a54c00c254c4df0a0d6f28f53705b1a09de62917527e1f314dd5c31aba7b8`).
      Journal `clear`; this hop's fallback is the same `0.208.54`
      sibling. DR published index_revision 268; retention remains
      `review_required`. Backend and `friday-bridge` active. Units
      hashes unchanged
      (`friday-backend.service`
      `17b997cad4d013f9b7950b56849d4e2dba495b8706d63c620503b718beeaf99d`,
      `friday-bridge.service`
      `846f8be6f28041301daba8518636c93c5478e767455531e90884494a61e1ed3e`).
      Durable S3 promoted-product row `evt_1827c6dff24b40b4` still
      present; do not delete it. Do not re-activate `e60860ea` or
      `9c8a4eed`. Next product sibling that changes friday sources is
      `assist_to_shadow`.
- [x] Luna-1 `s3-web-evidence-truncation` ACCEPT, cherry-picked as
      `2d6f8c16abcf1aab7a627aafdf494df208e05fce` onto `origin/main`.
      Honest empty upstream titles stay empty; the public URL remains.
      Do not invent titles.
- [x] Luna-2 `s3-synthesis-projection-budget` ACCEPT, cherry-picked as
      `ce9a10ed5a61ca5666372ef6a577dd851f067185` onto `origin/main`.
      Q38 dual-partial projection no longer burns a doubled verifier
      reserve.

Decision chain of the extra-hop restore (body-free; success-path
record the owner required in this audit commit, not a failure dump):

1. `0.208.51` 05:25Z consume left one durable
   `semantic_supervisor.promoted_product` row. Same-release env-only
   assist restore is forbidden (`candidate_rollback_identity_not_distinct`).
2. `0.208.53` `shadow_to_assist` issue against live `0.208.52` failed
   HTTP 400: old assist-issue path required
   `sample.promoted_product_events == 0`.
3. Product `0.208.54` (`9326cd64` / dense `9c8a4eed`): assist issue
   still fails closed if `precursor_assist_promotion_evidence_sha256`
   is not None; it no longer fails on historical promoted rows.
   Current-window promoted execution still fails closed via
   `shadow.actual_promoted_execution is False`.
4. `0.208.54` install-units required journal previous = live identity.
   Live was already `0.208.53` keep-shadow (`71479392`); 054
   CHANGELOG still lists predecessor `0.208.52` because it was sealed
   before that hop. Do not rewrite 054 docs after the fact.
5. Do not `shadow_to_assist` `0.208.53` after it became live (old
   issue path). 053 stayed the predecessor hop; 054 keep-shadow
   carried the product fix (DR 264).
6. `0.208.55` no-product extra-hop (`170475eb` / dense `e60860ea`).
   Assist issue posted against live 054; `shadow_to_assist` until
   health `0.208.55` / `assist` (DR 268).
7. Luna S3 residual packets above were already on `origin/main` in
   the `0.208.52` product sibling. CLOSE is Grok-only after this
   identity. No further Luna packet. Remaining owner-end: Pandora.

### Open and implementable

F.11 `0.208.57` is live assist. Mixed-journey consume provider identity
is live (`c8e245ea`). The prior implementation hop is complete. N6 TinyFish
is queued after current R10 acceptance; its implementation/live are NOT_RUN.
Do not issue a no-product `0.208.58`. Live configured-model N2 acceptance is
not fabricated.
EXECUTE/RUN of uploaded programs stay fail-closed. Preserve the 05:25Z
S3 witness and its honest partial evidence; do not fabricate traffic
or touch Pandora.

### Open and blocked

- [ ] Gemini parity needs a paired scored set. Do not claim it.
      Private N2 self-score is Friday's own gates, not Gemini.
      Owner-parked into Pandora 2026-09-05 (P0W cancelled; no Google billing;
      Gemini API Search grounding unavailable). Evidence:
      `handoffs/SolGoodman/P0W-CANCELLED-BY-USER-001.md`. Not `[x]`.

### Operating invariants (never "done", always in force)

- Keep deployed P0/P1 paths green; do not expand `EngineerWorkItem v1`.
- S3 consumed witness exists (durable 05:25Z graph and promoted-product
  row). Do not fabricate additional S3 traffic. Compact health after a
  cutover may still show `effective=off` (`material_loaded_not_accepted`);
  that is not a missing witness. Live assist is `0.208.57`.
- Primary-only path when the laptop/secondary is absent.
- One turn / one effect owner / one final publisher. No new orchestrator.
- Do not use Docker to certify primary Friday.
- Do not touch the Obsidian companion without a separate owner request.
- Do not merge old feature branches wholesale.
- No new product logic in `friday/agent_runtime/__init__.py` unless no
  narrow seam exists.
- During implementation: focused tests. Full exact-release gate only at a
  clean release boundary.
- Never overlap our full native/UI gate with SolGoodman's full gate.

## Canonical golden-journey/evidence registry

This is the single source of truth for product-level journey states. The
machine contract in `tests/test_golden_journey_registry.py` parses this table
directly; detailed trackers may link here but must not duplicate its states.

The readiness vocabulary is closed to `READY`, `DEGRADED`, `UNVERIFIED`,
`BLOCKED` and `OUT_OF_SCOPE`. Evidence is closed to `VERIFIED`, `AVAILABLE`,
`MISSING`, `STALE`, `FAILED` and `NOT_APPLICABLE`. `AVAILABLE` means that a
journey-specific contract, executable test or runbook exists, not that the
complete journey passed. Generic release, rollback and backup tests are not
journey evidence.

The validator now admits only a closed machine-produced receipt bound to the
exact commit, tree, wheel, schema and named executable tests, plus an
authenticated owner Telegram smoke where the journey crosses an external edge.
Public validation independently reruns the closed test inventory from the exact
source commit and rejects forged or mutable `PASSED`/`FAILED` outcomes even when
their surrounding manifest digests are recomputed.

A manifest and its sanitized receipt must use their single deterministic
privacy-safe paths derived from journey, class, result and the full release
identity. They bind the exact deployed source, tree, wheel and database schema,
closed executable-test node IDs, independently observed outcomes and SHA-256
digests of Git-blob source bytes at the manifest source commit, never the
mutable checkout. Closed allowlists forbid raw content, people, conversations,
prompts, responses, runtime paths, tool arguments, test bodies and logs. `READY`
requires every applicable journey class to be current `VERIFIED`; generic
release/rollback/backup proof stays at release level instead of being copied
into every row. Obsidian remains `UNVERIFIED` without current physical Android
evidence, unless current `FAILED` evidence makes it honestly `BLOCKED`. There
are no `READY` claims at this checkpoint.

| Journey ID | Journey | Readiness | deterministic contract | integration path | clean artifact path | synthetic live path | production read-only observation | physical device evidence | restart and recovery evidence | rollback evidence | backup and restore evidence | Limitation codes |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `conversation_recall` | Conversation recall | `DEGRADED` | `AVAILABLE`<br>[friday/interaction_control_plane/work_item_contract.py](../friday/interaction_control_plane/work_item_contract.py)<br>[tests/test_message_window_runtime_integration.py::test_promoted_exact_window_is_deterministic_scoped_and_receipted](../tests/test_message_window_runtime_integration.py) | `AVAILABLE`<br>[friday/orchestration/message_window_outcome.py](../friday/orchestration/message_window_outcome.py)<br>[tests/test_message_window_runtime_integration.py::test_promoted_exact_window_is_deterministic_scoped_and_receipted](../tests/test_message_window_runtime_integration.py)<br>[tests/test_archive_search_runtime_publication.py::test_selected_message_archive_evidence_replays_after_restart_then_fails_closed](../tests/test_archive_search_runtime_publication.py) | `VERIFIED`<br>[evidence/golden_journeys/manifests/conversation_recall--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json](../evidence/golden_journeys/manifests/conversation_recall--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json) | `MISSING` | `MISSING` | `NOT_APPLICABLE` | `AVAILABLE`<br>[tests/test_message_window_work_item_runtime.py::test_restart_temporal_followup_reuses_identity_role_and_zone_with_one_cas_update](../tests/test_message_window_work_item_runtime.py)<br>[tests/test_archive_search_runtime_publication.py::test_selected_message_archive_evidence_replays_after_restart_then_fails_closed](../tests/test_archive_search_runtime_publication.py) | `MISSING` | `MISSING` | `semantic_recall_missing`<br>`cross_lane_coverage_missing` |
| `document_recall_answer` | Document recall and answer | `DEGRADED` | `AVAILABLE`<br>[friday/file_evidence_reader.py](../friday/file_evidence_reader.py)<br>[tests/test_v12_file_evidence_reader.py::test_current_turn_native_files_form_one_process_owned_bundle](../tests/test_v12_file_evidence_reader.py) | `AVAILABLE`<br>[friday/orchestration/file_read.py](../friday/orchestration/file_read.py)<br>[tests/test_v12_file_evidence_reader.py::test_reader_contract_matches_real_ingestion_projections](../tests/test_v12_file_evidence_reader.py)<br>[tests/test_archive_search_runtime_publication.py::test_selected_canonical_archive_evidence_replays_exactly_after_runtime_restart](../tests/test_archive_search_runtime_publication.py)<br>[tests/test_archive_search_runtime_publication.py::test_locate_select_and_explain_document_survives_both_runtime_restarts](../tests/test_archive_search_runtime_publication.py) | `VERIFIED`<br>[evidence/golden_journeys/manifests/document_recall_answer--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json](../evidence/golden_journeys/manifests/document_recall_answer--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json) | `AVAILABLE`<br>[tools/document_contour_live_battery.py](../tools/document_contour_live_battery.py)<br>[tests/test_document_contour_live_battery.py::test_manifest_is_exactly_ten_unique_document_scenarios](../tests/test_document_contour_live_battery.py) | `MISSING` | `NOT_APPLICABLE` | `AVAILABLE`<br>[tests/test_archive_search_runtime_publication.py::test_selected_canonical_archive_evidence_replays_exactly_after_runtime_restart](../tests/test_archive_search_runtime_publication.py)<br>[tests/test_archive_search_runtime_publication.py::test_locate_select_and_explain_document_survives_both_runtime_restarts](../tests/test_archive_search_runtime_publication.py)<br>[tests/test_archive_search_runtime_publication.py::test_selected_archive_replay_failure_is_source_free_and_suspends](../tests/test_archive_search_runtime_publication.py) | `MISSING` | `MISSING` | `cross_lane_coverage_missing` |
| `obsidian_write_sync` | Obsidian write and synchronization | `UNVERIFIED` | `AVAILABLE`<br>[friday/organs/obsidian/contracts.py](../friday/organs/obsidian/contracts.py)<br>[friday/orchestration/effect_outcome.py](../friday/orchestration/effect_outcome.py)<br>[tests/test_effect_outcome.py::test_effect_outcome_is_immutable_canonical_closed_and_round_trips](../tests/test_effect_outcome.py)<br>[tests/test_obsidian_structured_acceptance_core.py::test_conflict_preview_is_non_destructive_and_contains_both_versions](../tests/test_obsidian_structured_acceptance_core.py) | `AVAILABLE`<br>[friday/organs/obsidian/runtime.py](../friday/organs/obsidian/runtime.py)<br>[tests/test_agent_obsidian_acceptance_message_matrix.py::test_every_exact_tier_a_b_message_routes_through_full_chat_once](../tests/test_agent_obsidian_acceptance_message_matrix.py)<br>[tests/test_agent_obsidian_production_composition.py::test_note_create_append_and_daily_exact_messages_mutate_the_real_vault](../tests/test_agent_obsidian_production_composition.py) | `MISSING` | `AVAILABLE`<br>[tests/test_obsidian_syncthing_live.py::test_pinned_syncthing_generates_and_accepts_the_managed_rest_contract](../tests/test_obsidian_syncthing_live.py) | `MISSING` | `MISSING` | `AVAILABLE`<br>[tests/test_obsidian_runtime.py::test_resume_reuses_daily_operation_identity_without_duplicate_text](../tests/test_obsidian_runtime.py)<br>[tests/test_obsidian_operations.py::test_unproved_append_stays_uncertain_and_never_mutates_the_vault](../tests/test_obsidian_operations.py) | `MISSING` | `MISSING` | `physical_android_round_trip_missing`<br>`real_conflict_evidence_missing` |
| `durable_scheduled_work` | Durable scheduled work | `DEGRADED` | `AVAILABLE`<br>[friday/reminder_schedule.py](../friday/reminder_schedule.py)<br>[tests/test_a_reminder_is_set_before_the_model_speaks.py::test_the_tool_is_removed_so_nobody_is_woken_twice](../tests/test_a_reminder_is_set_before_the_model_speaks.py) | `AVAILABLE`<br>[friday/storage/_missions.py](../friday/storage/_missions.py)<br>[tests/test_a_reminder_is_set_before_the_model_speaks.py::test_the_reminder_is_set_without_asking_the_model](../tests/test_a_reminder_is_set_before_the_model_speaks.py) | `VERIFIED`<br>[evidence/golden_journeys/manifests/durable_scheduled_work--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json](../evidence/golden_journeys/manifests/durable_scheduled_work--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json) | `AVAILABLE`<br>[tools/synthetic_live_battery.py](../tools/synthetic_live_battery.py)<br>[tests/test_synthetic_live_battery.py::test_exact_reminder_oracle_owns_the_model_boundary](../tests/test_synthetic_live_battery.py) | `STALE`<br>[evidence/golden_journeys/manifests/durable_scheduled_work--production_read_only--verified--b02177bb427c5ff5aa537ffb7460a69aec897a8ac9eb573f5a272eb0105c68e8.json](../evidence/golden_journeys/manifests/durable_scheduled_work--production_read_only--verified--b02177bb427c5ff5aa537ffb7460a69aec897a8ac9eb573f5a272eb0105c68e8.json) | `NOT_APPLICABLE` | `VERIFIED`<br>[evidence/golden_journeys/manifests/durable_scheduled_work--restart_recovery--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json](../evidence/golden_journeys/manifests/durable_scheduled_work--restart_recovery--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json) | `MISSING` | `MISSING` | `journey_specific_rollback_backup_evidence_missing` |
| `honest_degradation` | Honest degradation | `DEGRADED` | `AVAILABLE`<br>[friday/orchestration/capability_outcome.py](../friday/orchestration/capability_outcome.py)<br>[tests/test_search_provider_refusal_is_not_emptiness.py::test_202_from_duckduckgo_is_a_refusal_not_an_empty_result](../tests/test_search_provider_refusal_is_not_emptiness.py) | `AVAILABLE`<br>[tests/test_search_provider_refusal_is_not_emptiness.py::test_the_chain_moves_on_when_the_first_provider_refuses](../tests/test_search_provider_refusal_is_not_emptiness.py)<br>[tests/test_message_window_runtime_integration.py::test_final_message_snapshot_drift_is_unavailable_source_free_and_not_retried](../tests/test_message_window_runtime_integration.py) | `VERIFIED`<br>[evidence/golden_journeys/manifests/honest_degradation--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json](../evidence/golden_journeys/manifests/honest_degradation--clean_artifact--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json) | `AVAILABLE`<br>[tools/synthetic_live_battery.py](../tools/synthetic_live_battery.py)<br>[tests/test_synthetic_live_battery.py::test_full_package_a_oracle_accepts_natural_honest_refusals](../tests/test_synthetic_live_battery.py) | `MISSING` | `NOT_APPLICABLE` | `VERIFIED`<br>[evidence/golden_journeys/manifests/honest_degradation--restart_recovery--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json](../evidence/golden_journeys/manifests/honest_degradation--restart_recovery--verified--27d26dbd1ea9250dc1a947082449b947bc38578f81cabfd93938b3a78594aabd.json) | `MISSING` | `MISSING` | `product_multi_lane_coverage_missing` |
| `current_file_web_comparison` | Current file and web comparison | `UNVERIFIED` | `AVAILABLE`<br>[tests/test_compare_current_file_web_work_graph_schema45.py::test_schema45_exact_binding_is_durable_immutable_and_revision_cas](../tests/test_compare_current_file_web_work_graph_schema45.py) | `AVAILABLE`<br>[tests/test_supervisor_assist_controller.py::test_review_and_web_recovery_are_strictly_bounded](../tests/test_supervisor_assist_controller.py) | `MISSING` | `MISSING` | `MISSING` | `NOT_APPLICABLE` | `AVAILABLE`<br>[tests/test_supervisor_assist_graph_adapter.py::test_terminal_cancel_and_startup_reconcile_publish_closed_receipts](../tests/test_supervisor_assist_graph_adapter.py) | `MISSING` | `MISSING` | `assist_promotion_evidence_missing`<br>`clean_release_artifact_missing`<br>`activation_rollback_evidence_missing` |

## Update rule

After every production release update the source/live/fallback identities,
health, completed package, active package, evidence rows and next order here.
Never mark device-dependent or external-service observations complete from
local tests. No other tracked file may become a mutable backlog or status log.
