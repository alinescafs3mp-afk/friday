# Открытый список — состояние на 2026-08-07

> ## Приоритет владельца (2026-08-05): ФУНКЦИОНАЛ, а не уборка данных
>
> Прямая формулировка: «эта база мне даром не нужна, я её дал для калибровки и
> доработки системы, делай с ней что угодно. Интересует функционал; данные юзеры
> накидают свои, плюс планируется подключение полноценных баз».
>
> Что из этого следует для списка ниже:
>
> * **Уборка живого корпуса — НЕ делать.** Разделы 2.1 (482 документа без
>   сущностей), 2.3 (55 мёртвых кандидатов), 2.4 (теги входящих) и ручной разбор
>   очередей из 1.5 переводятся в «не делать»: это работа по данным, которые
>   будут заменены.
> * **Механизм разбора очереди — ДЕЛАТЬ.** Разница принципиальная: разобрать 144
>   конфликта руками — уборка; проход, который разбирает их сам, с единогласием
>   двух прогонов и отчётом по каждой строке, — функция, и она переживёт смену
>   данных. Такой проход уже написан для связей (`review-relation-candidates`) и
>   закрыл 491 строку из 542.
> * **Архив остаётся ПРИБОРОМ.** Чистить незачем, мерить обязательно: все числа
>   проекта (recall@10, доля видов, точность извлечения) сняты на нём, и без него
>   «сделано» превращается в «написано».
> * **Прятать корпус от пользователей — решено НЕ делать** (2026-08-05). Разбор:
>   флаг `FRIDAY_SHARED_ARCHIVE` делает арендатором всех учётку владельца, и под
>   ней лежит не только корпус, а **1277 разговоров и 2884 сообщения** живых
>   людей. Сняв флаг, спрятали бы корпус и заодно отняли у пятерых их же историю
>   переписки. Точечная пометка «калибровочный материал» возможна, но это правка
>   пути поиска ради данных, которые всё равно сменятся. Владелец: «пусть так
>   лежит, не перебирать и не причёсывать, если того не требуют функциональные
>   правки».
> * ~~**Новое крупное направление — внешние источники данных.**~~ **СДЕЛАНО
>   2026-08-05.** Владелец снял неопределённость сам: «не переезд точно, скорее
>   всего надо будет просто научить пятницу ходить за данными в какую-то СУБД по
>   сети». Сделано так: внешняя база — ИСТОЧНИК рядом с архивом (`sqlite`,
>   `postgres`, `mysql`), только чтение, потолки названы вслух, строка
>   подключения в базе не хранится — только имя переменной окружения. Модели три
>   инструмента, человеку — CLI `data-source` и вкладка «Источники» в панели.
>   Ключ источника — пара «владелец + имя» (схема 29): одним именем он пробивал
>   границу арендаторов.

Этот файл заведён потому, что правило «единый список работ живёт в репозитории»
нарушалось: `TASKS.md` стоял на 3 августа, а задачи #104–#131 существовали только
в памяти ассистента, к которой у владельца доступа нет. Здесь — всё незакрытое,
собранное раскопками по четырём источникам сразу: бумаги проекта (`TASKS.md`,
`sol/`, `grok/`), сам код, спецификации (спека v3, `grok.txt`, `answer.txt`) и
память ассистента. Каждый пункт проверен на HEAD: то, что успели сделать, сюда не
попало.

Порядок разделов — по тому, кто может сдвинуть пункт: сначала то, где нужен
владелец, потом то, что делается без него.

---

## 1. Нужно решение владельца

Здесь не «сделать», а «выбрать». Пока выбора нет, работа стоит честно.

### ~~1.1. Автор у накопленных документов~~ — ЗАКРЫТО 2026-08-06

Историческим строкам автор не выдумывается: отсутствие метки честно означает
«принято до появления контракта». Все восемь нынешних дорог приёма теперь пишут
один `uploaded_by`: семь аутентифицированных путей — точный `actor.own_id`, а
дисковый CLI-импорт — явный `--uploaded-by` либо JSON `null`. Tenant общего
архива автором не считается, повторный импорт старую строку задним числом не
переписывает. Полный AST-переучёт вызовов и runtime-матрица с различными
person/tenant закрепляют не наличие слова в модуле, а значение на каждом Raw
Object; мутация `own_id -> user_id` красит соответствующую дорогу.

### ~~1.2. Файл из Telegram и `/api/files` идёт в знания без ревью~~ — ЗАКРЫТО 2026-08-06

Безопасная политика `unless_explicit` стала умолчанием в коде, example-env
и setup-шаблоне. Теперь и подписанное Telegram-вложение, и `/api/files`
создают Raw Object и pending Inbox suggestion, но не канонический
Knowledge Object. Обе дороги зовут один `review_required`; runtime-матрица
закрепляет одинаковый исход всех трёх политик, а отдельный clean-env
тест защищает именно умолчание. Явные `/note`, `force_knowledge` и
«запомни»/«сохрани» по-прежнему продвигаются сразу; массовый
`force_review` остаётся неснимаемым полом. `assessed` сохранён только как явный
режим совместимости.

### ~~1.3. TLS готов к включению, но live cutover ещё не выполнен~~ — ЗАКРЫТО 2026-08-06

Кодовый блокер снят в 0.157.0, после чего live-контур переведён поэтапно:
защищённый локальный сертификат покрывает фактические loopback/LAN/DNS-имена,
backend принимает только HTTPS, а bridge проверяет его через установленный
public certificate. HTTPS health, Admin UI, authenticated API и подписанные
bridge-вызовы прошли; plaintext HTTP отвергается. В активный browser profile
импортирована только публичная часть сертификата, private key туда не попадал.
Insecure fallback (`-k`, `verify=False`) в постоянной конфигурации отсутствует.
Base Compose намеренно остаётся private-network HTTP за отдельным TLS reverse
proxy — это другой deployment contract, не откат live systemd-контура.

### 1.4. Offsite-зеркало бэкапов и файлов: сделано, выключено

`friday/config/__init__.py:354` — `backup_mirror_dir`, пустое значение = выключено.
Код написан, покрыт тестами и лежит мёртвым с 0.150.0. Сегодня резервных копий
вне этой машины нет вовсе; в комментарии самого проекта сказано: «копия на том же
диске — не копия».

### 1.5. Очереди, ждущие человека, растут

| очередь | было 3 августа | сейчас |
|---|---:|---:|
| входящие (`inbox` pending) | 135 | **220** |
| конфликты знаний | 144 | **75** |
| кандидаты слияния | 16 | 23 |
| кандидаты связей | 64 | 192 |

За ночь 2026-08-05 две очереди разобраны МЕХАНИЗМОМ, а не руками: связи —
проходом с единогласием (205 принято, 286 отвергнуто), конфликты — правилом
«соседний документ серии не копия» (69 закрыто с причиной). Остаток требует
человека: 192 связи, по которым арбитр разошёлся сам с собой или воздержался, и
75 конфликтов, где различающего признака нет.

~~Пути к конфликтам из Telegram нет~~ — **ЗАКРЫТО ранее в 0.155.0, отмечено
2026-08-06.** `/conflicts` показывает bounded очередь с triage-hint и
capability-gated кнопками «оставить 1 / оставить 2 / не конфликт»; повторно
решённая строка не появляется. Synthetic bridge/API tests закрепляют listing,
решение, tenant boundary и снятие старой inline-разметки.

~~Пути к кандидатам связей из Telegram нет~~ — **ЗАКРЫТО в 0.159.0.**
`/relations` показывает максимум пять content-free карточек и точный total;
tenant/capability boundary, живость обоих концов, terminal/idempotent решение и
кнопки, привязанные к вызвавшему команду человеку, закреплены synthetic API,
storage и bridge-регрессиями. Evidence и reviewer identity в Telegram не уходят.

### ~~1.6. Помощники простаивают четвёртые сутки~~ — ЗАКРЫТО в 0.165.0

S10b (#71) завершён по заранее зафиксированному контракту. На 12 русских
human-authored synthetic сообщениях baseline дал TP/FP/TN/FN `6/6/0/0`, а
единственный кандидат — `6/0/6/0`: все шесть false positive исправлены, ни один
настоящий relational-запрос не потерян. Product-классификатор теперь подавляет
только непосредственно отброшенное совпадение; второе живое совпадение и вводная
часть сохраняются. Деидентифицированный handoff принят fail-closed, исходные тексты
в Git не вошли. Квадратичный prefix-rescan отдельно закрыт линейным эквивалентом.

### ~~1.7. Отклонённый sing-box лежит на диске с паролем~~ — ЗАКРЫТО 2026-08-06

Точный отвергнутый каталог сначала изолирован owner-only и перемещён в
пользовательскую корзину, затем после окна восстановления окончательно удалён
вместе только с соответствующей записью корзины. Исходный путь отсутствует,
соседние артефакты не затронуты, содержимое при удалении не открывалось.

### 1.8. Bi-temporal рёбра в ранжировании — PRODUCT ACCEPTED

Заморожены 40 полностью синтетических temporal-relational cases, split целыми
мирами: 20 calibration и 20 sealed holdout. Baseline воспроизвёл дефект `4/20`:
все 16 expected graph candidates доходили до text-only reranker и снимались им.
Единственный заранее объявленный candidate защищает только exact
query-grounded, structurally published, non-implicit temporal path внутри
фактического rerank-slice; веса, classifier и non-temporal путь не менялись.
Три production-like calibration-прогона после реализации дали `20/20`, MRR
`0.875`, no-answer `4/4`, forbidden hits `0`, infrastructure failures `0`.
Manifest v2 связывает exact candidate diff, frozen gold и evaluator/helper blobs
из HEAD; запуск идёт из приватной capability-bound изолированной проекции, а
атомарный durable latch расходует попытку до запуска любой руки. После commit
`8c4c334` seal check и committed isolated calibration прошли, единственный paired
holdout штатно израсходован и принят: baseline `4/20`, candidate `20/20`,
`wins=16`, `losses=0`, expected hits `0→16`, forbidden hits `0→0`, MRR
`0→0.8542`. Обе руки не имели infrastructure failures и не меняли структуру;
non-temporal control побайтно идентичен. Product acceptance завершён.

### 1.9. «Решения» как первоклассная запись — ни кода, ни отказа

Спека v3 (`sol/OPUS_INTEGRATED_SYSTEM_AUDIT_PROMPT.md:113`) требует записи
решений: кто решил, какие данные были, что рассматривали, чего ждали, что вышло.
`Risk` и `Metric` получили честный разбор и отказ с замером; `Decision` не получил
ни строчки — просто выпал.

### 1.10. Осознанное снятие material-quarantine требует отдельной capability

Privacy closure 0.154.0 намеренно учитывает authenticated entity history. Если
нынешняя карточка B очищена, но её v1 всё ещё содержит ID, имя или alias личного
напоминания A, generic API продолжает считать B private; обычный `update_entity`
отвечает как на отсутствующую строку. Это fail-closed и не мешает чтению публичных
данных, но штатного пути вернуть ошибочно загрязнённую карточку сейчас нет.

Автоматически стирать или переписывать версию нельзя: это provenance и может быть
единственным доказательством того, почему строка попала в карантин. Добавление новой
sanitized version ничего не снимает, пока прежний authenticated snapshot остаётся
identity authority. Нужна отдельная owner-only destructive capability, которая
явно retire/redact выбранные snapshots, показывает preview точных затрагиваемых
типов, пишет audit без payload, выполняет `secure_delete`/WAL cleanup, применяет
явную backup/export retention policy и через code-owned maintenance path атомарно
пересобирает cache/work/state с проверкой `cache == live closure`. До такого решения
quarantine остаётся монотонным на обычных mutation paths.

### 1.11. Перевыпустить внешний web-search credential у провайдера

Четыре лишние локальные копии одного действующего cloud web-search credential
удалены безвозвратно 2026-08-06; active configuration и runtime data не тронуты,
повторный exact-value scan не показал находок в прочитанных small-first 256 МиБ.
Покрытие честно неполно: после 4 505 файлов byte budget исчерпан, 60 крупных файлов
проверены не полностью; scanner не выдаёт это за `clean`. Локальное удаление не
отзывает уже скомпрометированное значение: владелец должен перевыпустить/отозвать его в кабинете
провайдера и затем штатно обновить единственный protected env-файл. Значение, прежние
пути и содержимое файлов в репозиторий/отчёт не записываются. Этот внешний шаг не
блокирует code release, но privacy-инцидент нельзя считать полностью закрытым до
ротации.

---

## 2. Живые данные: то, что уже накопилось и мешает

### 2.1. 482 документа без единой сущности, 187 без собственной даты

Замер на живой базе. Эти документы выпадают из именных и хронологических путей:
их не найти ни через карточку человека, ни через `/timeline`. За двое суток
почти не изменилось (514 → 482) — прохода не было.

### ~~2.2. Потолок 5000 отрезает 1999 сущностей~~ — ЗАКРЫТО ранее, сверено 2026-08-06

Поиск дублей больше не строится на `list_entities(limit=5000)`: один SQL-проход
читает весь tenant-граф, но только bounded поля, нужные для сравнения. Тест
`test_duplicate_scan_sees_the_whole_graph.py` создаёт 5001 сущность, помещает
дубликат за границей первой страницы и доказывает, что sweep его видит. Поднимать
публичный листинговый потолок не потребовалось.

### 2.3. 55 кандидатов связей с мёртвым концом застряли навсегда (НЕ делать)

Очередь их не показывает (JOIN по живым сущностям), принять их нельзя, отклонить
некому — они просто лежат. Нужен проход, закрывающий их с причиной.

### 2.4. `inbox.suggested_tags_json` не тронут

Чистка тегов прошла по `knowledge_objects`, а надзор «о чём пишет человек» читает
ДРУГОЙ столбец: `friday/storage/_oversight.py:363`. Там `document` стоит на 1655
поступлениях из 2064 (80%).

### 2.5. ~~Vault на диске отстаёт от базы~~ — ПРОВЕРЕНО, отстаёт НЕ он

Опасение не подтвердилось: после массовой правки тегов проверка показала **0
файлов** со снятым тегом `document` и **1469** с новым `вид:` — ровно столько же,
сколько объектов в базе. Синхронизация идёт по сменившейся версии объекта и
подхватила всё сама. Принудительный ресинк не нужен.

---

## 3. Граф и время (задание `grok.txt`)

Фазы 1 и 2 сделаны. **Фаза 3 закрыта в ночь на 2026-08-05** — время стало
фактом, который можно спросить:

* `valid_from` берётся из документа-основания при принятии связи (`INSERT` при
  этом вообще не переносил временные поля — найдено чтением); проход
  `backfill-relation-dates` дал дату 191 связи из 192;
* `as_of` выведен в обход графа, в HTTP (`/api/kg/graph/{id}`, админский
  `/api/admin/graph/{id}`) и к модели (`entity_lookup`); дата названа в ответе;
* отмена связи доступна модели инструментом `relation_end` («он перевёлся»
  меняет граф, а не только ответ);
* локальный вид графа получил фильтры общего вида — раньше не принимал НИ
  ОДНОГО, и вид при этом выглядел отфильтрованным.

Проверено на живом: `as_of=2019` → 0 связей, `as_of=2026-01-03` → 5, без даты → 5;
фильтр «только организации» на узле с пятью связями → 1 узел, «только родня» → 5.

**Фильтры даты и уверенности + подсветка путей закрыты 2026-08-05.**

* общая картина графа приняла `as_of` — прежде дату знала только окрестность
  узла, и человек, поставив дату, при переключении на весь граф молча получал
  сегодняшнюю картину;
* порог уверенности заведён своим именем (`min_confidence`) в обоих видах.
  Прежде его роль играл орган «общих документов не меньше», делённый на 50: в
  окрестности узла человек двигал число документов, а менял порог связи. Теперь
  у общей картины свой орган (документы), у окрестности свой (уверенность), и в
  окрестности первый не показывается вовсе — совместной встречаемости там нет;
* поиск подсвечивает ПУТЬ от узла-фокуса до найденного (поиск в ширину по
  нарисованным рёбрам: путь через отсеянное фильтром ребро был бы враньём). На
  общей картине точки отсчёта нет, и вид говорит об этом вслух — молчание
  читалось бы как «пути нет»;
* серверные `paths` больше не холостая проекция: тот же bounded snapshot, который
  участвовал в ranking, доходит без второго обхода до Agent Runtime и
  `memory_search`. `as_of` применяется как valid-time на каждом hop, пути
  сохраняют ordered edges с временем и allowlisted provenance, а `grounded=true`
  допускается только через уже переданный модели доверенный `[K#]`-якорь.

**Timeline закрыта 2026-08-05.** `/api/kg/timeline` и Telegram `/timeline`
смешивают события с подтверждением relation на `valid_from` и завершением на
`valid_to`, применяют один общий cap и отдельно называют точный полный размер
периода. Пустое начало не подменяется временем записи; invalidated relation
остаётся историей, soft-deleted и чужая — исключаются.

**Transaction-time отношений закрыт кодом 2026-08-05 и живой миграцией
31 → 32 2026-08-06.** Append-only `relation_revisions` сохранены без изменения
evidence/floor/event sequence; `known_at` проходит storage → KG → retrieval →
runtime/tools и HTTP, сообщает completeness floor и fail-closed отказывает до него
либо через последующий identity/topology change сущности (merge/unmerge,
soft-delete/undelete, смену canonical/merged target). Schema 32 отдельно доказывает
exact predecessor/current DDL и полную SQLite UNIQUE-conflict surface до любого
DDL; verified backups есть с обеих сторон миграции. Это не закрывает §1.8: снимок
стал честным, веса ranking остались прежними до заранее замороженного gold set.

---

## 4. Веб: три отложенных предложения, условие которых изменилось

Все три отложены с формулировкой «веб-путь почти не используется». Условие
отменилось 1 августа: веб стал приоритетом и работает 20/20 через Яндекс.

* **Sol №3 — реестр открытых адресов. ЗАМЕР НЕ ПОДТВЕРДИЛ, отложено.** Механизма
  действительно нет: ни `WebEvidence`, ни `[W#]`, ни `web_citations`. Но на живой
  переписке дефект не встречается: из 1562 ответов ассистента адреса содержат
  251, и у 240 из них веб-инструмент в этом ходе ЗВАЛСЯ. Оставшиеся 11 — цитаты
  адресов из документа личного архива («Нашёл в личной базе: VPN-конфигурации…»),
  то есть законные. Выдуманных адресов ноль.
  Доказательство Сола было синтетической трассой («неоткрытый URL принят 1/1»),
  и класс реален — но 5–7 дней работы против нулевой наблюдаемой частоты не
  окупаются. Вернуться, если появится хоть один живой случай; замер повторяется
  одним запросом по `messages`.
* ~~**Grok №4 — дедуп, фильтры доменов, `freshness`, `lang` и `region`**~~ —
  **ПОЛНОСТЬЮ СДЕЛАНО 2026-08-06 в 0.161.0.** Претензия
  подтвердилась замером, но оказалась прежде всего про другое: зеркала —
  не разные сайты, а ОДИН сайт на нескольких местах. Десять живых запросов по
  восемь результатов: в девяти из десяти домен занимал два места и больше, 21
  «лишний» результат из 80. Канонизация адреса (схема, `www.`, `utm_*`, якорь,
  хвостовой слэш) ловит из них всего 2 — поэтому главный механизм не она, а
  потолок «не больше двух мест одному сайту» с добором отброшенных в конец, чтобы
  узкий вопрос не схлопывал выдачу. Контроль на ОДНОМ сыром ответе провайдера:
  слотов сверх потолка 10 → 0, разных доменов в первой пятёрке 37 → 42 из 50, ни
  одна выдача не стала короче. Строгие `site` и
  `freshness=day|week|month|year` теперь объявлены в закрытой схеме инструмента и
  валидируются до квоты/провайдера. Brave и Serper получают свои нативные окна,
  Tavily — `include_domains`/`time_range`, Yandex — документированный
  `date:>YYYYMMDD`; кавычки исходного запроса сохраняются. Brave HTML, DuckDuckGo
  HTML и Wikipedia не умеют доказанно соблюдать именно публикационную свежесть и
  отказываются до сети: цепочка пробует следующий адаптер, а если способного нет,
  `web_surfer` поднимает структурный `freshness_unavailable`; существующий kernel
  переводит его в обычный явный `search_failed`, никогда не в старую выдачу без
  предупреждения.
  Ответ дополнительно фильтруется по exact/subdomain hostname, поэтому
  проигнорированный провайдером оператор не выпускает чужой домен и не мешает
  fallback. В append-only audit домен остаётся только как keyed `site_ref`, в
  логи не попадает. Контракт закреплён полностью синтетическими provider-stub
  тестами, включая мутацию удаления финального site-filter (ожидаемый FAIL).
  В 0.161.0 добавлены bounded `include_domains`/`exclude_domains`: `site` остаётся
  shorthand одного allow-домена и не смешивается со списком, exclude выигрывает
  для вложенных поддоменов, а полностью вычитающая комбинация отклоняется до
  квоты. Ответ любого провайдера повторно фильтруется по exact/subdomain; deny-list
  не отправляется наружу даже Tavily, непроверяемые relative/opaque URL не
  проходят. Двукратный overfetch ограничен 20 строками и только уменьшает
  недобор у того же провайдера; запрещённые строки не возвращаются и не вызывают
  лишний fan-out, если допустимые уже есть.
  `lang` (ISO-639-1) и `region` (ISO-3166-1 alpha-2) названы ровно тем, чем
  являются у разных API: локализацией/market ranking, а не доказательством языка
  каждого документа или географии сайта. Yandex применяет документированный
  `lang:` и только RU/TR segment, Brave — закрытые `search_lang`/`country`,
  Tavily — country boost, Serper — `hl`/`gl`, Wikipedia — проверенные RU/EN.
  Неподдерживаемая комбинация отказывается до socket и перечисляет все
  обнаруженные blockers; пустая Wikipedia вне явно выбранного корпуса больше не
  становится ложным «во всём интернете ничего нет». Audit получает только
  counts и keyed aggregate refs списков, исходный IDN проверяется privacy gate до
  punycode. `null`, canonical-дубли и старые adapter-signature regression закрыты
  отдельными тестами.
* **Grok №5 — квота и пауза СДЕЛАНЫ 2026-08-05, robots.txt остался.** Потолка не
  было вовсе. Размер взят замером на живом архиве, а не из головы: пик 135
  вызовов веб-инструментов на человека за сутки, медиана по человеко-дням 76 —
  потолок 400 (`FRIDAY_WEB_DAILY_QUOTA`, ноль = без ограничения) даёт тройной
  запас, потому что защита нужна не от работающего человека, а от цикла. Счёт
  атомарный (прибавляет SQL, а не питон: ходы разговора идут параллельно), сутки
  местные для человека, квота на человека — шумный участник не отрезает интернет
  соседям. Отказ НАЗЫВАЕТ причину и число: молчаливую пустую выдачу модель
  пересказала бы как факт об интернете. Ворота стоят на всех ТРЁХ дорогах
  (`web_search`, `web_fetch`, `web_research`) — `_what_must_not_leave` зовут
  только две, и квота на том же месте охраняла бы через раз.
  Пауза между обращениями к одному сайту — 1 с (`FRIDAY_WEB_HOST_PAUSE_SEC`),
  замок на хост, а не общий: пауза для `ria` не задерживает `tass`, иначе
  исследование по трём источникам стало бы последовательным и втрое дольше.
  `robots.txt` тоже сделан: правила читаются ОДИН раз на сайт (спрашивать перед
  каждой страницей — удвоить нагрузку на чужой сервер ради вежливости к нему
  же), недоступные или битые правила означают РАЗРЕШЕНО (обратное превращало бы
  сбой сети в запрет на весь интернет), названный сайтом `Crawl-delay` главнее
  нашего умолчания, но не опускает наш пол. Правило спрашивается для `*`, потому
  что заголовок мы шлём браузерный: представляться одним, а правила читать для
  другого — обман. Отказ НАЗЫВАЕТСЯ: пустая страница без причины читается как
  «там ничего нет». Замечено на стандартном разборщике: дробный `Crawl-delay`
  (`0.1`) он молча отбрасывает — до сравнения доходят только целые секунды.

---

## 5. Права, надёжность, следы

* ~~**`default_requires_hitl` валит старт**~~ — ЗАКРЫТО 2026-08-05. Ядро
  спрашивает авторизацию (`capability_requires_person`) и задерживает любой
  инструмент помеченной способности, независимо от класса риска инструмента.
* **Нет записи «работа началась» у обычного инструмента (§14 Сола) — ЗАМЕР НЕ
  ПОДТВЕРДИЛ, отложено 2026-08-05.** Механизма действительно нет: «started»
  пишется только тем инструментам, что меняют данные. Но на живом архиве класс
  не встречается: из 983 следов вызова 979 — `ok`, 4 — `started` у мутирующих, и
  НИ ОДНОГО оборванного, просроченного или отказавшего. То есть вызова, который
  начался и не кончился, за всю жизнь архива не было — включая сутки с падением
  по SIGBUS.
  Цена постройки при этом реальная: лишняя запись в базу на КАЖДЫЙ вызов
  инструмента (это горячий путь ответа) и рост следа вызовов вдвое — с 983 до
  ~1962 строк, то есть +22% ко всему журналу. Вернуться, если появится хоть один
  живой случай; замер повторяется одним запросом по `audit_log`, поле `reason`.
* ~~**Полный текст исключения уходит в заявку и наружу**~~ — ЗАКРЫТО 2026-08-05.
  `safe_failure_text` оставляет класс сбоя и вычищенный остаток; вычищенное
  названо меткой (`‹адрес›`, `‹секрет›`, `‹путь›`), а не вырезано молча.
* ~~**Восстановление версии знания пишет автором арендатора**~~ — ЗАКРЫТО
  2026-08-05. Из десяти мест, передающих `reviewed_by`, девять брали `own_id`
  (учётку человека), и только маршрут отката знания брал `user_id`. Аудит при
  этом всё время писал правильно — журнал и сама запись называли РАЗНЫХ авторов
  одного действия. Подпись живёт на записи ради вопроса «кто это откатил»: тот,
  кто откроет документ через полгода, должен видеть ответ на нём самом, а в
  общем архиве `user_id` один на всех.
  Первая редакция теста дефект НЕ ловила (в личном архиве оба идентификатора
  совпадают — «тест, мокающий место дефекта»), и сторож на класс обрывал строку
  на `reviewed_by=getattr(actor` до слова `user_id`. Обе проверки переписаны и
  подтверждены мутацией.
* **P1: pathological dependency closure всё ещё держит редкий writer секунды; cold
  triple rebuild ЗАКРЫТ 2026-08-06.** Исходный стенд 4 500 сущностей / 1 500 KO дал
  `7.648 s` на поздний closure commit и `22.080 s` на reopen. Усиленный повтор с
  4 500 authenticated snapshots доказал причину структурно: validator после exact
  rebuild дважды заново раскрывал те же live views, поэтому cold делал ровно `3x`
  identity UDF. Fresh tier теперь валидируется по атомной паре work/cache, untouched
  tier по-прежнему независимо сверяется с live. На усиленном стенде reopen сократился
  `161.944 -> 48.615 s` (`0.3002x`), UDF `3x -> 1x`, authority tuples совпали точно;
  commit не изменился (`51.821 -> 51.770 s`). Hot read cliff ранее закрыт ID-only
  authority: sparse Raw point `0.074 ms`, search20 `151 ms`, Inbox `0.96/0.41 ms`,
  ingest `27 ms`. Открыт только сам pathological write: `54M` identity-match в
  derivative rebuild. Его следующая оптимизация также обязана сохранить current +
  authenticated-history семантику без depth cap или неполного cache.
* **Надзор «вижу своих, а не всех» — privacy/functional gap ЗАКРЫТ в 0.164.0.**
  Потеря scope в `change` **ЗАКРЫТА в 0.163.0**: сравнение текущего и предыдущего окна раньше
  забывало `uploaded_by`, поэтому в общем tenant количество и темы одного человека
  включали поступления остальных. Теперь totals и full/redacted topic-ветки строятся
  тем же `_arrival_window`, что лента, сводка, ритм и объём; регрессия с двумя
  авторами и двумя окнами подтверждена мутацией удаления scope.

  ~~`user_knowledge_search` также больше не ищет общий Knowledge Object по отдельной
  учётке человека и не фильтрует готовую tenant-страницу постфактум. Shared-ветка
  использует точный Raw `uploaded_by` **до** FTS/LIKE candidate cap; строки без
  достоверного текстового автора никому не приписываются. Тридцать более сильных
  чужих совпадений, LIKE fallback и параллельные запросы разных людей закреплены
  синтетически. Ответ помечен `scoped_lexical`.~~ **Полный используемый hybrid
  закрыт в 0.167.0:** exact uploader теперь входит до caps в FTS/LIKE, recent,
  date window, whole-document и passage dense; resident tenant cache обходится,
  reranker получает только author-scoped canonical rows, а relevant passage доходит
  до выдержки. Без поисковика остаётся fail-safe `scoped_lexical`.

  Открыт отдельный graph-provenance этап: shared entities/relations не имеют
  достоверного `uploaded_by`, поэтому graph expansion, entity-name signal и
  relation history на авторской дороге намеренно fail-closed. Author-aware repair
  словаря также не включён: глобальный словарь мог бы внести чужие названия.
* ~~**SIGBUS живого экземпляра при втором процессе на WAL**~~ — **ЗАКРЫТО
  2026-08-06 ПОСЛЕ НЕЗАВИСИМОГО REVIEW.** Первое падение 2026-08-05 произошло в
  `libsqlite3`/отображении `-shm`, пока второй процесс писал той же базой. Повтор
  2026-08-06 снова дал `status=7/BUS`; core не сохранился и kernel не сообщил
  I/O/OOM/hardware error, но в том же окне внешний `status --check-llm` трижды
  открывал live WAL read-only, включая полный integrity check. Причинность второго
  случая недоказуема, зато второй mmap не даёт ничего, чего уже не знает владелец
  connection, поэтому сама поверхность удалена.
  При active backend CLI получает snapshot у локального authenticated API; отказ
  API означает degraded без SQLite fallback. Offline reader держит тот же OS lease
  на всём окне DB/worker/auth snapshots, а bridge queue — свой lease; проигравший
  гонку не открывает WAL. Переданный `storage` не является доказательством
  владения. Secret scan не делает даже raw-open main/queue DB, WAL/SHM/journal и
  их hardlink-inodes. Admin overview/settings/diagnostics целиком вынесены с event
  loop, а bridge открывает queue только после lease. Независимо прошли 140 focused,
  Ruff, mypy и Bandit; после принятого кода release-blocker снят.
* **Неотменяемый `entity_mention_backfill` переживал timeout и останавливал
  обслуживание** — ЗАКРЫТО 2026-08-06 ПОСЛЕ ТРЁХ REVIEW. Перед повторным `status=7/BUS` supervisor
  уже видел оставшийся от прошлого запуска blocking call: `asyncio.to_thread`
  отменяет ожидающую coroutine, но не Python-поток, который продолжал грузить CPU
  и обращаться к SQLite. Поэтому рестарт без убийства всего cgroup оставлял ту же
  работу в процессе, Telegram ждал, а следующий tick правильно отказывался запускать
  дубль. Первая bounded-реализация была отклонена независимым review: теряла 801-ю
  сущность, нарушала longest-first между страницами, не ограничивала regex-unit и
  имела version TOCTOU. Вторая также не принята: повторный review нашёл stale
  entity-material TOCTOU, orphan secondary-spool после crash и расхождение с
  online-semantics на больших token gaps. Принятый repair сохраняет внешние пределы
  `8 documents / 25 links / <=10 s`, читает content bounded UTF-8 blob-страницами,
  возобновляет character+byte cursors, связывает spool с document version и entity
  ID+version, а process-local keyed authority не позволяет persisted checkpoint самому
  себя авторизовать после рестарта. Полный literal fallback покрывает canonical
  material до 240 символов, включая пунктуацию и sentence-final dot. Финальный
  независимый прогон: 54/54 focused, cleanup 520/520, fuzz 250+300+200, static PASS.
  Внешний timeout 300 s остаётся последним предохранителем, а не штатным механизмом.
* ~~**Online `inflected_mentions` прекращал сбор после 5000 spans**~~ — **ЗАКРЫТО
  в 0.162.0.** Публичный предел остался ровно 500 первых по тексту
  непересекающихся упоминаний, но общий промежуточный список исчез: bounded
  max-heap хранит только выдачу, longest-first occupancy больше не обрывается на
  произвольной 5000-й позиции, а dynamic frontier ограничивает повторный поиск
  первой страницей и overlap-halo. Две отдельные регрессии воспроизводят старые
  исходы: раннее короткое имя терялось после 5000 поздних длинных, а падежный
  suffix просачивался или нет в зависимости от того, какая равнодлинная строка
  первой заполнила wall. Невозможный хвост слова теперь читает не больше закрытого
  ending bound; admin-маршрут выполняет CPU matcher вне event loop.

  По дороге найден и закрыт соседний online-дефект: unordered SQL мог поставить
  «Иван» раньше «Иван Петров». Canonical/alias/inflected matchers теперь имеют
  собственный фактический приоритет в одном global pass; blocked длинный alias не
  повышает короткий fallback, а проигравшая падежная форма не стирает следующую.
  Reported-card отделён от occupancy: все повторы длинного имени остаются заняты,
  хотя наружу сущность возвращается один раз, и короткий prefix/suffix не
  просачивается через второе вхождение.
  Равнодлинные чисто падежные кандидаты сохраняют единый stable raw-length/rowid
  tie contract с cooperative backfill — это явная детерминированная неоднозначность,
  не результат внутреннего потолка.

---

## 6. Качество ответов и поиска

### 6.0. Чистка тегов стоила одного эталона из 78 — решение не принято

Замер на боевой сборке, 78 эталонов, до и после ретро-разметки:

| | recall@10 | MRR |
|---|---:|---:|
| до чистки тегов | **0.7308** | 0.4382 |
| после | **0.7179** | **0.4449** |

Один эталон выпал из первой десятки, у остальных ранг вырос. Причина понятна по
устройству: теги весят 0.20 в лексической формуле, и до чистки запрос со словом
«рядовой» или «рапорт» совпадал с тремя сотнями документов — шум, который одному
эталону случайно помогал. Какой именно эталон потерян, не установлено: отчёт
хранит только сводку, поимённый список стоит ещё одного прогона (25 минут).

Решать: считать ли −1 эталон приемлемой ценой за фасет `вид:` и осмысленные
теги, или подкрутить вес поля `tags` теперь, когда его содержимое изменилось.

Абляцией это не мерится: прибор отключает только объявленные `ABLATABLE_SIGNALS`,
и `tags` в их число не входит — запрос вернул `no ablatable signals`. Значит либо
расширять прибор, либо два полных прогона с разным весом (по 25 минут каждый).

* **21 эталон из 78 вне первой десятки.** Оба очевидных рычага уже отвергнуты
  замером (граф в поиске, понижение порога переранжировщика) — дальше только
  ранжирование.
* **#128 — граф в поиске** объявлен «следующим шагом», а единственный замер
  требует его НЕ включать. Пункт нужно либо закрыть отказом с числами, либо
  перемерить на боевой сборке: граф с тех пор вырос с 51 связи до 192.

  **Побочно закрыто в 0.168.0:** дороги, по крайней мере, перестали расходиться.
  Умолчание `graph_expansion` стояло в `True`, поэтому режим выбирал тот, кто про
  него промолчал, — а молчали публичный `GET /api/search`, админский `eval_search`
  и синтетический прибор `tools/retrieval_bench.py`. Человек в панели искал одним
  поиском, Пятница в чате отвечала другим, прибор мерил третий. Умолчание
  перевёрнуто в `False`, три дороги получили предикат агента, семь мутаций красные.
  На перемер #128 это не влияет: числа снимать всё равно на боевой сборке.
* ~~**#116 — арбитр путает факт с правилом**~~ — **ЗАКРЫТО в 0.160.0.** Два
  модельных голоса больше не являются полномочием на долговременную мутацию:
  исходная реплика кодом доказывает постоянную область, тему сохраняемого текста
  и цель отмены/замены. Отдельный закрытый gate действует для фактических
  поправок. Живая матрица: факты **0/16**, разовые просьбы **0/8**, настоящие
  правила **16/16** долговременных записей.
* ~~**Хвост #104 — recovery acceptance**~~ — **ЗАКРЫТО в 0.160.0.** После cold
  recovery проверены не только health/metrics, но реальная короткая генерация,
  строгий JSON, одиночные и пакетные эмбеддинги, reranker и матрица #116. Все
  каналы завершили запросы; ложноположительный HTTP health критерием не считался.
* ~~**Сортировка чанкового отбора** отложена «до живой модели».~~ **ЗАКРЫТО в
  0.166.0.** На synthetic 5 000×16 с 1024-float BLOB полный production method
  ускорен с 469.19 до 60.06 мс (7.81×) с byte/order parity. Плотный актуальный
  индекс идёт object-first без corpus-wide sort, sparse/rolling остаётся
  chunk-first по заранее замеренной матрице; newest-N получил total ID tie-break.
* **Пять находок веера 2026-07-28** остались непроверенными, выдача потеряна.

### 6.1. OfficeStructureIndex v1 и code-owned полный список — ЗАКРЫТО 2026-08-06

Полностью synthetic DOCX/XLSX с 16 строками проходят нынешний extractor и prompt
как 16 из 16, но дальше таблица становится плоским текстом без row IDs,
authoritative record count и проверяемого множества ответа. Отдельный synthetic
вариант показывает вторую границу: 16 literal person candidates могут превратиться
в 8 после graph-oriented per-method cap, который нельзя использовать для слова
«все».

Немедленные continuity для persisted-вложения и attachment-aware verifier теперь
дополнены самим `OfficeStructureIndex v1`. `DocumentResult.text` и legacy corpus
остались byte-identical; отдельный Raw-only индекс связан с exact UTF-8 SHA-256 и
хранит только closed enums, IDs, counts и spans, без литералов документа. DOCX
восстанавливает настоящий порядок paragraph/table, XLSX — sheet/row/cell; однозначная
person-column создаёт authoritative record set и ровно один row-scoped candidate на
каждую запись без graph caps. Согласованная мутация 16→15 повторно выводится кодом из
bound text и отвергается строгим validator; durable canonical index дополнительно
подписан installation-local HMAC вместе с SHA-256 байтов конкретного Raw-файла, так
что перенос подписи на другой Office-файл не даёт authority.

Synthesis, verifier и repair получают ровно один канонический
`FRIDAY_ATTACHMENT_DATA` JSON-блок, куда входят только целые records. Exact
count/list и immediate/restored follow-up обходят модель; ordinary model output,
который сам объявляет ложное число или полный состав, отбрасывается вместе с
file/voice/attribution carriers. Непоместившийся prompt, index/text/row budget,
formula без cached result, merged ambiguity, nested table, header/footer, text box,
tracked change, content control или omitted footnote/endnote/comment снимают
полноту и дают deterministic UNKNOWN. Индекс живёт только в Raw metadata; no-save
full source остаётся в одном stack frame и отсутствует в API/idempotency/storage.
Text-dedup двух Office-файлов разрешён лишь при двух полных, валидных и полностью
равных индексах, поэтому одинаковый плоский текст с иной раскладкой не заимствует
чужой инвентарь.

---

## 7. Мелкое, но с ценой

* ~~`/compact` в Telegram, мёртвые коды происшествий, право `compact.run`~~ —
  ЗАКРЫТО 2026-08-05. Команда `/compact` показывает три последних сводки там, где
  человек живёт (наблюдение за системой существовало только для того, кто откроет
  браузер). Кодов без детектора оказалось не три, а два: `structural_softened`
  снят вовсе — пути, которым он мог бы сработать, в системе нет (утверждение
  структуры приписывается дословно, модель его не видит, доставка режет текст, а
  не обрезает); `correction_not_applied` оставлен с записанной причиной, потому
  что мерить его нечем — структурная метка есть у 21 ответа из 1562, и ходов с
  принятой поправкой среди них ноль. За тем, чтобы объявленный код имел детектор
  ИЛИ причину, теперь следит тест. `compact.run` заведён и спрашивается: сборка
  перечитывает все ходы суток и пишет в базу, а `compact.read` роздан пресету
  `user` — сборку за любые сутки запускал каждый из одиннадцати участников.
* **G23: счётчик провалов доставки голоса — ЗАМЕР НЕ ПОДТВЕРДИЛ, отложено
  2026-08-05.** Провал действительно виден только в логе. Но за всю историю
  установки провалов НОЛЬ: озвучка звалась 14 раз, в журналах моста нет ни одной
  строки `tts:`. Счётчик показывал бы ноль, а стоил бы дороги через границу
  процессов (мост в базу не пишет). Вернуться, если появится хоть один случай.
* ~~Потоковая выдача модели написана и никем не используется~~ — УДАЛЕНА
  2026-08-05 (`chat_stream` + `_stream_impl`, ~85 строк). Ни одного потребителя,
  ни одного теста: непроверяемый сетевой код доверия не заслуживает и при
  возвращении всё равно писался бы заново — вместе с потребителем. История в
  git; поле `stream` в запросе осталось прибитым к `False` с объяснением.
* **Мёртвые помощники — ЗАЯВКА НЕ ПРОВЕРЯЕТСЯ ИМЕННЫМ ПОИСКОМ.** Разбор
  синтаксического дерева даёт 132 имени, «ни разу не упомянутых нигде», и почти
  все они живые: обработчики маршрутов, привязанные декоратором
  (`bulk_review_conflicts`, `authentication_middleware`, `api_docs`…). То есть
  прежняя формулировка ни на что не указывает без конкретного списка. Нужен
  инструмент, понимающий декораторы (vulture), либо исходный перечень из аудита,
  который завёл этот пункт. Удалять по имени нельзя — снесёт работающие дороги.
* ~~Инструкции помощникам гоняют гейт по пакету `jericho`~~ — ЗАКРЫТО, сверено
  2026-08-07: и `sol/SOL.md:100`, и `grok/GROK.md:89` уже зовут
  `.venv/bin/python tools/quality_gate.py`.
* Шаг 3 жизненного цикла (проверенный export перед purge) остаётся ручным.

---

## 8. Граф как картина — приоритет владельца с 2026-08-07

Владелец сказал прямо: «доделать уже этот обсидианообразный граф». Шесть рук
раскопали вкладку и её серверную половину; каждая находка сверена с кодом
отдельным проверяющим. Двенадцать подтверждено, пять закрыто в 0.169.0.

### Закрыто в 0.169.0

* ~~**Картинка статична: ни одного кадра анимации.**~~ `requestAnimationFrame` не
  встречался в `app.js` вовсе. Теперь шаг за кадр, картина оседает на глазах.
* ~~**Раскладка O(n²) на главном потоке.**~~ Замер боевой функции: 15 114 мс на
  4500 узлах, синхронно, до первой отрисовки. Квадродерево Барнса-Хата: 553 мс на
  всю укладку, худший кадр 5.24 мс. Потолок картины 150 → 1000.
* ~~**Одно перетаскивание пришпиливало ВСЕ узлы вида.**~~ Закрепляется ровно
  сдвинутый.
* ~~**Нажатие фильтра обнуляло зум и сдвиг.**~~ Камера живёт в состоянии экрана.
* ~~**Окрестность молчала об обрезе.**~~ Сервер честно отдавал
  `nodes_matched_at_least`, экран их не читал.

### Закрыто в 0.170.0

* ~~**Общий и локальный вид — два разных графа.**~~ Обход окрестности получил
  второй род рёбер по явному параметру `include_cooccurrence`; умолчание `False`
  держит агента и публичный маршрут на прежнем ответе, потому что соседство в
  концентраторе — замеренная не-улика.
* ~~**От узла нельзя дойти до документов, от документа — до узла.**~~ Карточка
  узла перечисляет документы с кнопкой «Открыть», а у подтверждённой связи в
  инспекции документа появилась кнопка «В графе».

### Открыто

* **Граф есть только в админке.** Пользовательский маршрут окрестности
  (`friday/api/kg.py:909-944`) не подключён ни к чему, а закон проекта
  (`sol/SOL.md` §1.6) требует, чтобы новая возможность сначала работала в чате.
  Крупная.
* **Нет кластеризации и группового цвета**; палитра типов неполная
  (`GRAPH_COLORS` против `GRAPH_TYPES`), легенда неполна вдвойне. Средняя.
* **Поиск по графу не видит псевдонимы**; `known_at` не выведен ни одним органом
  управления, хотя сервер его принимает. Средняя.
* **Нет стрелок направления и разведения кратных рёбер.** Мелкая.
* **Выше тысячи узлов нужен canvas.** Замер: один кадр SVG стоит 6.5 мс при 1000,
  13.4 мс при 2000 и 27.2 мс при 4500 при бюджете 16.7 мс; 44 994 элемента DOM и
  4.3 МиБ разметки на 4500 узлах. Пока картина честно останавливается на тысяче.

---

## 9. Телеграм и документы — разведка 2026-08-07

Те же шесть рук прошли по мосту и по разбору документов; каждая находка сверена
с кодом отдельным проверяющим. Двадцать четыре подтверждено, одна закрыта.

### Телеграм

* ~~**Мост сдаётся через 270 с, а ядро работает до 720.**~~ ЗАКРЫТО в 0.171.0:
  оба числа выведены из одного, потолок хода стал настоящим.
* ~~**Одна медленная реплика замораживает все чаты и все кнопки.**~~ ЗАКРЫТО в
  0.172.0: обход перестал быть блокирующим и раздаёт готовые строки задачам, по
  одной на чат, не больше восьми сразу. Порядок внутри чата держит хранилище;
  учёт работы в полёте закрывает то, чего оно не ловит, — повторный запуск ТОЙ ЖЕ
  строки на следующем обороте опроса.
* **Запись знания из чата нельзя ИСПРАВИТЬ.** Удаление закрыто в 0.172.0 —
  у открытой записи есть кнопка с подтверждением. Правка текста отложена с
  причиной: она требует захвата следующей реплики как ответа на конкретное
  сообщение, а чтение `reply_to_message` в мосте отсутствует вовсе (пункт ниже).
  Делать правку раньше — значит завести второй механизм ввода и выбросить его.
* ~~**429 от Telegram не читается.**~~ ЗАКРЫТО в 0.173.0: мост ждёт столько,
  сколько просит Telegram, и повторяет ТОТ ЖЕ кусок. Остаток назван честно —
  обрыв сети посреди многокускового ответа по-прежнему повторит доставленное;
  полное решение требует помнить число ушедших кусков, а ответов длиннее одного
  куска на живой переписке ровно один из 367.
* ~~**Отказ по правам показывается как «действие недоступно».**~~ ЗАКРЫТО в
  0.173.0: `refusal_notice` умел различать «нет права» и «нет такого» с самого
  начала, но на кнопках его никто не звал.
* ~~**«Открыть источник целиком» обрывается на 3000 знаках.**~~ ЗАКРЫТО в
  0.174.0: под куском кнопка «Дальше», смещение едет в самой кнопке. Потолок
  куска остался — в архиве есть документы под восемьсот тысяч знаков.
* **Ответ на реплику и упоминание не читаются вовсе** (`_process_update`).
  Средняя.
* **Альбом из нескольких файлов разбирается как несколько независимых
  сообщений.** Средняя.
* **`/start` обещает расширение доступа, а механизма в чате нет.** Средняя.
* **Таблицы модели приходят сырыми палками** (`_markup.py` их не знает). Средняя.
  Замер на живой переписке: из 367 ответов ассистента таблицы встречаются в 3.
* ~~**Признака «думаю» нет у `/mission`.**~~ ЗАКРЫТО в 0.173.0.
* **Раскол длинного ответа оставляет сырые звёздочки.** Мелкая; замер на живой
  переписке подтверждает редкость: длиннее 4096 знаков ровно 1 ответ из 367.

Замер на живой переписке 2026-08-07 (40% корпуса, остальное упёрлось в защиту от
частых запросов, а не в дефект): 367 ответов ассистента, медиана 464 знака,
p90 1628, максимум 6531. Разметку `**` содержат **146** ответов — то есть путь
преобразования в HTML нагружен постоянно и стоит того, чтобы быть точным.
Заголовки `#` — 11 ответов. Отказов и «не знаю» — 20. Переспросов сразу после
ответа — 14.

### Документы

* **Скан на 40 страниц распознаётся по ЧЕТЫРЁМ картинкам, признака покрытия
  нет** (`documents/__init__.py:412`, `516-521`). Человек получает документ,
  уверенный, что прочитано всё. Крупная.
* **Целые семейства обиходных форматов не приняты на входе** (диспетчер
  `extract`, `:317-345`); почта разбирается только органом-импортёром. Крупная.
* **Колонтитулы, сноски, примечания и надписи docx только ПОМЕЧАЮТСЯ как
  утраченные, но не извлекаются** (`_office_structure.py:437-503`). Крупная.
* **ZIP и RAR не сообщают, что перестали разбирать; TAR сообщает.** Средняя.
* **Обрез исходника ДО разбора помечается и не читается ни одним боевым
  потребителем** (`:597-604`). Средняя.
* **Структура таблицы доезжает до модели только у `.docx` и `.xlsx`**, CSV
  остаётся плоским текстом. Средняя.
* **Точный путь по таблице закрыт четырьмя намерениями на русских регулярках**
  (`_office_attachments.py:47-192`) — ровно тот случай, где нужен арбитр, а не
  регулярка. Средняя.
* **Собственная дата документа есть только у pdf/docx/xlsx/pptx.** Средняя.
* **Причина отказа известна коду и не доходит до человека.** Мелкая.
* **Картинка с РАСПОЗНАННЫМ текстом объявляется нечитаемой.** Мелкая.
* **У PDF и PPTX обрезка по знакам не доезжает до приёмного пути.** Мелкая.
* **Массовый импорт папки не считает файлы, приехавшие без содержимого.**
  Мелкая.

---

## Что закрыто и больше не висит

* ~~Неподтверждённое досье при виде `человек` и нулевых источниках~~ — ЗАКРЫТО
  2026-08-06. Нерелевантный `user_model` больше не считается evidence; названное
  non-account имя сохраняет обычный поиск по архиву, а при нуле Knowledge/entity/
  person-tool/attachment/graph evidence модельное тело вместе с file/voice/
  attribution carriers отбрасывается до persistence. Пять новых регрессий и 127
  смежных person/grounding/citation тестов прошли независимый review.
* ~~Persisted-вложение терялось на естественном follow-up, а incomplete count мог
  стать verified после repair~~ — ЗАКРЫТО 2026-08-06. Exact uploader/capability и
  immutable source pointer перепроверяются каждый ход; common repeat-count/list
  continuation rehydrate исходный файл, sticky private-lineage блокирует outbound
  и глобальное обучение, verifier/repair видят один 24k slice до последнего знака,
  а answer-only exhaustiveness получает UNKNOWN и до, и после repair. Независимый
  adversarial review: 124 focused PASS. Структурный Office-инвентарь, который был
  отдельным продолжением этой дороги, закрыт позднее в §6.1.

Проверено на HEAD и вычеркнуто из прежних списков: разбор очереди связей,
вид документа как факт, отбор тегов (служебные слова, имена, корпусная частота,
надписи бланка), #112 (живость концов связи), #113 вперёд, scoping надзора,
ремонтный проход после неудачной проверки, дедуп активной заявки, события моста в
`runtime_events`, соединение-на-поток в хранилище, guard пустого PCM в TTS,
«система не помнит человека» в части модели пользователя, privacy closure личных
напоминаний и fail-closed миграция relation history schema 31 → 32.
