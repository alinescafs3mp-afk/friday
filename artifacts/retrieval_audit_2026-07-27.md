# Аудит стадий поиска, 2026-07-27

Пять агентов по стадиям отбора + состязательная проверка каждой находки на настоящем
корпусе (стенд из 342 рабочих документов). **22 находки, 12 пережили опровержение.**
Содержимого документов здесь нет — агентам это было запрещено, вывод проверен.

Закрыто: №9 (идентификаторы) — v0.109.0. Разобрано и сознательно отложено: №11 (засев графа).

## 1. [fts] Членство в fts_ranking считается доказательством: попадание по префиксу стоп-слова отключает фильтр insufficient_evidence

`friday/retrieval/__init__.py:966` · уверенность высокая

**Почему важно.** Барьер, который должен отсеивать документы из recent_pool без реальных признаков совпадения, полностью обходится для всего, что вернул FTS. Так как FTS впускает документы по префиксу служебного слова, в ответ пользователю попадают объекты вообще без лексического, полевого, векторного и графового сигнала.

**Доказательство.** В _exclusion_reason условие insufficient_evidence начинается с 'document_id not in fts_ranking' (строка 966); та же проверка снимает deprecated_weak (строка 975) и глушит разрешение спана пассажа (строка 1026), а строка 918 добавляет w_fts_bonus. Прогон настоящего HybridSearcher.search на копии стенда (эмбеддинги и граф выключены, limit=20, 232 запроса, 2091 результат): 1715 результатов (82%) имеют lexical<0.075 и field<0.12, то есть проходят гейт ИСКЛЮЧИТЕЛЬНО по членству в fts_ranking; из них 251 (12% всего ответа) не совпадают ни с одним содержательным термом запроса. В 42 из 232 запросов ответ забился полностью (20/20) и целевой документ был вытеснен.

**Проверка на опровержение.** Находка подтверждена измерением, опровергнуть не удалось. (1) Код читается именно так: friday/retrieval/__init__.py:981 (в находке 966 — файл сдвинулся ровно на +15 строк, те же +15 для :990 deprecated_weak, :1041 подавления спана и :933 w_fts_bonus) — условие insufficient_evidence начинается с `document_id not in fts_ranking`, то есть членство в FTS снимает гейт целиком. (2) FTS действительно впускает по префиксу служебного слова: пока запрос укладывается в бюджет 12 термов, _fts_terms (storage/_knowledge.py:222-236) отдаёт ВСЕ токены, включая стоп-слова, и каждый оборачивается в `"term"*` (:583). Замер на стенде (342 объекта): `"по"*` матчит 314 документов, `"на"*` 290, `"за"*` 259, `"до"*` 247, `"под"*` 246, `"от"*` 237, `"про"*` 201; в среднем 41.9% строк FTS-выдачи присутствуют ТОЛЬКО из-за стоп-слов. ARCHITECTURE.md:177 объясняет стоп-слова в FTS как осознанное решение, но обосновывает его исключительно как recall-стадию — нигде не сказано, что членство в FTS годится как доказательство в точностном гейте. (3) Пользовательский эффект реален: прогон настоящего HybridSearcher.search на копии стенда при боевом лимите (agent_runtime использует 10/12/16, не 20), 120 «трудных» запросов из слов тела документа — 26.7-27.2% выданных результатов не содержат ни одного содержательного терма запроса, то есть попали в ответ вообще без сигнала. (4) Исправимость проверена: если оставить исключение только для документов, которые FTS нашёл по НЕслужебному терму (вторая search_knowledge п

## 2. [fts] Бюджет термов режет хвост запроса по позиции в тексте: при более чем 12 несловарных токенах решающий терм теряется

`friday/storage/_knowledge.py:226` · уверенность высокая

**Почему важно.** Именно тот сценарий, ради которого писалась функция (специфичное слово в конце длинного вопроса), продолжает ломаться — защита работает только против стоп-слов. Длинный формальный вопрос с названием/идентификатором в конце перестаёт находить свой документ, хотя короткий вопрос с тем же словом находит его сразу.

**Доказательство.** Отбор в ветке превышения бюджета — [token for token in unique if token.casefold() not in _STOPWORDS][:12] — берёт первые 12 несловарных токенов в порядке текста. Проверка вызовом: запрос из 13 несловарных токенов, решающий последний -> 12 термов, решающего среди них нет; тот же запрос, набитый стоп-словами, решающий терм сохраняет. Замер на стенде: 120 документов, запрос «служебное слово + 12 обобщённых несловарных слов + редкий терм документа (df<=3) последним» — терм отброшен в 113/113 случаях, и в 85 из 113 (75%) целевой документ выпадает из топ-100 FTS целиком, тогда как для двухсловного запроса с тем же термом он в топ-100 в 111 из 113 случаев.

**Проверка на опровержение.** Опровергнуть не удалось — находка подтверждается и кодом, и замером на стенде; более того, автор недооценил реалистичность сценария.

1) Код читается именно так. friday/storage/_knowledge.py:222-230: в `unique` попадают только токены длиной >=2, при len(unique) > 12 берётся `[token for token in unique if token.casefold() not in _STOPWORDS][:_FTS_TERM_BUDGET]` — первые 12 несловарных токенов в порядке текста. Граница замерена вызовом функции: 11 содержательных слов + решающий терм -> терм сохранён; 12 содержательных слов + терм -> отброшен. Добивка стоп-словами ничего не меняет (терм всё равно отброшен), то есть защита действительно работает только против стоп-слов.

2) Возражение «поведение намеренное и объяснено рядом» не проходит. Докстринг ссылается на замеры tools/retrieval_bench.py (0.583 -> 0.458, «reordering by length scored the same 0.458»), но ветка превышения бюджета в этом бенче не исполняется ни разу: у всех 24 записей GOLD максимум 7 различных токенов (>=2 символов) и максимум 6 несловарных. Бенч структурно слеп к этому случаю, так что приведённые числа не про хвост, а про безусловные варианты отбора.

3) Тестом не покрыто, а покрывающий тест стоит ровно на границе. tests/test_identifier_tokens.py:113 — вопрос из 14 различных токенов, из них 12 несловарных, и идентификатор оказывается ровно 12-м несловарным. Одно добавленное содержательное слово в тот же самый вопрос возвращает дефект: утверждение на строке 178 (`assert идентификатор in chosen`) снова ложно. Тес

## 3. [chunks] Корроборационная поправка mean(top-3) слабеет с ростом числа чанков, поэтому max-over-passages пробивает порог insufficient_evidence, откалиброванный на ОДНОМ векторе документа

`friday/retrieval/__init__.py:623` · уверенность высокая

**Почему важно.** Длинный документ, не имеющий ни одного релевантного пассажа, получает embedding-скор выше 0.35 просто потому, что у него много чанков: он не отсекается гейтом insufficient_evidence, входит в выдачу, получает +0.17*emb в блендe и вытесняет короткую заметку, которая действительно отвечает на вопрос. Именно длинные личные файлы (таблицы, реестры) начинают приходить в ответ на почти любой запрос, и модель строит ответ на них.

**Доказательство.** Порог _DENSE_EVIDENCE_MIN_DEFAULT=0.35 (retrieval/__init__.py:312) откалиброван, по комментарию строк 306-312, на 56 парах "короткий запрос x ТЕЛО документа": p50=0.2361, p90=0.3255, max=0.3878 — то есть на ОДНОМ замере на документ. После чанкования скор документа = max(doc, aggregate), где aggregate=0.75*best+0.25*mean(top3) по N пассажам (строка 623), то есть статистика максимума из N. Поправка mean(top-3) устроена ровно наоборот к заявленной в docstring цели: при большом N три лучших из N сами близки к максимуму, и скидка исчезает. Симуляция (20k-200k прогонов, шум N(0.2361,0.0698) — подгонка по p50/p90 из комментария; контроль: медиана максимума из 56 выборок = 0.3925 против наблюдённого max 0.3878): скидка 1-agg/max = 0.0% (N=1), 5.0% (N=3), 2.6% (N=8), 1.7% (N=32), 1.4% (N=62). Доля документов БЕЗ единого релевантного пассажа, проходящих гейт 0.35: 5.2% (N=1, рабочая точка калибровки), 15% (N=5), 26% (N=8), 50% (N=16), 78% (N=32), 95% (N=62). Консервативная модель с общим документным эффектом (rho=0.5): 19% (N=8), 38% (N=32), 48% (N=62). На стенде из 342 объектов чанкуются 260, у 93 объектов >=8 пассажей, у 36 >=32 (реальные N посчитаны вызовом knowledge_chunk_units на настоящих content с дефолтами 1200/200/63). Эффект усиливается тем, что knowledge_chunk_units (строка ~448) приклеивает к КАЖДОМУ пассажу общий заголовок до 300 символов — это коррелированная добавка ко всем N чанкам сразу.

**Проверка на опровержение.** НЕ ОПРОВЕРГНУТО. Механизм подтверждён чтением кода, историей репозитория и замерами на стенде; неверна только количественная часть (масштаб завышен в 2–4 раза) и один побочный аргумент.

ЧТО ПОДТВЕРДИЛОСЬ

1. Код читается именно так. /home/friday/friday/friday/retrieval/__init__.py:623 — agg = 0.75*best + 0.25*mean(top-3); :1396-1400 — combined = max(doc_score, agg); :1007 кладёт этот combined в embedding_scores, а :984 сравнивает его с self._dense_evidence_min (:319 = 0.35). То есть с гейтом сравнивается статистика максимума из N, а не одиночный замер. chunk_recall в проде включён по умолчанию (server.py:610-616 не передаёт chunk_recall, дефолт True, retrieval/__init__.py:720), embeddings_chunk_chars=1200, chunk_blend=0.25 (config/__init__.py:562,566).

2. Калибровка действительно «на один вектор документа» — и это подтверждает сам проект. CHANGELOG.md 0.92.0 (сегодня) вводит 0.35 по 56 парам «короткий запрос × ТЕЛО документа»; docs/ARCHITECTURE.md:179 повторяет ту же рабочую точку. Чанкинг приехал в 0.41.0 (CHANGELOG.md:1675) — раньше перекалибровки, и в его же описании гейт назван «0.16», а docstring aggregate_chunk_scores (:601) до сих пор ссылается на «the 0.16 evidence gate». Порог поднимали, распределение рантайм-скора после чанкинга не перемеряли.

3. Корроборационная поправка арифметически не способна компенсировать рост максимума и слабеет с N. Симуляция (200k прогонов, безразмерные единицы sigma, поэтому вывод не зависит от масштаба шума): доля инфляции E[max]-m

## 4. [chunks] Происхождение чанка сравнивается с ОБРЕЗАННЫМ вектором документа (первые 20 000 символов), и при его выигрыше индекс лучшего пассажа выбрасывается — цитата в ответ берётся из начала документа

`friday/retrieval/__init__.py:1434` · уверенность высокая

**Почему важно.** Для длинного документа, найденного семантически, в контекст LLM и верификатору уходит не тот фрагмент, который совпал, а первые 520 символов всего тела. Пользователь получает ответ, обоснованный шапкой документа, при том что система знала точный индекс сработавшего пассажа и молча его выбросила. В explain-трейсе это выглядит как embedding_chunk=-1 с комментарием "the whole-object vector carried it", что для таких объектов неправда.

**Доказательство.** Фильтр на строке 1434 сравнивает aggregate (со скидкой корроборации) с сырым косинусом doc-вектора. Алгебраически aggregate >= (1-0.25+0.25/3)*best = 0.8333*best, поэтому провенанс теряется во всей полосе 0.8333*best <= doc < best — то есть лучший пассаж МОЖЕТ обгонять вектор документа и всё равно потерять происхождение. При этом doc-вектор для длинных объектов строится из усечённого текста: workers/__init__.py:796, knowledge_search_text(row)[:_DOC_VECTOR_MAX_CHARS] при _DOC_VECTOR_MAX_CHARS=20000 (workers/__init__.py:53). Замер на стенде: у 44 из 342 объектов search_text длиннее 20 000 символов; доля текста, попавшая в doc-вектор: p50=33.7%, min=2.6% (объект с content 757 031 символ представлен первыми 20 000). Дальше цепочка детерминирована: без ключа в chunk_provenance не выставляется item['_embedding_chunk'] (строка 1017), не запрашивается get_chunk_spans (строка ~1045), _matched_region (agent_runtime/__init__.py:60-77) возвращает всё тело, а best_snippet (retrieval/__init__.py:231) при отсутствии токенов запроса в теле возвращает body[:520] — для документа в 757 031 символ это 0.07% текста, выбранных без всякой связи с запросом.

**Проверка на опровержение.** НЕ ОПРОВЕРГНУТО в своей сути, но формулировка автора содержит две неточности.

ЧТО ПОДТВЕРДИЛОСЬ (чтением кода и воспроизведением).

1. Фильтр реален. `friday/retrieval/__init__.py:1430-1435`: в `meta["chunk_provenance"]` попадают только объекты, у которых `chunk_scores[id] >= doc_scores[id]`, где `chunk_scores` — агрегат из `aggregate_chunk_scores` (строки 590-625): `(1-blend)*best + blend*mean(top-3)`, `_CHUNK_CORROBORATION_K = 3` (строка 536), `embeddings_chunk_blend` по умолчанию 0.25 (`friday/config/__init__.py:566`). При неотрицательных косинусах агрегат >= 0.8333*best, значит полоса `aggregate <= doc < best` непуста: лучший пассаж обгоняет doc-вектор, а провенанс всё равно выбрасывается. Комментария, объясняющего именно это сравнение, рядом нет; тестами полоса не покрыта (`tests/test_chunk_recall.py` проверяет только «провенанс есть при чисто семантическом попадании» и «нет при лексическом»).

2. Цепочка детерминирована и подтверждена запуском. Написал воспроизведение на фикстурах репозитория (`_FakeTopicEmbeddings`, `_make_ko`, `WorkersManager._embeddings_index_all`, `HybridSearcher.search`), объект из трёх пассажей, релевантный — последний: косинусы чанков [0.0, 0.0, 1.0], doc-косинус 0.8575, агрегат 0.8333 → 0.8575 >= 0.8333. Результат: объект найден, `_embedding_score = 0.857`, `_embedding_chunk` НЕ выставлен (строка 1016), спан не запрошен (строки 1032-1051), `_matched_region` (`agent_runtime/__init__.py:60-77`) вернул всё тело, `best_snippet(..., max_chars=520)

## 5. [chunks] Расширение окна пассажей в chunk_spans перескакивает бюджет: самые большие документы режутся в ~1.4 раза грубее, чем позволяет лимит, треть слотов пассажей не используется

`friday/retrieval/__init__.py:411` · уверенность высокая

**Почему важно.** Именно на самых длинных документах — ради которых чанкование и делалось — пассаж получается вдвое-втрое крупнее нужного. Релевантный абзац занимает меньшую долю эмбеддируемого текста, косинус лучшего чанка падает (документ хуже находится), а выигранный _embedding_chunk_span отдаёт _matched_region регион в 18 326 символов, из которого в ответ попадает 520 — то есть 2.8%, выбранные лексически. Семантическая локализация, ради которой всё это строилось, на этих документах почти полностью теряется.

**Доказательство.** Цикл расширения делает ровно одну попытку по оценке estimate=ceil(len/limit)+overlap (строка 407), а дальше растёт геометрически на 1.5 (строка 411). Замер на стенде на 4 самых больших объектах (chunk_spans с дефолтами 1200/200, max_chunks=63): content=757 031 — лестница окон (12 217 -> 66 пассажей), (18 326 -> 43); отгружается 43 пассажа по 17 799 символов, тогда как бинарный поиск даёт окно 12 588 и ровно 63 пассажа. Аналогично: 717 515 -> 43 по 16 880 против окна 11 897 и 63 пассажей; 333 965 -> 42 по 8 143 против 5 578/63; 262 653 -> 43 по 6 301 против 4 524/63. Оценка промахивается всего на 3 пассажа (66 против 63), но шаг x1.5 перелетает. По всем 21 документу стенда, где окно пришлось расширять: размер пассажа против самого мелкого, ещё влезающего в бюджет 63 — p50=1.40x, max=1.71x; доля неиспользованного бюджета пассажей p50=30%, max=44%. Распределение размеров пассажей по всему стенду: p50=1161, p90=4939, max=18326 при сконфигурированных 1200.

**Проверка на опровержение.** Находка подтверждается чтением кода и замером на стенде; опровергнуть не удалось.

Код. friday/retrieval/__init__.py:388-412. estimate (строка 407) не зависит от состояния цикла, поэтому на второй итерации условие `estimate > window` ложно по построению и ветка всегда уходит в `math.ceil(window * 1.5)` (строка 411).

Замер (SQLite стенда через mode=ro, дефолты 1200/200, max_chunks=63 = min(64, cap-1) из workers/__init__.py:783). Трассировка крупнейшего объекта (757 031 символ): window=1200 -> 926 спанов; window=12217 -> 66; window=18326 -> 43 (FITS). Развёртка окон подтверждает, что 63 спана достижимы при окне ~12 588-12 750 и что число спанов там монотонно по окну, то есть сравнение корректно. По стенду: 342 объекта, 260 чанкуются, 22 потребовали расширения окна; размер пассажа против минимального ещё влезающего окна — p50=1.40x, max=1.84x; неиспользованный бюджет слотов p50=31%, max=44%. Размеры пассажей по стенду: p50=1161, p90=4937, p99=18000, max=18326 при сконфигурированных 1200. Четыре крупнейших объекта дают 43/43/42/43 пассажа по 18326/17385/8253/6539. Все числа автора воспроизводятся с точностью до округления.

Защита «покрыто тестом» не работает и, наоборот, играет за находку. tests/test_chunk_recall.py:246 (test_a_very_long_document_is_covered_to_its_last_character) в конце утверждает именно наполненность бюджета: len(spans) >= 60 и средний спан < 2500 для 100 КБ. Я прогнал его синтетическое тело: получается ровно 63 спана, средний 1745 — потому что равномерный н

## 6. [blend] Штраф identifier_penalty (−0.22) недостижим: любой документ, который его получает, уже безусловно исключён

`friday/retrieval/__init__.py:920` · уверенность высокая

**Почему важно.** Это самая большая по модулю константа в блендe, и она читается как «мягкое понижение за неполное покрытие идентификаторов». Фактически это мёртвый код: поведение задаёт жёсткий гейт, а не вес. Тот, кто будет калибровать строгость требования к идентификаторам, изменит 0.22 и не увидит ни малейшего эффекта на выдаче, а реальную ручку (условие на стр. 976-977) не тронет.

**Доказательство.** Условие штрафа на стр. 920 (`query_identifiers and identifiers < 1.0`, где `identifiers = identifier_coverage.get(id, 1.0)`) и первая проверка в `_exclusion_reason` на стр. 976-977 (`query_identifiers and identifier_coverage.get(id, 0.0) < 1.0`) логически тождественны: словарь `identifier_coverage` заполняется в цикле стр. 853-865 для КАЖДОГО ключа candidate_map, а `_exclusion_reason` вызывается только для ключей candidate_map, поэтому разные значения по умолчанию (1.0 и 0.0) недостижимы. Проверка identifier_mismatch стоит первой, до всех прочих, поэтому ничто не может «спасти» такой документ. Замер на стенде (10 запросов, содержащих числовые/буквенно-цифровые токены, HybridSearcher(storage, None, record_usage=False), limit=10, explain=True): 118 кандидатов получили identifier_coverage<1.0; у всех 118 status=discarded, reason='identifier_mismatch'; в выдачу не попал ни один. Итог: слагаемое `- identifier_penalty` (стр. 935) не может изменить относительный порядок ни одного возвращаемого результата.

**Проверка на опровержение.** Находка подтверждается по существу — я пытался её сломать и не смог, но одну формулировку надо поправить.

ЧТО ПРОВЕРЕНО ПО КОДУ (/home/friday/friday/friday/retrieval/__init__.py)
1. `identifier_coverage` заполняется в цикле по `candidate_map.items()` (стр. 853-865). После стр. 865 и до `ordered = sorted(candidate_map, ...)` (стр. 997) `candidate_map` не мутируется: расширения пула происходят раньше — `_dense_recall` (вызов стр. 776, мутация стр. 1426) и графовое расширение (стр. 813-828), оба до стр. 850. `_exclusion_reason` вызывается только с ключами `candidate_map` — из цикла выдачи (стр. 1009) и из explain-трейса (стр. 1125). Значит дефолты `1.0` (стр. 902) и `0.0` (стр. 976) недостижимы, и оба условия сводятся к одному предикату `query_identifiers and coverage < 1.0`. Проверка `identifier_mismatch` стоит первой в `_exclusion_reason`, «спасти» кандидата нечем.
2. Обходных путей выдачи нет: единственный ранний return — пустой запрос (стр. 758); все результаты проходят через `_exclusion_reason` (стр. 1009). `_ablate` (стр. 881-886) не трогает ни штраф, ни гейт.

ЗАМЕР (контрфактный, сильнее авторского). Копия стенда в скретчпаде (стенд читался только через mode=ro, живая база не тронута), 342 объекта; три варианта модуля из одного исходника, отличающиеся ровно строкой 920: 0.22 (как есть), 0.0 и 99.0. 55 запросов с идентификаторами (токены собраны из самого корпуса + 5 заведомо отсутствующих), `HybridSearcher(storage, None, record_usage=False)`, limit=10, explain=True:


## 7. [blend] explain-трасса не содержит слагаемого +0.018 (fts_bonus): по её components заявленный score не воспроизводится

`friday/retrieval/__init__.py:933` · уверенность высокая

**Почему важно.** Докстринг `_exclusion_reason` (стр. 972-973) и блок explain обещают, что трасса показывает ровно то, что применил ранкер. На деле администратор, разбирающий «почему A выше B», получит два набора компонент, из которых следует обратный порядок — и не сможет понять, что разницу сделал невидимый бонус за попадание в FTS.

**Доказательство.** Слагаемое применяется на стр. 933 (`+ (w_fts_bonus if document_id in fts_ranking else 0.0)`, w_fts_bonus=0.018, стр. 885), но словарь `components` (стр. 939-962) не содержит ни этого слагаемого, ни флага принадлежности к fts_ranking; запись трассы (id/title/knowledge_kind/lifecycle_stage/score/status/rank/reason/components) тоже. Замер: реконструкция score по формуле стр. 921-938 из полей components даёт невязку РОВНО 0.000000 либо 0.018000 (с точностью до округления компонент до 6 знаков) — то есть единственное неучтённое слагаемое и есть fts_bonus. Масштаб: на 20 запросах по стенду медианный разрыв между соседними позициями возвращаемой десятки = 0.00319, при этом 89.4% соседних разрывов меньше 0.018 и 71.1% меньше 0.0073. То есть невидимое слагаемое решает исход почти каждой соседней пары. Тот же дефект у `identifier_penalty` и у множителя `quality_factor` (стр. 937) — в components есть только `lifecycle_factor`.

**Проверка на опровержение.** НЕ ОПРОВЕРГНУТО в ядре, но существенно завышено по масштабу; два из трёх подпунктов неверны.

ЧТО ПОДТВЕРДИЛОСЬ (чтение + замер).
Слагаемое действительно применяется в `friday/retrieval/__init__.py:933` (`+ (w_fts_bonus if document_id in fts_ranking else 0.0)`, значение 0.018 на стр. 885), а словарь `components` (стр. 939-962) не содержит ни его, ни признака членства в `fts_ranking`; в записи трассы (стр. 1143-1154) его тоже нет. Ни одного комментария, объясняющего пропуск, рядом нет; бонус существует с первого коммита (c47b159, тогда литералом `0.018`), а `components` собирается там же — то есть это упущение, а не решение. Ablation-сема (`ABLATABLE_SIGNALS`, стр. 540-546) даёт измерить сигнал глобально на gold-наборе, но не объясняет конкретную пару.

Замер (стенд, 342 объекта, 24 запроса из частотных токенов заголовков, limit=10, embeddings=None, kg=None): реконструкция score по формуле стр. 921-938 ТОЛЬКО из опубликованных полей `components` не сходится у 217 из 221 возвращённых строк (98.2%); невязка всегда положительная, в диапазоне 0.014411…0.018000 — это ровно `0.018 * lifecycle_factor * quality_factor`. Отрицательных невязок нет. То есть «единственное неучтённое слагаемое = fts_bonus» — верно.

ГДЕ АВТОР ОШИБСЯ.
1) Масштаб завышен примерно в 15 раз. «Разрыв < 0.018» — необходимое, но не достаточное условие: бонус меняет исход, только если соседи РАЗЛИЧАЮТСЯ по членству в FTS (или различаются множители lf·qf). У меня 97% соседних разрывов < 0.018, но соседних пар со с

## 8. [blend] Канал curated-fields входит в RRF по абсолютному порогу 0.035, лежащему внутри рабочего диапазона сигнала: канал то исчезает целиком, то даёт ступеньку больше собственного веса

`friday/retrieval/__init__.py:869` · уверенность высокая

**Почему важно.** Совпадение в заголовке/тегах/сущностях — самый доверенный сигнал в системе — ведёт себя не как градуированное свидетельство, а как бинарный триггер с произвольно выбранным порогом. Два документа с field 0.0349 и 0.0351 расходятся в итоговом скоре на 0.0217 — больше, чем весь наблюдаемый разброс самого сигнала, и примерно на 7 позиций выдачи. А в 40% запросов канал вообще не участвует в фьюжне, и об этом ничто не сообщает (в strategy такого флага нет).

**Доказательство.** `field_ranking` (стр. 866-870) отсекается абсолютным порогом `score >= 0.035`, затем добавляется в RRF (стр. 872, k=45), где член ранга 1 получает 1/(45+1)=0.02174 независимо от длины ранжирования. Замер на стенде (20 запросов, топ-30 трассы): максимум field-скора по запросу — медиана 0.0500, p90 0.1914; в 8 запросах из 20 ни один кандидат не дотягивает до 0.035 → канала в фьюжне нет вовсе. Когда канал есть, его размер наблюдался равным 1, 2, 2, 2, 3, 4, 5, 6, 7, 8, 9, 11 — то есть единственный член ранжирования из одного документа получает ту же максимальную RRF-долю, что и лучший FTS-хит из 50. При типичном field≈0.05 прямой вклад `field * 0.17` (стр. 924) равен 0.0085, то есть пересечение порога стоит в ~2.5 раза дороже градуированного веса; при медианном соседнем разрыве 0.00319 это ~7 позиций.

**Проверка на опровержение.** НЕ ОПРОВЕРГНУТО: механизм воспроизвёлся независимо, но числа автора надо исправить (часть — в его пользу, часть — против).

ПОДТВЕРЖДЕНО ПО КОДУ
- /home/friday/friday/friday/retrieval/__init__.py:869 — действительно абсолютный литерал `if score >= 0.035`, без объясняющего комментария (комментарий на :843-844 объясняет только доверие к curated-полям), без единого теста (в tests/ нет упоминаний field_ranking/0.035), не менялся с первого коммита.
- `reciprocal_rank_fusion` (:284-289) — простая сумма `1/(k+pos+1)` без нормировки на длину, k=45 (:874): членство в канале стоит целый ранговый член.
- В `strategy` (:1078-1084) флага про field нет. Но `"fts": True` и `"lexical": True` там захардкожены, т.е. участие каналов не сообщает ни один флаг; в explain-трассе per-document поля `field`/`title_match`/`tag_match`/`entity_match` присутствуют.

ЗАМЕР (реплика БД стенда в скратчпаде, FRIDAY_HOME на неё, record_usage=False; исходный стенд не изменён — размер и mtime основного файла те же)
1. Порог стоит в НЕПУСТОЙ области: 20 запросов × топ-30 порядка скоринга = 600 документов, 20 из них (3.3%, в 8 запросах из 20) лежат в ±0.005 от 0.035; в другом прогоне — 64 таких документа на 16 запросов в топ-60. Наблюдался разрыв «последний принятый / первый отсечённый» до 0.00076. Ножевой край реален.
2. Величина ступеньки МЕНЬШЕ заявленной: краевой документ стоит в хвосте ранжирования и теряет 1/(45+N), а не 1/46, и итог ещё умножается на `lifecycle_factor * quality_factor`. Замерено 0.0076–

## 9. [gates] _query_identifiers считает жёстким идентификатором любой токен с точкой — десятичные числа, время и русские сокращения («т.д», «т.п», «12.5», «9.00»), после чего identifier_mismatch выбрасывает почти весь корпус

`friday/retrieval/__init__.py:1197` · уверенность высокая

**Почему важно.** Одно бытовое слово в конце вопроса («… и т.д.») или число с точкой («примерно 12.5 процента») превращается в обязательное точное совпадение. Гейт стоит ПЕРВЫМ в _exclusion_reason и не имеет исключения по fts_ranking, поэтому выбрасывается даже документ №1 по bm25. Пользователь при этом не получает ни ошибки, ни пустого ответа: список остаётся заполненным документами, которые случайно содержат «т.д.», то есть выдаётся правдоподобный, но полностью неверный результат. Причина исключения («несовпадение идентификатора») не соответствует действительности — никакого идентификатора в запросе не было.

**Доказательство.** Замер на стенде (342 объекта, только чтение, uri=True, mode=ro). Взято 25 запросов из 2–4 значащих слов заголовка документа, у которых _query_identifiers пуст. Базовый случай: гейт пропускает 342/342 кандидата, целевой документ выживает 25/25. Тот же запрос + « и т.д.»: _query_identifiers -> {'т.д'}, выживает 52/342 кандидата, целевой документ 0/25. + « и т.п.»: 51/342, 0/25. + « 12.5 процента»: _query_identifiers -> {'12.5'}, выживает 0/342, 0/25. Сквозной прогон полного блендa (rrf+lex+fld+bonus+гейты, limit=10) на 15 запросах: базовый — 10.0 результатов, gold@1 9/15, gold@10 14/15; + « и т.д.» — 7.3 результата, gold@1 0/15, gold@10 0/15; + « примерно 12.5 процента» — 0.0 результатов. Разбор кода: _TOKEN_RE (строка 30) оставляет '.' внутри токена, tokens_of срезает только КОНЦЕВУЮ пунктуацию, поэтому «т.д.» -> «т.д»; has_discrete_separator (1197) видит точку и объявляет токен идентификатором; гейт в _exclusion_reason (976–977) требует identifier_coverage ровно 1.0. Число документов стенда, содержащих такие токены: «т.д» — 52, «т.п» — 51, «т.е» — 0, «т.к» — 0, «12.5» — 0, «9.00» — 0, «2020-2024» — 0, «3-5» — 2.

**Проверка на опровержение.** НЕ ОПРОВЕРГНУТО — находка подтверждена и чтением кода, и сквозным прогоном настоящего HybridSearcher.

Что проверено в коде (/home/friday/friday/friday/retrieval/__init__.py):
- строка 1197: `has_discrete_separator = any(character in token for character in "._/#")` — единственная проверка, никаких ограничений на длину, язык или наличие цифр; любой токен с внутренним разделителем становится идентификатором;
- строка 55 (`tokens_of`): срезается только КОНЦЕВАЯ пунктуация ".-", поэтому «т.д.» -> «т.д», «12.5» остаётся «12.5»;
- строка 851: `_query_identifiers(clean_query)`, где clean_query — это только нормализация пробелов; стоп-слова и сокращения не отфильтровываются;
- строки 976–977: гейт стоит ПЕРВЫМ в `_exclusion_reason`, требует coverage ровно 1.0 и не имеет исключения по `fts_ranking` (в отличие от идущих ниже insufficient_evidence и deprecated_weak). Фолбэка «если результатов нет — повторить без идентификаторов» в search() нет (проверил до конца функции, строки 997–1159). Плюс на строке 920 к тому же кандидату применяется штраф −0.22, но он уже неважен: гейт абсолютный.
- Комментарий на 974–975 объясняет намерение («BRK.A не должен удовлетворяться BRK.B») — то есть намеренным является гейт для НАСТОЯЩИХ идентификаторов; ложное срабатывание детектора нигде не оговорено, git log/доки такого замера не содержат (гейт существует с initial commit), теста на этот случай нет (tests/test_identifier_tokens.py покрывает противоположный сценарий — идентификатор в конце предложен

## 10. [gates] deprecated_weak не смотрит ни на embedding, ни на field, а его порог lex < 0.25 практически недостижим — правило вырождается в «любой deprecated-объект вне FTS выбрасывается всегда»

`friday/retrieval/__init__.py:994` · уверенность высокая

**Почему важно.** Гейт прямо противоречит стоящему на две строки выше insufficient_evidence, который считает emb >= 0.35 и fld >= 0.12 полноценным доказательством: объект сначала признаётся доказанным, а затем удаляется как «слабый». Документ со стадией deprecated, найденный по смыслу (косинус 0.9) или по точному совпадению заголовка/тега, невозможно вернуть вообще — то есть ровно тот сценарий, ради которого существует плотный отбор, для deprecated не работает. При этом штраф уже применён дважды в скоре (lifecycle_factor 0.36), так что жёсткое исключение — третье наказание. Причина «deprecated_weak» вводит в заблуждение: доказательства были сильные, просто их не посмотрели.

**Доказательство.** Разбор кода (988–994): в условии участвуют только lifecycle_stage, членство в fts_ranking, lex и grp; emb и fld отсутствуют, хотя обе величины уже вычислены и переданы в _exclusion_reason как параметры. Замер достижимости порогов на стенде: распределение lexical по 11286 парам (запрос x кандидат, 33 запроса) — p50=0.0154, p90=0.0560, p99=0.1530, max=0.4382; lex >= 0.25 у 0.151% пар. Даже для «идеального» случая (запрос целиком составлен из слов заголовка самого документа, n=269) lex >= 0.25 достигается лишь в 8.6% случаев, медиана 0.0867. Второе условие grp < 0.45 тоже почти всегда истинно: accepted-связей с сущностями имеют 71 из 342 объектов (79% корпуса имеют grp = 0.0). Итог: для deprecated-объекта вне fts_ranking условие ложно менее чем в 1 случае из 100.

**Проверка на опровержение.** Находка подтверждена исполнением, а не только чтением. Прямой эксперимент на временной БД (не стенд): два объекта, запрос лексически не пересекается с телом, фейковый бэкенд эмбеддингов даёт косинус 1.0. Как active объект возвращается (trace: returned, emb=1.0); после перевода в lifecycle_stage="deprecated" тот же объект — discarded, reason=deprecated_weak, emb=1.0, lex=0.0, fld=0.0. То есть insufficient_evidence (friday/retrieval/__init__.py:980-987) уже признал плотное доказательство достаточным, а гейт на :988-994 снёс объект, не взглянув на emb/fld.

Замер достижимости порога на стенде (read-only, file:...?mode=ro): 70 запросов из заголовков/тегов x 342 объекта = 23940 пар; вне FTS top-100 — 17799 пар, из них lex >= 0.25 лишь 181 (1.0%), порядок совпадает с числами автора. Остальные 17618 гейт снёс бы; у 138 из них fld >= 0.12, то есть insufficient_evidence их уже принял.

Аргументы против версии «намеренно»: рядом с гейтом нет комментария (в отличие от соседнего insufficient_evidence, у которого измеренное обоснование), ни один тест поведение не пиннит (tests/test_answer_diagnostics.py лишь перечисляет строку причины среди допустимых), а docs/DATA_LIFECYCLE.md:107 обещает, что проигравший конфликт «остаётся в поиске» — понижение ранга, не удаление; промпт агента (friday/agent_runtime/__init__.py:200) рассчитан на присутствие deprecated-записей в контексте. Состояние deprecated возникает не только из дедупа: lifecycle_stage входит в allowed-поля PATCH /api/knowledge/{id

## 11. [graph] Граф безусловно засевается из top-8 лексического ранжирования, поэтому запрос без единого совпадения возвращает выдуманные результаты

`friday/retrieval/__init__.py:788` · уверенность высокая

**Почему важно.** Запрос, по которому в базе нет ничего, перестаёт давать честный пустой ответ: пользователь (и LLM в agent_runtime, который получает те же результаты) видит 10 чужих личных документов и обязан считать их релевантными. Пустая выдача — единственный сигнал «этого у меня нет» — уничтожается.

**Доказательство.** seed_ids = [*fts_ranking[:8], *lexical_ranking[:8]]; _lexical_rank (строки 1276-1299) возвращает ВСЕ кандидаты, отсортированные по косинусу, без нижнего порога, поэтому 8 «сидов» есть всегда. В knowledge_graph/__init__.py:644 каждый принятый линк сида становится корнем с фиксированным score=0.72*confidence (ранг сида и его релевантность не учитываются), в :852 документ получает entity_score*link_confidence≈0.677, а в _exclusion_reason (:965-972) исключение требует одновременно grp < 0.20 — то есть 0.677 снимает гейт insufficient_evidence в одиночку. Замер: 4 запроса из несуществующих слов (0 попаданий FTS) дают count=0 при kg=None и count=10 при kg=kg; у всех 10 результатов lexical=0.0000, field=0.0000, embedding=0.0000, graph=0.677; косинусы top-8 сидов при этом 0.0002-0.038. Живой путь всегда передаёт kg (agent_runtime/__init__.py:504, server.py:1242).

**Проверка на опровержение.** Опровергнуть не удалось — находка подтверждается и чтением кода, и замером на стенде (работал по копии базы стенда в scratchpad, оригинал не трогал; record_usage=False).

ЧТО ПОДТВЕРДИЛОСЬ КОДОМ
1. /home/friday/friday/friday/retrieval/__init__.py:803 (в HEAD 2dd69a5; в находке указано :788 — номер устарел, но строка та самая): seed_ids = list(dict.fromkeys([*fts_ranking[:8], *lexical_ranking[:8]])). Никакого порога.
2. _lexical_rank (:1291-1314) действительно кладёт в scored КАЖДОГО кандидата с его косинусом, включая 0.0, и сортирует по убыванию. recent_pool = list_knowledge_objects(...) (:763) непуст всегда, когда база непуста, значит 8 «сидов» есть всегда, даже при нулевом косинусе.
3. knowledge_graph/__init__.py:636-652: каждый accepted-линк сида делает сущность корнем со score = 0.72 * confidence, ранг и релевантность сида не учитываются. На :845-862 документ получает entity_score * link_confidence. Проверка «if not root_scores: вернуть пусто» (:654) обходится именно сидами.
4. _exclusion_reason (:964-995, гейт insufficient_evidence на :980-987) требует ОДНОВРЕМЕННО lex<0.075 and fld<0.12 and emb<dense_min and grp<0.20; graph 0.63-0.68 в одиночку снимает гейт. Живой путь всегда передаёт kg: agent_runtime/__init__.py:508, server.py:1242 (в эндпоинте /api/search никакой пост-фильтрации по скору нет).

ЗАМЕР (копия стенда, 342 объекта, 147 accepted-линков, 47 сущностей; embeddings выключены)
- 21 запрос с нулём попаданий FTS (20 из бессмысленных слогов + 1 естественный во

## 12. [graph] Графовый вклад почти бинарный: нет ни нормировки на степень сущности, ни накопления по числу общих сущностей

`friday/knowledge_graph/__init__.py:852` · уверенность высокая

**Почему важно.** Графовый канал не умеет ранжировать: он добавляет практически одинаковую константу (+0.16*0.677≈0.108 к base, при типичном итоговом скоре 0.23-0.27) целому кластеру документов, связанных с одной «хабовой» сущностью. Документ, разделяющий с запросом 16 сущностей, оценивается ровно так же, как документ с одной случайной общей сущностью, а редкая сущность ценится так же, как сущность-хаб.

**Доказательство.** candidate_score = entity_score * link_confidence (:852), агрегация — максимум, а не сумма (:862 и retrieval/__init__.py:809-812), множитель степени сущности отсутствует. Распределение 1729 счётов: 911 значений ровно 0.677 и 517 ровно 0.276 — 83% принимают одно из двух значений. Замер по возвращённым результатам: документ с 16 общими сущностями и документы с 1-7 общими сущностями получили идентичный graph=0.6774. Плотность связей: 147 принятых линков на 44 сущности, топ-2 сущности держат 29 и 18 линков; из 160 графово-оценённых возвращённых результатов 100 пришли именно от этих двух хабов.

**Проверка на опровержение.** Опровергнуть не удалось — находка подтверждается и чтением кода, и независимым замером.

ЧТО ПОДТВЕРДИЛОСЬ В КОДЕ (точные строки те самые):
- friday/knowledge_graph/__init__.py:852 — `candidate_score = entity_score * max(0.0, min(1.0, link_confidence))`;
- :862 — `if current is None or candidate_score > float(current.get("_graph_score", 0.0))` — агрегация действительно максимум, а не накопление;
- множителя степени сущности нет нигде на пути скора: корневой вес это либо `_match_score` из `search_entities` (:591-593 — это просто confidence 0.99/0.96 для точного упоминания или token-overlap ≤0.85), либо `0.72 * link.confidence` для сущностей, поднятых из seed-документов (:643). `_knowledge_count`/`_relation_count` считаются (:598-599, :756-757), но используются только для отображения в agent_runtime/__init__.py:1025-1026 — в скор не входят;
- потребитель: retrieval/__init__.py:926 `+ graph * 0.16`, плюс тот же graph работает порогом допуска в `_exclusion_reason` (`grp < graph_evidence_threshold`, 0.20 при depth=1).

ЗАМЕР НА СТЕНДЕ (копия БД, живая база не тронута; эмбеддинги выключены):
- структура графа ровно как у автора: 147 accepted-линков на 44 сущности, степени [29, 18, 8, 6, 6, 6, 5, …], один документ держит 16 линков; confidence принимает всего два значения — 0.97 (99 линков) и 0.90 (45).
- отсюда и «почти бинарность»: наблюдаемые значения graph — 0.9603 (=0.99·0.97), 0.891, 0.6774 (=0.72·0.97·0.97), 0.6286, 0.5832, 0.276 (co-occurrence на глубине 1). Внутри одного за
