--- a/sglang/srt/layers/moe/fused_moe_triton/layer.py
+++ b/sglang/srt/layers/moe/fused_moe_triton/layer.py
@@ -565,15 +565,6 @@
-        if (
-            self.quant_config is not None
-            and "modelopt" in self.quant_config.get_name()
-            and (expert_data.dim() != 2 or loaded_weight.dim() != 2)
-        ):
-            raise ValueError(
-                f"Expected 2D tensors, got expert_data shape {expert_data.shape} and loaded_weight shape {loaded_weight.shape}"
-            )
-
--- a/sglang/srt/models/gpt_oss.py
+++ b/sglang/srt/models/gpt_oss.py
@@ -516,6 +516,7 @@
             attention_bias=attention_bias,
+            quant_config=quant_config,
             prefix=add_prefix("self_attn", prefix),
